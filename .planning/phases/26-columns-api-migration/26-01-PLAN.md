---
phase: 26-columns-api-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
  - packages/gantt-lib/src/components/TaskList/taskListColumns.ts
  - packages/gantt-lib/src/index.ts
  - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
  - packages/gantt-lib/src/components/TaskList/TaskList.tsx
  - packages/gantt-lib/src/__tests__/taskListColumns.test.tsx
  - packages/website/src/components/AdditionalColumnsChart.tsx
autonomous: true
requirements: [MIG-01, MIG-02, MIG-03, MIG-04, MIG-05, MIG-06, MIG-07]

must_haves:
  truths:
    - "Legacy `editor` property не работает и не поддерживается в runtime"
    - "Все кастомные редакторы работают через `renderEditor`"
    - "Импорт TaskListColumn идёт из `columns/types`, bridge файл удалён"
    - "Демо-страница Additional Columns работает через новый API"
    - "Все тесты проходят после миграции"
  artifacts:
    - path: "packages/gantt-lib/src/components/TaskList/TaskListRow.tsx"
      provides: "Runtime без legacy editor fallback"
      contains: "col.renderEditor"
    - path: "packages/gantt-lib/src/index.ts"
      provides: "Экспорт из канонического пути columns/types"
      contains: "from './components/TaskList/columns/types'"
    - path: "packages/website/src/components/AdditionalColumnsChart.tsx"
      provides: "Демо с renderEditor вместо editor"
      contains: "renderEditor"
  key_links:
    - from: "packages/gantt-lib/src/index.ts"
      to: "packages/gantt-lib/src/components/TaskList/columns/types.ts"
      via: "direct re-export"
      pattern: "from './components/TaskList/columns/types'"
    - from: "packages/gantt-lib/src/components/TaskList/TaskListRow.tsx"
      to: "col.renderEditor"
      via: "direct property access"
      pattern: "col\\.renderEditor"
---

<objective>
Удалить legacy column editor API из runtime и перевести все импорты на канонический путь.

Purpose: Устранить двойственность API — оставить только `renderEditor`, убрать bridge re-export `taskListColumns.ts`, обновить все импорты и примеры.
Output: Чистый runtime без fallback, единый путь импорта, рабочий демо.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/gantt-lib/src/components/TaskList/columns/types.ts
@packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
@packages/gantt-lib/src/components/TaskList/taskListColumns.ts
@packages/gantt-lib/src/index.ts

<interfaces>
<!-- Канонический тип колонки из columns/types.ts — это единственный контракт после миграции -->
From packages/gantt-lib/src/components/TaskList/columns/types.ts:
```typescript
export type BuiltInTaskListColumnId =
  | 'number' | 'name' | 'startDate' | 'endDate'
  | 'duration' | 'progress' | 'dependencies' | 'actions';

export type TaskListColumnAnchor =
  | { after: BuiltInTaskListColumnId | string }
  | { before: BuiltInTaskListColumnId | string }
  | {};

export interface TaskListColumnContext<TTask extends Task> {
  task: TTask;
  rowIndex: number;
  isEditing: boolean;
  openEditor: () => void;
  closeEditor: () => void;
  updateTask: (patch: Partial<TTask>) => void;
}

export type TaskListColumn<TTask extends Task> = TaskListColumnAnchor & {
  id: string;
  header: ReactNode;
  width?: number;
  minWidth?: number;
  editable?: boolean;
  renderCell: (ctx: TaskListColumnContext<TTask>) => ReactNode;
  renderEditor?: (ctx: TaskListColumnContext<TTask>) => ReactNode;
  meta?: Record<string, unknown>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Удалить legacy editor fallback из runtime и обновить все внутренние импорты</name>
  <files>
    packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
    packages/gantt-lib/src/components/TaskList/taskListColumns.ts
    packages/gantt-lib/src/index.ts
    packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
    packages/gantt-lib/src/components/TaskList/TaskList.tsx
    packages/gantt-lib/src/__tests__/taskListColumns.test.tsx
  </files>
  <action>
    1. **TaskListRow.tsx, строка ~2194**: Заменить
       ```ts
       const editorFn = col.renderEditor ?? (col as any).editor;
       ```
       на:
       ```ts
       const editorFn = col.renderEditor;
       ```
       Все остальные ссылки на `editorFn` в этом блоке (строки 2200, 2219, 2222, 2228) остаются без изменений — они уже используют `editorFn`.

    2. **Также в TaskListRow.tsx, строка ~31**: Проверить импорт типа. Сейчас:
       ```ts
       import type { TaskListColumn as NewTaskListColumn } from "./columns/types";
       ```
       Оставить как есть — он уже на каноническом пути.

    3. **Удалить файл** `packages/gantt-lib/src/components/TaskList/taskListColumns.ts` целиком (bridge re-export). Это Option B из PRD — hard break.

    4. **index.ts, строка ~36**: Заменить
       ```ts
       } from './components/TaskList/taskListColumns';
       ```
       на:
       ```ts
       } from './components/TaskList/columns/types';
       ```

    5. **GanttChart.tsx**: Найти
       ```ts
       import type { TaskListColumn } from '../TaskList/taskListColumns';
       ```
       Заменить на:
       ```ts
       import type { TaskListColumn } from '../TaskList/columns/types';
       ```

    6. **TaskList.tsx**: Найти
       ```ts
       import type { TaskListColumn } from './taskListColumns';
       ```
       Заменить на:
       ```ts
       import type { TaskListColumn } from './columns/types';
       ```

    7. **taskListColumns.test.tsx, строка 5**: Заменить
       ```ts
       import type { TaskListColumn } from '../components/TaskList/taskListColumns';
       ```
       на:
       ```ts
       import type { TaskListColumn } from '../components/TaskList/columns/types';
       ```
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Файл taskListColumns.ts удалён
    - Все 4 импорта перенаправлены на columns/types
    - Fallback `(col as any).editor` удалён из TaskListRow.tsx
    - Все тесты проходят
  </done>
</task>

<task type="auto">
  <name>Task 2: Мигрировать демо AdditionalColumnsChart на новый API</name>
  <files>
    packages/website/src/components/AdditionalColumnsChart.tsx
  </files>
  <action>
    В файле `packages/website/src/components/AdditionalColumnsChart.tsx`:

    1. **Строка 22**: Заменить `editor:` на `renderEditor:` в определении колонки 'assignee':
       ```ts
       // Было:
       editor: ({ task, updateTask, closeEditor }) => (
       // Стало:
       renderEditor: ({ task, updateTask, closeEditor }) => (
       ```

    2. **Строка 41**: Заменить `editor:` на `renderEditor:` в определении колонки 'priority':
       ```ts
       // Было:
       editor: ({ task, updateTask, closeEditor }) => (
       // Стало:
       renderEditor: ({ task, updateTask, closeEditor }) => (
       ```

    3. Добавить `editable: true` к обоим column definitions (после `after: 'name'`), чтобы колонки корректно работали через unified pipeline. Проверить, нужно ли это — если без `editable` клик на ячейку всё равно открывает редактор (через наличие `renderEditor`), то не добавлять.

    4. Убедиться что импорт `TaskListColumn` из `gantt-lib` корректен (строка 4 — уже ОК, идёт из package).
  </action>
  <verify>
    <automated>cd packages/website && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - AdditionalColumnsChart использует только `renderEditor`, нет `editor:`
    - Website собирается без ошибок
    - Демо-страница рендерит кастомные колонки с редакторами
  </done>
</task>

</tasks>

<verification>
1. `grep -r "editor:" packages/website/src/components/AdditionalColumnsChart.tsx` — не должен найти `editor:` (кроме `renderEditor:`)
2. `grep -rn "(col as any).editor" packages/gantt-lib/src/` — пусто
3. `ls packages/gantt-lib/src/components/TaskList/taskListColumns.ts` — файл не существует
4. `grep -rn "from.*taskListColumns" packages/gantt-lib/src/` — пусто
5. `cd packages/gantt-lib && npx vitest run` — все тесты зелёные
</verification>

<success_criteria>
- Legacy `editor` fallback удалён из TaskListRow.tsx
- Bridge файл taskListColumns.ts удалён
- Все импорты перенаправлены на columns/types
- Демо использует renderEditor
- Все тесты проходят
- Website собирается
</success_criteria>

<output>
After completion, create `.planning/phases/26-columns-api-migration/26-01-SUMMARY.md`
</output>
