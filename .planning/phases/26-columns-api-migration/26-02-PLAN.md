---
phase: 26-columns-api-migration
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [MIG-01, MIG-07]

must_haves:
  truths:
    - "Документация не содержит упоминаний legacy `editor` property как текущего API"
    - "ROADMAP Phase 23 описание не содержит `editor?:` как текущий контракт"
    - "PROJECT.md описывает только renderEditor"
    - "Migration note присутствует в проекте"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Обновлённое описание без legacy editor"
    - path: ".planning/STATE.md"
      provides: "Обновлённый state без legacy editor"
  key_links: []
---

<objective>
Обновить проектную документацию и добавить migration note.

Purpose: Убрать все упоминания legacy `editor` property из проектной документации, обновить Phase 23 описание в ROADMAP, обновить requirements, добавить migration note о breaking changes.
Output: Чистая документация, migration note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Обновить проектную документацию</name>
  <files>
    .planning/PROJECT.md
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
    1. **PROJECT.md**: Найти строку с `renderCell/editor` и заменить на `renderCell/renderEditor`. Найти любые упоминания legacy editor pattern и обновить. Добавить в описание Phase 25/26 output: "unified column API with `renderEditor` as the only editor property".

    2. **STATE.md**: Найти строку `renderCell/editor` (строка ~32) и заменить на `renderCell/renderEditor`. Обновить любые references к `editor` property в контексте custom columns.

    3. **ROADMAP.md**:
       - Phase 23 Success Criteria пункт 4 (строка ~98): заменить `editor?: (row: GanttRow) => ReactNode` на `renderEditor?: (ctx: TaskListColumnContext<TTask>) => ReactNode`
       - Phase 23 Success Criteria пункт 5 (строка ~99): заменить `width?: string | number` на `width?: number`
       - Phase 26: заменить `**Requirements:** TBD` на `**Requirements:** MIG-01, MIG-02, MIG-03, MIG-04, MIG-05, MIG-06, MIG-07`
       - Phase 26: заменить `**Plans:** TBD` на `**Plans:** 2 plans`
       - Добавить список планов:
         ```
         - [ ] 26-01-PLAN.md — Удалить legacy editor fallback, bridge файл, обновить импорты и демо
         - [ ] 26-02-PLAN.md — Обновить документацию и добавить migration note
         ```
       - Исправить merge conflict marker `<<<<<<< HEAD` на строке 131 если всё ещё присутствует (удалить marker, оставить контент)

    4. **REQUIREMENTS.md**: Найти COL-02 и COL-04 — добавить пометку что `editor` заменён на `renderEditor` (если уместно, добавить note "[migrated to renderEditor in Phase 26]"). Добавить секцию Phase 26 requirements:
       ```
       ### Phase 26: columns-api-migration
       - [x] **MIG-01**: Legacy `editor` property удалён из runtime
       - [x] **MIG-02**: Все примеры используют только `renderEditor`
       - [x] **MIG-03**: Документация описывает только `renderEditor`
       - [x] **MIG-04**: Единственный документированный editor field — `renderEditor`
       - [x] **MIG-05**: Column examples используют numeric `width`
       - [x] **MIG-06**: Один поддерживаемый стиль авторинга
       - [x] **MIG-07**: Тесты проходят после удаления legacy support
       ```
  </action>
  <verify>
    <automated>grep -rn "renderCell/editor" .planning/ 2>/dev/null; grep -c "MIG-0" .planning/REQUIREMENTS.md; echo "---"; grep "26-01-PLAN\|26-02-PLAN" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    - PROJECT.md и STATE.md не содержат `renderCell/editor` (только `renderCell/renderEditor`)
    - ROADMAP Phase 23 описание обновлено
    - ROADMAP Phase 26 содержит список планов и requirements
    - REQUIREMENTS.md содержит MIG-01..MIG-07
    - Merge conflict в ROADMAP.md исправлен
  </done>
</task>

<task type="auto">
  <name>Task 2: Добавить migration note</name>
  <files>
    CHANGELOG.md
  </files>
  <action>
    Добавить запись в начало CHANGELOG.md (после заголовка, перед существующими записями):

    ```markdown
    ## [Unreleased] — Phase 26: columns-api-migration

    ### Breaking Changes

    - **`editor` property removed** — Custom columns must use `renderEditor` instead of `editor`. Simply rename the property:
      ```diff
      - editor: ({ task, updateTask, closeEditor }) => ...
      + renderEditor: ({ task, updateTask, closeEditor }) => ...
      ```
    - **Import path changed** — `TaskListColumn` type must be imported from the package root (`gantt-lib`), not from internal `taskListColumns` path
    - **Numeric `width` only** — Column `width` accepts only `number` (pixels), string values are not supported
    - **`before` / `after` placement** — The only supported way to position custom columns
    ```

    НЕ добавлять отдельный migration guide файл — CHANGELOG entry достаточен per PRD §10.4.
  </action>
  <verify>
    <automated>head -20 CHANGELOG.md</automated>
  </verify>
  <done>
    - CHANGELOG.md содержит breaking changes запись для Phase 26
    - Migration instructions: editor → renderEditor, numeric width, import path
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "renderCell/editor" .planning/` — пусто (только `renderCell/renderEditor`)
2. `grep "MIG-0" .planning/REQUIREMENTS.md` — 7 записей
3. `grep "Breaking Changes" CHANGELOG.md` — присутствует
4. `grep "<<<<<<" .planning/ROADMAP.md` — пусто (conflict resolved)
</verification>

<success_criteria>
- Вся проектная документация описывает только новый API
- Migration note в CHANGELOG.md
- ROADMAP обновлён с планами Phase 26
- Merge conflict в ROADMAP исправлен
</success_criteria>

<output>
After completion, create `.planning/phases/26-columns-api-migration/26-02-SUMMARY.md`
</output>
