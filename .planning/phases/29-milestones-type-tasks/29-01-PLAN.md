---
phase: 29-milestones-type-tasks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
  - packages/gantt-lib/src/types/index.ts
  - packages/gantt-lib/src/utils/taskType.ts
  - packages/gantt-lib/src/__tests__/taskRowMilestone.test.tsx
  - packages/gantt-lib/src/__tests__/useTaskDragMilestone.test.ts
  - packages/gantt-lib/src/__tests__/dependencyLinesMilestone.test.tsx
  - packages/gantt-lib/src/__tests__/taskListMilestone.test.tsx
  - packages/gantt-lib/src/__tests__/sampleMilestones.test.tsx
autonomous: true
requirements: [PH29-1]

must_haves:
  truths:
    - "Потребитель может явно задать `task.type: 'milestone'` без изменения существующего `parentId`-based hierarchy"
    - "Milestone-нормализация держит `endDate === startDate`, а обычная same-day task не становится milestone без явного type"
    - "Исполнителю следующей волны не нужно заново изобретать milestone contract и test targets"
  artifacts:
    - path: "packages/gantt-lib/src/utils/taskType.ts"
      provides: "Shared milestone helpers and single-date normalization contract"
      exports: ["TASK_TYPE_DEFAULT", "getTaskType", "isMilestoneTask", "normalizeTaskDatesForType"]
    - path: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      provides: "Public Task contract with explicit milestone typing"
      contains: "type?: 'task' | 'milestone'"
    - path: "packages/gantt-lib/src/__tests__/taskRowMilestone.test.tsx"
      provides: "Milestone render test target"
      contains: "milestone"
  key_links:
    - from: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      to: "packages/gantt-lib/src/utils/taskType.ts"
      via: "shared task type helpers"
      pattern: "taskType"
    - from: "packages/gantt-lib/src/types/index.ts"
      to: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      via: "matching Task.type public contract"
      pattern: "type\\?: 'task' \\| 'milestone'"
---

<objective>
Зафиксировать milestone как узкий публичный subtype и подготовить shared helpers/test targets для следующих волн.

Purpose: Реализация не должна расползтись в project/parent redesign. Эта волна блокирует ровно тот контракт, который нужен для diamond milestone support.
Output: explicit `Task.type` contract, shared normalization helpers, milestone-specific test files with named coverage targets.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-milestones-type-tasks/29-RESEARCH.md
@.planning/phases/29-milestones-type-tasks/29-VALIDATION.md
@packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
@packages/gantt-lib/src/types/index.ts

<interfaces>
From `packages/gantt-lib/src/components/GanttChart/GanttChart.tsx`:
```typescript
export interface Task {
  id: string;
  name: string;
  startDate: string | Date;
  endDate: string | Date;
  color?: string;
  parentId?: string;
  progress?: number;
  accepted?: boolean;
  dependencies?: TaskDependency[];
  locked?: boolean;
  divider?: 'top' | 'bottom';
}
```

From `packages/gantt-lib/src/types/index.ts`:
```typescript
export interface Task {
  id: string;
  name: string;
  startDate: string | Date;
  endDate: string | Date;
  color?: string;
  parentId?: string;
  progress?: number;
  accepted?: boolean;
  dependencies?: TaskDependency[];
  locked?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock explicit milestone task contract and shared normalization helpers</name>
  <read_first>
    packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
    packages/gantt-lib/src/types/index.ts
    .planning/phases/29-milestones-type-tasks/29-RESEARCH.md
  </read_first>
  <files>packages/gantt-lib/src/components/GanttChart/GanttChart.tsx, packages/gantt-lib/src/types/index.ts, packages/gantt-lib/src/utils/taskType.ts</files>
  <acceptance_criteria>
    `packages/gantt-lib/src/components/GanttChart/GanttChart.tsx` contains the exact string `type?: 'task' | 'milestone'`
    `packages/gantt-lib/src/types/index.ts` contains the exact string `type?: 'task' | 'milestone'`
    `packages/gantt-lib/src/utils/taskType.ts` exports `TASK_TYPE_DEFAULT`, `getTaskType`, `isMilestoneTask`, and `normalizeTaskDatesForType`
    `packages/gantt-lib/src/utils/taskType.ts` contains logic that returns `'task'` when `type` is undefined
    `packages/gantt-lib/src/utils/taskType.ts` contains milestone normalization that writes `endDate: startDate`
  </acceptance_criteria>
  <action>
Implement the phase contract narrowly, per locked scope decisions L-01/L-02/L-03/L-05:

- L-01: milestone support only; do not add `project` typing, do not redesign parent/group behavior.
- L-02: use explicit task typing, not same-day inference.
- L-03: milestone is single-date only.
- L-05: existing `parentId` hierarchy remains the project/group mechanism.

Make these exact changes:

1. In `packages/gantt-lib/src/components/GanttChart/GanttChart.tsx`, add `type?: 'task' | 'milestone'` to the public `Task` interface with a doc comment that milestone is single-date and defaults to regular `'task'` behavior when omitted.
2. In `packages/gantt-lib/src/types/index.ts`, mirror the same `type?: 'task' | 'milestone'` field so public package types and component-local types stay in sync.
3. Create `packages/gantt-lib/src/utils/taskType.ts` with a small shared helper surface for later waves:
   - `export const TASK_TYPE_DEFAULT = 'task'`
   - `export function getTaskType(task): 'task' | 'milestone'`
   - `export function isMilestoneTask(task): boolean`
   - `export function normalizeTaskDatesForType<TTask extends { startDate: string | Date; endDate: string | Date; type?: 'task' | 'milestone' }>(task: TTask): TTask`
4. `normalizeTaskDatesForType` must enforce exact milestone behavior: if `task.type === 'milestone'`, normalize to a single UTC day by returning a copy with `endDate` set to the same calendar day as `startDate`. For non-milestones, return the task unchanged.
5. Do not introduce validation that rejects parent milestones here. Keep the scope to explicit typing + normalization helper only; later waves will decide render precedence when a task also has children.

Avoid:
- adding `project` to any union
- inferring milestone from `startDate === endDate`
- changing dependency math or hierarchy helpers in this task
  </action>
  <verify>
    <automated>rg -n "type\\?: 'task' \\| 'milestone'|TASK_TYPE_DEFAULT|getTaskType|isMilestoneTask|normalizeTaskDatesForType|endDate: startDate" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx packages/gantt-lib/src/types/index.ts packages/gantt-lib/src/utils/taskType.ts</automated>
  </verify>
  <done>Shared milestone contract exists in both public Task interfaces, defaults to `task`, and exposes one normalization helper file for the remaining plans.</done>
</task>

<task type="auto">
  <name>Task 2: Create milestone test targets with exact behavior names for later implementation waves</name>
  <read_first>
    .planning/phases/29-milestones-type-tasks/29-VALIDATION.md
    .planning/phases/29-milestones-type-tasks/29-RESEARCH.md
    packages/gantt-lib/src/__tests__/geometry.test.ts
    packages/gantt-lib/src/__tests__/dependencyLines.test.tsx
    packages/gantt-lib/src/__tests__/taskListDuration.test.tsx
    packages/gantt-lib/src/__tests__/useTaskDrag.test.ts
  </read_first>
  <files>packages/gantt-lib/src/__tests__/taskRowMilestone.test.tsx, packages/gantt-lib/src/__tests__/useTaskDragMilestone.test.ts, packages/gantt-lib/src/__tests__/dependencyLinesMilestone.test.tsx, packages/gantt-lib/src/__tests__/taskListMilestone.test.tsx, packages/gantt-lib/src/__tests__/sampleMilestones.test.tsx</files>
  <acceptance_criteria>
    `packages/gantt-lib/src/__tests__/taskRowMilestone.test.tsx` exists and contains the string `renders milestone as diamond`
    `packages/gantt-lib/src/__tests__/useTaskDragMilestone.test.ts` exists and contains the string `forces milestone resize attempts into move mode`
    `packages/gantt-lib/src/__tests__/dependencyLinesMilestone.test.tsx` exists and contains the string `uses milestone anchor points for dependency endpoints`
    `packages/gantt-lib/src/__tests__/taskListMilestone.test.tsx` exists and contains the string `keeps end date synchronized with start date for milestones`
    `packages/gantt-lib/src/__tests__/sampleMilestones.test.tsx` exists and contains the string `sample tasks include milestone entries`
  </acceptance_criteria>
  <action>
Create the five milestone-specific test files named in `29-VALIDATION.md`. These are wave-target files, so keep them lightweight but concrete: each file must contain real `describe`/`it` blocks with the final behavior names, not placeholder comments.

Required behavior names to encode exactly:
- `taskRowMilestone.test.tsx`: `renders milestone as diamond`, `keeps regular same-day task rectangular`
- `useTaskDragMilestone.test.ts`: `forces milestone resize attempts into move mode`, `keeps milestone drag result single-date`
- `dependencyLinesMilestone.test.tsx`: `uses milestone anchor points for dependency endpoints`, `keeps dependency semantics unchanged for FS SS FF SF`
- `taskListMilestone.test.tsx`: `keeps end date synchronized with start date for milestones`, `prevents independent duration edits for milestones`
- `sampleMilestones.test.tsx`: `sample tasks include milestone entries`, `sample milestones are normalized to a single date`

Scaffold these files so later plans can fill in the implementation with minimal discovery:
- import the exact production modules they will exercise
- include basic task fixtures with `type: 'milestone'`
- use `it.skip(...)` only where a later wave must finish the assertion; do not leave files empty
- keep filenames and test titles stable because later plan verification commands reference them directly

Do not start changing production behavior in this task beyond using the new `task.type` contract in fixtures.
  </action>
  <verify>
    <automated>rg -n "renders milestone as diamond|keeps regular same-day task rectangular|forces milestone resize attempts into move mode|keeps milestone drag result single-date|uses milestone anchor points for dependency endpoints|keeps dependency semantics unchanged for FS SS FF SF|keeps end date synchronized with start date for milestones|prevents independent duration edits for milestones|sample tasks include milestone entries|sample milestones are normalized to a single date" packages/gantt-lib/src/__tests__/taskRowMilestone.test.tsx packages/gantt-lib/src/__tests__/useTaskDragMilestone.test.ts packages/gantt-lib/src/__tests__/dependencyLinesMilestone.test.tsx packages/gantt-lib/src/__tests__/taskListMilestone.test.tsx packages/gantt-lib/src/__tests__/sampleMilestones.test.tsx</automated>
  </verify>
  <done>All milestone test target files exist with stable filenames and exact behavior titles that the next waves will green.</done>
</task>

</tasks>

<verification>
Run `rg -n "type\\?: 'task' \\| 'milestone'" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx packages/gantt-lib/src/types/index.ts`.
Run `rg -n "renders milestone as diamond|forces milestone resize attempts into move mode|sample tasks include milestone entries" packages/gantt-lib/src/__tests__/taskRowMilestone.test.tsx packages/gantt-lib/src/__tests__/useTaskDragMilestone.test.ts packages/gantt-lib/src/__tests__/sampleMilestones.test.tsx`.
</verification>

<success_criteria>
The repo exposes one explicit milestone contract, one shared normalization helper file, and all milestone test targets exist under the exact filenames expected by `29-VALIDATION.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/29-milestones-type-tasks/29-01-SUMMARY.md`
</output>
