---
phase: 29-milestones-type-tasks
plan: 03
type: execute
wave: 3
depends_on: ["29-02"]
files_modified:
  - packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
  - packages/gantt-lib/src/components/TaskList/columns/createBuiltInColumns.tsx
  - packages/website/src/data/sampleTasks.ts
  - packages/gantt-lib/src/__tests__/taskListMilestone.test.tsx
  - packages/gantt-lib/src/__tests__/sampleMilestones.test.tsx
  - docs/reference/02-task-interface.md
  - docs/reference/03-dependencies.md
  - docs/reference/04-props.md
autonomous: true
requirements: [PH29-4, PH29-6]

must_haves:
  truths:
    - "Task list edits keep milestone as one date and do not allow independent duration growth"
    - "Demo/sample data shows milestone authoring in a real task array"
    - "Docs describe milestone typing, single-date invariant, unchanged dependency semantics, and unchanged parentId project/group behavior"
  artifacts:
    - path: "packages/gantt-lib/src/components/TaskList/TaskListRow.tsx"
      provides: "Milestone-aware task list editing and display rules"
      contains: "milestone"
    - path: "packages/website/src/data/sampleTasks.ts"
      provides: "Website sample milestone tasks"
      contains: "type: 'milestone'"
    - path: "docs/reference/02-task-interface.md"
      provides: "Task interface documentation for milestone typing"
      contains: "milestone"
  key_links:
    - from: "packages/gantt-lib/src/components/TaskList/TaskListRow.tsx"
      to: "packages/gantt-lib/src/utils/taskType.ts"
      via: "single-date milestone editing rules"
      pattern: "isMilestoneTask"
    - from: "packages/website/src/data/sampleTasks.ts"
      to: "docs/reference/02-task-interface.md"
      via: "documented milestone examples"
      pattern: "milestone"
---

<objective>
Закрыть milestone support в task list, sample data и docs.

Purpose: После этой волны milestone должен быть не только видимым на chart, но и корректно редактируемым/объяснённым для потребителя библиотеки.
Output: milestone-aware TaskList row behavior, sample milestone tasks, green task-list/sample tests, updated docs.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-milestones-type-tasks/29-RESEARCH.md
@.planning/phases/29-milestones-type-tasks/29-VALIDATION.md
@.planning/phases/29-milestones-type-tasks/29-02-PLAN.md
@packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
@packages/gantt-lib/src/components/TaskList/columns/createBuiltInColumns.tsx
@packages/website/src/data/sampleTasks.ts

<interfaces>
From `packages/gantt-lib/src/components/TaskList/columns/createBuiltInColumns.tsx`:
```typescript
export const BUILT_IN_COLUMN_WIDTHS: Record<string, number> = {
  number: 40,
  name: 200,
  startDate: 90,
  endDate: 90,
  duration: 60,
  progress: 50,
  dependencies: 120,
  actions: 80,
};
```

From `packages/gantt-lib/src/components/TaskList/TaskListRow.tsx`:
```typescript
const startDateCell = <DatePicker ... onChange={handleStartDateChange} />
const endDateCell = <DatePicker ... onChange={handleEndDateChange} />
const durationCell = (
  <div className="gantt-tl-cell gantt-tl-cell-duration" onClick={handleDurationClick}>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make TaskListRow milestone-aware without redesigning the column system</name>
  <read_first>
    packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
    packages/gantt-lib/src/components/TaskList/columns/createBuiltInColumns.tsx
    packages/gantt-lib/src/utils/taskType.ts
    packages/gantt-lib/src/__tests__/taskListMilestone.test.tsx
    .planning/phases/29-milestones-type-tasks/29-RESEARCH.md
  </read_first>
  <files>packages/gantt-lib/src/components/TaskList/TaskListRow.tsx, packages/gantt-lib/src/components/TaskList/columns/createBuiltInColumns.tsx, packages/gantt-lib/src/__tests__/taskListMilestone.test.tsx</files>
  <acceptance_criteria>
    `packages/gantt-lib/src/components/TaskList/TaskListRow.tsx` uses `isMilestoneTask(`
    `packages/gantt-lib/src/components/TaskList/TaskListRow.tsx` contains milestone-specific logic that synchronizes `endDate` with `startDate`
    `packages/gantt-lib/src/components/TaskList/TaskListRow.tsx` prevents independent duration editing for milestones
    `packages/gantt-lib/src/__tests__/taskListMilestone.test.tsx` actively asserts `keeps end date synchronized with start date for milestones`
    `packages/gantt-lib/src/__tests__/taskListMilestone.test.tsx` actively asserts `prevents independent duration edits for milestones`
  </acceptance_criteria>
  <action>
Implement task-list milestone rules per locked decisions L-01/L-03/L-05, while preserving the existing built-in column pipeline:

1. In `packages/gantt-lib/src/components/TaskList/TaskListRow.tsx`:
   - import `isMilestoneTask` and `normalizeTaskDatesForType`
   - when editing a milestone start date, emit an updated task where `startDate` and `endDate` both become the chosen date
   - when editing a milestone end date, do not allow a different date to persist; either disable the picker or mirror the chosen value back to the same single date
   - prevent independent duration edits for milestones; keep the column visible but render a fixed read-only `1д` display so the layout stays unchanged
   - keep progress/dependencies/editors otherwise unchanged
2. In `packages/gantt-lib/src/components/TaskList/columns/createBuiltInColumns.tsx`, keep the existing built-in column order. Only adjust labels if truly needed; do not add/remove columns for milestones.
3. Update `packages/gantt-lib/src/__tests__/taskListMilestone.test.tsx` so it proves:
   - changing milestone start date synchronizes end date
   - duration editor cannot expand a milestone into multiple days
   - same-day non-milestone tasks still keep normal duration behavior

Avoid:
- hiding columns just for milestones
- redesigning TaskList architecture
- changing project/parent row semantics
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npm test -- src/__tests__/taskListMilestone.test.tsx -x</automated>
  </verify>
  <done>TaskList keeps milestones single-date and move-only in practice, without changing the existing built-in column structure.</done>
</task>

<task type="auto">
  <name>Task 2: Add milestone examples to sample data and document the public API contract</name>
  <read_first>
    packages/website/src/data/sampleTasks.ts
    packages/gantt-lib/src/__tests__/sampleMilestones.test.tsx
    docs/reference/02-task-interface.md
    docs/reference/03-dependencies.md
    docs/reference/04-props.md
    .planning/phases/29-milestones-type-tasks/29-RESEARCH.md
  </read_first>
  <files>packages/website/src/data/sampleTasks.ts, packages/gantt-lib/src/__tests__/sampleMilestones.test.tsx, docs/reference/02-task-interface.md, docs/reference/03-dependencies.md, docs/reference/04-props.md</files>
  <acceptance_criteria>
    `packages/website/src/data/sampleTasks.ts` contains at least one exact string `type: 'milestone'`
    every sample milestone in `packages/website/src/data/sampleTasks.ts` has matching start and end dates
    `packages/gantt-lib/src/__tests__/sampleMilestones.test.tsx` actively asserts `sample tasks include milestone entries`
    `docs/reference/02-task-interface.md` documents `type?: 'task' | 'milestone'`
    `docs/reference/03-dependencies.md` states milestone dependency semantics stay unchanged
    `docs/reference/04-props.md` states `parentId` hierarchy remains the project/group mechanism for this phase
  </acceptance_criteria>
  <action>
Document and demonstrate milestone usage with exact scope wording:

1. In `packages/website/src/data/sampleTasks.ts`, add milestone entries to the sample task array. Use real examples that make dependency geometry visible, for example an inspection/approval milestone or handoff milestone. Every sample milestone must:
   - include `type: 'milestone'`
   - use the same date for `startDate` and `endDate`
   - participate in at least one dependency so the demo exercises milestone endpoints
2. Update `packages/gantt-lib/src/__tests__/sampleMilestones.test.tsx` so it proves the sample data contains milestone entries and that all milestone examples are single-date.
3. Update docs:
   - `docs/reference/02-task-interface.md`: add the `type?: 'task' | 'milestone'` field, explain default `'task'`, explain that milestone is single-date and rendered as a diamond
   - `docs/reference/03-dependencies.md`: explicitly say milestone does not change FS/SS/FF/SF semantics; only the rendered endpoints differ
   - `docs/reference/04-props.md`: explain that existing `parentId` hierarchy continues to represent parent/project grouping; milestone support does not introduce a new project/group type

Avoid:
- promising a `project` type
- documenting zero-duration milestones
- changing docs to imply dependency rules changed
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npm test -- src/__tests__/sampleMilestones.test.tsx -x</automated>
  </verify>
  <done>Sample data demonstrates milestone authoring, docs match the shipped contract, and users can discover milestone support without reading implementation code.</done>
</task>

</tasks>

<verification>
Run `cd packages/gantt-lib && npm test -- src/__tests__/taskListMilestone.test.tsx src/__tests__/sampleMilestones.test.tsx -x`.
Run `rg -n "type: 'milestone'|type\\?: 'task' \\| 'milestone'|dependency semantics stay unchanged|parentId" packages/website/src/data/sampleTasks.ts docs/reference/02-task-interface.md docs/reference/03-dependencies.md docs/reference/04-props.md`.
</verification>

<success_criteria>
Milestones are editable as single-date items in TaskList, documented in the public API, and visible in the website sample data with unchanged dependency semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/29-milestones-type-tasks/29-03-SUMMARY.md`
</output>
