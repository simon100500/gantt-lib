---
phase: 29-milestones-type-tasks
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - packages/gantt-lib/src/utils/geometry.ts
  - packages/gantt-lib/src/components/TaskRow/TaskRow.tsx
  - packages/gantt-lib/src/components/TaskRow/TaskRow.css
  - packages/gantt-lib/src/components/DependencyLines/DependencyLines.tsx
  - packages/gantt-lib/src/hooks/useTaskDrag.ts
  - packages/gantt-lib/src/__tests__/geometry.test.ts
  - packages/gantt-lib/src/__tests__/taskRowMilestone.test.tsx
  - packages/gantt-lib/src/__tests__/useTaskDragMilestone.test.ts
  - packages/gantt-lib/src/__tests__/dependencyLinesMilestone.test.tsx
autonomous: true
requirements: [PH29-2, PH29-3, PH29-5]

must_haves:
  truths:
    - "Milestone on the chart is rendered as a visible diamond on one date"
    - "Regular one-day tasks remain rectangular unless explicitly typed as milestone"
    - "Milestone drag is move-only; resize handles and resize branches are disabled"
    - "Dependency arrows still follow FS/SS/FF/SF semantics, but milestone endpoints use milestone geometry instead of bar edges"
  artifacts:
    - path: "packages/gantt-lib/src/utils/geometry.ts"
      provides: "Milestone geometry helpers separate from rectangular bar math"
      contains: "calculateMilestoneGeometry"
    - path: "packages/gantt-lib/src/components/TaskRow/TaskRow.tsx"
      provides: "Diamond milestone rendering branch"
      contains: "gantt-tr-milestone"
    - path: "packages/gantt-lib/src/hooks/useTaskDrag.ts"
      provides: "Move-only milestone drag gating"
      contains: "isMilestoneTask"
  key_links:
    - from: "packages/gantt-lib/src/components/TaskRow/TaskRow.tsx"
      to: "packages/gantt-lib/src/utils/geometry.ts"
      via: "calculateMilestoneGeometry"
      pattern: "calculateMilestoneGeometry"
    - from: "packages/gantt-lib/src/hooks/useTaskDrag.ts"
      to: "packages/gantt-lib/src/utils/taskType.ts"
      via: "milestone drag mode gating"
      pattern: "isMilestoneTask"
    - from: "packages/gantt-lib/src/components/DependencyLines/DependencyLines.tsx"
      to: "packages/gantt-lib/src/utils/geometry.ts"
      via: "milestone endpoint geometry"
      pattern: "milestone"
---

<objective>
Реализовать chart-side milestone behavior: diamond rendering, move-only drag, and dependency endpoint geometry.

Purpose: Эта волна закрывает то, что пользователь видит и двигает на timeline, не трогая parent/project semantics и не переписывая dependency math.
Output: milestone geometry helper, TaskRow diamond branch, move-only drag gating, dependency endpoint updates, green milestone chart tests.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-milestones-type-tasks/29-RESEARCH.md
@.planning/phases/29-milestones-type-tasks/29-VALIDATION.md
@.planning/phases/29-milestones-type-tasks/29-01-PLAN.md
@packages/gantt-lib/src/components/TaskRow/TaskRow.tsx
@packages/gantt-lib/src/components/DependencyLines/DependencyLines.tsx
@packages/gantt-lib/src/hooks/useTaskDrag.ts
@packages/gantt-lib/src/utils/geometry.ts

<interfaces>
From `packages/gantt-lib/src/components/TaskRow/TaskRow.tsx`:
```typescript
const { left, width } = calculateTaskBar(taskStartDate, taskEndDate, monthStart, dayWidth);
const {
  isDragging,
  dragMode,
  currentLeft,
  currentWidth,
  dragHandleProps,
} = useTaskDrag(...)
```

From `packages/gantt-lib/src/hooks/useTaskDrag.ts`:
```typescript
let mode: 'move' | 'resize-left' | 'resize-right' | null = null;
if (mode === 'resize-left' || mode === 'resize-right') {
  const currentTask = allTasks.find(t => t.id === taskId);
  if (currentTask && isTaskParent(taskId, allTasks)) {
    mode = 'move';
  }
}
```

From `packages/gantt-lib/src/components/DependencyLines/DependencyLines.tsx`:
```typescript
const fromX = (edge.type === 'SS' || edge.type === 'SF')
  ? predecessor.left
  : predecessor.right;
const toX = (edge.type === 'FF' || edge.type === 'SF')
  ? successor.right
  : successor.left;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement milestone geometry and TaskRow diamond rendering</name>
  <read_first>
    packages/gantt-lib/src/utils/geometry.ts
    packages/gantt-lib/src/components/TaskRow/TaskRow.tsx
    packages/gantt-lib/src/components/TaskRow/TaskRow.css
    packages/gantt-lib/src/utils/taskType.ts
    packages/gantt-lib/src/__tests__/geometry.test.ts
    packages/gantt-lib/src/__tests__/taskRowMilestone.test.tsx
    .planning/phases/29-milestones-type-tasks/29-RESEARCH.md
  </read_first>
  <files>packages/gantt-lib/src/utils/geometry.ts, packages/gantt-lib/src/components/TaskRow/TaskRow.tsx, packages/gantt-lib/src/components/TaskRow/TaskRow.css, packages/gantt-lib/src/__tests__/geometry.test.ts, packages/gantt-lib/src/__tests__/taskRowMilestone.test.tsx</files>
  <acceptance_criteria>
    `packages/gantt-lib/src/utils/geometry.ts` exports `calculateMilestoneGeometry`
    `packages/gantt-lib/src/components/TaskRow/TaskRow.tsx` contains the class string `gantt-tr-milestone`
    `packages/gantt-lib/src/components/TaskRow/TaskRow.tsx` uses `isMilestoneTask(`
    `packages/gantt-lib/src/components/TaskRow/TaskRow.tsx` does not render `gantt-tr-resizeHandle` inside the milestone branch
    `packages/gantt-lib/src/__tests__/taskRowMilestone.test.tsx` contains active assertions for `renders milestone as diamond` and `keeps regular same-day task rectangular`
  </acceptance_criteria>
  <action>
Implement milestone visuals per locked decisions L-01/L-02/L-03:

- use explicit `task.type === 'milestone'`
- render milestone as a diamond on a single date
- keep regular same-day tasks rectangular
- no resize handles for milestones

Make these exact changes:

1. In `packages/gantt-lib/src/utils/geometry.ts`, add `calculateMilestoneGeometry(taskDate, monthStart, dayWidth, size = 14)` that derives:
   - `centerX` from the single-day bar position
   - `left` and `right` from a fixed-size diamond box
   - `size` for rendering
   - keep `calculateTaskBar()` unchanged for normal tasks
2. In `packages/gantt-lib/src/components/TaskRow/TaskRow.tsx`:
   - import `isMilestoneTask` and `normalizeTaskDatesForType`
   - normalize milestone dates before geometry calculations so `endDate` mirrors `startDate`
   - add a dedicated milestone render branch before the standard bar JSX
   - render a visible diamond element with class `gantt-tr-milestone`, using fixed size and `transform: rotate(45deg)` in CSS or equivalent styling
   - do not render progress fill or resize handles in the milestone branch
   - keep label/name/locked handling working around the diamond; if a locked icon is shown, position it relative to the milestone geometry rather than bar width
3. In `packages/gantt-lib/src/components/TaskRow/TaskRow.css`, add the milestone class styling with a fixed square rotated into a diamond. Keep ASCII-only CSS and reuse existing color variables where possible.
4. Update `packages/gantt-lib/src/__tests__/geometry.test.ts` and `packages/gantt-lib/src/__tests__/taskRowMilestone.test.tsx` so they prove:
   - milestone geometry has a fixed visible size
   - same-day non-milestone tasks still use rectangular geometry
   - milestone rendering path is active only when `type: 'milestone'`

Avoid:
- changing parent aggregate rendering rules
- using zero-width milestones
- inferring milestone from same-day dates alone
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npm test -- src/__tests__/geometry.test.ts src/__tests__/taskRowMilestone.test.tsx -x</automated>
  </verify>
  <done>Milestones render as diamonds with fixed geometry, normal tasks keep existing rectangle behavior, and milestone render tests are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make milestone drag move-only and update dependency endpoints without changing dependency semantics</name>
  <read_first>
    packages/gantt-lib/src/hooks/useTaskDrag.ts
    packages/gantt-lib/src/components/DependencyLines/DependencyLines.tsx
    packages/gantt-lib/src/utils/geometry.ts
    packages/gantt-lib/src/utils/taskType.ts
    packages/gantt-lib/src/__tests__/useTaskDragMilestone.test.ts
    packages/gantt-lib/src/__tests__/dependencyLinesMilestone.test.tsx
    .planning/phases/29-milestones-type-tasks/29-RESEARCH.md
  </read_first>
  <files>packages/gantt-lib/src/hooks/useTaskDrag.ts, packages/gantt-lib/src/components/DependencyLines/DependencyLines.tsx, packages/gantt-lib/src/__tests__/useTaskDragMilestone.test.ts, packages/gantt-lib/src/__tests__/dependencyLinesMilestone.test.tsx</files>
  <acceptance_criteria>
    `packages/gantt-lib/src/hooks/useTaskDrag.ts` contains milestone gating that rewrites resize attempts to `move`
    `packages/gantt-lib/src/hooks/useTaskDrag.ts` uses `isMilestoneTask(`
    `packages/gantt-lib/src/components/DependencyLines/DependencyLines.tsx` contains milestone endpoint handling separate from plain `calculateTaskBar`
    `packages/gantt-lib/src/__tests__/useTaskDragMilestone.test.ts` actively asserts `forces milestone resize attempts into move mode`
    `packages/gantt-lib/src/__tests__/dependencyLinesMilestone.test.tsx` actively asserts `uses milestone anchor points for dependency endpoints`
  </acceptance_criteria>
  <action>
Implement interaction and dependency geometry per locked decisions L-03/L-04:

- milestone is move-only on chart
- no resize behavior
- dependency semantics stay unchanged
- only endpoint geometry changes where a milestone participates

Make these exact changes:

1. In `packages/gantt-lib/src/hooks/useTaskDrag.ts`:
   - import `isMilestoneTask`
   - when drag intent resolves to `resize-left` or `resize-right`, force `mode = 'move'` for milestone tasks exactly like parent tasks are already forced to move
   - keep the completion path single-date for milestone tasks by normalizing drag results so `endDate === startDate`
   - do not add a new drag mode; keep the existing `'move' | 'resize-left' | 'resize-right'` contract
2. In `packages/gantt-lib/src/components/DependencyLines/DependencyLines.tsx`:
   - detect milestone tasks explicitly via `task.type`
   - derive milestone anchor points from milestone geometry, not rectangular bar edges
   - keep current link semantics unchanged: FS/SS still target the start side, FF/SF still target the finish side
   - for milestone endpoints, use left/right midpoint of the diamond box rather than the center point so arrow direction stays consistent
3. Update `packages/gantt-lib/src/__tests__/useTaskDragMilestone.test.ts` and `packages/gantt-lib/src/__tests__/dependencyLinesMilestone.test.tsx` to green the exact milestone behaviors defined in plan 29-01.

Avoid:
- changing `calculateSuccessorDate`, `universalCascade`, or dependency lag rules
- introducing milestone-specific link semantics
- allowing resize handles to sneak back in through drag edge detection
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npm test -- src/__tests__/useTaskDragMilestone.test.ts src/__tests__/dependencyLinesMilestone.test.tsx -x</automated>
  </verify>
  <done>Milestones drag only by moving, never resizing, and dependency lines connect to milestone-aware endpoints while FS/SS/FF/SF scheduling rules remain unchanged.</done>
</task>

</tasks>

<verification>
Run `cd packages/gantt-lib && npm test -- src/__tests__/geometry.test.ts src/__tests__/taskRowMilestone.test.tsx src/__tests__/useTaskDragMilestone.test.ts src/__tests__/dependencyLinesMilestone.test.tsx -x`.
Run `rg -n "gantt-tr-milestone|calculateMilestoneGeometry|isMilestoneTask" packages/gantt-lib/src/components/TaskRow/TaskRow.tsx packages/gantt-lib/src/hooks/useTaskDrag.ts packages/gantt-lib/src/utils/geometry.ts`.
</verification>

<success_criteria>
Timeline milestones are visible, draggable only as single-date diamonds, and dependency arrows attach to milestone-aware geometry without changing existing dependency math.
</success_criteria>

<output>
After completion, create `.planning/phases/29-milestones-type-tasks/29-02-SUMMARY.md`
</output>
