---
phase: 21
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - packages/gantt-lib/src/utils/__tests__/createDateKey.test.ts
  - packages/gantt-lib/src/utils/__tests__/createIsWeekendPredicate.test.ts
autonomous: true
requirements:
  - CAL-01
  - CAL-02
  - CAL-03
  - CAL-04
  - CAL-05

must_haves:
  truths:
    - Тестовые файлы созданы с описанием ожидаемого поведения
    - Тесты покрывают все требования CAL-01 до CAL-05
    - Запуск `npm test -- createIsWeekendPredicate` показывает failing tests
  artifacts:
    - path: packages/gantt-lib/src/utils/__tests__/createDateKey.test.ts
      provides: Тесты для createDateKey utility
      min_lines: 40
    - path: packages/gantt-lib/src/utils/__tests__/createIsWeekendPredicate.test.ts
      provides: Тесты для createIsWeekendPredicate utility
      min_lines: 80
  key_links:
    - from: createIsWeekendPredicate.test.ts
      to: dateUtils.ts
      via: import statements
      pattern: import.*createDateKey.*from.*dateUtils
---

<objective>
Создать тестовые stubs для утилит кастомных выходных дней (TDD RED phase).

Цель: Подготовить тестовую инфраструктуру для реализации утилит `createDateKey` и `createIsWeekendPredicate`. Это Wave 0 план — создаем failing tests перед реализацией.

Output: Два тестовых файла с failing tests, которые описывают ожидаемое поведение новых утилит.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-custom-weekend-calendar/21-CONTEXT.md
@.planning/phases/21-custom-weekend-calendar/21-RESEARCH.md

# Ссылки на существующие паттерны
@packages/gantt-lib/src/utils/dateUtils.ts — существующие утилиты для работы с датами
@packages/gantt-lib/src/__tests__/dateUtils.test.ts — примеры тестов для date утилит
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Создать тесты для createDateKey utility</name>
  <files>packages/gantt-lib/src/utils/__tests__/createDateKey.test.ts</files>
  <read_first>
    - packages/gantt-lib/src/utils/__tests__/dateUtils.test.ts (паттерны написания тестов)
  </read_first>
  <behavior>
    - Test 1: createDateKey генерирует уникальный ключ для каждой даты в формате "YYYY-M-D"
    - Test 2: createDateKey использует UTC методы (getUTCFullYear, getUTCMonth, getUTCDate)
    - Test 3: createDateKey создает одинаковый ключ для одной и той же даты
    - Test 4: createDateKey создает разные ключи для разных дат
  </behavior>
  <action>
    Создать файл packages/gantt-lib/src/utils/__tests__/createDateKey.test.ts со следующими тестами:

    1. Тест на генерацию ключа в формате "YYYY-M-D":
       ```typescript
       test('createDateKey generates key in YYYY-M-D format', () => {
         const date = new Date(Date.UTC(2026, 2, 15)); // March 15, 2026
         const key = createDateKey(date);
         expect(key).toBe('2026-2-15');
       });
       ```

    2. Тест на использование UTC методов:
       ```typescript
       test('createDateKey uses UTC methods', () => {
         // Создаем дату в локальном часовом поясе, которая отличается от UTC
         const date = new Date('2026-03-15T00:00:00-05:00'); // UTC: 2026-03-15T05:00:00Z
         const key = createDateKey(date);
         // Должен использовать UTC дату (15 марта), не локальную (14 марта или 15 марта в зависимости от TZ)
         const utcDate = new Date(Date.UTC(2026, 2, 15));
         expect(key).toBe(createDateKey(utcDate));
       });
       ```

    3. Тест на идентичность ключей для одной даты:
       ```typescript
       test('createDateKey returns same key for same date', () => {
         const date1 = new Date(Date.UTC(2026, 2, 15));
         const date2 = new Date(Date.UTC(2026, 2, 15));
         expect(createDateKey(date1)).toBe(createDateKey(date2));
       });
       ```

    4. Тест на различие ключей для разных дат:
       ```typescript
       test('createDateKey returns different keys for different dates', () => {
         const date1 = new Date(Date.UTC(2026, 2, 15));
         const date2 = new Date(Date.UTC(2026, 2, 16));
         expect(createDateKey(date1)).not.toBe(createDateKey(date2));
       });
       ```

    Импортировать createDateKey из '../dateUtils' (когда будет реализован).

    Использовать vitest паттерн: describe() для группировки тестов, test() для отдельных кейсов.
  </action>
  <verify>
    <automated>npm test -- createDateKey.test.ts --run</automated>
  </verify>
  <done>
    - Файл createDateKey.test.ts создан с 4 тестами
    - Тесты failing (RED phase) — createDateKey еще не реализован
    - npm test показывает 4 failing tests для createDateKey
  </done>
</task>

<task type="auto" tdd="true">
  <name>Создать тесты для createIsWeekendPredicate utility</name>
  <files>packages/gantt-lib/src/utils/__tests__/createIsWeekendPredicate.test.ts</files>
  <read_first>
    - packages/gantt-lib/src/utils/__tests__/dateUtils.test.ts (паттерны написания тестов)
  </read_first>
  <behavior>
    - Test 1: Без пропсов возвращает предикат для Saturday/Sunday (default)
    - Test 2: С weekends[] добавляет к default Saturday/Sunday
    - Test 3: С workdays[] исключает из default Saturday/Sunday
    - Test 4: С обоими массивами: workdays имеет приоритет над weekends
    - Test 5: С isWeekend predicate использует кастомный предикат (highest priority)
    - Test 6: Пустые массивы fallback к default behavior
    - Test 7: Предикат работает с multi-month date ranges
  </behavior>
  <action>
    Создать файл packages/gantt-lib/src/utils/__tests__/createIsWeekendPredicate.test.ts со следующими тестами:

    1. Тест на default behavior (Saturday/Sunday):
       ```typescript
       test('returns Saturday/Sunday predicate when no props provided', () => {
       const predicate = createIsWeekendPredicate({});
       const saturday = new Date(Date.UTC(2026, 2, 15)); // Saturday
       const sunday = new Date(Date.UTC(2026, 2, 16)); // Sunday
       const monday = new Date(Date.UTC(2026, 2, 17)); // Monday
       expect(predicate(saturday)).toBe(true);
       expect(predicate(sunday)).toBe(true);
       expect(predicate(monday)).toBe(false);
       });
       ```

    2. Тест на добавление кастомных выходных:
       ```typescript
       test('adds custom weekends to default Saturday/Sunday', () => {
       const march8 = new Date(Date.UTC(2026, 2, 8)); // Monday (holiday)
       const predicate = createIsWeekendPredicate({
       weekends: [march8]
       });
       expect(predicate(march8)).toBe(true); // Monday is now weekend
       const regularMonday = new Date(Date.UTC(2026, 2, 17));
       expect(predicate(regularMonday)).toBe(false); // Regular Monday still workday
       const saturday = new Date(Date.UTC(2026, 2, 15));
       expect(predicate(saturday)).toBe(true); // Default Saturday still weekend
       });
       ```

    3. Тест на исключение выходных:
       ```typescript
       test('excludes dates from default weekends when workdays provided', () => {
       const march15 = new Date(Date.UTC(2026, 2, 15)); // Saturday
       const predicate = createIsWeekendPredicate({
       workdays: [march15]
       });
       expect(predicate(march15)).toBe(false); // Saturday is now workday
       const regularSaturday = new Date(Date.UTC(2026, 2, 22));
       expect(predicate(regularSaturday)).toBe(true); // Regular Saturday still weekend
       });
       ```

    4. Тест на приоритет workdays над weekends:
       ```typescript
       test('workdays takes precedence over weekends when date in both arrays', () => {
       const march15 = new Date(Date.UTC(2026, 2, 15)); // Saturday
       const predicate = createIsWeekendPredicate({
       weekends: [march15],
       workdays: [march15] // Same date in both arrays
       });
       expect(predicate(march15)).toBe(false); // workdays wins
       });
       ```

    5. Тест на использование кастомного предиката:
       ```typescript
       test('uses custom predicate when isWeekend provided (highest priority)', () => {
       const customPredicate = (date: Date) => date.getUTCDay() === 0; // Sunday only
       const predicate = createIsWeekendPredicate({
       isWeekend: customPredicate,
       weekends: [new Date(Date.UTC(2026, 2, 15))], // Ignored
       workdays: [new Date(Date.UTC(2026, 2, 8))]   // Ignored
       });
       const saturday = new Date(Date.UTC(2026, 2, 15));
       const sunday = new Date(Date.UTC(2026, 2, 16));
       expect(predicate(saturday)).toBe(false); // Not Sunday
       expect(predicate(sunday)).toBe(true);    // Sunday
       });
       ```

    6. Тест на пустые массивы:
       ```typescript
       test('empty arrays fallback to default behavior', () => {
       const predicate = createIsWeekendPredicate({
       weekends: [],
       workdays: []
       });
       const saturday = new Date(Date.UTC(2026, 2, 15));
       const sunday = new Date(Date.UTC(2026, 2, 16));
       const monday = new Date(Date.UTC(2026, 2, 17));
       expect(predicate(saturday)).toBe(true);
       expect(predicate(sunday)).toBe(true);
       expect(predicate(monday)).toBe(false);
       });
       ```

    7. Тест на multi-month ranges:
       ```typescript
       test('works correctly across month boundaries', () => {
       const feb28 = new Date(Date.UTC(2026, 1, 28)); // Saturday
       const mar1 = new Date(Date.UTC(2026, 2, 1));  // Sunday
       const mar8 = new Date(Date.UTC(2026, 2, 8));  // Monday (holiday)
       const predicate = createIsWeekendPredicate({
       weekends: [mar8]
       });
       expect(predicate(feb28)).toBe(true); // Default Saturday
       expect(predicate(mar1)).toBe(true);  // Default Sunday
       expect(predicate(mar8)).toBe(true);  // Custom holiday Monday
       });
       ```

    Импортировать createIsWeekendPredicate и createDateKey из '../dateUtils'.

    Использовать интерфейс WeekendConfig:
    ```typescript
    interface WeekendConfig {
      weekends?: Date[];
      workdays?: Date[];
      isWeekend?: (date: Date) => boolean;
    }
    ```
  </action>
  <verify>
    <automated>npm test -- createIsWeekendPredicate.test.ts --run</automated>
  </verify>
  <done>
    - Файл createIsWeekendPredicate.test.ts создан с 7 тестами
    - Тесты failing (RED phase) — createIsWeekendPredicate еще не реализован
    - npm test показывает 7 failing tests для createIsWeekendPredicate
  </done>
</task>

</tasks>

<verification>
Общая проверка фазы:
- Запуск `npm test -- --run` показывает все новые тесты как failing
- Тестовые файлы созданы в правильной директории
- Тесты покрывают все требования CAL-01 до CAL-05
</verification>

<success_criteria>
- Два тестовых файла созданы: createDateKey.test.ts и createIsWeekendPredicate.test.ts
- Всего 11 failing tests (4 для createDateKey + 7 для createIsWeekendPredicate)
- Все тесты следуют vitest паттернам проекта
- Тесты документируют ожидаемое поведение утилит
</success_criteria>

<output>
После завершения создать `.planning/phases/21-custom-weekend-calendar/21-01-SUMMARY.md`
</output>
