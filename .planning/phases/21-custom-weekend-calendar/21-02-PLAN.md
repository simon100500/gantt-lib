---
phase: 21
plan: 02
type: execute
wave: 1
depends_on: ["21-01"]
files_modified:
  - packages/gantt-lib/src/utils/dateUtils.ts
  - packages/gantt-lib/src/utils/geometry.ts
  - packages/gantt-lib/src/utils/__tests__/createDateKey.test.ts
  - packages/gantt-lib/src/utils/__tests__/createIsWeekendPredicate.test.ts
autonomous: true
requirements:
  - CAL-01
  - CAL-02
  - CAL-04
  - CAL-05

must_haves:
  truths:
    - createDateKey генерирует уникальный ключ для каждой даты в формате "YYYY-M-D"
    - createIsWeekendPredicate возвращает предикат с правильным приоритетом: isWeekend > workdays > weekends > default
    - calculateWeekendBlocks принимает опциональный параметр isCustomWeekend
    - Все тесты passing (GREEN phase)
  artifacts:
    - path: packages/gantt-lib/src/utils/dateUtils.ts
      provides: createDateKey и createIsWeekendPredicate utilities
      exports: ["createDateKey", "createIsWeekendPredicate", "WeekendConfig"]
    - path: packages/gantt-lib/src/utils/geometry.ts
      provides: Модифицированная calculateWeekendBlocks с isCustomWeekend параметром
      exports: ["calculateWeekendBlocks"]
  key_links:
    - from: createIsWeekendPredicate
      to: createDateKey
      via: Set-based lookup
      pattern: new Set\(weekends\.map\(createDateKey\)\)
    - from: calculateWeekendBlocks
      to: isCustomWeekend predicate
      via: Опциональный параметр
      pattern: isCustomWeekend\?: \(date: Date\) => boolean
---

<objective>
Реализовать утилиты для кастомных выходных дней и модифицировать calculateWeekendBlocks (TDD GREEN phase).

Цель: Реализовать функции `createDateKey`, `createIsWeekendPredicate` и модифицировать `calculateWeekendBlocks` для поддержки кастомных выходных. Все тесты должны пройти.

Output: Рабочие утилиты с полной тестовойカバー, модифицированная calculateWeekendBlocks с обратной совместимостью.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-custom-weekend-calendar/21-CONTEXT.md
@.planning/phases/21-custom-weekend-calendar/21-RESEARCH.md
@.planning/phases/21-custom-weekend-calendar/21-01-SUMMARY.md

# Существующие реализации
@packages/gantt-lib/src/utils/dateUtils.ts — добавить createDateKey и createIsWeekendPredicate
@packages/gantt-lib/src/utils/geometry.ts — модифицировать calculateWeekendBlocks (строки 162-196)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Реализовать createDateKey utility</name>
  <files>packages/gantt-lib/src/utils/dateUtils.ts</files>
  <read_first>
    - packages/gantt-lib/src/utils/dateUtils.ts (существующие утилиты, паттерны UTC)
    - packages/gantt-lib/src/utils/__tests__/createDateKey.test.ts (тесты из Wave 0)
  </read_first>
  <behavior>
    - createDateKey генерирует ключ "YYYY-M-D" используя UTC методы
    - Ключ используется для Set-based lookup в createIsWeekendPredicate
  </behavior>
  <action>
    В файл packages/gantt-lib/src/utils/dateUtils.ts добавить функцию:

    ```typescript
    /**
     * Create a UTC-safe key for Set-based date lookup
     * @param date - Date object to create key from
     * @returns String key in "YYYY-M-D" format using UTC date components
     *
     * Example:
     * createDateKey(new Date(Date.UTC(2026, 2, 15))) // "2026-2-15"
     *
     * Note: Uses UTC methods to prevent DST and timezone issues.
     * Month is 0-indexed (0=January, 11=December) per JavaScript Date convention.
     */
    export const createDateKey = (date: Date): string => {
      return `${date.getUTCFullYear()}-${date.getUTCMonth()}-${date.getUTCDate()}`;
    };
    ```

    Разместить функцию после `isWeekend` (строка 96) и перед `getMultiMonthDays` (строка 98).

    Использовать UTC методы: getUTCFullYear(), getUTCMonth(), getUTCDate().

    Формат: "YYYY-M-D" (год-месяц-день через дефисы).
  </action>
  <verify>
    <automated>npm test -- createDateKey.test.ts --run</automated>
  </verify>
  <done>
    - createDateKey экспортируется из dateUtils.ts
    - Все 4 теста в createDateKey.test.ts passing
    - Функция использует UTC методы (не getFullYear/getMonth/getDate)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Реализовать createIsWeekendPredicate utility</name>
  <files>packages/gantt-lib/src/utils/dateUtils.ts</files>
  <read_first>
    - packages/gantt-lib/src/utils/dateUtils.ts (существующие утилиты, только что добавленный createDateKey)
    - packages/gantt-lib/src/utils/__tests__/createIsWeekendPredicate.test.ts (тесты из Wave 0)
  </read_first>
  <behavior>
    - Принимает WeekendConfig интерфейс с опциональными weekends, workdays, isWeekend
    - Возвращает предикат (date: Date) => boolean
    - Приоритет: isWeekend (highest) > workdays > weekends > default (Saturday/Sunday)
    - Использует Set-based lookup через createDateKey для O(1) проверки
  </behavior>
  <action>
    В файл packages/gantt-lib/src/utils/dateUtils.ts добавить:

    1. Интерфейс WeekendConfig перед функцией (после createDateKey):

    ```typescript
    /**
     * Configuration for custom weekend calendar
     */
    export interface WeekendConfig {
      /** Array of dates to ADD to default weekends (e.g., holidays) */
      weekends?: Date[];
      /** Array of dates to EXCLUDE from default weekends (e.g., shifted workdays) */
      workdays?: Date[];
      /** Custom predicate for flexible weekend logic (overrides arrays) */
      isWeekend?: (date: Date) => boolean;
    }
    ```

    2. Функцию createIsWeekendPredicate:

    ```typescript
    /**
     * Create a weekend predicate with custom calendar support
     *
     * Precedence order (highest to lowest):
     * 1. isWeekend (custom predicate) - use directly, ignore arrays
     * 2. workdays - exclude these dates from default weekends
     * 3. weekends - add these dates to default weekends
     * 4. default - Saturday (6) and Sunday (0)
     *
     * @param config - Weekend configuration with optional arrays and predicate
     * @returns Predicate function (date: Date) => boolean
     *
     * Example:
     * // Add March 8 as holiday (Monday becomes weekend)
     * const predicate = createIsWeekendPredicate({
     *   weekends: [new Date(Date.UTC(2026, 2, 8))]
     * });
     *
     * // Make March 15 a workday (Saturday becomes workday)
     * const predicate2 = createIsWeekendPredicate({
     *   workdays: [new Date(Date.UTC(2026, 2, 15))]
     * });
     *
     * // Custom shift pattern (Sunday-only weekends)
     * const predicate3 = createIsWeekendPredicate({
     *   isWeekend: (date) => date.getUTCDay() === 0
     * });
     */
    export const createIsWeekendPredicate = (
      config: WeekendConfig
    ): ((date: Date) => boolean) => {
      const { weekends, workdays, isWeekend: customPredicate } = config;

      // Priority 1: Custom predicate (highest) - use directly
      if (customPredicate) {
        return customPredicate;
      }

      // Priority 2: Workdays - exclude these from default weekends
      if (workdays && workdays.length > 0) {
        const workdaySet = new Set(workdays.map(createDateKey));
        return (date: Date) => {
          const key = createDateKey(date);
          if (workdaySet.has(key)) {
            return false; // Workday takes precedence
          }
          const dayOfWeek = date.getUTCDay();
          return dayOfWeek === 0 || dayOfWeek === 6; // Default Sat/Sun
        };
      }

      // Priority 3: Weekends - add these to default weekends
      if (weekends && weekends.length > 0) {
        const weekendSet = new Set(weekends.map(createDateKey));
        return (date: Date) => {
          const key = createDateKey(date);
          if (weekendSet.has(key)) {
            return true; // Custom weekend
          }
          const dayOfWeek = date.getUTCDay();
          return dayOfWeek === 0 || dayOfWeek === 6; // Default Sat/Sun
        };
      }

      // Priority 4: Default - Saturday/Sunday only
      return (date: Date) => {
        const dayOfWeek = date.getUTCDay();
        return dayOfWeek === 0 || dayOfWeek === 6;
      };
    };
    ```

    Разместить интерфейс и функцию после createDateKey, перед getMultiMonthDays.

    Использовать Set-based lookup через createDateKey для O(1) проверки.

    Проверять длину массивов (workdays.length > 0, weekends.length > 0) чтобы избегать создания Set для пустых массивов.
  </action>
  <verify>
    <automated>npm test -- createIsWeekendPredicate.test.ts --run</automated>
  </verify>
  <done>
    - WeekendConfig интерфейс экспортируется из dateUtils.ts
    - createIsWeekendPredicate экспортируется из dateUtils.ts
    - Все 7 тестов в createIsWeekendPredicate.test.ts passing
    - Функция использует Set-based lookup (не Array.includes)
  </done>
</task>

<task type="auto">
  <name>Модифицировать calculateWeekendBlocks для поддержки кастомных выходных</name>
  <files>packages/gantt-lib/src/utils/geometry.ts</files>
  <read_first>
    - packages/gantt-lib/src/utils/geometry.ts (существующая calculateWeekendBlocks на строках 162-196)
  </read_first>
  <action>
    Модифицировать функцию calculateWeekendBlocks в packages/gantt-lib/src/utils/geometry.ts:

    1. Обновить JSDoc комментарий (строки 156-161):

    ```typescript
    /**
     * Calculate weekend background blocks for a date range
     * @param dateRange - Array of Date objects representing the visible range
     * @param dayWidth - Width of each day column in pixels
     * @param isCustomWeekend - Optional predicate for custom weekend logic (e.g., holidays, shift patterns)
     * @returns Array of weekend block objects with left position and width
     *
     * Example:
     * // Default behavior (Saturday/Sunday)
     * calculateWeekendBlocks(dateRange, dayWidth)
     *
     * // Custom weekends (holidays, shifted workdays)
     * const isCustomWeekend = createIsWeekendPredicate({
     *   weekends: [new Date(Date.UTC(2026, 2, 8))], // March 8 holiday
     *   workdays: [new Date(Date.UTC(2026, 2, 15))] // March 15 workday
     * });
     * calculateWeekendBlocks(dateRange, dayWidth, isCustomWeekend)
     */
    ```

    2. Обновить сигнатуру функции (строка 162):

    ```typescript
    export const calculateWeekendBlocks = (
      dateRange: Date[],
      dayWidth: number,
      isCustomWeekend?: (date: Date) => boolean
    ): Array<{ left: number; width: number }> => {
    ```

    3. Обновить логику проверки weekend (строки 172-173):

    ```typescript
    for (let i = 0; i < dateRange.length; i++) {
      const date = dateRange[i];
      // Use custom predicate if provided, otherwise default Saturday/Sunday
      const isWeekend = isCustomWeekend
        ? isCustomWeekend(date)
        : date.getUTCDay() === 0 || date.getUTCDay() === 6;
    ```

    Остальную логику функции НЕ менять (блоки, слияние соседних дней).

    Обратная совместимость: если isCustomWeekend не передан, работает как раньше (Saturday/Sunday).
  </action>
  <verify>
    <automated>npm test -- geometry.test.ts --run</automated>
    <automated>grep -n "isCustomWeekend" packages/gantt-lib/src/utils/geometry.ts</automated>
  </verify>
  <done>
    - calculateWeekendBlocks принимает опциональный параметр isCustomWeekend
    - Существующие тесты geometry.test.ts passing (обратная совместимость)
    - JSDoc обновлен с примерами использования
    - grep показывает isCustomWeekend в параметрах функции
  </done>
</task>

</tasks>

<verification>
Общая проверка фазы:
- Все тесты passing: npm test -- --run
- createDateKey и createIsWeekendPredicate экспортируются из dateUtils.ts
- calculateWeekendBlocks модифицирован с обратной совместимостью
- Существующие тесты не сломаны
</verification>

<success_criteria>
- 11 passing tests (4 createDateKey + 7 createIsWeekendPredicate)
- createDateKey использует UTC методы
- createIsWeekendPredicate использует Set-based lookup
- calculateWeekendBlocks принимает isCustomWeekend параметр
- Обратная совместимость сохранена (существующие тесты passing)
</success_criteria>

<output>
После завершения создать `.planning/phases/21-custom-weekend-calendar/21-02-SUMMARY.md`
</output>
