---
phase: 21
plan: 04
type: execute
wave: 3
depends_on: ["21-03"]
files_modified:
  - packages/website/app/gantt/page.tsx
autonomous: false
requirements:
  - CAL-01
  - CAL-02
  - CAL-03
  - CAL-04
  - CAL-05

must_haves:
  truths:
    - Demo страница показывает примеры с кастомными выходными
    - Пользователь может визуально проверить подсветку праздников
    - Пользователь может визуально проверить исключение выходных (workdays)
    - Пользователь может визуально проверить приоритет workdays > weekends
    - Multi-month view корректно подсвечивает кастомные выходные
  artifacts:
    - path: packages/website/app/gantt/page.tsx
      provides: Demo примеры с weekends, workdays, isWeekend пропами
      min_lines: 100
  key_links:
    - from: GanttChart demo
      to: requirements CAL-01 to CAL-05
      via: Visual verification
      pattern: weekends=\{.*\}|workdays=\{.*\}|isWeekend=\{.*\}
---

<objective>
Создать demo страницу с примерами кастомных выходных для визуальной верификации требований.

Цель: Добавить примеры использования weekends, workdays, isWeekend пропов на demo страницу. Пользователь должен видеть визуальные результаты: подсветку праздников, исключение выходных, приоритет workdays над weekends.

Output: Demo страница с примерами кастомных выходных, визуальная верификация всех требований CAL-01 до CAL-05.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-custom-weekend-calendar/21-CONTEXT.md
@.planning/phases/21-custom-weekend-calendar/21-RESEARCH.md
@.planning/phases/21-custom-weekend-calendar/21-03-SUMMARY.md

# Существующие реализации
@packages/website/app/gantt/page.tsx — demo страница с примерами GanttChart
@packages/gantt-lib/src/components/GanttChart/GanttChart.tsx — новые пропсы weekends, workdays, isWeekend
</context>

<tasks>

<task type="auto">
  <name>Добавить примеры кастомных выходных на demo страницу</name>
  <files>packages/website/app/gantt/page.tsx</files>
  <read_first>
    - packages/website/app/gantt/page.tsx (существующие demo примеры)
  </read_first>
  <action>
    В файл packages/website/app/gantt/page.tsx:

    1. Добавить новые примеры после существующих GanttChart компонентов:

    Пример 1: Праздники (weekends prop):
    ```typescript
    {/* Example: Custom weekends (holidays) */}
    <h2 className="text-2xl font-bold mt-8 mb-4">Custom Weekends (Holidays)</h2>
    <p className="mb-4 text-gray-700">
      March 8 (International Women's Day) and March 10 are added as weekends (highlighted in red).
      Default Saturday/Sunday weekends still apply.
    </p>
    <GanttChart
      tasks={[
        { id: '1', name: 'Task 1', startDate: '2026-03-05', endDate: '2026-03-12' },
        { id: '2', name: 'Task 2', startDate: '2026-03-11', endDate: '2026-03-18' },
      ]}
      weekends={[
        new Date(Date.UTC(2026, 2, 8)),  // March 8 (Saturday) - holiday
        new Date(Date.UTC(2026, 2, 10)), // March 10 (Monday) - holiday
      ]}
    />
    ```

    Пример 2: Исключение выходных (workdays prop):
    ```typescript
    {/* Example: Workdays (exclude weekends) */}
    <h2 className="text-2xl font-bold mt-8 mb-4">Workdays (Exclude Weekends)</h2>
    <p className="mb-4 text-gray-700">
      March 15 (Saturday) and March 16 (Sunday) are workdays (not highlighted).
      Used for shifted workdays when weekends are moved to other dates.
    </p>
    <GanttChart
      tasks={[
        { id: '1', name: 'Task 1', startDate: '2026-03-10', endDate: '2026-03-20' },
        { id: '2', name: 'Task 2', startDate: '2026-03-14', endDate: '2026-03-22' },
      ]}
      workdays={[
        new Date(Date.UTC(2026, 2, 15)), // March 15 (Saturday) - workday
        new Date(Date.UTC(2026, 2, 16)), // March 16 (Sunday) - workday
      ]}
    />
    ```

    Пример 3: Приоритет workdays над weekends:
    ```typescript
    {/* Example: Precedence - workdays > weekends */}
    <h2 className="text-2xl font-bold mt-8 mb-4">Precedence: Workdays > Weekends</h2>
    <p className="mb-4 text-gray-700">
      March 22 (Saturday) is in both arrays. workdays takes precedence,
      so it's NOT highlighted as a weekend.
    </p>
    <GanttChart
      tasks={[
        { id: '1', name: 'Task 1', startDate: '2026-03-17', endDate: '2026-03-28' },
      ]}
      weekends={[
        new Date(Date.UTC(2026, 2, 22)), // March 22 (Saturday) - in both arrays
      ]}
      workdays={[
        new Date(Date.UTC(2026, 2, 22)), // March 22 (Saturday) - in both arrays
      ]}
    />
    ```

    Пример 4: Кастомный предикат (isWeekend prop):
    ```typescript
    {/* Example: Custom predicate (Sunday-only weekends) */}
    <h2 className="text-2xl font-bold mt-8 mb-4">Custom Predicate (Sunday-Only Weekends)</h2>
    <p className="mb-4 text-gray-700">
      Using a custom predicate for Sunday-only weekends (6-day work week).
      Saturdays are workdays (not highlighted).
    </p>
    <GanttChart
      tasks={[
        { id: '1', name: 'Task 1', startDate: '2026-03-03', endDate: '2026-03-16' },
      ]}
      isWeekend={(date: Date) => date.getUTCDay() === 0} // Sunday only
    />
    ```

    Пример 5: Multi-month view с кастомными выходными:
    ```typescript
    {/* Example: Multi-month view with custom weekends */}
    <h2 className="text-2xl font-bold mt-8 mb-4">Multi-Month View with Custom Weekends</h2>
    <p className="mb-4 text-gray-700">
      Custom weekends highlighted correctly across month boundaries.
      March 8 and April 10 are holidays (added to weekends).
    </p>
    <GanttChart
      tasks={[
        { id: '1', name: 'Task 1', startDate: '2026-03-01', endDate: '2026-04-15' },
        { id: '2', name: 'Task 2', startDate: '2026-03-20', endDate: '2026-04-25' },
      ]}
      weekends={[
        new Date(Date.UTC(2026, 2, 8)),  // March 8 (holiday)
        new Date(Date.UTC(2026, 3, 10)), // April 10 (holiday)
      ]}
    />
    ```

    Использовать Date.UTC для создания дат (преотвращает проблемы с часовыми поясами).

    Использовать новую строку (`)}`) для разделения примеров.
  </action>
  <verify>
    <automated>grep -n "weekends\|workdays\|isWeekend" packages/website/app/gantt/page.tsx | head -20</automated>
  </verify>
  <done>
    - На demo странице добавлены 5 примеров кастомных выходных
    - Пример 1: Праздники (weekends prop)
    - Пример 2: Исключение выходных (workdays prop)
    - Пример 3: Приоритет workdays над weekends
    - Пример 4: Кастомный предикат (isWeekend prop)
    - Пример 5: Multi-month view
    - Все примеры используют Date.UTC для создания дат
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Визуальная проверка кастомных выходных</name>
  <what-built>Полная интеграция кастомных выходных в GanttChart с визуальной демонстрацией на demo странице</what-built>
  <how-to-verify>
    ## Визуальная проверка

    1. Запустить dev сервер:
       ```bash
       cd packages/website
       npm run dev
       ```

    2. Открыть http://localhost:3000/gantt в браузере

    3. Проверить каждый пример:

    **Пример 1: Custom Weekends (Holidays)**
    - March 8 (Saturday) подсвечен красным (праздник)
    - March 10 (Monday) подсвечен красным (праздник)
    - Остальные Saturday/Sunday также подсвечены красным (default)

    **Пример 2: Workdays (Exclude Weekends)**
    - March 15 (Saturday) НЕ подсвечен (рабочий день)
    - March 16 (Sunday) НЕ подсвечен (рабочий день)
    - Остальные Saturday/Sunday подсвечены красным (default)

    **Пример 3: Precedence: Workdays > Weekends**
    - March 22 (Saturday) НЕ подсвечен (workdays имеет приоритет над weekends)

    **Пример 4: Custom Predicate (Sunday-Only)**
    - Все Saturday НЕ подсвечены (только Sunday выходные)
    - Все Sunday подсвечены красным

    **Пример 5: Multi-Month View**
    - March 8 подсвечен красным в марте
    - April 10 подсвечен красным в апреле
    - Подсветка корректно работает на границе месяцев

    4. Проверить TimeScaleHeader:
    - Числа кастомных выходных покрашены красным/розовым цветом
    - Числа default выходных (Saturday/Sunday) также покрашены

    5. Проверить DatePicker (если используется):
    - Кастомные выходные дни отмечены цветом в календаре
    - Выбор дат разрешен (не заблокирован)

    6. Проверить отсутствие регрессии:
    - Прокрутить вверх к существующим примерам (без кастомных выходных)
    - Убедиться что default Saturday/Sunday подсветка работает как раньше
    - Убедиться что задача drag-and-drop работает корректно
  </how-to-verify>
  <resume-signal>Введите "approved" если визуальная проверка прошла успешно, или опишите найденные проблемы</resume-signal>
</task>

</tasks>

<verification>
Общая проверка фазы:
- Demo страница запускается без ошибок
- Все 5 примеров отображаются корректно
- Визуальная проверка подтверждает:
  - CAL-01: weekends[] prop работает (праздники подсвечены)
  - CAL-02: isWeekend predicate работает (Sunday-only пример)
  - CAL-03: Подсветка в GridBackground работает
  - CAL-04: Приоритет workdays > weekends работает
  - CAL-05: Default поведение (Saturday/Sunday) сохраняется в примерах без пропсов
- Multi-month view корректно подсвечивает кастомные выходные
- Отсутствие регрессии в существующих примерах
</verification>

<success_criteria>
- Все 5 примеров добавлены на demo страницу
- Визуальная проверка подтверждает корректность работы всех требований
- Пользователь может видеть разницу между default и custom выходными
- Приоритет workdays > weekends > default визуально подтвержден
- Multi-month подсветка работает корректно
</success_criteria>

<output>
После завершения создать `.planning/phases/21-custom-weekend-calendar/21-04-SUMMARY.md`
</output>
