---
phase: 21
plan: 03
type: execute
wave: 2
depends_on: ["21-02"]
files_modified:
  - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
  - packages/gantt-lib/src/components/GridBackground/GridBackground.tsx
  - packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx
  - packages/gantt-lib/src/components/ui/Calendar.tsx
autonomous: true
requirements:
  - CAL-01
  - CAL-02
  - CAL-03
  - CAL-04
  - CAL-05

must_haves:
  truths:
    - GanttChart принимает пропсы weekends, workdays, isWeekend
    - GridBackground подсвечивает кастомные выходные в day-view
    - TimeScaleHeader красит числа кастомных выходных в красный/розовый
    - Calendar отмечает кастомные выходные визуально
    - Приоритет workdays > weekends > default работает корректно
    - По умолчанию (без пропсов) поведение Saturday/Sunday сохраняется
  artifacts:
    - path: packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
      provides: Пропсы weekends?, workdays?, isWeekend? в GanttChartProps
      exports: ["GanttChart", "GanttChartProps", "Task", "TaskDependency"]
    - path: packages/gantt-lib/src/components/GridBackground/GridBackground.tsx
      provides: Пропс isCustomWeekend? в GridBackgroundProps
      exports: ["GridBackground", "GridBackgroundProps"]
    - path: packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx
      provides: Пропс isCustomWeekend? в TimeScaleHeaderProps
      exports: ["TimeScaleHeader", "TimeScaleHeaderProps"]
    - path: packages/gantt-lib/src/components/ui/Calendar.tsx
      provides: Пропс isWeekend? в CalendarProps
      exports: ["Calendar", "CalendarProps"]
  key_links:
    - from: GanttChart
      to: GridBackground
      via: isCustomWeekend prop
      pattern: <GridBackground.*isCustomWeekend=\{isCustomWeekend\}
    - from: GanttChart
      to: TimeScaleHeader
      via: isCustomWeekend prop
      pattern: <TimeScaleHeader.*isCustomWeekend=\{isCustomWeekend\}
    - from: GanttChart
      to: createIsWeekendPredicate
      via: useMemo для создания предиката
      pattern: useMemo\(\(\) => createIsWeekendPredicate
---

<objective>
Интегрировать кастомные выходные в компоненты GanttChart, GridBackground, TimeScaleHeader и Calendar.

Цель: Добавить пропсы для кастомных выходных в GanttChart, передать их в дочерние компоненты, реализовать подсветку и покраску чисел. Сохранить обратную совместимость.

Output: Полностью рабочая интеграция кастомных выходных во все компоненты с визуальной подсветкой и покраской чисел.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-custom-weekend-calendar/21-CONTEXT.md
@.planning/phases/21-custom-weekend-calendar/21-RESEARCH.md
@.planning/phases/21-custom-weekend-calendar/21-02-SUMMARY.md

# Существующие реализации
@packages/gantt-lib/src/components/GanttChart/GanttChart.tsx — добавить пропсы
@packages/gantt-lib/src/components/GridBackground/GridBackground.tsx — добавить isCustomWeekend prop
@packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx — добавить isCustomWeekend prop
@packages/gantt-lib/src/components/ui/Calendar.tsx — добавить isWeekend prop
@packages/gantt-lib/src/utils/dateUtils.ts — createIsWeekendPredicate уже реализован
</context>

<tasks>

<task type="auto">
  <name>Добавить пропсы кастомных выходных в GanttChart</name>
  <files>packages/gantt-lib/src/components/GanttChart/GanttChart.tsx</files>
  <read_first>
    - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx (интерфейс GanttChartProps, существующие пропсы)
    - packages/gantt-lib/src/utils/dateUtils.ts (createIsWeekendPredicate, WeekendConfig)
  </read_first>
  <action>
    В файл packages/gantt-lib/src/components/GanttChart/GanttChart.tsx:

    1. Добавить импорт в начале файла (после существующих импортов из utils):

    ```typescript
    import { createIsWeekendPredicate, type WeekendConfig } from '../../utils/dateUtils';
    ```

    2. В интерфейсе GanttChartProps (после viewMode prop, строка 128) добавить:

    ```typescript
    /** Custom weekend dates to ADD to default weekends (e.g., holidays) */
    weekends?: Date[];
    /** Custom workday dates to EXCLUDE from default weekends (e.g., shifted workdays) */
    workdays?: Date[];
    /** Flexible weekend logic predicate (overrides arrays) */
    isWeekend?: (date: Date) => boolean;
    ```

    3. В теле компонента GanttChart (после деструктуризации пропсов, после viewMode = 'day') добавить useMemo для создания предиката:

    ```typescript
    // Create custom weekend predicate from props (memoized for performance)
    const isCustomWeekend = useMemo(
      () => createIsWeekendPredicate({ weekends, workdays, isWeekend }),
      [weekends, workdays, isWeekend]
    );
    ```

    Не передавать isCustomWeekend в компоненты в этом task — это будет сделано в следующих задачах.

    Пропсы являются опциональными (?), поэтому обратная совместимость сохраняется.
  </action>
  <verify>
    <automated>grep -n "weekends\|workdays" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx</automated>
    <automated>grep -n "createIsWeekendPredicate" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx</automated>
  </verify>
  <done>
    - GanttChartProps включает weekends?, workdays?, isWeekend? пропы
    - createIsWeekendPredicate импортируется из dateUtils
    - isCustomWeekend создается через useMemo с правильными зависимостями
    - TypeScript компилируется без ошибок
  </done>
</task>

<task type="auto">
  <name>Передать isCustomWeekend в GridBackground</name>
  <files>packages/gantt-lib/src/components/GridBackground/GridBackground.tsx</files>
  <read_first>
    - packages/gantt-lib/src/components/GridBackground/GridBackground.tsx (GridBackgroundProps, использование calculateWeekendBlocks)
  </read_first>
  <action>
    В файл packages/gantt-lib/src/components/GridBackground/GridBackground.tsx:

    1. В интерфейсе GridBackgroundProps (после viewMode prop, строка 16) добавить:

    ```typescript
    /** Optional predicate for custom weekend logic (e.g., holidays, shift patterns) */
    isCustomWeekend?: (date: Date) => boolean;
    ```

    2. В теле компонента GridBackground (в деструктуризации пропсов, после viewMode = 'day') добавить:

    ```typescript
    const GridBackground: React.FC<GridBackgroundProps> = React.memo(
      ({ dateRange, dayWidth, totalHeight, viewMode = 'day', isCustomWeekend }) => {
    ```

    3. В вызове calculateWeekendBlocks (строка 73) передать isCustomWeekend:

    ```typescript
    const weekendBlocks = useMemo(() => {
      if (viewMode === 'week' || viewMode === 'month') return [];
      return calculateWeekendBlocks(dateRange, dayWidth, isCustomWeekend);
    }, [dateRange, dayWidth, viewMode, isCustomWeekend]);
    ```

    4. В функции arePropsEqual (строка 25) добавить сравнение isCustomWeekend:

    ```typescript
    const arePropsEqual = (prevProps: GridBackgroundProps, nextProps: GridBackgroundProps) => {
      return (
        prevProps.dayWidth === nextProps.dayWidth &&
        prevProps.dateRange.length === nextProps.dateRange.length &&
        prevProps.totalHeight === nextProps.totalHeight &&
        prevProps.viewMode === nextProps.viewMode &&
        prevProps.isCustomWeekend === nextProps.isCustomWeekend
      );
    };
    ```

    Изменения в arePropsEqual нужны для корректной работы React.memo — при изменении isCustomWeekend компонент должен перерендериться.
  </action>
  <verify>
    <automated>grep -n "isCustomWeekend" packages/gantt-lib/src/components/GridBackground/GridBackground.tsx</automated>
  </verify>
  <done>
    - GridBackgroundProps включает isCustomWeekend? prop
    - isCustomWeekend передается в calculateWeekendBlocks
    - arePropsEqual сравнивает isCustomWeekend для React.memo
    - Зависимость isCustomWeekend добавлена в useMemo для weekendBlocks
  </done>
</task>

<task type="auto">
  <name>Передать isCustomWeekend в TimeScaleHeader и обновить стили чисел</name>
  <files>packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx</files>
  <read_first>
    - packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx (TimeScaleHeaderProps, рендер day cells)
  </read_first>
  <action>
    В файл packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx:

    1. В интерфейсе TimeScaleHeaderProps (после viewMode prop, строка 18) добавить:

    ```typescript
    /** Optional predicate for custom weekend logic (e.g., holidays, shift patterns) */
    isCustomWeekend?: (date: Date) => boolean;
    ```

    2. В теле компонента TimeScaleHeader (в деструктуризации пропсов, после viewMode = 'day') добавить:

    ```typescript
    const TimeScaleHeader: React.FC<TimeScaleHeaderProps> = ({
      days,
      dayWidth,
      headerHeight,
      viewMode = 'day',
      isCustomWeekend,
    }) => {
    ```

    3. Найти секцию рендера day-view row 2 (для day numbers). Найти map по days (приблизительно строка 140+). Добавить проверку isCustomWeekend:

    Найти код:
    ```typescript
    {days.map((day, index) => {
      // ... existing code
    })}
    ```

    Перед return добавить:
    ```typescript
    const isWeekendDay = isCustomWeekend
      ? isCustomWeekend(day)
      : day.getUTCDay() === 0 || day.getUTCDay() === 6;
    ```

    В className для day cell добавить условие для weekend styling:
    ```typescript
    className={`gantt-tsh-dayCell ${isWeekendDay ? 'gantt-tsh-weekendDay' : ''}`}
    ```

    Существующий CSS класс gantt-tsh-weekendDay уже определен в TimeScaleHeader.css и красит числа в красный/розовый цвет.

    Если в day-view нет отдельного map для day numbers, найти секцию где рендерится вторая строка шапки (row 2) и добавить туда логику покраски.
  </action>
  <verify>
    <automated>grep -n "isCustomWeekend\|isWeekendDay" packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx</automated>
  </verify>
  <done>
    - TimeScaleHeaderProps включает isCustomWeekend? prop
    - isCustomWeekend используется для проверки weekend days
    - gantt-tsh-weekendDay класс применяется к выходным дням
    - Существующий CSS (красный/розовый цвет) используется для покраски
  </done>
</task>

<task type="auto">
  <name>Передать isCustomWeekend в Calendar и GridBackground из GanttChart</name>
  <files>packages/gantt-lib/src/components/GanttChart/GanttChart.tsx</files>
  <read_first>
    - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx (рендер GridBackground, TimeScaleHeader)
  </read_first>
  <action>
    В файл packages/gantt-lib/src/components/GanttChart/GanttChart.tsx:

    1. Найти рендер GridBackground (приблизительно строка 250+). Добавить prop isCustomWeekend:

    ```typescript
    <GridBackground
      dateRange={dateRange}
      dayWidth={dayWidth}
      totalHeight={totalHeight}
      viewMode={viewMode}
      isCustomWeekend={isCustomWeekend}
    />
    ```

    2. Найти рендер TimeScaleHeader (приблизительно строка 260+). Добавить prop isCustomWeekend:

    ```typescript
    <TimeScaleHeader
      days={dateRange}
      dayWidth={dayWidth}
      headerHeight={headerHeight}
      viewMode={viewMode}
      isCustomWeekend={isCustomWeekend}
    />
    ```

    В GanttChart компоненте Calendar не используется напрямую (DatePicker — отдельный компонент в ui/), поэтому передача в Calendar не требуется.
  </action>
  <verify>
    <automated>grep -A2 "GridBackground" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx | grep "isCustomWeekend"</automated>
    <automated>grep -A2 "TimeScaleHeader" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx | grep "isCustomWeekend"</automated>
  </verify>
  <done>
    - isCustomWeekend передается в GridBackground
    - isCustomWeekend передается в TimeScaleHeader
    - TypeScript компилируется без ошибок
  </done>
</task>

<task type="auto">
  <name>Обновить Calendar для поддержки кастомных выходных</name>
  <files>packages/gantt-lib/src/components/ui/Calendar.tsx</files>
  <read_first>
    - packages/gantt-lib/src/components/ui/Calendar.tsx (CalendarProps, getDayClassName, рендер day cells)
  </read_first>
  <action>
    В файл packages/gantt-lib/src/components/ui/Calendar.tsx:

    1. В интерфейсе CalendarProps (после disabled prop, строка 30) добавить:

    ```typescript
    /** Optional predicate for custom weekend logic (e.g., holidays, shift patterns) */
    isWeekend?: (date: Date) => boolean;
    ```

    2. В функции getDayClassName (строка 34) обновить логику для проверки weekend:

    Существующий код:
    ```typescript
    if (isWeekend(day)) classes.push('weekend');
    ```

    Заменить на:
    ```typescript
    // Use custom predicate if provided, otherwise default
    if (isWeekendProp ? isWeekendProp(day) : isWeekend(day)) {
      classes.push('weekend');
    }
    ```

    И обновить сигнатуру функции:
    ```typescript
    function getDayClassName(
      day: Date,
      selected: Date | undefined,
      isWeekendProp?: (date: Date) => boolean
    ): string {
    ```

    3. В теле компонента Calendar (в деструктуризации пропсов, после disabled = false) добавить:

    ```typescript
    export const Calendar: React.FC<CalendarProps> = ({
      selected,
      onSelect,
      initialDate,
      mode = 'single',
      disabled = false,
      isWeekend: isWeekendProp,
    }) => {
    ```

    4. Найти рендер day cells (map по days). В вызове getDayClassName добавить isWeekendProp:

    ```typescript
    className={getDayClassName(day, selected, isWeekendProp)}
    ```

    Существующий CSS класс weekend уже определен в ui.css и красит дни в красный/розовый цвет.
  </action>
  <verify>
    <automated>grep -n "isWeekend" packages/gantt-lib/src/components/ui/Calendar.tsx</automated>
  </verify>
  <done>
    - CalendarProps включает isWeekend? prop
    - getDayClassName использует кастомный предикат если передан
    - isWeekendProp передается в getDayClassName при рендере day cells
    - Существующий CSS (красный/розовый цвет) используется для покраски
  </done>
</task>

</tasks>

<verification>
Общая проверка фазы:
- Все компоненты компилируются без ошибок TypeScript
- isCustomWeekend корректно передается из GanttChart в GridBackground и TimeScaleHeader
- GridBackground использует isCustomWeekend в calculateWeekendBlocks
- TimeScaleHeader красит числа кастомных выходных
- Calendar принимает isWeekend prop и использует его
- Обратная совместимость сохранена (все пропсы опциональные)
</verification>

<success_criteria>
- GanttChart принимает weekends, workdays, isWeekend пропы
- GridBackground подсвечивает кастомные выходные (использует isCustomWeekend в calculateWeekendBlocks)
- TimeScaleHeader красит числа кастомных выходных (применяет gantt-tsh-weekendDay класс)
- Calendar принимает isWeekend prop и отмечает выходные дни
- Все пропсы опциональные, обратная совместимость сохранена
- TypeScript компилируется без ошибок
</success_criteria>

<output>
После завершения создать `.planning/phases/21-custom-weekend-calendar/21-03-SUMMARY.md`
</output>
