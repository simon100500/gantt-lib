---
phase: 25-columns-refactoring
plan: 03
type: execute
wave: 3
depends_on: ["25-02"]
files_modified:
  - packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Single editingColumnId state controls all editors (built-in and custom) in a row"
    - "Only one editor open per row at any time"
    - "Built-in editors (name, duration, progress) open/close via editingColumnId mechanism"
    - "Custom editors open/close via same editingColumnId mechanism"
    - "autoEdit logic (new task auto-focuses name) works with editingColumnId"
    - "col.renderEditor is used instead of col.editor for custom columns"
  artifacts:
    - path: "packages/gantt-lib/src/components/TaskList/TaskListRow.tsx"
      provides: "Unified editor lifecycle via editingColumnId"
      contains: "editingColumnId"
  key_links:
    - from: "TaskListRow.tsx editingColumnId"
      to: "built-in editors (name, duration, progress)"
      via: "editingColumnId === 'name' etc."
      pattern: "editingColumnId === 'name'"
    - from: "TaskListRow.tsx editingColumnId"
      to: "custom editors"
      via: "editingColumnId === col.id"
      pattern: "editingColumnId === col\\.id"
---

<objective>
Phase C: Editor Unification — replace separate `editingName`, `editingDuration`, `editingProgress`, `editingCustomColumnId` boolean/string states with a single `editingColumnId: string | null` state.

Purpose: One editor lifecycle for all columns (built-in and custom). Eliminates possibility of multiple editors open simultaneously.
Output: Refactored TaskListRow.tsx with unified editor state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-columns-refactoring/25-CONTEXT.md
@.planning/phases/25-columns-refactoring/25-RESEARCH.md
@.planning/phases/25-columns-refactoring/25-02-SUMMARY.md

@packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
@packages/gantt-lib/src/components/TaskList/columns/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify editor state — replace editingName/editingDuration/editingProgress/editingCustomColumnId with editingColumnId</name>
  <files>
    packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
  </files>
  <read_first>
    packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
    packages/gantt-lib/src/components/TaskList/columns/types.ts
    packages/gantt-lib/src/__tests__/taskListColumns.test.tsx
  </read_first>
  <action>
    **Migration strategy:** Migrate built-in editors one by one, starting with simple ones (duration, progress), then complex (name with autoEdit). Test after each sub-migration.

    1. **Replace state declarations** (lines ~758-783):
       Remove:
       ```ts
       const [editingName, setEditingName] = useState(false);
       const [editingDuration, setEditingDuration] = useState(false);
       const [editingProgress, setEditingProgress] = useState(false);
       const [editingCustomColumnId, setEditingCustomColumnId] = useState<string | null>(null);
       ```
       Add:
       ```ts
       const [editingColumnId, setEditingColumnId] = useState<string | null>(null);
       ```

    2. **Create derived booleans** for backward compat within the component (temporary, can be inlined later in Phase E):
       ```ts
       const editingName = editingColumnId === 'name';
       const editingDuration = editingColumnId === 'duration';
       const editingProgress = editingColumnId === 'progress';
       ```

    3. **Replace all `setEditingName(true)`** calls with `setEditingColumnId('name')`, `setEditingName(false)` with `setEditingColumnId(null)`.

    4. **Replace all `setEditingDuration(true)`** with `setEditingColumnId('duration')`, `setEditingDuration(false)` with `setEditingColumnId(null)`.

    5. **Replace all `setEditingProgress(true)`** with `setEditingColumnId('progress')`, `setEditingProgress(false)` with `setEditingColumnId(null)`.

    6. **Replace all `setEditingCustomColumnId(col.id)`** with `setEditingColumnId(col.id)`, `setEditingCustomColumnId(null)` with `setEditingColumnId(null)`.

    7. **Replace `editingCustomColumnId === col.id`** checks with `editingColumnId === col.id` in the custom column render block.

    8. **Update autoEdit logic** (lines ~953-969): The `useEffect` that auto-opens name editor when `editingTaskId` matches should call `setEditingColumnId('name')` instead of `setEditingName(true)`.

    9. **Update F2 keyboard handler** (line ~999): `setEditingName(true)` → `setEditingColumnId('name')`.

    10. **Update custom column render block** to use `col.renderEditor` instead of `col.editor`:
        Change any reference to `col.editor(ctx)` to `col.renderEditor?.(ctx)` and `col.editor` existence check to `col.renderEditor`.

    11. **Single-editor-per-row guarantee:** Because there is now one `editingColumnId` state, opening any editor automatically closes any previously open editor. No explicit "close other" logic needed.

    **Pitfall from RESEARCH.md (Pitfall 1):** Do NOT keep both old boolean states and new `editingColumnId`. The derived booleans in step 2 ensure all existing JSX `{editingName && (...)}` conditionals work without changing every template reference. This is the safe migration path.

    **Pitfall from RESEARCH.md (Pitfall 3):** `col.editor` → `col.renderEditor` must happen in this task. The test file was already updated in Plan 01 Task 2.

    **Testing:** After changes, run all tests. The editor test in `taskListColumns.test.tsx` ("opens a custom editor and saves a merged task patch") must pass. Also verify name editing still works (existing tests or manual).
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - TaskListRow.tsx contains `const [editingColumnId, setEditingColumnId] = useState<string | null>(null)`
    - TaskListRow.tsx does NOT contain `const [editingName, setEditingName] = useState`
    - TaskListRow.tsx does NOT contain `const [editingDuration, setEditingDuration] = useState`
    - TaskListRow.tsx does NOT contain `const [editingProgress, setEditingProgress] = useState`
    - TaskListRow.tsx does NOT contain `const [editingCustomColumnId, setEditingCustomColumnId]`
    - TaskListRow.tsx contains `const editingName = editingColumnId === 'name'`
    - TaskListRow.tsx contains `const editingDuration = editingColumnId === 'duration'`
    - TaskListRow.tsx contains `const editingProgress = editingColumnId === 'progress'`
    - TaskListRow.tsx contains `col.renderEditor` (not `col.editor`)
    - TaskListRow.tsx contains `setEditingColumnId('name')` (autoEdit logic)
    - All tests pass (`npx vitest run` exits 0)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Single editingColumnId state controls all editors, col.renderEditor used for custom columns, all tests pass</done>
</task>

</tasks>

<verification>
- Full test suite: `cd packages/gantt-lib && npx vitest run`
- TypeScript compiles: `cd packages/gantt-lib && npx tsc --noEmit`
- Editor test passes: `cd packages/gantt-lib && npx vitest run src/__tests__/taskListColumns.test.tsx`
</verification>

<success_criteria>
- Single `editingColumnId` state replaces 4 separate editor states
- Built-in editors work via derived booleans (`editingColumnId === 'name'` etc.)
- Custom editors use `col.renderEditor` instead of `col.editor`
- autoEdit (new task auto-focuses name) works correctly
- Only one editor open per row at any time
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/25-columns-refactoring/25-03-SUMMARY.md`
</output>
