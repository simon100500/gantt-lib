---
phase: 25-columns-refactoring
plan: 04
type: execute
wave: 4
depends_on: ["25-03"]
files_modified:
  - packages/gantt-lib/src/components/TaskList/TaskList.tsx
  - packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
  - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
  - packages/gantt-lib/src/__tests__/taskListColumns.test.tsx
  - packages/gantt-lib/src/components/TaskList/taskListColumns.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "GanttChart passes additionalColumns to TaskList without `as TaskListColumn<Task>[]` cast"
    - "TaskList and TaskListRow accept TTask generic parameter"
    - "Consumer code compiles with ExtendedTask columns without `as TaskListColumn<Task>[]` casts"
    - "Dead code removed: normalizeColumnWidth, getColumnWidthPx, additionalColumnsByAnchor bucket logic, BUILT_IN_COLUMN_ORDER"
    - "Old taskListColumns.ts bridge still re-exports correctly"
  artifacts:
    - path: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      provides: "No cast on additionalColumns prop passing"
    - path: "packages/gantt-lib/src/components/TaskList/TaskList.tsx"
      provides: "Generic TaskList<TTask> or clean Task usage without casts"
    - path: "packages/gantt-lib/src/__tests__/taskListColumns.test.tsx"
      provides: "Tests compile without `as TaskListColumn<Task>[]` casts"
  key_links:
    - from: "GanttChart.tsx"
      to: "TaskList.tsx"
      via: "additionalColumns prop without cast"
      pattern: "additionalColumns=\\{additionalColumns\\}"
---

<objective>
Phase D + E: Generic Tightening and Cleanup — remove `as Task` casts from the generic chain, remove dead code, finalize the refactoring.

Purpose: Complete the refactoring by ensuring generics flow correctly and cleaning up obsolete code.
Output: Clean generic chain, no casts, no dead code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-columns-refactoring/25-CONTEXT.md
@.planning/phases/25-columns-refactoring/25-03-SUMMARY.md

@packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
@packages/gantt-lib/src/components/TaskList/TaskList.tsx
@packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
@packages/gantt-lib/src/components/TaskList/taskListColumns.ts
@packages/gantt-lib/src/__tests__/taskListColumns.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove as-Task casts from GanttChart → TaskList → TaskListRow chain</name>
  <files>
    packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
    packages/gantt-lib/src/components/TaskList/TaskList.tsx
    packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
  </files>
  <read_first>
    packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
    packages/gantt-lib/src/components/TaskList/TaskList.tsx
    packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
    packages/gantt-lib/src/components/TaskList/columns/types.ts
  </read_first>
  <action>
    1. **GanttChart.tsx line ~925:** Remove `as TaskListColumn<Task>[] | undefined` cast:
       Change: `additionalColumns={additionalColumns as TaskListColumn<Task>[] | undefined}`
       To: `additionalColumns={additionalColumns}`

       For this to work, `TaskListProps.additionalColumns` must accept `TaskListColumn<TTask>[]` where TTask comes from GanttChart's generic. If TaskList is not yet generic, make `additionalColumns` prop use the new `TaskListColumn` from `columns/types.ts` which has `<TTask extends Task>` — and since GanttChart already constrains `TTask extends Task`, the types align.

    2. **TaskList.tsx:** Update `TaskListProps` interface to accept `additionalColumns?: NewTaskListColumn<Task>[]` (using the new type from `columns/types.ts`). The internal component continues to work with `Task` since built-in columns know `Task` shape. If needed, make `TaskListProps` generic: `TaskListProps<TTask extends Task>` and update `additionalColumns?: NewTaskListColumn<TTask>[]`. Then the component function signature becomes `function TaskList<TTask extends Task>(props: TaskListProps<TTask>)`.

       **Simpler approach if full generics cause too many cascading changes:** Keep `TaskList` as non-generic but change `additionalColumns` type to `NewTaskListColumn<any>[]` — this accepts any TTask without cast. Then GanttChart can pass without cast. The internal code uses `Task` for built-in operations.

       Choose whichever approach compiles cleanly with minimal changes. The key constraint is: **no `as TaskListColumn<Task>[]` cast in GanttChart.tsx**.

    3. **TaskListRow.tsx:** Similarly, if `TaskListRowProps` has `resolvedColumns?: NewTaskListColumn<Task>[]`, update to match whatever TaskList passes. The important thing: no explicit casts at component boundaries.

    4. Run `npx tsc --noEmit` — if generic inference fails somewhere, add minimal type annotations (not casts). Local casts inside `createBuiltInColumns` (`ctx.task as Task`) are acceptable per CONTEXT.md.
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - GanttChart.tsx does NOT contain `as TaskListColumn<Task>[]` on the additionalColumns prop
    - GanttChart.tsx contains `additionalColumns={additionalColumns}` (no cast)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Generic chain flows without casts at component boundaries</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead code and update test casts</name>
  <files>
    packages/gantt-lib/src/components/TaskList/TaskList.tsx
    packages/gantt-lib/src/__tests__/taskListColumns.test.tsx
    packages/gantt-lib/src/components/TaskList/taskListColumns.ts
  </files>
  <read_first>
    packages/gantt-lib/src/components/TaskList/TaskList.tsx
    packages/gantt-lib/src/__tests__/taskListColumns.test.tsx
    packages/gantt-lib/src/components/TaskList/taskListColumns.ts
  </read_first>
  <action>
    1. **TaskList.tsx cleanup:** Verify and remove any remaining dead code:
       - `normalizeColumnWidth` function (should be gone from Plan 02, verify)
       - `getColumnWidthPx` function (should be gone from Plan 02, verify)
       - `BUILT_IN_COLUMN_ORDER` constant (should be gone from Plan 02, verify)
       - `DEFAULT_ADDITIONAL_COLUMN_WIDTH` constant (should be gone from Plan 02, verify)
       - Any remaining `additionalColumnsByAnchor` references
       - Unused imports that were only needed by removed code

    2. **taskListColumns.test.tsx:** Remove `as TaskListColumn<Task>[]` casts from test file:
       - Line ~80: `additionalColumns={additionalColumns as TaskListColumn<Task>[]}` → `additionalColumns={additionalColumns}`
       - Lines ~126, ~147, ~168, ~217, ~239: same pattern, remove all `as TaskListColumn<Task>[]` casts
       - This works because GanttChart<TTask> now accepts `TaskListColumn<TTask>[]` and `ExtendedTask extends Task`.

    3. **taskListColumns.ts bridge:** Verify it only contains re-exports. If there's any leftover code, clean it up. The file should be:
       ```ts
       export type { BuiltInTaskListColumnId, TaskListColumnAnchor, TaskListColumnContext, TaskListColumn } from './columns/types';
       ```

    4. Run full test suite to confirm nothing broke.
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - TaskList.tsx does NOT contain `normalizeColumnWidth` function
    - TaskList.tsx does NOT contain `getColumnWidthPx` function
    - TaskList.tsx does NOT contain `BUILT_IN_COLUMN_ORDER` constant
    - TaskList.tsx does NOT contain `DEFAULT_ADDITIONAL_COLUMN_WIDTH`
    - taskListColumns.test.tsx does NOT contain `as TaskListColumn<Task>[]`
    - taskListColumns.ts contains only `export type` re-exports from `./columns/types`
    - All tests pass
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All dead code removed, test casts eliminated, bridge file is pure re-export, full suite green</done>
</task>

</tasks>

<verification>
- Full test suite: `cd packages/gantt-lib && npx vitest run`
- TypeScript compiles: `cd packages/gantt-lib && npx tsc --noEmit`
- No casts grep: `grep -r "as TaskListColumn" packages/gantt-lib/src/` should return nothing except internal `createBuiltInColumns` local casts
</verification>

<success_criteria>
- No `as TaskListColumn<Task>[]` casts at component boundaries (GanttChart, TaskList, TaskListRow, tests)
- All dead code removed (normalizeColumnWidth, getColumnWidthPx, BUILT_IN_COLUMN_ORDER, bucket logic)
- taskListColumns.ts is a clean re-export bridge
- Full test suite passes
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/25-columns-refactoring/25-04-SUMMARY.md`
</output>
