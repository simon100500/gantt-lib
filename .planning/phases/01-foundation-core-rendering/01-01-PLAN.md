---
phase: 01-foundation-core-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.js
  - vitest.config.ts
  - .gitignore
  - src/app/page.tsx
  - src/app/layout.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Developer can run `npm install` without errors"
    - "Developer can run `npm run dev` and see Next.js welcome page"
    - "Developer can run `npm test` and see Vitest running"
    - "Project has TypeScript configured with strict mode"
    - "All required dependencies are installed"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "\"react\": \"^19.0.0\""
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
      contains: "\"strict\": true"
    - path: "vitest.config.ts"
      provides: "Test configuration"
      contains: "vitest"
    - path: "src/app/page.tsx"
      provides: "Entry page"
      min_lines: 5
  key_links:
    - from: "package.json"
      to: "npm install"
      via: "dependency installation"
      pattern: "dependencies.*react.*19"
---

<objective>
Set up Next.js project foundation with TypeScript, dependencies, and build configuration for the Gantt chart library.

Purpose: Create the project scaffolding that all subsequent development depends on. This establishes the build tooling, type system, and development environment.

Output: Working Next.js project with all required dependencies installed and configured.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-core-rendering/01-CONTEXT.md
@.planning/phases/01-foundation-core-rendering/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Initialize Next.js project with TypeScript</name>
  <files>package.json, tsconfig.json, next.config.js, src/app/page.tsx, src/app/layout.tsx</files>
  <action>
    Create a new Next.js 15+ project with App Router:
    1. Initialize package.json with React 19.0+, Next.js 15+, and TypeScript 5.7+
    2. Add dev dependencies: typescript-eslint, eslint, vitest
    3. Configure tsconfig.json with strict mode, ES2020 target, and proper path aliases (@/*)
    4. Set up next.config.js for library development
    5. Create minimal App Router structure (src/app/layout.tsx, src/app/page.tsx)
    6. Use `npm create next-app@latest` pattern but manually create files for control

    Do NOT use the interactive CLI - create files directly for reproducibility.
  </action>
  <verify>
    Run `npm install` followed by `npm run dev` - should start Next.js dev server on port 3000
  </verify>
  <done>
    Next.js dev server starts successfully, TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Install and configure dependencies</name>
  <files>package.json</files>
  <action>
    Install all required dependencies from RESEARCH.md Standard Stack section:

    Runtime dependencies:
    - react@^19.0.0
    - date-fns@^4.1.0
    - clsx@^2.1.0

    Dev dependencies:
    - typescript@^5.7.0
    - vitest@^3.0.0
    - eslint@^9.17.0
    - typescript-eslint@^8.18.0

    Add npm scripts:
    - "dev": "next dev"
    - "build": "next build"
    - "test": "vitest"
    - "test:ui": "vitest --ui"
    - "lint": "eslint . --ext .ts,.tsx"
  </action>
  <verify>
    Run `npm list` to confirm all packages are installed without conflicts
  </verify>
  <done>
    All dependencies installed, package.json contains correct versions
  </done>
</task>

<task type="auto">
  <name>Configure Vitest for unit testing</name>
  <files>vitest.config.ts</files>
  <action>
    Create vitest.config.ts with:
    1. TypeScript support via @vitejs/plugin-react
    2. Test environment: jsdom (for React component testing)
    3. Coverage configuration
    4. Include pattern: src/**/__tests__/**/*.ts
    5. Exclude: node_modules, .next, dist

    Configuration should match the pattern from RESEARCH.md "Development Tools" section.
  </action>
  <verify>
    Run `npm test` - Vitest should start with zero tests passing (no tests yet)
  </verify>
  <done>
    Vitest starts successfully, finds test directory when tests exist
  </done>
</task>

<task type="auto">
  <name>Create project directory structure</name>
  <files>src/components, src/utils, src/types</files>
  <action>
    Create the directory structure from RESEARCH.md "Recommended Project Structure (Phase 1)":
    - src/components/ (for React components)
    - src/utils/ (for utility functions)
    - src/types/ (for TypeScript types)
    - src/__tests__/ (for test files)

    Create index.ts files in each directory as barrel exports for future use.
  </action>
  <verify>
    Run `ls -la src/` to confirm all directories exist with index files
  </verify>
  <done>
    Directory structure matches RESEARCH.md recommendation
  </done>
</task>

</tasks>

<verification>
1. Run `npm install` - completes without errors
2. Run `npm run dev` - dev server starts on http://localhost:3000
3. Run `npm test` - Vitest starts with 0 tests
4. Run `npx tsc --noEmit` - TypeScript compiles without errors
5. Confirm all dependencies from research are installed
</verification>

<success_criteria>
- [ ] Next.js 15+ project with App Router configured
- [ ] TypeScript 5.7+ with strict mode enabled
- [ ] All required dependencies installed (React 19, date-fns, clsx, vitest)
- [ ] Vitest configured and running
- [ ] Project structure created (src/components, src/utils, src/types)
- [ ] Dev server starts successfully
- [ ] No TypeScript errors in fresh project
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-rendering/01-01-SUMMARY.md`
</output>
