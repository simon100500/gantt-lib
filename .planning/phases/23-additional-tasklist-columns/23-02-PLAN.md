---
phase: 23-additional-tasklist-columns
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
  - packages/gantt-lib/src/components/TaskList/TaskList.tsx
  - packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
  - packages/gantt-lib/src/components/TaskList/TaskList.css
  - packages/gantt-lib/src/__tests__/taskListColumns.test.tsx
autonomous: true
requirements: [COL-01, COL-02, COL-03, COL-05, COL-06, COL-07, COL-08]
user_setup: []
must_haves:
  truths:
    - "Пользователь может передать `additionalColumns` в GanttChart и увидеть новые колонки в TaskList"
    - "Дополнительные колонки появляются после нужного built-in anchor, а при неверном/пустом `after` встают после Name"
    - "renderCell получает расширенный task без кастов и рендерится на каждой видимой строке"
    - "Ширины custom columns одинаково применяются к header и row cells"
    - "Базовые колонки `№`, `Имя`, `Начало`, `Окончание`, `Дн./Дн. (р)`, `%`, `Связи` остаются на месте и продолжают работать"
  artifacts:
    - path: packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
      provides: "generic public prop additionalColumns?: TaskListColumn<TTask>[]"
      contains: ["additionalColumns?: TaskListColumn<", "function GanttChartInner<"]
    - path: packages/gantt-lib/src/components/TaskList/TaskList.tsx
      provides: "anchor bucketing, width budget growth и header wiring"
      contains: ["const BUILT_IN_COLUMN_ORDER", "const DEFAULT_ADDITIONAL_COLUMN_WIDTH = 120", "data-column-id"]
    - path: packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
      provides: "custom cell rendering path for renderCell"
      contains: ["data-custom-column-id", "renderCell("]
    - path: packages/gantt-lib/src/components/TaskList/TaskList.css
      provides: "custom header/body cell classes"
      contains: [".gantt-tl-cell-custom", ".gantt-tl-headerCell-custom"]
  key_links:
    - from: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      to: "packages/gantt-lib/src/components/TaskList/TaskList.tsx"
      via: "additionalColumns prop threading"
      pattern: "additionalColumns=\\{additionalColumns\\}"
    - from: "packages/gantt-lib/src/components/TaskList/TaskList.tsx"
      to: "packages/gantt-lib/src/components/TaskList/TaskListRow.tsx"
      via: "ordered/bucketed additional columns"
      pattern: "additionalColumnsByAnchor|additionalColumns="
    - from: "packages/gantt-lib/src/components/TaskList/TaskList.tsx"
      to: "packages/gantt-lib/src/components/TaskList/TaskList.css"
      via: "custom header/body CSS classes"
      pattern: "gantt-tl-cell-custom"
---

<objective>
Реализовать generic wiring и inline layout additionalColumns без вмешательства в built-in behavior

Purpose: Дать пользователю рабочие display columns с типобезопасным API, корректным порядком, шириной и scroll-sync в существующем overlay TaskList
Output: GanttChart/TaskList/TaskListRow поддерживают renderCell columns, anchored placement и width budget; wave-0 tests для display path проходят
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-additional-tasklist-columns/23-CONTEXT.md
@.planning/phases/23-additional-tasklist-columns/23-RESEARCH.md
@.planning/phases/23-additional-tasklist-columns/23-01-PLAN.md
@.planning/phases/22-filters/22-02-SUMMARY.md
@.planning/phases/21.1-custom-weekend-refactoring/21.1-01-SUMMARY.md
@packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
@packages/gantt-lib/src/components/TaskList/TaskList.tsx
@packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
@packages/gantt-lib/src/components/TaskList/TaskList.css
@packages/gantt-lib/src/components/TaskList/taskListColumns.ts
@packages/gantt-lib/src/__tests__/taskListColumns.test.tsx

<interfaces>
From packages/gantt-lib/src/components/TaskList/taskListColumns.ts:
```typescript
export type BuiltInTaskListColumnId =
  | 'number'
  | 'name'
  | 'startDate'
  | 'endDate'
  | 'duration'
  | 'progress'
  | 'dependencies'
  | 'actions';

export interface TaskListColumnContext<TTask extends Task = Task> {
  task: TTask;
  rowIndex: number;
  columnId: string;
  isEditing: boolean;
  openEditor: () => void;
  closeEditor: () => void;
  updateTask: (patch: Partial<TTask>) => void;
}
```

From packages/gantt-lib/src/components/TaskList/TaskList.tsx:
```typescript
const MIN_TASK_LIST_WIDTH = 530;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Превратить GanttChart и TaskList в generic boundary для additionalColumns per D-04, D-10, D-11</name>
  <files>packages/gantt-lib/src/components/GanttChart/GanttChart.tsx, packages/gantt-lib/src/components/TaskList/TaskList.tsx</files>
  <read_first>
    - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
    - packages/gantt-lib/src/components/TaskList/TaskList.tsx
    - packages/gantt-lib/src/components/TaskList/taskListColumns.ts
    - packages/gantt-lib/src/__tests__/taskListColumns.test.tsx
    - .planning/phases/23-additional-tasklist-columns/23-CONTEXT.md
  </read_first>
  <action>
    В `packages/gantt-lib/src/components/GanttChart/GanttChart.tsx`:
    - Добавить import `TaskListColumn` из `../TaskList/taskListColumns`.
    - Изменить публичный интерфейс на `export interface GanttChartProps<TTask extends Task = Task>`.
    - Поле `tasks` сделать `TTask[]`.
    - Поле `onTasksChange` сделать `(tasks: TTask[]) => void`.
    - Добавить публичный prop `additionalColumns?: TaskListColumn<TTask>[]`.
    - Перевести компонент на generic inner-function pattern:
      ```typescript
      function GanttChartInner<TTask extends Task = Task>(
        props: GanttChartProps<TTask>,
        ref: React.ForwardedRef<GanttChartHandle>
      ) { ... }

      export const GanttChart = forwardRef(GanttChartInner) as <
        TTask extends Task = Task
      >(props: GanttChartProps<TTask> & { ref?: React.Ref<GanttChartHandle> }) => React.ReactElement;
      ```
    - Все массивы/колбэки внутри компонента, которые касаются задач, привести к `TTask`.
    - При рендере `TaskList` пробросить `additionalColumns={additionalColumns}`.

    В `packages/gantt-lib/src/components/TaskList/TaskList.tsx`:
    - Добавить import `TaskListColumn` и `BuiltInTaskListColumnId`.
    - Изменить интерфейс и компонент на `TaskListProps<TTask extends Task = Task>` и `TaskList<TTask extends Task = Task>`.
    - Добавить prop `additionalColumns?: TaskListColumn<TTask>[]`.
    - Поля `tasks`, `onTasksChange`, `onAdd`, `onInsertAfter`, `onReorder` привести к `TTask` там, где это типобезопасно.
    - Объявить exact anchor order:
      ```typescript
      const BUILT_IN_COLUMN_ORDER: BuiltInTaskListColumnId[] = [
        'number',
        'name',
        'startDate',
        'endDate',
        'duration',
        'progress',
        'dependencies',
        'actions',
      ];
      ```
  </action>
  <verify>
    <automated>rg -n "additionalColumns\\?: TaskListColumn<|function GanttChartInner<|forwardRef\\(GanttChartInner\\)|BUILT_IN_COLUMN_ORDER" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx packages/gantt-lib/src/components/TaskList/TaskList.tsx</automated>
  </verify>
  <done>
    GanttChart и TaskList принимают generic `additionalColumns` без пользовательских cast, а anchor ids зафиксированы кодом как стабильные `'number' | 'name' | 'startDate' | 'endDate' | 'duration' | 'progress' | 'dependencies' | 'actions'`.
  </done>
  <acceptance_criteria>
    - `packages/gantt-lib/src/components/GanttChart/GanttChart.tsx` содержит `additionalColumns?: TaskListColumn<TTask>[]`
    - `packages/gantt-lib/src/components/GanttChart/GanttChart.tsx` содержит `function GanttChartInner<TTask extends Task = Task>`
    - `packages/gantt-lib/src/components/GanttChart/GanttChart.tsx` содержит `additionalColumns={additionalColumns}`
    - `packages/gantt-lib/src/components/TaskList/TaskList.tsx` содержит `const BUILT_IN_COLUMN_ORDER: BuiltInTaskListColumnId[] = [`
    - `packages/gantt-lib/src/components/TaskList/TaskList.tsx` содержит `'actions'`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Встроить header/body rendering, fallback after='name' и width budget per D-05, D-06, COL-07, COL-08</name>
  <files>packages/gantt-lib/src/components/TaskList/TaskList.tsx, packages/gantt-lib/src/components/TaskList/TaskListRow.tsx, packages/gantt-lib/src/components/TaskList/TaskList.css, packages/gantt-lib/src/__tests__/taskListColumns.test.tsx</files>
  <read_first>
    - packages/gantt-lib/src/components/TaskList/TaskList.tsx
    - packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
    - packages/gantt-lib/src/components/TaskList/TaskList.css
    - packages/gantt-lib/src/__tests__/taskListColumns.test.tsx
    - .planning/phases/23-additional-tasklist-columns/23-RESEARCH.md
  </read_first>
  <action>
    В `packages/gantt-lib/src/components/TaskList/TaskList.tsx` реализовать helpers:

    ```typescript
    const DEFAULT_ADDITIONAL_COLUMN_WIDTH = 120;

    const normalizeColumnWidth = (width?: string | number) =>
      typeof width === 'number' ? `${width}px` : width ?? `${DEFAULT_ADDITIONAL_COLUMN_WIDTH}px`;

    const getColumnWidthPx = (width?: string | number) =>
      typeof width === 'number' ? width : DEFAULT_ADDITIONAL_COLUMN_WIDTH;
    ```

    Затем:
    - Сгруппировать `additionalColumns` по anchor buckets; если `after` отсутствует или не входит в `BUILT_IN_COLUMN_ORDER`, класть колонку в bucket `'name'`.
    - Сохранять исходный order внутри bucket.
    - Увеличивать overlay width budget так:
      ```typescript
      const additionalWidth = additionalColumns.reduce((sum, column) => sum + getColumnWidthPx(column.width), 0);
      const effectiveTaskListWidth = Math.max(taskListWidth, MIN_TASK_LIST_WIDTH + additionalWidth);
      ```
    - В header добавить custom cells после каждого built-in anchor через helper `renderAdditionalHeaders(anchorId)`.
    - Для каждого custom header/cell выставлять:
      - `data-column-id="custom:{column.id}"`
      - `data-custom-column-id="{column.id}"`
      - inline style `{ width, minWidth: width, flexShrink: 0 }`
    - В built-in header cells добавить `data-column-id` со stable ids: `number`, `name`, `startDate`, `endDate`, `duration`, `progress`, `dependencies`.

    В `packages/gantt-lib/src/components/TaskList/TaskListRow.tsx`:
    - Добавить prop `additionalColumnsByAnchor` или equivalent уже упорядоченной структуры.
    - Для каждой custom column собирать row context:
      ```typescript
      const columnContext = {
        task,
        rowIndex,
        columnId: column.id,
        isEditing: false,
        openEditor: () => {},
        closeEditor: () => {},
        updateTask: () => {},
      };
      ```
    - Рендерить `column.renderCell(columnContext)` в custom body cells и не открывать editor в этом плане.
    - Добавить custom cells после тех же built-in anchors, что и header.

    В `packages/gantt-lib/src/components/TaskList/TaskList.css` добавить стили:
    ```css
    .gantt-tl-headerCell-custom,
    .gantt-tl-cell-custom {
      flex-shrink: 0;
      overflow: hidden;
      white-space: nowrap;
      text-overflow: ellipsis;
    }
    ```
    и не вводить второй горизонтальный scroll container.

    После реализации обновить `packages/gantt-lib/src/__tests__/taskListColumns.test.tsx`, чтобы display tests проходили на новых `data-column-id`/`data-custom-column-id` маркерах.
  </action>
  <verify>
    <automated>rg -n "DEFAULT_ADDITIONAL_COLUMN_WIDTH = 120|normalizeColumnWidth|getColumnWidthPx|data-column-id|data-custom-column-id|gantt-tl-cell-custom|gantt-tl-headerCell-custom" packages/gantt-lib/src/components/TaskList/TaskList.tsx packages/gantt-lib/src/components/TaskList/TaskListRow.tsx packages/gantt-lib/src/components/TaskList/TaskList.css</automated>
    <automated>npm run test -- --run src/__tests__/taskListColumns.test.tsx</automated>
  </verify>
  <done>
    Display path для additionalColumns работает: колонки рендерятся в нужном порядке, с одинаковой шириной в header/body, увеличивают width budget overlay и не ломают built-in columns.
  </done>
  <acceptance_criteria>
    - `packages/gantt-lib/src/components/TaskList/TaskList.tsx` содержит `const DEFAULT_ADDITIONAL_COLUMN_WIDTH = 120`
    - `packages/gantt-lib/src/components/TaskList/TaskList.tsx` содержит `Math.max(taskListWidth, MIN_TASK_LIST_WIDTH + additionalWidth)`
    - `packages/gantt-lib/src/components/TaskList/TaskList.tsx` содержит `data-column-id="custom:`
    - `packages/gantt-lib/src/components/TaskList/TaskListRow.tsx` содержит `data-custom-column-id`
    - `packages/gantt-lib/src/components/TaskList/TaskList.css` содержит `.gantt-tl-cell-custom`
    - `packages/gantt-lib/src/components/TaskList/TaskList.css` содержит `.gantt-tl-headerCell-custom`
    - `npm run test -- --run src/__tests__/taskListColumns.test.tsx` проходит для display scenarios
  </acceptance_criteria>
</task>

</tasks>

<verification>
После завершения плана:
1. `additionalColumns` доступен в public `GanttChart` API.
2. Custom columns рендерятся после нужных built-in anchors и fallback-ятся на `name`.
3. Width budget overlay увеличивается без nested scroll.
4. Все display/path tests из `taskListColumns.test.tsx` проходят.
</verification>

<success_criteria>
1. Выполнены `COL-01`, `COL-02`, `COL-03`, `COL-05`, `COL-06`, `COL-07`, `COL-08`.
2. API остаётся extension-layer поверх built-in columns, не переходя в deferred unified `columns`.
3. Типизация extended task проходит через весь путь `GanttChart -> TaskList -> TaskListRow`.
</success_criteria>

<output>
After completion, create `.planning/phases/23-additional-tasklist-columns/23-02-SUMMARY.md`
</output>
