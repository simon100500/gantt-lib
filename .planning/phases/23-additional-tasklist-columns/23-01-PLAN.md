---
phase: 23-additional-tasklist-columns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/gantt-lib/src/components/TaskList/taskListColumns.ts
  - packages/gantt-lib/src/index.ts
  - packages/gantt-lib/src/__tests__/taskListColumns.test.tsx
autonomous: true
requirements: [COL-01, COL-02, COL-03, COL-05, COL-06, COL-07, COL-08]
user_setup: []
must_haves:
  truths:
    - "Пользователь может импортировать тип конфигурации дополнительных колонок из публичного API библиотеки"
    - "Тестовый файл фиксирует порядок колонок, ширины, renderCell и сохранность базовых колонок до реализации"
    - "Фаза получает Wave 0 покрытие для всех ключевых сценариев additionalColumns"
  artifacts:
    - path: packages/gantt-lib/src/components/TaskList/taskListColumns.ts
      provides: "Публичные generic-типы TaskListColumn, TaskListColumnContext и BuiltInTaskListColumnId"
      contains: ["export type BuiltInTaskListColumnId", "export interface TaskListColumnContext", "export interface TaskListColumn"]
    - path: packages/gantt-lib/src/index.ts
      provides: "Публичный экспорт column API"
      contains: ["TaskListColumn", "TaskListColumnContext", "BuiltInTaskListColumnId"]
    - path: packages/gantt-lib/src/__tests__/taskListColumns.test.tsx
      provides: "Wave 0 интеграционные тесты для additionalColumns"
      contains: ["describe('GanttChart additionalColumns'", "it('renders additional columns after the configured built-in anchor'"]
  key_links:
    - from: "packages/gantt-lib/src/components/TaskList/taskListColumns.ts"
      to: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      via: "generic TaskListColumn<TTask> prop contract"
      pattern: "additionalColumns\\?: TaskListColumn<"
    - from: "packages/gantt-lib/src/index.ts"
      to: "packages/gantt-lib/src/components/TaskList/taskListColumns.ts"
      via: "public type export"
      pattern: "TaskListColumn"
    - from: "packages/gantt-lib/src/__tests__/taskListColumns.test.tsx"
      to: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      via: "integration render against public GanttChart API"
      pattern: "render\\(<GanttChart"
---

<objective>
Зафиксировать публичный контракт дополнительных колонок и создать wave-0 тестовый каркас до реализации UI

Purpose: Убрать двусмысленность в generic API и дать последующим планам конкретные тесты, а не словесное ожидание
Output: Новый файл контрактов колонок, публичный экспорт из root API и failing integration tests для additionalColumns
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-additional-tasklist-columns/23-CONTEXT.md
@.planning/phases/23-additional-tasklist-columns/23-RESEARCH.md
@.planning/phases/23-additional-tasklist-columns/23-VALIDATION.md
@packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
@packages/gantt-lib/src/components/TaskList/TaskList.tsx
@packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
@packages/gantt-lib/src/index.ts
@packages/gantt-lib/src/__tests__/taskFilter.test.tsx
@packages/gantt-lib/src/__tests__/taskListDuration.test.tsx

<interfaces>
From packages/gantt-lib/src/components/GanttChart/GanttChart.tsx:
```typescript
export interface GanttChartProps {
  tasks: Task[];
  onTasksChange?: (tasks: Task[]) => void;
  showTaskList?: boolean;
  taskListWidth?: number;
}
```

From packages/gantt-lib/src/components/TaskList/TaskList.tsx:
```typescript
const MIN_TASK_LIST_WIDTH = 530;

export interface TaskListProps {
  tasks: Task[];
  rowHeight: number;
  headerHeight: number;
  taskListWidth?: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать публичный generic контракт additionalColumns per D-01, D-04, D-07, D-10, D-11, D-15</name>
  <files>packages/gantt-lib/src/components/TaskList/taskListColumns.ts, packages/gantt-lib/src/index.ts</files>
  <read_first>
    - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
    - packages/gantt-lib/src/components/TaskList/TaskList.tsx
    - packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
    - packages/gantt-lib/src/index.ts
    - .planning/phases/23-additional-tasklist-columns/23-CONTEXT.md
  </read_first>
  <action>
    Создать файл `packages/gantt-lib/src/components/TaskList/taskListColumns.ts` и объявить в нём ровно эти публичные контракты:

    ```typescript
    import type { ReactNode } from 'react';
    import type { Task } from '../GanttChart';

    export type BuiltInTaskListColumnId =
      | 'number'
      | 'name'
      | 'startDate'
      | 'endDate'
      | 'duration'
      | 'progress'
      | 'dependencies'
      | 'actions';

    export interface TaskListColumnContext<TTask extends Task = Task> {
      task: TTask;
      rowIndex: number;
      columnId: string;
      isEditing: boolean;
      openEditor: () => void;
      closeEditor: () => void;
      updateTask: (patch: Partial<TTask>) => void;
    }

    export interface TaskListColumn<TTask extends Task = Task> {
      id: string;
      header: ReactNode;
      renderCell: (row: TaskListColumnContext<TTask>) => ReactNode;
      editor?: (row: TaskListColumnContext<TTask>) => ReactNode;
      width?: string | number;
      after?: BuiltInTaskListColumnId;
      meta?: Record<string, unknown>;
    }
    ```

    Не добавлять unified `columns` API и не описывать системные колонки конфигом. Использовать имя `TaskListColumn`; поле редактирования назвать `editor`; `meta` оставить декларативным `Record<string, unknown>`.

    Затем обновить `packages/gantt-lib/src/index.ts`, добавив:

    ```typescript
    export type {
      BuiltInTaskListColumnId,
      TaskListColumn,
      TaskListColumnContext,
    } from './components/TaskList/taskListColumns';
    ```
  </action>
  <verify>
    <automated>rg -n "export type BuiltInTaskListColumnId|export interface TaskListColumnContext|export interface TaskListColumn" packages/gantt-lib/src/components/TaskList/taskListColumns.ts</automated>
    <automated>rg -n "BuiltInTaskListColumnId|TaskListColumn|TaskListColumnContext" packages/gantt-lib/src/index.ts</automated>
  </verify>
  <done>
    Контракт additionalColumns существует в отдельном файле, generic по `TTask extends Task`, экспортирован из root API и не вводит deferred unified `columns` API.
  </done>
  <acceptance_criteria>
    - `packages/gantt-lib/src/components/TaskList/taskListColumns.ts` содержит строку `export type BuiltInTaskListColumnId =`
    - `packages/gantt-lib/src/components/TaskList/taskListColumns.ts` содержит строку `editor?: (row: TaskListColumnContext<TTask>) => ReactNode;`
    - `packages/gantt-lib/src/components/TaskList/taskListColumns.ts` содержит строку `after?: BuiltInTaskListColumnId;`
    - `packages/gantt-lib/src/components/TaskList/taskListColumns.ts` содержит строку `meta?: Record<string, unknown>;`
    - `packages/gantt-lib/src/index.ts` содержит `TaskListColumn`
    - `packages/gantt-lib/src/index.ts` содержит `TaskListColumnContext`
    - `packages/gantt-lib/src/index.ts` содержит `BuiltInTaskListColumnId`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать wave-0 failing tests для ordering, widths, renderCell и base-column regression per D-05, D-06</name>
  <files>packages/gantt-lib/src/__tests__/taskListColumns.test.tsx</files>
  <read_first>
    - packages/gantt-lib/src/__tests__/taskFilter.test.tsx
    - packages/gantt-lib/src/__tests__/taskListDuration.test.tsx
    - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
    - packages/gantt-lib/src/components/TaskList/TaskList.tsx
    - packages/gantt-lib/src/components/TaskList/taskListColumns.ts
    - .planning/phases/23-additional-tasklist-columns/23-VALIDATION.md
  </read_first>
  <action>
    Создать файл `packages/gantt-lib/src/__tests__/taskListColumns.test.tsx` по существующему паттерну vitest/testing-library и зафиксировать в нём следующие тесты:

    - `it('renders additional columns after the configured built-in anchor')`
    - `it('falls back to the name anchor when after is omitted or invalid')`
    - `it('renders custom cell content for every visible row')`
    - `it('applies numeric and string widths to matching header and body cells')`
    - `it('keeps base columns visible when additional columns are present')`
    - `it('grows task list width budget when additional columns are provided')`
    - `it('opens a custom editor and saves a merged task patch through onTasksChange')`

    Использовать exact API shape:

    ```tsx
    type ExtendedTask = Task & { assignee?: string; status?: string };

    const additionalColumns: TaskListColumn<ExtendedTask>[] = [
      { id: 'assignee', header: 'Исполнитель', after: 'name', width: 140, renderCell: ({ task }) => task.assignee ?? '-' },
      {
        id: 'status',
        header: 'Статус',
        after: 'progress',
        width: '96px',
        renderCell: ({ task }) => task.status ?? 'new',
        editor: ({ task, updateTask, closeEditor }) => (
          <button type="button" onClick={() => { updateTask({ status: 'done' }); closeEditor(); }}>
            edit-{task.id}
          </button>
        ),
      },
    ];
    ```

    Мокнуть `DatePicker` и `Popover` как в существующих тестах. Рендерить `GanttChart` с `showTaskList`, `rowHeight={36}`, `headerHeight={40}`, `taskListWidth={660}`. Для DOM-проверок использовать `data-column-id`, `data-custom-column-id` и `data-testid`; если маркеров ещё нет, тесты должны падать и тем самым зафиксировать требуемый DOM contract.
  </action>
  <verify>
    <automated>npm run test -- --run src/__tests__/taskListColumns.test.tsx</automated>
  </verify>
  <done>
    Создан wave-0 тестовый файл, который падает до реализации и фиксирует exact DOM/data contracts для additionalColumns, включая editing pipeline.
  </done>
  <acceptance_criteria>
    - `packages/gantt-lib/src/__tests__/taskListColumns.test.tsx` содержит `describe('GanttChart additionalColumns'`
    - `packages/gantt-lib/src/__tests__/taskListColumns.test.tsx` содержит `it('renders additional columns after the configured built-in anchor'`
    - `packages/gantt-lib/src/__tests__/taskListColumns.test.tsx` содержит `it('opens a custom editor and saves a merged task patch through onTasksChange'`
    - `packages/gantt-lib/src/__tests__/taskListColumns.test.tsx` содержит `type ExtendedTask = Task & { assignee?: string; status?: string }`
    - `packages/gantt-lib/src/__tests__/taskListColumns.test.tsx` содержит `width: 140`
    - `packages/gantt-lib/src/__tests__/taskListColumns.test.tsx` содержит `width: '96px'`
  </acceptance_criteria>
</task>

</tasks>

<verification>
После завершения плана:
1. `taskListColumns.ts` экспортирует все публичные контракты additionalColumns.
2. `src/index.ts` открывает их наружу без дополнительных alias.
3. `taskListColumns.test.tsx` существует и фиксирует все критические сценарии из validation strategy.
4. Фокусный тестовый запуск падает ожидаемо до реализации UI, а не из-за отсутствия файла или неверной конфигурации vitest.
</verification>

<success_criteria>
1. Контракт колонок зафиксирован в коде, а не только в плане.
2. Wave 0 для `COL-01..COL-08` создан до реализации.
3. Следующий план может реализовывать UI по already-written test contract без дополнительных решений.
</success_criteria>

<output>
After completion, create `.planning/phases/23-additional-tasklist-columns/23-01-SUMMARY.md`
</output>
