---
phase: 21.1
plan: 01
type: execute
wave: 1
depends_on: ["21-custom-weekend-calendar"]
files_modified:
  - packages/gantt-lib/src/utils/dateUtils.ts
  - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
  - packages/gantt-lib/src/__tests__/dateUtils.test.ts
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "User can pass single customDays array with {date, type} objects"
    - "customDays.type 'weekend' marks date as weekend (override)"
    - "customDays.type 'workday' marks date as workday (override)"
    - "isWeekend predicate still works as base logic"
    - "customDays overrides isWeekend when both specify same date"
    - "Old three-prop API (weekends, workdays, isWeekend) is removed"
    - "All tests pass with new API"
  artifacts:
    - path: "packages/gantt-lib/src/utils/dateUtils.ts"
      provides: "CustomDayConfig interface, createCustomDayPredicate utility"
      exports: ["CustomDayConfig", "createCustomDayPredicate"]
      contains: "type CustomDayConfig = { date: Date; type: 'weekend' | 'workday' }"
    - path: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      provides: "Updated GanttChartProps with new API"
      exports: ["GanttChartProps"]
      contains: "customDays?: CustomDayConfig[]"
    - path: "packages/gantt-lib/src/__tests__/dateUtils.test.ts"
      provides: "Tests for new customDays API"
      contains: "describe('createCustomDayPredicate')"
  key_links:
    - from: "GanttChart.customDays"
      to: "createCustomDayPredicate"
      via: "useMemo memoization"
      pattern: "useMemo.*createCustomDayPredicate"
    - from: "GanttChart.isCustomWeekend"
      to: "GridBackground.isCustomWeekend"
      via: "prop pass-through"
      pattern: "isCustomWeekend=\\{isCustomWeekend\\}"
    - from: "createCustomDayPredicate"
      to: "customDays array"
      via: "Set-based lookup"
      pattern: "new Set.*createDateKey"
---

<objective>
Рефакторинг API кастомных выходных: заменить три пропа (weekends, workdays, isWeekend) на единый массив customDays с типизацией.

**Цель:** Упростить API, уменьшить когнитивную нагрузку (один проп вместо трёх), сохранить гибкость через объекты {date, type}.

**Результат:** Компонент принимает `customDays?: Array<{date: Date; type: 'weekend' | 'workday'}>` + опциональный `isWeekend` для базовой логики. Старые три пропа удалены.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/21-custom-weekend-calendar/21-CONTEXT.md
@.planning/phases/21-custom-weekend-calendar/21-02-SUMMARY.md
@.planning/phases/21-custom-weekend-calendar/21-03-SUMMARY.md

# Current Implementation (Phase 21)

From Phase 21 CONTEXT.md:
```typescript
// Current API (three props)
weekends?: Date[]      // Добавить к выходным (праздники)
workdays?: Date[]      // Исключить из выходных (рабочие субботы)
isWeekend?: (date: Date) => boolean  // Кастомный предикат
```

From Phase 21 SUMMARY.md (createIsWeekendPredicate):
```typescript
// Precedence order (highest to lowest):
// 1. isWeekend (custom predicate) - use directly, ignore arrays
// 2. workdays - exclude these dates from default weekends
// 3. weekends - add these dates to default weekends
// 4. default - Saturday (6) and Sunday (0)
```

# New Design (Phase 21.1)

```typescript
// New API (unified array)
interface CustomDayConfig {
  date: Date;
  type: 'weekend' | 'workday';
}

// Props
customDays?: CustomDayConfig[];  // Unified array
isWeekend?: (date: Date) => boolean;  // Optional base logic

// Logic:
// 1. Check if date in customDays.workdays → workday (override)
// 2. Check if date in customDays.weekends → weekend (override)
// 3. Check isWeekend(date) → result (base logic)
// 4. Default → Saturday/Sunday
```

# Examples

```tsx
// Simple holidays + working Saturdays
<GanttChart
  customDays={[
    { date: new Date('2026-01-07'), type: 'workday' }, // working Saturday
    { date: new Date('2026-01-01'), type: 'weekend' }  // holiday Tuesday
  ]}
/>

// 4-day work week + occasional overrides
<GanttChart
  isWeekend={(date) => {
    const day = date.getDay();
    return day === 0 || day === 6 || day === 5; // Sun+Sat+Fri
  }}
  customDays={[
    { date: new Date('2026-01-10'), type: 'workday' } // working Friday
  ]}
/>
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Обновить dateUtils.ts — новый API customDays</name>
  <files>packages/gantt-lib/src/utils/dateUtils.ts</files>
  <read_first>
    1. packages/gantt-lib/src/utils/dateUtils.ts (текущая реализация WeekendConfig, createIsWeekendPredicate)
    2. packages/gantt-lib/src/__tests__/dateUtils.test.ts (текущие тесты для createIsWeekendPredicate)
  </read_first>
  <action>
    **Выполнить в packages/gantt-lib/src/utils/dateUtils.ts:**

    1. Добавить новый интерфейс CustomDayConfig:
    ```typescript
    export interface CustomDayConfig {
      date: Date;
      type: 'weekend' | 'workday';
    }
    ```

    2. Создать новую функцию createCustomDayPredicate (заменяет createIsWeekendPredicate):
    ```typescript
    export interface CustomDayPredicateConfig {
      customDays?: CustomDayConfig[];
      isWeekend?: (date: Date) => boolean;
    }

    export const createCustomDayPredicate = (
      config: CustomDayPredicateConfig
    ): ((date: Date) => boolean) => {
      const { customDays, isWeekend: basePredicate } = config;

      // Build Set-based lookups for O(1) performance
      const workdaySet = new Set<string>();
      const weekendSet = new Set<string>();

      if (customDays && customDays.length > 0) {
        for (const item of customDays) {
          const key = createDateKey(item.date);
          if (item.type === 'workday') {
            workdaySet.add(key);
          } else { // weekend
            weekendSet.add(key);
          }
        }
      }

      return (date: Date): boolean => {
        const key = createDateKey(date);

        // Priority 1: customDays workdays (highest override)
        if (workdaySet.has(key)) {
          return false; // Explicitly a workday
        }

        // Priority 2: customDays weekends (override)
        if (weekendSet.has(key)) {
          return true; // Explicitly a weekend
        }

        // Priority 3: base predicate (if provided)
        if (basePredicate) {
          return basePredicate(date);
        }

        // Priority 4: default Saturday/Sunday
        const dayOfWeek = date.getUTCDay();
        return dayOfWeek === 0 || dayOfWeek === 6;
      };
    };
    ```

    3. УДАЛИТЬ старый интерфейс WeekendConfig и функцию createIsWeekendPredicate (или пометить как @deprecated если нужна обратная совместимость в переходный период).

    4. Обновить JSDoc комментарии для новой функции с примерами использования.
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npx tsc --noEmit</automated>
    <automated>grep -n "export interface CustomDayConfig" packages/gantt-lib/src/utils/dateUtils.ts</automated>
    <automated>grep -n "export const createCustomDayPredicate" packages/gantt-lib/src/utils/dateUtils.ts</automated>
  </verify>
  <done>
    - CustomDayConfig интерфейс создан и экспортируется
    - createCustomDayPredicate функция создана с логикой приоритетов (customDays > isWeekend > default)
    - Set-based lookup для O(1) производительности
    - TypeScript компилируется без ошибок
  </done>
</task>

<task type="auto">
  <name>Task 2: Обновить GanttChart.tsx — новый API customDays</name>
  <files>packages/gantt-lib/src/components/GanttChart/GanttChart.tsx</files>
  <read_first>
    1. packages/gantt-lib/src/components/GanttChart/GanttChart.tsx (строки 129-134 — текущие пропсы weekends, workdays, isWeekend)
    2. packages/gantt-lib/src/utils/dateUtils.ts (новый импорт CustomDayConfig, createCustomDayPredicate)
  </read_first>
  <action>
    **Выполнить в packages/gantt-lib/src/components/GanttChart/GanttChart.tsx:**

    1. Обновить импорты (строка 4):
    ```typescript
    import { getMultiMonthDays, createCustomDayPredicate, type CustomDayConfig, type CustomDayPredicateConfig } from '../../utils/dateUtils';
    ```

    2. В интерфейсе GanttChartProps (строки 129-134) ЗАМЕНИТЬ:
    ```typescript
    // СТАРОЕ (удалить):
    // /** Custom weekend dates to ADD to default weekends (e.g., holidays) */
    // weekends?: Date[];
    // /** Custom workday dates to EXCLUDE from default weekends (e.g., shifted workdays) */
    // workdays?: Date[];
    // /** Flexible weekend logic predicate (overrides arrays) */
    // isWeekend?: (date: Date) => boolean;

    // НОВОЕ (добавить):
    /** Custom day configurations with explicit type (weekend or workday) */
    customDays?: CustomDayConfig[];
    /** Optional base weekend predicate (checked before customDays overrides) */
    isWeekend?: (date: Date) => boolean;
    ```

    3. В теле компонента GanttChart (примерно строка 195-197) ЗАМЕНИТЬ деструктуризацию пропсов:
    ```typescript
    // СТАРОЕ (удалить):
    // weekends,
    // workdays,
    // isWeekend,

    // НОВОЕ (добавить):
    customDays,
    isWeekend,
    ```

    4. Обновить useMemo для isCustomWeekend (найти существующий useMemo, использующий createIsWeekendPredicate):
    ```typescript
    // СТАРОЕ (удалить):
    // const isCustomWeekend = useMemo(
    //   () => createIsWeekendPredicate({ weekends, workdays, isWeekend }),
    //   [weekends, workdays, isWeekend]
    // );

    // НОВОЕ (добавить):
    const isCustomWeekend = useMemo(
      () => createCustomDayPredicate({ customDays, isWeekend }),
      [customDays, isWeekend]
    );
    ```

    5. Убедиться, что isCustomWeekend передается в GridBackground и TimeScaleHeader (это уже должно быть реализовано в Phase 21, изменений не требуется).
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npx tsc --noEmit</automated>
    <automated>grep -n "customDays?: CustomDayConfig\[\]" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx</automated>
    <automated>grep -n "createCustomDayPredicate" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx</automated>
    <automated>grep -n "weekends\?: Date\[\]" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx && exit 1 || echo "Old API removed"</automated>
  </verify>
  <done>
    - GanttChartProps использует customDays вместо weekends/workdays
    - isWeekend prop сохранён как опциональная базовая логика
    - createCustomDayPredicate вызывается с новым config объектом
    - Старые пропсы (weekends, workdays) удалены из интерфейса
    - TypeScript компилируется без ошибок
  </done>
</task>

<task type="auto">
  <name>Task 3: Обновить тесты для нового API customDays</name>
  <files>packages/gantt-lib/src/__tests__/dateUtils.test.ts</files>
  <read_first>
    1. packages/gantt-lib/src/__tests__/dateUtils.test.ts (существующие тесты для createIsWeekendPredicate)
    2. packages/gantt-lib/src/utils/dateUtils.ts (новая сигнатура createCustomDayPredicate)
  </read_first>
  <action>
    **Выполнить в packages/gantt-lib/src/__tests__/dateUtils.test.ts:**

    1. УДАЛИТЬ старые тесты для createIsWeekendPredicate (найти блок describe('createIsWeekendPredicate')).

    2. ДОБАВИТЬ новые тесты для createCustomDayPredicate:
    ```typescript
    describe('createCustomDayPredicate', () => {
      it('should return default weekends (Sat/Sun) when no config provided', () => {
        const predicate = createCustomDayPredicate({});
        const saturday = new Date(Date.UTC(2026, 2, 15)); // Sat
        const sunday = new Date(Date.UTC(2026, 2, 16));   // Sun
        const monday = new Date(Date.UTC(2026, 2, 17));   // Mon

        expect(predicate(saturday)).toBe(true);
        expect(predicate(sunday)).toBe(true);
        expect(predicate(monday)).toBe(false);
      });

      it('should mark customDays.type=weekend as weekend', () => {
        const predicate = createCustomDayPredicate({
          customDays: [
            { date: new Date(Date.UTC(2026, 2, 17)), type: 'weekend' } // Monday
          ]
        });
        const monday = new Date(Date.UTC(2026, 2, 17));

        expect(predicate(monday)).toBe(true);
      });

      it('should mark customDays.type=workday as workday', () => {
        const predicate = createCustomDayPredicate({
          customDays: [
            { date: new Date(Date.UTC(2026, 2, 15)), type: 'workday' } // Saturday
          ]
        });
        const saturday = new Date(Date.UTC(2026, 2, 15));

        expect(predicate(saturday)).toBe(false);
      });

      it('should prioritize customDays.workdays over customDays.weekends', () => {
        const predicate = createCustomDayPredicate({
          customDays: [
            { date: new Date(Date.UTC(2026, 2, 15)), type: 'weekend' },
            { date: new Date(Date.UTC(2026, 2, 15)), type: 'workday' } // Same date, workday wins
          ]
        });
        const saturday = new Date(Date.UTC(2026, 2, 15));

        // workdaySet has priority over weekendSet in implementation
        expect(predicate(saturday)).toBe(false);
      });

      it('should use base isWeekend predicate before default', () => {
        const predicate = createCustomDayPredicate({
          isWeekend: (date: Date) => date.getUTCDay() === 0 // Sunday-only
        });
        const saturday = new Date(Date.UTC(2026, 2, 15)); // Sat
        const sunday = new Date(Date.UTC(2026, 2, 16));   // Sun

        expect(predicate(saturday)).toBe(false);
        expect(predicate(sunday)).toBe(true);
      });

      it('should prioritize customDays over base isWeekend predicate', () => {
        const predicate = createCustomDayPredicate({
          isWeekend: (date: Date) => date.getUTCDay() === 0, // Sunday-only
          customDays: [
            { date: new Date(Date.UTC(2026, 2, 15)), type: 'weekend' } // Saturday
          ]
        });
        const saturday = new Date(Date.UTC(2026, 2, 15));

        expect(predicate(saturday)).toBe(true); // customDays wins
      });

      it('should handle empty customDays array', () => {
        const predicate = createCustomDayPredicate({
          customDays: []
        });
        const saturday = new Date(Date.UTC(2026, 2, 15));

        expect(predicate(saturday)).toBe(true); // Default behavior
      });

      it('should work with multiple custom days', () => {
        const predicate = createCustomDayPredicate({
          customDays: [
            { date: new Date(Date.UTC(2026, 2, 15)), type: 'workday' }, // Sat -> workday
            { date: new Date(Date.UTC(2026, 2, 17)), type: 'weekend' }, // Mon -> weekend
            { date: new Date(Date.UTC(2026, 2, 18)), type: 'workday' }  // Tue -> workday
          ]
        });
        const saturday = new Date(Date.UTC(2026, 2, 15));
        const sunday = new Date(Date.UTC(2026, 2, 16));
        const monday = new Date(Date.UTC(2026, 2, 17));
        const tuesday = new Date(Date.UTC(2026, 2, 18));
        const wednesday = new Date(Date.UTC(2026, 2, 19));

        expect(predicate(saturday)).toBe(false);
        expect(predicate(sunday)).toBe(true); // Default Sunday
        expect(predicate(monday)).toBe(true); // Custom weekend
        expect(predicate(tuesday)).toBe(false); // Custom workday
        expect(predicate(wednesday)).toBe(false); // Default workday
      });
    });
    ```

    3. Сохранить существующие тесты для createDateKey (они не меняются).
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npm test -- dateUtils.test.ts --run</automated>
    <automated>grep -n "describe('createCustomDayPredicate')" packages/gantt-lib/src/__tests__/dateUtils.test.ts</automated>
    <automated>grep -n "describe('createIsWeekendPredicate')" packages/gantt-lib/src/__tests__/dateUtils.test.ts && exit 1 || echo "Old tests removed"</automated>
  </verify>
  <done>
    - Старые тесты createIsWeekendPredicate удалены
    - Новые тесты createCustomDayPredicate добавлены (8 тестов)
    - Все тесты проходят (npm test -- dateUtils.test.ts)
    - Покрыты все случаи: default, custom.weekend, custom.workday, приоритеты, base predicate, edge cases
  </done>
</task>

</tasks>

<verification>
## Общая проверка фазы

1. **TypeScript компиляция:**
   ```bash
   cd packages/gantt-lib && npx tsc --noEmit
   ```
   Ожидается: Нет ошибок компиляции

2. **Все тесты проходят:**
   ```bash
   cd packages/gantt-lib && npm test -- --run
   ```
   Ожидается: Все тесты проходят (старые тесты geometry + новые тесты createCustomDayPredicate)

3. **Проверка удаления старого API:**
   ```bash
   grep -r "weekends\?: Date\[\]" packages/gantt-lib/src && exit 1 || echo "Old API removed"
   ```
   Ожидается: Ничего не найдено (старый API удалён)

4. **Проверка нового API:**
   ```bash
   grep -n "customDays\?: CustomDayConfig\[\]" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
   ```
   Ожидается: Найдено в GanttChartProps

5. **Проверка экспортов:**
   ```bash
   grep -n "export.*CustomDayConfig" packages/gantt-lib/src/utils/dateUtils.ts
   grep -n "export.*createCustomDayPredicate" packages/gantt-lib/src/utils/dateUtils.ts
   ```
   Ожидается: Оба экспорта присутствуют
</verification>

<success_criteria>
## Критерии успеха

1. ✅ Новый API реализован: `customDays?: CustomDayConfig[]` + `isWeekend?: (date: Date) => boolean`
2. ✅ Старый API удалён: пропсы `weekends` и `workdays` отсутствуют в GanttChartProps
3. ✅ Логика приоритетов работает: customDays > isWeekend > default (Sat/Sun)
4. ✅ Set-based lookup для O(1) производительности сохранён
5. ✅ Все тесты проходят (новые + существующие)
6. ✅ TypeScript компилируется без ошибок
7. ✅ Обратная совместимость НЕ сохранена (breaking change — это рефакторинг API)
</success_criteria>

<output>
After completion, create `.planning/phases/21.1-custom-weekend-refactoring/21.1-01-SUMMARY.md`
</output>
