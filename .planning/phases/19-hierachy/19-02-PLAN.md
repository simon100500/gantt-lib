---
phase: 19-hierachy
plan: 02
type: execute
wave: 1
depends_on: [19-01]
files_modified:
  - packages/gantt-lib/src/types/index.ts
  - packages/gantt-lib/src/components/TaskList/TaskList.tsx
  - packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
  - packages/gantt-lib/src/components/TaskList/TaskList.css
  - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Task interface has optional parentId field for hierarchy"
    - "Child rows have left padding (indentation) in task list"
    - "Parent rows have bold font and subtle background tint"
    - "Parent rows show collapse/expand button (+/-) instead of row number"
    - "Collapsed state hides children from both task list and chart"
    - "isParent is computed from task array (not stored field)"
    - "TaskList filters out children when parent is collapsed"
  artifacts:
    - path: "packages/gantt-lib/src/types/index.ts"
      provides: "Task interface with parentId field"
      contains: "parentId?: string"
    - path: "packages/gantt-lib/src/components/TaskList/TaskList.tsx"
      provides: "Collapse state management and filtered task rendering"
      exports: ["collapsedParentIds state", "handleToggleCollapse callback"]
    - path: "packages/gantt-lib/src/components/TaskList/TaskListRow.tsx"
      provides: "Parent row detection and collapse button rendering"
      contains: "isParent, isChild computed props"
    - path: "packages/gantt-lib/src/components/TaskList/TaskList.css"
      provides: "Child row indentation and parent row styling"
      contains: ".gantt-tl-row-child, .gantt-tl-row-parent, .gantt-tl-collapse-btn"
  key_links:
    - from: "TaskListRow.tsx"
      to: "dependencyUtils.ts"
      via: "import { isTaskParent, getChildren }"
      pattern: "isTaskParent\\(task.id, allTasks\\)"
    - from: "TaskList.tsx"
      to: "GanttChart.tsx"
      via: "collapsedParentIds prop and filtered tasks"
      pattern: "tasks=.*filteredTasks|collapsedParentIds"
    - from: "TaskListRow.tsx"
      to: "TaskList.tsx"
      via: "onToggleCollapse callback"
      pattern: "onToggleCollapse\\?\\.\\(task\\.id\\)"
---

<objective>
Extend Task type with parentId field, implement TaskList hierarchy UI with indentation and collapse/expand buttons

Purpose: Enable parent-child task relationships with visual hierarchy in task list
Output: Working task list with child row indentation, parent row styling, and collapse functionality
</objective>

<execution_context>
@D:/Projects/gantt-lib/.claude/get-shit-done/workflows/execute-plan.md
@D:/Projects/gantt-lib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-hierachy/19-CONTEXT.md
@.planning/phases/19-hierachy/19-RESEARCH.md
@.planning/phases/19-hierachy/19-01-PLAN.md

# Existing patterns to follow
From Phase 12-01 SUMMARY - TaskList component:
- TaskListRow receives all task data as props (no task fetching in component)
- Row selection state managed in GanttChart, passed to TaskList
- CSS class toggling based on props (e.g., gantt-tl-row-selected)
- Inline editing uses useState + useEffect for auto-focus

From Phase 18-02 SUMMARY - TaskList drag handle:
- Hover-reveal pattern: opacity 0→1 on row hover
- Span elements with onClick and e.stopPropagation()
- Number cell modified to include additional UI elements

# Key interfaces from existing code
From packages/gantt-lib/src/types/index.ts:
```typescript
export interface Task {
  id: string;
  name: string;
  startDate: string | Date;
  endDate: string | Date;
  color?: string;
  progress?: number;
  accepted?: boolean;
  dependencies?: TaskDependency[];
  locked?: boolean;
  // NEW: Add parentId?: string
}
```

From packages/gantt-lib/src/utils/dependencyUtils.ts (Phase 19-01):
```typescript
export function isTaskParent(taskId: string, tasks: Task[]): boolean;
export function getChildren(parentId: string, tasks: Task[]): Task[];
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Task type with parentId field</name>
  <files>packages/gantt-lib/src/types/index.ts</files>
  <action>
    Add parentId field to Task interface:
    - Add optional field: `parentId?: string;`
    - Place after locked field (maintain logical grouping of relationship fields)
    - Add JSDoc comment: "/** Optional ID of parent task for hierarchy (undefined for root-level tasks) */"

    Do NOT add isParent field (computed from task array, not stored).
  </action>
  <verify>
    <automated>grep "parentId\?: string" packages/gantt-lib/src/types/index.ts</automated>
  </verify>
  <done>Task interface has optional parentId field with documentation</done>
</task>

<task type="auto">
  <name>Task 2: Add collapse state and filtered rendering to TaskList</name>
  <files>packages/gantt-lib/src/components/TaskList/TaskList.tsx</files>
  <action>
    Extend TaskList component with hierarchy support:

    State:
    - Add `collapsedParentIds: Set<string>` useState (initialize as empty Set)
    - Import isTaskParent and getChildren from dependencyUtils

    Callbacks:
    - `handleToggleCollapse(parentId: string)`: useCallback that toggles parentId in Set
    - `getVisibleTasks()`: Memo that filters tasks:
      * Return all tasks where:
        - task.parentId is undefined (root level), OR
        - task.parentId is NOT in collapsedParentIds
      * Use useMemo with [tasks, collapsedParentIds] deps

    Props interface:
    - Add `collapsedParentIds?: Set<string>` to TaskListProps (for external control)
    - Add `onToggleCollapse?: (parentId: string) => void` to TaskListProps
    - If collapsedParentIds prop provided, use it (controlled mode)
    - Otherwise use internal state (uncontrolled mode)

    Render:
    - Pass filteredTasks to task list rendering (not raw tasks prop)
    - Pass collapsedParentIds and onToggleCollapse to each TaskListRow
    - Pass allTasks (unfiltered) to TaskListRow for isParent computation

    Pattern: Follow existing selectedTaskId controlled/uncontrolled pattern from Phase 12.
  </action>
  <verify>
    <automated>grep -E "(collapsedParentIds|handleToggleCollapse|getVisibleTasks)" packages/gantt-lib/src/components/TaskList/TaskList.tsx</automated>
  </verify>
  <done>TaskList manages collapse state and filters hidden children from rendering</done>
</task>

<task type="auto">
  <name>Task 3: Add hierarchy UI to TaskListRow (indentation, collapse button, parent styling)</name>
  <files>packages/gantt-lib/src/components/TaskList/TaskListRow.tsx</files>
  <action>
    Extend TaskListRow component with hierarchy UI:

    Props interface:
    - Add `allTasks?: Task[]` prop (for isParent computation)
    - Add `collapsedParentIds?: Set<string>` prop
    - Add `onToggleCollapse?: (parentId: string) => void` prop

    Computed values:
    - `isParent`: useMemo that checks if any task has this task as parentId
      * Use isTaskParent(task.id, allTasks || [])
    - `isChild`: Boolean check if task.parentId is defined
    - `isCollapsed`: Check if task.id is in collapsedParentIds

    Row className:
    - Add `gantt-tl-row-child` class when isChild is true
    - Add `gantt-tl-row-parent` class when isParent is true

    Number cell modification:
    - If isParent: render collapse button instead of number
      * Button element with className="gantt-tl-collapse-btn"
      * Display text: isCollapsed ? '+' : '-'
      * onClick: call onToggleCollapse(task.id) with e.stopPropagation()
      * aria-label: "Свернуть" when expanded, "Развернуть" when collapsed
    - If not isParent: render normal number span

    Follow Phase 18 pattern for button in number cell (drag handle example).
  </action>
  <verify>
    <automated>grep -E "(isParent|isChild|gantt-tl-collapse-btn|gantt-tl-row-child)" packages/gantt-lib/src/components/TaskList/TaskListRow.tsx</automated>
  </verify>
  <done>TaskListRow shows indentation for children, collapse button for parents, and appropriate styling</done>
</task>

<task type="auto">
  <name>Task 4: Add CSS styles for hierarchy UI (indentation, parent row, collapse button)</name>
  <files>packages/gantt-lib/src/components/TaskList/TaskList.css</files>
  <action>
    Add hierarchy CSS classes to TaskList.css:

    .gantt-tl-row-child:
    - padding-left: 24px (indent child tasks under parent)
    - Keep all other gantt-tl-row styles

    .gantt-tl-row-parent:
    - font-weight: 600 (bold text for parent tasks)
    - background-color: var(--gantt-parent-row-bg, rgba(99, 102, 241, 0.05))
    - Keep other gantt-tl-row styles

    .gantt-tl-collapse-btn:
    - width: 20px, height: 20px
    - border: 1px solid var(--gantt-grid-line-color, #e0e0e0)
    - background: white
    - border-radius: 4px
    - cursor: pointer
    - display: flex, align-items: center, justify-content: center
    - font-size: 14px, font-weight: bold
    - color: var(--gantt-text-color, #374151)

    .gantt-tl-collapse-btn:hover:
    - background-color: var(--gantt-cell-background, #f9fafb)

    Follow existing CSS patterns from TaskList.css (gantt-tl- prefix, CSS variables).
  </action>
  <verify>
    <automated>grep -E "(gantt-tl-row-child|gantt-tl-row-parent|gantt-tl-collapse-btn)" packages/gantt-lib/src/components/TaskList/TaskList.css</automated>
  </verify>
  <done>CSS provides visual hierarchy with child indentation and parent styling</done>
</task>

<task type="auto">
  <name>Task 5: Wire TaskList props through GanttChart</name>
  <files>packages/gantt-lib/src/components/GanttChart/GanttChart.tsx</files>
  <action>
    Thread hierarchy props through GanttChart to TaskList:

    GanttChart state:
    - Add `collapsedParentIds: Set<string>` useState (initialize as empty Set)
    - Import isTaskParent, getChildren from dependencyUtils if needed for internal logic

    GanttChart callbacks:
    - `handleToggleCollapse(parentId: string)`: useCallback that updates collapsedParentIds state
    - Use Set spread pattern: setCollapsedParentIds(prev => { const next = new Set(prev); if (next.has(parentId)) next.delete(parentId); else next.add(parentId); return next; })

    GanttChartProps interface:
    - No new props needed (collapse state is internal to GanttChart)

    TaskList render:
    - Pass collapsedParentIds={collapsedParentIds}
    - Pass onToggleCollapse={handleToggleCollapse}
    - Pass allTasks={tasks} (unfiltered array for isParent computation)

    TaskRow rendering:
    - Pass filteredTasks (not raw tasks) to task rows
    - Filter: tasks.filter(t => {
        const parentCollapsed = t.parentId && collapsedParentIds.has(t.parentId);
        return !parentCollapsed;
      })

    Pattern: Follow existing onTaskSelect, selectedTaskId pattern from Phase 12.
  </action>
  <verify>
    <automated>grep -E "(collapsedParentIds|handleToggleCollapse)" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx</automated>
  </verify>
  <done>GanttChart manages collapse state and filters visible tasks for both chart and task list</done>
</task>

</tasks>

<verification>
Overall phase checks:
- TypeScript compiles: npx tsc --noEmit
- All existing tests pass: npm test
- Library builds: npm run build
- Manual verification: Check task list shows indentation for children, collapse button for parents
</verification>

<success_criteria>
- [ ] Task interface has parentId field in types/index.ts
- [ ] TaskList manages collapsedParentIds state and filters tasks
- [ ] TaskListRow computes isParent/isChild and renders collapse button
- [ ] Child rows are indented (padding-left: 24px)
- [ ] Parent rows have bold font and background tint
- [ ] Collapse button toggles between '+' and '-'
- [ ] Collapsed children are hidden from both task list and chart
- [ ] CSS uses gantt-tl- prefix and CSS variables
- [ ] All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-hierachy/19-02-SUMMARY.md`
</output>
