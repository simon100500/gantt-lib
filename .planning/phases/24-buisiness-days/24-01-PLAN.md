---
phase: 24-buisiness-days
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/gantt-lib/src/__tests__/dateUtils.test.ts
  - packages/gantt-lib/src/utils/dateUtils.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "getBusinessDaysCount подсчитывает количество рабочих дней между датами (исключая выходные по предикату)"
    - "addBusinessDays вычисляет дату окончания по начальному дню и количеству рабочих дней"
    - "Обе функции работают с customDays из Phase 21 (кастомные выходные/рабочие дни)"
    - "UTC-безопасность — никаких DST багов при переходе через летнее/зимнее время"
  artifacts:
    - path: packages/gantt-lib/src/__tests__/dateUtils.test.ts
      provides: "Тесты для getBusinessDaysCount и addBusinessDays"
      contains: "describe('getBusinessDaysCount')", "describe('addBusinessDays')"
    - path: packages/gantt-lib/src/utils/dateUtils.ts
      provides: "Утилиты для расчёта рабочих дней"
      exports: ["getBusinessDaysCount", "addBusinessDays"]
  key_links:
    - from: "getBusinessDaysCount"
      to: "createCustomDayPredicate"
      via: "weekendPredicate parameter"
      pattern: "weekendPredicate.*date.*boolean"
    - from: "addBusinessDays"
      to: "getBusinessDaysCount"
      via: "inverse logic — days to date vs date to days"
      pattern: "while.*daysToAdd"
---

<objective>
Создать и реализовать UTC-безопасные утилиты для расчёта duration в рабочих днях с поддержкой кастомного календаря

Purpose: Расширить функциональность расчёта duration задач, поддерживая режим рабочих дней (исключая выходные по isWeekend/customDays)
Output: Две новые функции в dateUtils.ts с полным тестовым покрытием (TDD RED→GREEN)
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-buisiness-days/24-CONTEXT.md
@.planning/phases/24-buisiness-days/24-RESEARCH.md
@.planning/phases/21-custom-weekend-calendar/21-02-SUMMARY.md
@.planning/phases/21.1-custom-weekend-refactoring/21.1-01-SUMMARY.md

<interfaces>
<!-- Existing dateUtils patterns (from dateUtils.ts lines 9-207, 584-605) -->

From packages/gantt-lib/src/utils/dateUtils.ts:
```typescript
export const parseUTCDate = (date: string | Date): Date;
export const createDateKey = (date: Date): string;
export interface CustomDayConfig {
  date: Date;
  type: 'weekend' | 'workday';
}
export interface CustomDayPredicateConfig {
  customDays?: CustomDayConfig[];
  isWeekend?: (date: Date) => boolean;
}
export const createCustomDayPredicate = (
  config: CustomDayPredicateConfig
): ((date: Date) => boolean);
```

From packages/gantt-lib/src/components/TaskList/TaskListRow.tsx (lines 28-48):
```typescript
const getInclusiveDurationDays = (
  startDate: string | Date,
  endDate: string | Date,
): number => {
  const start = parseUTCDate(startDate);
  const end = parseUTCDate(endDate);
  return Math.max(1, Math.round((end.getTime() - start.getTime()) / DAY_MS) + 1);
};

const getEndDateFromDuration = (
  startDate: string | Date,
  durationDays: number,
): string => {
  const start = parseUTCDate(startDate);
  return new Date(start.getTime() + (durationDays - 1) * DAY_MS)
    .toISOString()
    .split("T")[0];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать failing tests для getBusinessDaysCount и addBusinessDays (TDD RED)</name>
  <files>packages/gantt-lib/src/__tests__/dateUtils.test.ts</files>

  <read_first>
    1. packages/gantt-lib/src/__tests__/dateUtils.test.ts (lines 1-100) — понять паттерн тестирования (describe/it структура)
    2. packages/gantt-lib/src/utils/dateUtils.ts (lines 109-207) — увидеть createDateKey и createCustomDayPredicate для импорта в тесты
    3. packages/gantt-lib/src/utils/dateUtils.ts (lines 28-48) — увидеть getInclusiveDurationDays паттерн для reference
  </read_first>

  <behavior>
    Тесты для getBusinessDaysCount:
    - Подсчитывает рабочие дни в календарном диапазоне (без выходных по умолчанию Sat/Sun)
    - Возвращает минимум 1 день даже для одного дня
    - Исключает субботы и воскресенья (используя weekendPredicate)
    - Работает через границы месяцев (Пт-Пн через Сб/Вс)
    - Работает с кастомными выходными (customDays.type='weekend')
    - Включает кастомные рабочие дни (customDays.type='workday')

    Тесты для addBusinessDays:
    - Вычисляет дату окончания для заданного количества рабочих дней
    - Минимум 1 рабочий день (businessDays >= 1)
    - Пропускает выходные по weekendPredicate
    - Возвращает YYYY-MM-DD формат (ISO date string)
    - Работает через границы месяцев
    - Работает с кастомным календарём (customDays)
  </behavior>

  <action>
    Добавить в packages/gantt-lib/src/__tests__/dateUtils.test.ts:

    1. Импортировать новые функции (пока не реализованы):
       ```typescript
       import { getBusinessDaysCount, addBusinessDays } from '../utils/dateUtils';
       ```

    2. Создать describe('getBusinessDaysCount') с тестами:
       - 'should count 2 business days for Fri-Mon (excludes Sat/Sun)'
       - 'should return 1 for single day range'
       - 'should exclude weekends using default predicate (Sat/Sun)'
       - 'should work with custom weekend predicate'
       - 'should include custom workdays from customDays'
       - 'should exclude custom weekends from customDays'

    3. Создать describe('addBusinessDays') с тестами:
       - 'should calculate end date for business days starting Friday'
       - 'should handle 1 business day (minimum)'
       - 'should skip weekends using default predicate'
       - 'should work with custom weekend predicate'
       - 'should include custom workdays from customDays'
       - 'should exclude custom weekends from customDays'

    Использовать тестовые данные из March 2026 (как в Phase 21):
    - Mar 14, 2026 = Friday (day 5)
    - Mar 15, 2026 = Saturday (day 6)
    - Mar 16, 2026 = Sunday (day 0)
    - Mar 17, 2026 = Monday (day 1)

    Пример теста:
    ```typescript
    describe('getBusinessDaysCount', () => {
      it('should count 2 business days for Fri-Mon (excludes Sat/Sun)', () => {
        const start = new Date(Date.UTC(2026, 2, 14)); // Fri
        const end = new Date(Date.UTC(2026, 2, 17));   // Mon
        const defaultPredicate = (date: Date) => {
          const day = date.getUTCDay();
          return day === 0 || day === 6; // Sun/Sat
        };
        expect(getBusinessDaysCount(start, end, defaultPredicate)).toBe(2);
      });
    });
    ```
  </action>

  <verify>
    <automated>npm test -- --run -t "getBusinessDaysCount|addBusinessDays"</automated>
  </verify>

  <done>
    - 12+ failing тестов создано (6+ для каждой функции)
    - Все тесты падают с ошибкой "(getBusinessDaysCount|addBusinessDays) is not a function"
    - Тесты покрывают все сценарии из CONTEXT.md: календарные дни, через выходные, customDays
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Реализовать getBusinessDaysCount и addBusinessDays (TDD GREEN)</name>
  <files>packages/gantt-lib/src/utils/dateUtils.ts</files>

  <read_first>
    1. packages/gantt-lib/src/utils/dateUtils.ts (lines 28-48) — изучить getInclusiveDurationDays и getEndDateFromDuration для понимания паттерна
    2. packages/gantt-lib/src/utils/dateUtils.ts (lines 109-207) — изучить createDateKey и createCustomDayPredicate для использования в новых функциях
    3. packages/gantt-lib/src/__tests__/dateUtils.test.ts — свежесозданные тесты для понимания ожидаемого поведения
  </read_first>

  <behavior>
    Реализация должна пройти все тесты из Task 1:
    - getBusinessDaysCount: итеративно проходит от start до end, считает только !weekendPredicate(date)
    - addBusinessDays: итеративно добавляет рабочие дни, пропуская weekendPredicate(date)
    - Обе функции используют parseUTCDate для входных данных
    - Обе функции используют getUTC*() методы (не getDate())
  </behavior>

  <action>
    Добавить в packages/gantt-lib/src/utils/dateUtils.ts ПОСЛЕ существующих функций (после normalizeTaskDates или в конец файла):

    1. getBusinessDaysCount функция:
       ```typescript
       /**
        * Count business days between two dates (inclusive), excluding weekends
        * @param startDate - Start date (string or Date)
        * @param endDate - End date (string or Date)
        * @param weekendPredicate - Function that returns true for weekends
        * @returns Number of business days (minimum 1)
        *
        * Example:
        * getBusinessDaysCount('2026-03-14', '2026-03-17', (d) => d.getUTCDay() === 0 || d.getUTCDay() === 6)
        * // Returns 2 (Fri + Mon, Sat/Sun excluded)
        */
       export function getBusinessDaysCount(
         startDate: string | Date,
         endDate: string | Date,
         weekendPredicate: (date: Date) => boolean
       ): number {
         const start = parseUTCDate(startDate);
         const end = parseUTCDate(endDate);
         let count = 0;
         const current = new Date(start);

         while (current.getTime() <= end.getTime()) {
           if (!weekendPredicate(current)) {
             count++;
           }
           current.setUTCDate(current.getUTCDate() + 1);
         }

         return Math.max(1, count);
       }
       ```

    2. addBusinessDays функция:
       ```typescript
       /**
        * Calculate end date by adding business days to start date
        * @param startDate - Start date (string or Date)
        * @param businessDays - Number of business days to add (minimum 1)
        * @param weekendPredicate - Function that returns true for weekends
        * @returns End date as YYYY-MM-DD string
        *
        * Example:
        * addBusinessDays('2026-03-14', 4, (d) => d.getUTCDay() === 0 || d.getUTCDay() === 6)
        * // Returns '2026-03-19' (Fri + Mon + Tue + Wed, Sat/Sun skipped)
        */
       export function addBusinessDays(
         startDate: string | Date,
         businessDays: number,
         weekendPredicate: (date: Date) => boolean
       ): string {
         const start = parseUTCDate(startDate);
         const current = new Date(start);
         let daysToAdd = Math.max(1, businessDays);

         while (daysToAdd > 0) {
           current.setUTCDate(current.getUTCDate() + 1);
           if (!weekendPredicate(current)) {
             daysToAdd--;
           }
         }

         return current.toISOString().split('T')[0];
       }
       ```

    3. Обновить экспорт в index.ts если необходимо (проверить структуру проекта)
  </action>

  <verify>
    <automated>npm test -- --run -t "getBusinessDaysCount|addBusinessDays"</automated>
  </verify>

  <done>
    - getBusinessDaysCount реализован с итеративным подсчётом и UTC-методами
    - addBusinessDays реализован с итеративным добавлением и UTC-методами
    - Все тесты из Task 1 проходят (12+ тестов green)
    - Функции экспортируются из dateUtils.ts
    - Обратная совместимость: без businessDays режима старый код не затрагивается
  </done>
</task>

</tasks>

<verification>
После завершения плана:
1. Все тесты для getBusinessDaysCount и addBusinessDays проходят
2. npm test -- --run показывает green для новых тестов
3. Функции доступны для импорта из dateUtils.ts
4. Нет breaking changes для существующего кода
</verification>

<success_criteria>
1. getBusinessDaysCount корректно подсчитывает рабочие дни (исключая выходные по предикату)
2. addBusinessDays корректно вычисляет дату окончания (пропуская выходные)
3. Обе функции работают с customDays из Phase 21
4. Все тесты проходят (TDD GREEN фаза complete)
5. Код готов к интеграции в TaskListRow (Plan 02)
</success_criteria>

<output>
После завершения создать `.planning/phases/24-buisiness-days/24-01-SUMMARY.md` с:
- Списком созданных тестов (RED фаза)
- Реализацией функций (GREEN фаза)
- Commit hashes для каждого task
- Результатами тестирования
</output>
