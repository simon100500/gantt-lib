---
phase: 24-buisiness-days
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
  - packages/gantt-lib/src/components/TaskList/TaskList.tsx
  - packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Пользователь может передать businessDays={true} проп в GanttChart"
    - "Duration задач считается в рабочих днях (исключая выходные по isWeekend/customDays)"
    - "При редактировании duration endDate пересчитывается правильно"
    - "Обратная совместимость: без пропса работает как раньше (календарные дни)"
    - "Не затронуты dependencyUtils.ts (calculateSuccessorDate, cascade)"
  artifacts:
    - path: packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
      provides: "businessDays prop в GanttChartProps"
      contains: "businessDays\\?:\\s*boolean"
    - path: packages/gantt-lib/src/components/TaskList/TaskList.tsx
      provides: "businessDays prop проброс"
      contains: "businessDays\\?:\\s*boolean"
    - path: packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
      provides: "memoized getDuration и getEndDate функции"
      contains: "const getDuration.*useCallback", "const getEndDate.*useCallback"
  key_links:
    - from: "GanttChart.tsx"
      to: "TaskList.tsx"
      via: "businessDays prop"
      pattern: "businessDays=\\{businessDays\\}"
    - from: "TaskList.tsx"
      to: "TaskListRow.tsx"
      via: "businessDays prop"
      pattern: "businessDays=\\{businessDays\\}"
    - from: "TaskListRow.tsx"
      to: "dateUtils.ts"
      via: "getBusinessDaysCount, addBusinessDays imports"
      pattern: "import.*getBusinessDaysCount.*addBusinessDays"
    - from: "TaskListRow.tsx getDuration"
      to: "dateUtils.ts getBusinessDaysCount"
      via: "conditional call based on businessDays"
      pattern: "businessDays.*getBusinessDaysCount.*getInclusiveDurationDays"
    - from: "TaskListRow.tsx getEndDate"
      to: "dateUtils.ts addBusinessDays"
      via: "conditional call based on businessDays"
      pattern: "businessDays.*addBusinessDays.*getEndDateFromDuration"
---

<objective>
Интегрировать режим рабочих дней в UI компоненты: GanttChart → TaskList → TaskListRow с memoized conditional functions

Purpose: Добавить бизнес-логику выбора алгоритма расчёта duration (календарные vs рабочие дни) на основе пропса businessDays
Output: Проброс businessDays пропа через компоненты с memoized функциями-обёртками в TaskListRow
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-buisiness-days/24-CONTEXT.md
@.planning/phases/24-buisiness-days/24-RESEARCH.md
@.planning/phases/24-buisiness-days/24-01-PLAN.md
@.planning/phases/21.1-custom-weekend-refactoring/21.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 24-01 (new utilities) -->
From packages/gantt-lib/src/utils/dateUtils.ts:
```typescript
export function getBusinessDaysCount(
  startDate: string | Date,
  endDate: string | Date,
  weekendPredicate: (date: Date) => boolean
): number;

export function addBusinessDays(
  startDate: string | Date,
  businessDays: number,
  weekendPredicate: (date: Date) => boolean
): string;
```

<!-- From TaskListRow.tsx (existing pattern to replace) -->
From packages/gantt-lib/src/components/TaskList/TaskListRow.tsx (lines 28-48):
```typescript
const getInclusiveDurationDays = (
  startDate: string | Date,
  endDate: string | Date,
): number => {
  const start = parseUTCDate(startDate);
  const end = parseUTCDate(endDate);
  return Math.max(1, Math.round((end.getTime() - start.getTime()) / DAY_MS) + 1);
};

const getEndDateFromDuration = (
  startDate: string | Date,
  durationDays: number,
): string => {
  const start = parseUTCDate(startDate);
  return new Date(start.getTime() + (durationDays - 1) * DAY_MS)
    .toISOString()
    .split("T")[0];
};
```

<!-- From TaskListRow.tsx (weekendPredicate pattern, line 704-707) -->
From packages/gantt-lib/src/components/TaskList/TaskListRow.tsx:
```typescript
const weekendPredicate = useMemo(
  () => createCustomDayPredicate({ customDays, isWeekend }),
  [customDays, isWeekend]
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить businessDays проп в GanttChart и TaskList</name>
  <files>packages/gantt-lib/src/components/GanttChart/GanttChart.tsx, packages/gantt-lib/src/components/TaskList/TaskList.tsx</files>

  <read_first>
    1. packages/gantt-lib/src/components/GanttChart/GanttChart.tsx (lines 83-144) — изучить GanttChartProps interface и проп customDays
    2. packages/gantt-lib/src/components/TaskList/TaskList.tsx (lines 94-147) — изучить TaskListProps interface и проп customDays
    3. packages/gantt-lib/src/components/GanttChart/GanttChart.tsx (lines 150-250) — найти место где TaskList рендерится для проброса пропа
  </read_first>

  <action>
    1. В packages/gantt-lib/src/components/GanttChart/GanttChart.tsx:
       - Добавить в GanttChartProps interface (после customDays или isWeekend пропа):
         ```typescript
         /** Считать duration в рабочих днях, исключая выходные (default: false) */
         businessDays?: boolean;
         ```
       - Найти рендер TaskList компонента (обычно <TaskList .../>)
       - Добавить проп businessDays={businessDays} в TaskList

    2. В packages/gantt-lib/src/components/TaskList/TaskList.tsx:
       - Добавить в TaskListProps interface (после customDays или isWeekend пропа):
         ```typescript
         /** Считать duration в рабочих днях */
         businessDays?: boolean;
         ```
       - Найти рендер TaskListRow компонента (обычно <TaskListRow .../> внутри map)
       - Добавить проп businessDays={businessDays} в TaskListRow

    Конкретные места (из CONTEXT.md):
    - GanttChart.tsx: проп добавить в interface после isWeekend (line 133)
    - GanttChart.tsx: проброс в TaskList (найти <TaskList ... />)
    - TaskList.tsx: проп добавить в interface после isWeekend (line 144)
    - TaskList.tsx: проброс в TaskListRow (найти <TaskListRow ... />)
  </action>

  <verify>
    <automated>grep -n "businessDays\?: boolean" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx packages/gantt-lib/src/components/TaskList/TaskList.tsx</automated>
    <automated>grep -n "businessDays={businessDays}" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx packages/gantt-lib/src/components/TaskList/TaskList.tsx</automated>
  </verify>

  <done>
    - GanttChartProps имеет businessDays?: boolean prop с JSDoc комментарием
    - TaskListProps имеет businessDays?: boolean prop с JSDoc комментарием
    - GanttChart пробрасывает businessDays в TaskList
    - TaskList пробрасывает businessDays в TaskListRow
    - TypeScript компилируется без ошибок
  </done>
</task>

<task type="auto">
  <name>Task 2: Интегрировать businessDays логику в TaskListRow</name>
  <files>packages/gantt-lib/src/components/TaskList/TaskListRow.tsx</files>

  <read_first>
    1. packages/gantt-lib/src/components/TaskList/TaskListRow.tsx (lines 1-50) — импорты и существующие getInclusiveDurationDays/getEndDateFromDuration
    2. packages/gantt-lib/src/components/TaskList/TaskListRow.tsx (lines 650-750) — TaskListRowProps и компонент начало, weekendPredicate
    3. packages/gantt-lib/src/components/TaskList/TaskListRow.tsx (lines 677, 867-868, 888, 895, 918-920) — места использования getInclusiveDurationDays/getEndDateFromDuration
  </read_first>

  <action>
    В packages/gantt-lib/src/components/TaskList/TaskListRow.tsx:

    1. Добавить импорты (после parseUTCDate, normalizeTaskDates, createCustomDayPredicate):
       ```typescript
       import { getBusinessDaysCount, addBusinessDays } from "../../utils/dateUtils";
       ```

    2. Найти TaskListRowProps interface (обычно после строки 200-300)
       - Добавить проп: businessDays?: boolean;

    3. Найти деструктуризацию пропов в компоненте (обычно после function TaskListRow):
       - Добавить businessDays в деструктуризацию (рядом с customDays, isWeekend)

    4. После weekendPredicate useMemo (строка 704-707), создать memoized функции:
       ```typescript
       const getDuration = useCallback(
         (start: string | Date, end: string | Date) => {
           return businessDays
             ? getBusinessDaysCount(start, end, weekendPredicate)
             : getInclusiveDurationDays(start, end);
         },
         [businessDays, weekendPredicate]
       );

       const getEndDate = useCallback(
         (start: string | Date, duration: number) => {
           return businessDays
             ? addBusinessDays(start, duration, weekendPredicate)
             : getEndDateFromDuration(start, duration);
         },
         [businessDays, weekendPredicate]
       );
       ```

    5. Заменить использования (согласно CONTEXT.md):
       - Строка 677: getInclusiveDurationDays(task.startDate, task.endDate) → getDuration(task.startDate, task.endDate)
       - Строка 868: getInclusiveDurationDays(task.startDate, task.endDate) → getDuration(task.startDate, task.endDate)
       - Строка 888: getEndDateFromDuration(task.startDate, normalizedDuration) → getEndDate(task.startDate, normalizedDuration)
       - Строка 895: getInclusiveDurationDays(task.startDate, task.endDate) → getDuration(task.startDate, task.endDate)
       - Строки 918-920: getEndDateFromDuration(task.startDate, normalizedDuration) → getEndDate(task.startDate, normalizedDuration)

    Примечание: getInclusiveDurationDays и getEndDateFromDuration остаются в файле (не удалять), они используются когда businessDays=false/undefined.
  </action>

  <verify>
    <automated>grep -n "import.*getBusinessDaysCount.*addBusinessDays" packages/gantt-lib/src/components/TaskList/TaskListRow.tsx</automated>
    <automated>grep -n "const getDuration.*useCallback\|const getEndDate.*useCallback" packages/gantt-lib/src/components/TaskList/TaskListRow.tsx</automated>
    <automated>grep -n "getDuration\|getEndDate" packages/gantt-lib/src/components/TaskList/TaskListRow.tsx | grep -v "getEndDateFromDuration\|getInclusiveDurationDays"</automated>
    <automated>npm test -- --run 2>&1 | head -50</automated>
  </verify>

  <done>
    - Импорты getBusinessDaysCount и addBusinessDays добавлены
    - TaskListRowProps имеет businessDays?: boolean prop
    - getDuration и getEndDate memoized функции созданы с правильными зависимостями
    - Все 5 мест использования заменены на getDuration/getEndDate
    - getInclusiveDurationDays и getEndDateFromDuration остались в файле (не удалены)
    - npm test проходит (нет regressions)
    - TypeScript компилируется без ошибок
  </done>
</task>

</tasks>

<verification>
После завершения плана:
1. Проп businessDays проброшен через GanttChart → TaskList → TaskListRow
2. TaskListRow использует memoized getDuration/getEndDate с conditional логикой
3. При businessDays={true} duration считается в рабочих днях
4. При businessDays={false} или undefined duration считается в календарных днях (обратная совместимость)
5. dependencyUtils.ts не изменён (calculateSuccessorDate, cascade не затронуты)
6. npm test проходит (нет regressions)
</verification>

<success_criteria>
1. Пользователь может передать businessDays={true} в GanttChart
2. Duration в TaskListRow пересчитывается в рабочих днях (исключая выходные по isWeekend/customDays)
3. При редактировании duration endDate пересчитывается правильно в режиме рабочих дней
4. Обратная совместимость сохранена (undefined/false = календарные дни)
5. Не затронуты зависимости (calculateSuccessorDate, cascade)
</success_criteria>

<output>
После завершения создать `.planning/phases/24-buisiness-days/24-02-SUMMARY.md` с:
- Списком изменённых файлов
- Местами добавления businessDays пропа
- Memoized функциями getDuration/getEndDate
- Заменёнными использованиями (строки 677, 868, 888, 895, 918-920)
- Commit hashes для каждого task
- Результатами проверки TypeScript и тестов
</output>
