---
phase: 20-month-view
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/gantt-lib/src/utils/dateUtils.ts
  - packages/gantt-lib/src/__tests__/dateUtils.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "getWeekStartDays(days) возвращает каждый 7-й элемент из массива days, начиная с индекса 0"
    - "getWeekSpans(days) группирует недельные колонки по календарным месяцам и возвращает массив WeekSpan"
    - "getWeekSpans корректно обрабатывает неполные первую и последнюю недели"
    - "getWeekSpans корректно обрабатывает диапазон, пересекающий границу года"
  artifacts:
    - path: "packages/gantt-lib/src/utils/dateUtils.ts"
      provides: "getWeekStartDays и getWeekSpans экспортированы"
      exports: ["getWeekStartDays", "getWeekSpans"]
    - path: "packages/gantt-lib/src/__tests__/dateUtils.test.ts"
      provides: "тесты для getWeekStartDays и getWeekSpans"
      contains: "getWeekStartDays"
  key_links:
    - from: "packages/gantt-lib/src/__tests__/dateUtils.test.ts"
      to: "packages/gantt-lib/src/utils/dateUtils.ts"
      via: "import { getWeekStartDays, getWeekSpans } from '../utils/dateUtils'"
      pattern: "getWeekStartDays|getWeekSpans"
---

<objective>
Добавить две новые утилиты week-view в dateUtils.ts методом TDD.

Purpose: Утилиты необходимы для TimeScaleHeader и GridBackground в week-view. Без них Plan 02 не может быть реализован.
Output: Экспортированные функции `getWeekStartDays` и `getWeekSpans` с полным тестовым покрытием.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-month-view/20-CONTEXT.md
@.planning/phases/20-month-view/20-RESEARCH.md
</context>

<interfaces>
<!-- Существующие экспорты dateUtils.ts, которые нужно сохранить -->
<!-- Source: packages/gantt-lib/src/utils/dateUtils.ts -->

```typescript
export const parseUTCDate = (date: string | Date): Date
export const getMonthDays = (date: Date | string): Date[]
export const getDayOffset = (date: Date, monthStart: Date): number
export const isToday = (date: Date): boolean
export const isWeekend = (date: Date): boolean
export const getMultiMonthDays = (tasks: Array<{ startDate: string | Date; endDate: string | Date }>): Date[]
export const getMonthSpans = (dateRange: Date[]): Array<{ month: Date; days: number; startIndex: number }>
export const formatDateLabel = (date: Date | string): string
export const normalizeTaskDates = (startDate: string | Date, endDate: string | Date): { startDate: string; endDate: string }
```

<!-- Паттерн getMonthSpans — образец для getWeekSpans -->
```typescript
// Группирует дни по месяцам. Аналогичный паттерн нужен для getWeekSpans.
export const getMonthSpans = (
  dateRange: Date[]
): Array<{ month: Date; days: number; startIndex: number }> => {
  // ...
};
```

<!-- Целевые типы и сигнатуры для новых функций -->

```typescript
// WeekSpan — для TimeScaleHeader row 1 (месяц, охватывающий N недельных колонок)
export interface WeekSpan {
  month: Date;         // первый день месяца (UTC)
  weeks: number;       // сколько недельных колонок этот месяц занимает в видимом диапазоне
  startIndex: number;  // индекс в массиве недельных колонок (0-based)
}

/**
 * Возвращает первый день каждого 7-дневного блока из массива days.
 * Используется TimeScaleHeader (row 2) в week-view.
 * Блоки: days[0], days[7], days[14]... (НЕ привязка к понедельнику).
 */
export const getWeekStartDays = (days: Date[]): Date[]

/**
 * Группирует 7-дневные блоки из массива days по календарным месяцам.
 * Используется TimeScaleHeader (row 1) в week-view для ширины ячейки месяца.
 * Возвращает WeekSpan[].
 *
 * Алгоритм:
 * 1. Вычислить weekStartDays = getWeekStartDays(days)
 * 2. Для каждой недельной колонки (i): определить, к какому месяцу относится weekStartDays[i]
 * 3. Группировать consecutive колонки одного месяца -> { month, weeks, startIndex }
 *
 * Граничный случай: последняя группа включает все оставшиеся колонки того же месяца.
 */
export const getWeekSpans = (days: Date[]): WeekSpan[]
```
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Тесты и реализация getWeekStartDays</name>
  <files>packages/gantt-lib/src/__tests__/dateUtils.test.ts, packages/gantt-lib/src/utils/dateUtils.ts</files>
  <read_first>
    - packages/gantt-lib/src/__tests__/dateUtils.test.ts (текущее содержимое — добавлять в конец файла)
    - packages/gantt-lib/src/utils/dateUtils.ts (текущая реализация — добавлять экспорты в конец файла)
  </read_first>
  <behavior>
    RED фаза — написать тесты ПЕРВЫМИ, они должны упасть:

    Тест 1: пустой массив → возвращает []
    - input: []
    - expected: []

    Тест 2: массив из 7 дней → возвращает 1 элемент (days[0])
    - input: [2026-03-01, 2026-03-02, ..., 2026-03-07] (7 дат)
    - expected: [2026-03-01]

    Тест 3: массив из 14 дней → возвращает 2 элемента (days[0], days[7])
    - input: [2026-03-01 .. 2026-03-14] (14 дат)
    - expected: [2026-03-01, 2026-03-08]

    Тест 4: неполный последний блок (10 дней) → возвращает 2 элемента
    - input: [2026-03-01 .. 2026-03-10] (10 дат)
    - expected: [2026-03-01, 2026-03-08]

    Тест 5: массив из 31 дня (март) → возвращает 5 элементов (days[0,7,14,21,28])
    - input: все дни марта 2026 (31 день)
    - expected: [2026-03-01, 2026-03-08, 2026-03-15, 2026-03-22, 2026-03-29]

    GREEN фаза — реализовать:
    ```typescript
    export const getWeekStartDays = (days: Date[]): Date[] => {
      const result: Date[] = [];
      for (let i = 0; i < days.length; i += 7) {
        result.push(days[i]);
      }
      return result;
    };
    ```
  </behavior>
  <action>
    RED: Добавить в конец packages/gantt-lib/src/__tests__/dateUtils.test.ts блок:

    ```typescript
    describe('getWeekStartDays', () => {
      it('should return empty array for empty input', () => {
        expect(getWeekStartDays([])).toEqual([]);
      });

      it('should return first day for 7-day range', () => {
        const days = Array.from({ length: 7 }, (_, i) =>
          new Date(Date.UTC(2026, 2, 1 + i))
        );
        const result = getWeekStartDays(days);
        expect(result).toHaveLength(1);
        expect(result[0].getUTCDate()).toBe(1);
      });

      it('should return two week starts for 14-day range', () => {
        const days = Array.from({ length: 14 }, (_, i) =>
          new Date(Date.UTC(2026, 2, 1 + i))
        );
        const result = getWeekStartDays(days);
        expect(result).toHaveLength(2);
        expect(result[0].getUTCDate()).toBe(1);
        expect(result[1].getUTCDate()).toBe(8);
      });

      it('should include partial last week', () => {
        const days = Array.from({ length: 10 }, (_, i) =>
          new Date(Date.UTC(2026, 2, 1 + i))
        );
        const result = getWeekStartDays(days);
        expect(result).toHaveLength(2);
        expect(result[1].getUTCDate()).toBe(8);
      });

      it('should return 5 week starts for 31-day March', () => {
        const days = Array.from({ length: 31 }, (_, i) =>
          new Date(Date.UTC(2026, 2, 1 + i))
        );
        const result = getWeekStartDays(days);
        expect(result).toHaveLength(5);
        expect(result[0].getUTCDate()).toBe(1);
        expect(result[1].getUTCDate()).toBe(8);
        expect(result[2].getUTCDate()).toBe(15);
        expect(result[3].getUTCDate()).toBe(22);
        expect(result[4].getUTCDate()).toBe(29);
      });
    });
    ```

    Обновить import в начале test-файла: добавить `getWeekStartDays` к деструктурируемому импорту.

    Запустить: `cd packages/gantt-lib && npx vitest run src/__tests__/dateUtils.test.ts`
    Убедиться: тесты getWeekStartDays падают (ReferenceError или TS-ошибка).

    GREEN: Добавить в конец packages/gantt-lib/src/utils/dateUtils.ts:

    ```typescript
    /**
     * Return the first day of each 7-day block in the days array.
     * Used by TimeScaleHeader row 2 in week-view.
     * Blocks start at days[0], days[7], days[14], ... (NOT aligned to Monday).
     * Partial last block is included.
     */
    export const getWeekStartDays = (days: Date[]): Date[] => {
      const result: Date[] = [];
      for (let i = 0; i < days.length; i += 7) {
        result.push(days[i]);
      }
      return result;
    };
    ```

    Запустить снова — все тесты getWeekStartDays должны пройти.
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npx vitest run src/__tests__/dateUtils.test.ts</automated>
  </verify>
  <done>Все тесты getWeekStartDays зелёные; функция экспортируется из dateUtils.ts</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Тесты и реализация getWeekSpans + WeekSpan interface</name>
  <files>packages/gantt-lib/src/__tests__/dateUtils.test.ts, packages/gantt-lib/src/utils/dateUtils.ts</files>
  <read_first>
    - packages/gantt-lib/src/__tests__/dateUtils.test.ts (текущее состояние после Task 1)
    - packages/gantt-lib/src/utils/dateUtils.ts (текущее состояние после Task 1)
    - packages/gantt-lib/src/types/index.ts (существующие типы — не дублировать WeekSpan если уже есть; скорее всего нет)
  </read_first>
  <behavior>
    RED фаза — написать тесты ПЕРВЫМИ:

    Тест 1: пустой массив → []

    Тест 2: 31 день одного месяца (март 2026) → 1 span
    - input: все дни марта 2026 (31 день)
    - weeks: ceil(31/7) = 5 (колонок недель: days[0], days[7], ..., days[28])
    - expected: [{ month: 2026-03-01, weeks: 5, startIndex: 0 }]

    Тест 3: март (31 дн.) + апрель (30 дн.) = 61 день → 2 span
    - Недельные колонки: days[0,7,14,21,28,35,42,49,56] = 9 колонок
    - Колонки 0-4 (days[0,7,14,21,28]) — начало в марте → март, weeks=5, startIndex=0
    - Колонки 5-8 (days[35,42,49,56]) — начало в апреле → апрель, weeks=4, startIndex=5
    - expected: [
        { month.getUTCMonth()===2, weeks===5, startIndex===0 },
        { month.getUTCMonth()===3, weeks===4, startIndex===5 }
      ]

    Тест 4: граница года — декабрь 2026 (31 дн.) + январь 2027 (31 дн.) = 62 дня → 2 span
    - 9 колонок: days[0,7,14,21,28,35,42,49,56]
    - days[0..28] в декабре (5 колонок), days[35..56] в январе (4 колонки)
    - expected:
        span[0]: month.getUTCFullYear()===2026, month.getUTCMonth()===11 (дек.), weeks===5
        span[1]: month.getUTCFullYear()===2027, month.getUTCMonth()===0 (янв.), weeks===4

    Тест 5: days.length < 7 (5 дней, один неполный блок) → 1 span с weeks=1

    GREEN фаза — реализовать:
    Алгоритм getWeekSpans:
    1. Вычислить weekStarts = getWeekStartDays(days)
    2. Если weekStarts.length === 0, вернуть []
    3. Инициализировать currentMonth = weekStarts[0] (год+месяц), startIndex = 0
    4. Итерировать по weekStarts с i=0:
       - monthYear = `${ws.getUTCFullYear()}-${ws.getUTCMonth()}`
       - При смене месяца: сохранить предыдущий span {month: firstDayOfMonth, weeks: i - startIndex, startIndex}
         обновить currentMonth, startIndex = i
    5. После цикла: сохранить последний span {month, weeks: weekStarts.length - startIndex, startIndex}
    6. Вернуть spans
  </behavior>
  <action>
    RED: Добавить в конец packages/gantt-lib/src/__tests__/dateUtils.test.ts (после getWeekStartDays тестов):

    ```typescript
    describe('getWeekSpans', () => {
      it('should return empty array for empty input', () => {
        expect(getWeekSpans([])).toEqual([]);
      });

      it('should return single span for 31-day March', () => {
        const days = Array.from({ length: 31 }, (_, i) =>
          new Date(Date.UTC(2026, 2, 1 + i))
        );
        const result = getWeekSpans(days);
        expect(result).toHaveLength(1);
        expect(result[0].month.getUTCMonth()).toBe(2); // March
        expect(result[0].month.getUTCDate()).toBe(1);
        expect(result[0].weeks).toBe(5); // days[0,7,14,21,28]
        expect(result[0].startIndex).toBe(0);
      });

      it('should return two spans for March+April (61 days)', () => {
        const march = Array.from({ length: 31 }, (_, i) => new Date(Date.UTC(2026, 2, 1 + i)));
        const april = Array.from({ length: 30 }, (_, i) => new Date(Date.UTC(2026, 3, 1 + i)));
        const days = [...march, ...april];
        const result = getWeekSpans(days);
        expect(result).toHaveLength(2);
        // March span: week columns 0-4 (days[0,7,14,21,28] all start in March)
        expect(result[0].month.getUTCMonth()).toBe(2);
        expect(result[0].weeks).toBe(5);
        expect(result[0].startIndex).toBe(0);
        // April span: week columns 5-8 (days[35,42,49,56] all start in April)
        expect(result[1].month.getUTCMonth()).toBe(3);
        expect(result[1].weeks).toBe(4);
        expect(result[1].startIndex).toBe(5);
      });

      it('should handle year boundary (December+January)', () => {
        const dec = Array.from({ length: 31 }, (_, i) => new Date(Date.UTC(2026, 11, 1 + i)));
        const jan = Array.from({ length: 31 }, (_, i) => new Date(Date.UTC(2027, 0, 1 + i)));
        const days = [...dec, ...jan];
        const result = getWeekSpans(days);
        expect(result).toHaveLength(2);
        expect(result[0].month.getUTCFullYear()).toBe(2026);
        expect(result[0].month.getUTCMonth()).toBe(11); // December
        expect(result[0].weeks).toBe(5);
        expect(result[1].month.getUTCFullYear()).toBe(2027);
        expect(result[1].month.getUTCMonth()).toBe(0); // January
        expect(result[1].weeks).toBe(4);
      });

      it('should handle partial first block (less than 7 days)', () => {
        const days = Array.from({ length: 5 }, (_, i) =>
          new Date(Date.UTC(2026, 2, 1 + i))
        );
        const result = getWeekSpans(days);
        expect(result).toHaveLength(1);
        expect(result[0].weeks).toBe(1);
        expect(result[0].startIndex).toBe(0);
      });
    });
    ```

    Обновить import в test-файле: добавить `getWeekSpans` и `WeekSpan` (если interface экспортируется).

    Запустить: тесты getWeekSpans должны упасть.

    GREEN: Добавить в packages/gantt-lib/src/utils/dateUtils.ts после getWeekStartDays:

    ```typescript
    /**
     * Represents a month span in week-view header row 1.
     * weeks = number of week-columns (7-day blocks) this month occupies.
     */
    export interface WeekSpan {
      /** First day of the calendar month (UTC) */
      month: Date;
      /** Number of week-columns this month occupies in the visible range */
      weeks: number;
      /** Start index in the week-columns array */
      startIndex: number;
    }

    /**
     * Group the days array into 7-day blocks and calculate month spans over those blocks.
     * Used by TimeScaleHeader row 1 in week-view.
     *
     * Blocks start at days[0], days[7], days[14]... (NOT Monday-aligned).
     * Each block's month is determined by its start day (days[i]).
     * Partial last block is included.
     */
    export const getWeekSpans = (days: Date[]): WeekSpan[] => {
      const weekStarts = getWeekStartDays(days);
      if (weekStarts.length === 0) return [];

      const spans: WeekSpan[] = [];
      let currentMonthYear = `${weekStarts[0].getUTCFullYear()}-${weekStarts[0].getUTCMonth()}`;
      let startIndex = 0;

      for (let i = 0; i < weekStarts.length; i++) {
        const ws = weekStarts[i];
        const monthYear = `${ws.getUTCFullYear()}-${ws.getUTCMonth()}`;

        if (monthYear !== currentMonthYear) {
          spans.push({
            month: new Date(Date.UTC(
              weekStarts[startIndex].getUTCFullYear(),
              weekStarts[startIndex].getUTCMonth(),
              1
            )),
            weeks: i - startIndex,
            startIndex,
          });
          currentMonthYear = monthYear;
          startIndex = i;
        }

        if (i === weekStarts.length - 1) {
          spans.push({
            month: new Date(Date.UTC(ws.getUTCFullYear(), ws.getUTCMonth(), 1)),
            weeks: weekStarts.length - startIndex,
            startIndex,
          });
        }
      }

      return spans;
    };
    ```

    Запустить полный suite — все тесты должны быть зелёными.
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npx vitest run src/__tests__/dateUtils.test.ts</automated>
  </verify>
  <done>Все тесты getWeekSpans и getWeekStartDays зелёные; WeekSpan interface и обе функции экспортируются из dateUtils.ts; полный test suite зелёный (`cd packages/gantt-lib && npx vitest run`)</done>
</task>

</tasks>

<verification>
- `cd packages/gantt-lib && npx vitest run src/__tests__/dateUtils.test.ts` — все тесты зелёные
- `cd packages/gantt-lib && npx vitest run` — полный suite зелёный
- `grep -n "getWeekStartDays\|getWeekSpans\|WeekSpan" packages/gantt-lib/src/utils/dateUtils.ts` — 3 результата (interface + 2 функции)
- `grep -n "getWeekStartDays\|getWeekSpans" packages/gantt-lib/src/__tests__/dateUtils.test.ts` — тесты присутствуют
</verification>

<success_criteria>
- Функции getWeekStartDays и getWeekSpans экспортируются из dateUtils.ts
- WeekSpan interface экспортируется из dateUtils.ts
- Все новые тесты зелёные, существующие тесты не сломаны
- Полный vitest suite зелёный
</success_criteria>

<output>
После завершения создать `.planning/phases/20-month-view/20-01-SUMMARY.md`
</output>
