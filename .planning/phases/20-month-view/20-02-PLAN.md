---
phase: 20-month-view
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx
  - packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.css
  - packages/gantt-lib/src/components/GridBackground/GridBackground.tsx
  - packages/gantt-lib/src/utils/geometry.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "TimeScaleHeader в режиме week-view показывает в строке 1 название месяца+год, охватывая недельные колонки этого месяца"
    - "TimeScaleHeader в режиме week-view показывает в строке 2 число начала недели (01, 08, 15...) в колонке шириной dayWidth*7"
    - "GridBackground в режиме week-view рисует тонкие линии на каждой недельной границе (каждые 7 дней) и яркие разделители на границах месяцев"
    - "GridBackground в режиме week-view НЕ рисует выходные блоки"
    - "В режиме day-view оба компонента ведут себя идентично текущему коду (обратная совместимость)"
  artifacts:
    - path: "packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx"
      provides: "Поддержка viewMode prop с ветками day/week"
      exports: ["TimeScaleHeaderProps с viewMode?"]
    - path: "packages/gantt-lib/src/components/GridBackground/GridBackground.tsx"
      provides: "Поддержка viewMode prop, пропуск weekendBlocks в week-view"
      exports: ["GridBackgroundProps с viewMode?"]
    - path: "packages/gantt-lib/src/utils/geometry.ts"
      provides: "calculateWeekGridLines — линии каждые 7 дней с флагом isMonthStart"
  key_links:
    - from: "packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx"
      to: "packages/gantt-lib/src/utils/dateUtils.ts"
      via: "import { getWeekSpans, getWeekStartDays } from '../../utils/dateUtils'"
      pattern: "getWeekSpans|getWeekStartDays"
    - from: "packages/gantt-lib/src/components/GridBackground/GridBackground.tsx"
      to: "packages/gantt-lib/src/utils/geometry.ts"
      via: "calculateWeekGridLines"
      pattern: "calculateWeekGridLines"
---

<objective>
Адаптировать TimeScaleHeader и GridBackground для поддержки week-view через новый prop `viewMode?: 'day' | 'week'`.

Purpose: Это основная визуальная реализация week-view. Утилиты (Plan 01) уже готовы.
Output: Два компонента с условными ветками для day/week рендеринга + вспомогательная функция calculateWeekGridLines.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-month-view/20-CONTEXT.md
@.planning/phases/20-month-view/20-RESEARCH.md
@.planning/phases/20-month-view/20-01-SUMMARY.md
</context>

<interfaces>
<!-- Из Plan 01: новые экспорты dateUtils.ts -->
```typescript
// packages/gantt-lib/src/utils/dateUtils.ts — после Plan 01
export interface WeekSpan {
  month: Date;
  weeks: number;
  startIndex: number;
}
export const getWeekStartDays = (days: Date[]): Date[]
export const getWeekSpans = (days: Date[]): WeekSpan[]
```

<!-- Текущие сигнатуры TimeScaleHeader (изменить) -->
```typescript
// packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx
export interface TimeScaleHeaderProps {
  days: Date[];
  dayWidth: number;
  headerHeight: number;
  // ДОБАВИТЬ: viewMode?: 'day' | 'week';
}
```

<!-- Текущие сигнатуры GridBackground (изменить) -->
```typescript
// packages/gantt-lib/src/components/GridBackground/GridBackground.tsx
export interface GridBackgroundProps {
  dateRange: Date[];
  dayWidth: number;
  totalHeight: number;
  // ДОБАВИТЬ: viewMode?: 'day' | 'week';
}

// ВАЖНО: arePropsEqual нужно обновить — добавить сравнение viewMode
const arePropsEqual = (prev: GridBackgroundProps, next: GridBackgroundProps) => {
  return (
    prev.dayWidth === next.dayWidth &&
    prev.dateRange.length === next.dateRange.length &&
    prev.totalHeight === next.totalHeight
    // ДОБАВИТЬ: && prev.viewMode === next.viewMode
  );
};
```

<!-- Существующие CSS классы для линий GridBackground -->
```css
/* packages/gantt-lib/src/components/GridBackground/GridBackground.css */
.gantt-gb-monthSeparator  /* 2px тёмная — граница месяца */
.gantt-gb-weekSeparator   /* 1px средняя — граница недели */
.gantt-gb-dayLine         /* 1px светлая — граница дня */
```

<!-- Существующая функция calculateGridLines — образец для calculateWeekGridLines -->
```typescript
// packages/gantt-lib/src/utils/geometry.ts
export const calculateGridLines = (
  dateRange: Date[],
  dayWidth: number
): Array<{ x: number; isMonthStart: boolean; isWeekStart: boolean }> => {
  // Рисует линию на каждом дне, флаги isMonthStart/isWeekStart для стиля
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: calculateWeekGridLines в geometry.ts + TimeScaleHeader week-view branch</name>
  <files>
    packages/gantt-lib/src/utils/geometry.ts,
    packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx,
    packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.css
  </files>
  <read_first>
    - packages/gantt-lib/src/utils/geometry.ts (целиком — добавить calculateWeekGridLines в конец)
    - packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx (целиком — добавить week-view branch)
    - packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.css (целиком — добавить стили week-view ячеек)
  </read_first>
  <action>
    ### Шаг 1: добавить calculateWeekGridLines в geometry.ts

    Добавить в конец packages/gantt-lib/src/utils/geometry.ts:

    ```typescript
    /**
     * Calculate grid line positions for week-view mode.
     * Lines appear at every 7-day boundary (each week column edge).
     * Month boundaries get isMonthStart=true for stronger styling.
     * No day-level lines; no weekend blocks.
     *
     * @param dateRange - Array of Date objects (from getMultiMonthDays, day-based)
     * @param dayWidth - Width of each day in pixels
     * @returns Array of grid line objects at week-column boundaries
     */
    export const calculateWeekGridLines = (
      dateRange: Date[],
      dayWidth: number
    ): Array<{ x: number; isMonthStart: boolean }> => {
      const lines: Array<{ x: number; isMonthStart: boolean }> = [];
      const weekColumnWidth = dayWidth * 7;

      // Number of week columns (ceil to include partial last week)
      const weekCount = Math.ceil(dateRange.length / 7);

      for (let w = 0; w < weekCount; w++) {
        const dayIndex = w * 7;
        const x = Math.round(dayIndex * dayWidth);
        // Skip the first line at x=0 (left border)
        if (w === 0) continue;

        // Determine if this week boundary is also a month boundary
        // A month boundary = the week-start day (dateRange[dayIndex]) is the 1st of a month,
        // OR the previous day (dateRange[dayIndex-1]) is the last day of its month.
        const weekStartDay = dateRange[dayIndex];
        const isMonthStart = weekStartDay
          ? weekStartDay.getUTCDate() === 1 ||
            (dayIndex > 0 && dateRange[dayIndex - 1].getUTCMonth() !== weekStartDay.getUTCMonth())
          : false;

        lines.push({ x, isMonthStart });
      }

      // Add final line at the right edge of the last week column
      const totalWeekWidth = weekCount * weekColumnWidth;
      if (weekCount > 0) {
        lines.push({ x: Math.round(dateRange.length * dayWidth), isMonthStart: false });
      }

      return lines;
    };
    ```

    ### Шаг 2: обновить TimeScaleHeader.tsx

    Добавить `viewMode?: 'day' | 'week'` в `TimeScaleHeaderProps`.
    Добавить импорт `getWeekSpans, getWeekStartDays, WeekSpan` из '../../utils/dateUtils'.

    Внутри компонента добавить (после существующих useMemo):
    ```typescript
    const weekColumnWidth = dayWidth * 7;

    const weekStartDays = useMemo(
      () => (viewMode === 'week' ? getWeekStartDays(days) : []),
      [days, viewMode]
    );

    const weekSpans = useMemo(
      () => (viewMode === 'week' ? getWeekSpans(days) : []),
      [days, viewMode]
    );
    ```

    Изменить JSX — добавить условный рендеринг:

    ```tsx
    {/* Month row - top */}
    <div
      className="gantt-tsh-monthRow"
      style={{ height: `${rowHeight}px` }}
    >
      {viewMode === 'week' ? (
        // Week-view row 1: month names spanning week columns
        weekSpans.map((span: WeekSpan, index: number) => (
          <div
            key={`wmonth-${index}`}
            className="gantt-tsh-monthCell"
            style={{ width: `${span.weeks * weekColumnWidth}px` }}
          >
            {format(span.month, 'LLLL yyyy', { locale: ru }).replace(/^./, (c) => c.toUpperCase())}
          </div>
        ))
      ) : (
        // Day-view row 1: month names spanning day columns (EXISTING CODE — НЕ МЕНЯТЬ)
        monthSpans.map((span: MonthSpan, index: number) => (
          <div
            key={`month-${index}`}
            className="gantt-tsh-monthCell"
            style={{ width: `${span.days * dayWidth}px` }}
          >
            {format(span.month, 'LLLL yyyy', { locale: ru }).replace(/^./, (c) => c.toUpperCase())}
          </div>
        ))
      )}
    </div>

    {/* Day/Week row - bottom */}
    <div
      className="gantt-tsh-dayRow"
      style={{
        height: `${rowHeight}px`,
        gridTemplateColumns: viewMode === 'week'
          ? `repeat(${weekStartDays.length}, ${weekColumnWidth}px)`
          : dayGridTemplate,
      }}
    >
      {viewMode === 'week' ? (
        // Week-view row 2: week start day numbers
        weekStartDays.map((day, index) => {
          const prevWeekStart = weekStartDays[index - 1];
          const isMonthBoundary =
            index > 0 && prevWeekStart &&
            prevWeekStart.getUTCMonth() !== day.getUTCMonth();
          return (
            <div
              key={`week-${index}`}
              className={`gantt-tsh-dayCell gantt-tsh-weekCell${isMonthBoundary ? ' gantt-tsh-monthBoundary' : ''}`}
            >
              <span className="gantt-tsh-dayLabel">
                {String(day.getUTCDate()).padStart(2, '0')}
              </span>
            </div>
          );
        })
      ) : (
        // Day-view row 2: individual day numbers (EXISTING CODE — НЕ МЕНЯТЬ)
        days.map((day, index) => {
          const isWeekend = day.getDay() === 0 || day.getDay() === 6;
          const prevDay = days[index - 1];
          const isMonthBoundary = index > 0 && prevDay && prevDay.getMonth() !== day.getMonth();
          const now = new Date();
          const isTodayDate =
            day.getUTCFullYear() === now.getFullYear() &&
            day.getUTCMonth() === now.getMonth() &&
            day.getUTCDate() === now.getDate();
          return (
            <div key={`day-${index}`} className={`gantt-tsh-dayCell ${isWeekend ? 'gantt-tsh-weekendDay' : ''} ${isMonthBoundary ? 'gantt-tsh-monthBoundary' : ''} ${isTodayDate ? 'gantt-tsh-today' : ''}`}>
              <span className="gantt-tsh-dayLabel">{format(day, 'd')}</span>
            </div>
          );
        })
      )}
    </div>
    ```

    ### Шаг 3: добавить CSS для week-view ячейки в TimeScaleHeader.css

    Добавить в конец файла:
    ```css
    /* Week-view cell in day row (wider column) */
    .gantt-tsh-weekCell {
      display: flex;
      align-items: center;
      justify-content: center;
      box-sizing: border-box;
    }
    ```

    ВАЖНО: НЕ применять класс `gantt-tsh-today` к week-view ячейкам (пункт из RESEARCH.md Pitfall 5).
    ВАЖНО: НЕ применять класс `gantt-tsh-weekendDay` к week-view ячейкам.
  </action>
  <acceptance_criteria>
    - `grep -n "viewMode" packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx` возвращает строки с prop, useMemo и JSX
    - `grep -n "getWeekSpans\|getWeekStartDays\|WeekSpan" packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx` — минимум 3 результата
    - `grep -n "gantt-tsh-weekCell" packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.css` — присутствует
    - `grep -n "calculateWeekGridLines" packages/gantt-lib/src/utils/geometry.ts` — присутствует
    - `grep -n "export const calculateWeekGridLines" packages/gantt-lib/src/utils/geometry.ts` — функция экспортируется
    - `cd packages/gantt-lib && npx vitest run` — все тесты зелёные (нет регрессий)
    - В JSX week-view ветке НЕТ классов `gantt-tsh-today` и `gantt-tsh-weekendDay` (grep покажет их только в day-view ветке)
  </acceptance_criteria>
  <verify>
    <automated>cd packages/gantt-lib && npx vitest run</automated>
  </verify>
  <done>calculateWeekGridLines добавлена в geometry.ts; TimeScaleHeader принимает viewMode, рендерит week-view row 1 (месяц) и row 2 (даты начала недель); все тесты зелёные</done>
</task>

<task type="auto">
  <name>Task 2: GridBackground week-view — пропуск выходных, недельные линии, обновление arePropsEqual</name>
  <files>
    packages/gantt-lib/src/components/GridBackground/GridBackground.tsx
  </files>
  <read_first>
    - packages/gantt-lib/src/components/GridBackground/GridBackground.tsx (целиком — нужно понять arePropsEqual и текущую структуру)
    - packages/gantt-lib/src/utils/geometry.ts (после Task 1 — проверить экспорт calculateWeekGridLines)
  </read_first>
  <action>
    ### Изменения в GridBackground.tsx:

    **1. Добавить `viewMode?: 'day' | 'week'` в GridBackgroundProps:**
    ```typescript
    export interface GridBackgroundProps {
      dateRange: Date[];
      dayWidth: number;
      totalHeight: number;
      viewMode?: 'day' | 'week'; // ДОБАВИТЬ
    }
    ```

    **2. Обновить arePropsEqual — добавить сравнение viewMode (Pitfall 4 из RESEARCH.md):**
    ```typescript
    const arePropsEqual = (prevProps: GridBackgroundProps, nextProps: GridBackgroundProps) => {
      return (
        prevProps.dayWidth === nextProps.dayWidth &&
        prevProps.dateRange.length === nextProps.dateRange.length &&
        prevProps.totalHeight === nextProps.totalHeight &&
        prevProps.viewMode === nextProps.viewMode  // ДОБАВИТЬ эту строку
      );
    };
    ```

    **3. Добавить импорт calculateWeekGridLines:**
    ```typescript
    import { calculateGridLines, calculateWeekendBlocks, calculateWeekGridLines } from '../../utils/geometry';
    ```

    **4. В теле компонента заменить вычисление gridLines и weekendBlocks на условные:**
    ```typescript
    // Для week-view: недельные линии
    const weekGridLines = useMemo(() => {
      if (viewMode !== 'week') return [];
      return calculateWeekGridLines(dateRange, dayWidth);
    }, [dateRange, dayWidth, viewMode]);

    // Для day-view: существующие дневные линии (СОХРАНИТЬ существующую логику)
    const gridLines = useMemo<GridLine[]>(() => {
      if (viewMode === 'week') return [];
      return calculateGridLines(dateRange, dayWidth);
    }, [dateRange, dayWidth, viewMode]);

    // Выходные блоки: только в day-view
    const weekendBlocks = useMemo(() => {
      if (viewMode === 'week') return []; // В week-view выходные НЕ выделяются (locked decision)
      return calculateWeekendBlocks(dateRange, dayWidth);
    }, [dateRange, dayWidth, viewMode]);
    ```

    **5. Обновить JSX рендеринг:**

    Секция weekend blocks остаётся как есть — она просто не рендерит ничего в week-view (weekendBlocks будет []).

    Секция vertical grid lines — добавить условный рендеринг:
    ```tsx
    {/* Vertical grid lines */}
    {viewMode === 'week' ? (
      // Week-view: одна линия на каждую недельную колонку
      weekGridLines.map((line, index) => {
        const lineClass = line.isMonthStart
          ? 'gantt-gb-monthSeparator'
          : 'gantt-gb-weekSeparator';
        return (
          <div
            key={`wgridline-${index}`}
            className={`gantt-gb-gridLine ${lineClass}`}
            style={{ left: `${line.x}px` }}
          />
        );
      })
    ) : (
      // Day-view: существующий код (НЕ МЕНЯТЬ)
      gridLines.map((line, index) => {
        const lineClass = line.isMonthStart
          ? 'gantt-gb-monthSeparator'
          : line.isWeekStart
            ? 'gantt-gb-weekSeparator'
            : 'gantt-gb-dayLine';
        return (
          <div
            key={`gridline-${index}`}
            className={`gantt-gb-gridLine ${lineClass}`}
            style={{ left: `${line.x}px` }}
          />
        );
      })
    )}
    ```

    ВАЖНО: gridWidth = `dateRange.length * dayWidth` — НЕ МЕНЯТЬ эту формулу (Pitfall 3 из RESEARCH.md).
    ВАЖНО: weekend blocks рендерятся до grid lines — порядок сохранить.
  </action>
  <acceptance_criteria>
    - `grep -n "viewMode" packages/gantt-lib/src/components/GridBackground/GridBackground.tsx` — минимум 5 строк (interface, arePropsEqual, 3x в useMemo/JSX)
    - `grep -n "prevProps.viewMode === nextProps.viewMode" packages/gantt-lib/src/components/GridBackground/GridBackground.tsx` — строка присутствует
    - `grep -n "calculateWeekGridLines" packages/gantt-lib/src/components/GridBackground/GridBackground.tsx` — присутствует
    - `grep -n "viewMode.*week.*return \[\]" packages/gantt-lib/src/components/GridBackground/GridBackground.tsx` — логика пропуска в week-view присутствует (или эквивалент)
    - `grep -n "dateRange.length \* dayWidth" packages/gantt-lib/src/components/GridBackground/GridBackground.tsx` — gridWidth формула не изменена
    - `cd packages/gantt-lib && npx vitest run` — все тесты зелёные
  </acceptance_criteria>
  <verify>
    <automated>cd packages/gantt-lib && npx vitest run</automated>
  </verify>
  <done>GridBackground принимает viewMode: в week-view рисует только недельные и месячные линии, не рисует выходные блоки; arePropsEqual учитывает viewMode; gridWidth формула не изменена; тесты зелёные</done>
</task>

</tasks>

<verification>
- `cd packages/gantt-lib && npx vitest run` — весь suite зелёный
- `grep -rn "viewMode" packages/gantt-lib/src/components/TimeScaleHeader/` — prop присутствует в tsx
- `grep -rn "viewMode" packages/gantt-lib/src/components/GridBackground/` — prop присутствует в tsx
- `grep -n "calculateWeekGridLines" packages/gantt-lib/src/utils/geometry.ts` — функция экспортируется
- `grep -n "prevProps.viewMode" packages/gantt-lib/src/components/GridBackground/GridBackground.tsx` — arePropsEqual обновлён
</verification>

<success_criteria>
- TimeScaleHeader принимает `viewMode?: 'day' | 'week'`, в week-view рендерит 2-строчный заголовок с месяцами и датами начала недель
- GridBackground принимает `viewMode?: 'day' | 'week'`, в week-view показывает только недельные + месячные линии, без выходных
- Компоненты в day-view (viewMode='day' или без prop) ведут себя идентично текущему коду
- Весь vitest suite зелёный
</success_criteria>

<output>
После завершения создать `.planning/phases/20-month-view/20-02-SUMMARY.md`
</output>
