---
phase: 20-month-view
plan: 03
type: execute
wave: 3
depends_on: [20-02]
files_modified:
  - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
  - packages/gantt-lib/src/index.ts
  - packages/website/src/app/page.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "GanttChart принимает viewMode?: 'day' | 'week' без нарушения обратной совместимости"
    - "В week-view диаграмма отображает недельный заголовок и недельную сетку"
    - "В week-view drag-and-drop сохраняет точные даты (без округления до начала недели)"
    - "TodayIndicator позиционируется корректно в обоих режимах"
    - "На демо-странице есть переключатель day/week с видимым эффектом"
  artifacts:
    - path: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      provides: "viewMode prop в GanttChartProps, проброс в TimeScaleHeader и GridBackground"
      exports: ["GanttChartProps с viewMode?"]
    - path: "packages/website/src/app/page.tsx"
      provides: "Переключатель режима просмотра на демо-странице"
      contains: "viewMode"
  key_links:
    - from: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      to: "packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx"
      via: "<TimeScaleHeader viewMode={viewMode} ...>"
      pattern: "TimeScaleHeader.*viewMode"
    - from: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      to: "packages/gantt-lib/src/components/GridBackground/GridBackground.tsx"
      via: "<GridBackground viewMode={viewMode} ...>"
      pattern: "GridBackground.*viewMode"
---

<objective>
Добавить `viewMode` prop в GanttChart, прокинуть в дочерние компоненты, обновить экспорты библиотеки и добавить переключатель на демо-странице с human verify.

Purpose: Финальный шаг — вся система подключается. После этого плана week-view доступен пользователям библиотеки.
Output: Рабочий viewMode prop в GanttChart + демонстрация на website.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-month-view/20-CONTEXT.md
@.planning/phases/20-month-view/20-02-SUMMARY.md
</context>

<interfaces>
<!-- Из Plan 02: обновлённые сигнатуры компонентов -->
```typescript
// TimeScaleHeader — добавлен viewMode
interface TimeScaleHeaderProps {
  days: Date[];
  dayWidth: number;
  headerHeight: number;
  viewMode?: 'day' | 'week';
}

// GridBackground — добавлен viewMode
interface GridBackgroundProps {
  dateRange: Date[];
  dayWidth: number;
  totalHeight: number;
  viewMode?: 'day' | 'week';
}
```

<!-- GanttChart текущая interface (нужно расширить) -->
```typescript
// packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
export interface GanttChartProps {
  tasks: Task[];
  dayWidth?: number;          // default: 40
  rowHeight?: number;         // default: 40
  headerHeight?: number;      // default: 40
  containerHeight?: number | string;
  onTasksChange?: (tasks: Task[]) => void;
  // ... (все существующие пропсы)
  viewMode?: 'day' | 'week'; // ДОБАВИТЬ — default: 'day'
}
```

<!-- Текущий способ передачи в TimeScaleHeader (строка ~734 в GanttChart.tsx) -->
```tsx
<TimeScaleHeader
  days={dateRange}
  dayWidth={dayWidth}
  headerHeight={headerHeight}
  // ДОБАВИТЬ: viewMode={viewMode}
/>

// Текущий способ передачи в GridBackground (строка ~749)
<GridBackground
  dateRange={dateRange}
  dayWidth={dayWidth}
  totalHeight={totalGridHeight}
  // ДОБАВИТЬ: viewMode={viewMode}
/>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: GanttChart viewMode prop + проброс в компоненты + экспорт типа</name>
  <files>
    packages/gantt-lib/src/components/GanttChart/GanttChart.tsx,
    packages/gantt-lib/src/index.ts
  </files>
  <read_first>
    - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx (целиком — нужно найти места передачи пропсов)
    - packages/gantt-lib/src/index.ts (текущие экспорты)
  </read_first>
  <action>
    ### Изменения в GanttChart.tsx:

    **1. Добавить `viewMode?: 'day' | 'week'` в GanttChartProps (после `enableAddTask`):**
    ```typescript
    /** View mode: 'day' renders one column per day, 'week' renders one column per 7 days (default: 'day') */
    viewMode?: 'day' | 'week';
    ```

    **2. Добавить `viewMode = 'day'` в деструктуризацию пропсов компонента** (в списке параметров forwardRef, после `enableAddTask = true`):
    ```typescript
    viewMode = 'day',
    ```

    **3. Прокинуть viewMode в TimeScaleHeader** (найти JSX `<TimeScaleHeader` и добавить prop):
    ```tsx
    <TimeScaleHeader
      days={dateRange}
      dayWidth={dayWidth}
      headerHeight={headerHeight}
      viewMode={viewMode}
    />
    ```

    **4. Прокинуть viewMode в GridBackground** (найти JSX `<GridBackground` и добавить prop):
    ```tsx
    <GridBackground
      dateRange={dateRange}
      dayWidth={dayWidth}
      totalHeight={totalGridHeight}
      viewMode={viewMode}
    />
    ```

    **5. TodayIndicator — НЕ МЕНЯТЬ.** Компонент работает через `getDayOffset` в пиксельном пространстве дней, что корректно в обоих режимах (см. RESEARCH.md Pattern 5 и Pitfall 5).

    **6. gridWidth — НЕ МЕНЯТЬ.** Формула `dateRange.length * dayWidth` остаётся корректной в обоих режимах (Pitfall 3).

    **7. useTaskDrag — НЕ МЕНЯТЬ.** Hook работает в пиксель/день пространстве; viewMode не влияет на drag.

    ### Изменения в index.ts:

    Проверить текущий экспорт GanttChartProps — он уже экспортируется:
    ```typescript
    export { GanttChart, type Task, type TaskDependency, type GanttChartProps, type GanttChartHandle } from './components/GanttChart';
    ```

    Добавить экспорт WeekSpan из utils (если не экспортируется через `export * from './utils'`).
    Проверить: `export * from './utils'` в index.ts уже экспортирует всё из utils/ включая WeekSpan.
    Если WeekSpan в types/index.ts — убедиться что он экспортируется через `export type { ..., WeekSpan }`.

    Добавить WeekSpan в список экспортированных типов в index.ts если его ещё нет:
    ```typescript
    export type {
      GanttDateRange,
      TaskBarGeometry,
      GridConfig,
      MonthSpan,
      GridLine,
      WeekendBlock,
      WeekSpan,    // ДОБАВИТЬ если отсутствует
    } from './types';
    ```

    ПРИМЕЧАНИЕ: WeekSpan определён в utils/dateUtils.ts (не в types/). Если `export * from './utils'` уже покрывает его — дополнительный экспорт не нужен. Проверить по наличию WeekSpan в src/types/index.ts.
  </action>
  <acceptance_criteria>
    - `grep -n "viewMode" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx` — минимум 4 строки (interface, destructure default, TimeScaleHeader prop, GridBackground prop)
    - `grep -n "viewMode={viewMode}" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx` — ровно 2 совпадения (TimeScaleHeader и GridBackground)
    - `grep -n "viewMode.*'day'" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx` — дефолтное значение присутствует
    - `cd packages/gantt-lib && npx vitest run` — все тесты зелёные
    - TypeScript компиляция без ошибок: `cd packages/gantt-lib && npx tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>cd packages/gantt-lib && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>GanttChart принимает viewMode prop со значением по умолчанию 'day'; prop прокинут в TimeScaleHeader и GridBackground; TypeScript компилируется без ошибок; тесты зелёные</done>
</task>

<task type="auto">
  <name>Task 2: Демо-страница — переключатель day/week с рекомендуемым dayWidth</name>
  <files>packages/website/src/app/page.tsx</files>
  <read_first>
    - packages/website/src/app/page.tsx (целиком — найти первый GanttChart, добавить переключатель)
  </read_first>
  <action>
    ### Изменения в packages/website/src/app/page.tsx:

    **Цель:** добавить переключатель режима для первого (главного) GanttChart на странице.

    **1. Добавить state для viewMode рядом с другими state переменными в компоненте:**
    ```typescript
    const [viewMode, setViewMode] = useState<'day' | 'week'>('day');
    ```

    **2. Добавить UI переключатель над первым GanttChart:**
    Разместить кнопки-переключатели непосредственно перед JSX элементом первого `<GanttChart>`.

    Стиль кнопок — в духе существующего UI страницы (проверить по контексту файла):
    ```tsx
    <div style={{ display: 'flex', gap: '8px', marginBottom: '8px', alignItems: 'center' }}>
      <span style={{ fontSize: '0.875rem', color: '#6b7280', fontWeight: 500 }}>Масштаб:</span>
      <button
        onClick={() => setViewMode('day')}
        style={{
          padding: '4px 12px',
          fontSize: '0.875rem',
          borderRadius: '6px',
          border: '1px solid',
          cursor: 'pointer',
          backgroundColor: viewMode === 'day' ? '#1f2937' : 'transparent',
          color: viewMode === 'day' ? '#ffffff' : '#374151',
          borderColor: viewMode === 'day' ? '#1f2937' : '#d1d5db',
        }}
      >
        По дням
      </button>
      <button
        onClick={() => setViewMode('week')}
        style={{
          padding: '4px 12px',
          fontSize: '0.875rem',
          borderRadius: '6px',
          border: '1px solid',
          cursor: 'pointer',
          backgroundColor: viewMode === 'week' ? '#1f2937' : 'transparent',
          color: viewMode === 'week' ? '#ffffff' : '#374151',
          borderColor: viewMode === 'week' ? '#1f2937' : '#d1d5db',
        }}
      >
        По неделям
      </button>
    </div>
    ```

    **3. Передать viewMode и рекомендуемый dayWidth в первый GanttChart:**
    ```tsx
    <GanttChart
      // ... существующие пропсы (НЕ УДАЛЯТЬ)
      viewMode={viewMode}
      dayWidth={viewMode === 'week' ? 8 : 40}  // 8px/день = 56px/неделю в week-view
    />
    ```

    ВАЖНО: если в компоненте dayWidth передаётся как отдельный state или константа, адаптировать соответственно. Приоритет — читаемость и чтобы dayWidth менялся при переключении режима.

    ВАЖНО: импортировать `useState` если он уже не импортирован в файле.

    ВАЖНО: НЕ менять другие GanttChart компоненты на странице (если есть).
  </action>
  <acceptance_criteria>
    - `grep -n "viewMode" packages/website/src/app/page.tsx` — минимум 4 строки (state, setter, JSX prop, условный dayWidth)
    - `grep -n "setViewMode\|По дням\|По неделям" packages/website/src/app/page.tsx` — кнопки присутствуют
    - `grep -n "viewMode === 'week' ? 8 : 40\|dayWidth.*8" packages/website/src/app/page.tsx` — адаптивный dayWidth присутствует
    - `cd packages/gantt-lib && npx vitest run` — тесты зелёные (демо-страница не ломает library тесты)
  </acceptance_criteria>
  <verify>
    <automated>cd packages/gantt-lib && npx vitest run</automated>
  </verify>
  <done>Демо-страница содержит переключатель "По дням" / "По неделям"; при переключении dayWidth изменяется (40 → 8); viewMode передаётся в GanttChart</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — week-view визуальная проверка</name>
  <what-built>
    Добавлен viewMode prop в GanttChart. В week-view: заголовок показывает месяц (строка 1) и дату начала недели 01/08/15/22/29 (строка 2); сетка показывает недельные разделители без подсветки выходных; drag-and-drop сохраняет точные даты.
  </what-built>
  <how-to-verify>
    1. Запустить dev сервер: `cd packages/website && npm run dev` — открыть http://localhost:3000

    2. Проверить DEFAULT (day-view):
       - Страница выглядит идентично предыдущей версии
       - Заголовок показывает числа дней (1, 2, 3...)
       - Выходные дни подсвечены розовым

    3. Нажать кнопку "По неделям":
       - Заголовок строка 1: название месяца + год («Март 2026», «Апрель 2026» и т.д.)
       - Заголовок строка 2: дата начала недели двузначным числом (01, 08, 15, 22, 29...)
       - Сетка: только недельные вертикальные линии (тонкие) + яркие разделители на границах месяцев
       - Нет розовой подсветки выходных
       - Диаграмма более «сжатая» — весь проект виден одновременно

    4. Проверить вертикальная линия сегодняшнего дня:
       - Линия "сегодня" видна и позиционируется в пределах правильной недельной колонки

    5. Проверить drag-and-drop в week-view:
       - Перетащить задачу на новое место
       - Отпустить
       - Дата изменилась точно (не округлилась до понедельника)
       - Связи (если есть) обновились корректно

    6. Вернуться в "По дням" — всё выглядит как до изменений (обратная совместимость).
  </how-to-verify>
  <resume-signal>Написать "approved" если всё выглядит правильно, или описать найденные проблемы</resume-signal>
</task>

</tasks>

<verification>
- `cd packages/gantt-lib && npx vitest run` — полный suite зелёный
- `cd packages/gantt-lib && npx tsc --noEmit` — TypeScript без ошибок
- Визуальная проверка через checkpoint Task 3
</verification>

<success_criteria>
- GanttChart экспортирует viewMode в GanttChartProps
- viewMode='day' (default) — идентичный визуальный результат с предыдущей версией
- viewMode='week' — 2-строчный заголовок с месяцами и датами начала недель, недельная сетка, нет выходных
- Drag-and-drop в week-view сохраняет точные даты
- Демо-страница показывает переключатель, работающий корректно
- Весь vitest suite зелёный
- TypeScript компилируется без ошибок
</success_criteria>

<output>
После завершения создать `.planning/phases/20-month-view/20-03-SUMMARY.md`
</output>
