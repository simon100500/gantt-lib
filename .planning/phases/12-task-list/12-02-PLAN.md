---
wave: 2
depends_on: ["12-01"]
files_modified:
  - packages/gantt-lib/src/styles.css
  - packages/website/src/app/page.tsx
  - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
autonomous: true
---

# Phase 12-02: Task List Integration - CSS Aggregation and Demo Page

## Goal

Integrate TaskList CSS into the main library styles, add TaskList to the main library export, and create a comprehensive demo page showing the TaskList feature with a toggle button and inline editing examples.

## Requirements

None — internal feature extension

## Success Criteria

1. TaskList styles are imported into the main styles.css file
2. TaskList is exported from the main library index
3. Demo page shows TaskList with a toggle button
4. Demo page demonstrates inline editing of task names and dates
5. Demo page shows row selection highlighting
6. All functionality works end-to-end (toggle, edit, select, scroll)

## Tasks

### Task 1: Aggregate TaskList CSS into main styles.css

**Action**: Import TaskList.css into the main library styles.css file

**Implementation details**:
- Add `@import './components/TaskList/TaskList.css';` to `packages/gantt-lib/src/styles.css`
- Place import after other component imports (maintain alphabetical/ordering consistency)
- Ensure CSS is imported after the existing GanttChart, TaskRow, TimeScaleHeader, GridBackground, TodayIndicator, and DragGuideLines imports
- Verify that CSS class names don't conflict (all use `gantt-tl-` prefix)
- Test that styles load correctly in the website build

**Verification**:
- styles.css includes the TaskList CSS import
- Running `npm run build` in gantt-lib succeeds
- CSS is properly bundled in the dist output

### Task 2: Export TaskList from main library index

**Action**: Add TaskList to the main library exports

**Implementation details**:
- Modify `packages/gantt-lib/src/index.ts`
- Add named export: `export { TaskList } from './components/TaskList';`
- Verify TypeScript compilation succeeds
- Check that TaskList is available for import from 'gantt-lib'

**Verification**:
- Importing `import { TaskList } from 'gantt-lib'` works in website code
- TypeScript types are available for TaskList props
- No build errors in the library

### Task 3: Create TaskList demo page with toggle and examples

**Action**: Add TaskList demo to the website showing all features

**Implementation details**:
- Modify `packages/website/src/app/page.tsx`:
  - Add `useState` for `showTaskList` boolean state (default: false)
  - Add toggle button: "Show Task List" / "Hide Task List"
  - Pass `showTaskList={showTaskList}` to GanttChart in the main Construction Project demo
  - Optionally add `taskListWidth={300}` to demonstrate width customization
  - Add demo with inline editing instructions
  - Add row selection demonstration
- Style the toggle button to match existing demo UI
- Add explanatory text:
  - "Toggle the task list to see tasks in a table format"
  - "Click on task names or dates to edit them inline"
  - "Press Enter to save, Escape to cancel"
  - "Click a row to select it and highlight the corresponding task"
- Test that all interactions work:
  - Toggle shows/hides the overlay
  - Inline editing updates the chart in real-time
  - Row selection highlights the selected task
  - Scrolling is synchronized

**Verification**:
- Toggle button is visible and functional
- TaskList appears/disappears when toggle is clicked
- Inline editing works for name and dates
- Changes in TaskList are reflected in the chart (task bars update)
- Row selection highlights the selected task
- Vertical scroll is synchronized between list and chart
- All text instructions are clear and accurate

## Files Modified

- `packages/gantt-lib/src/styles.css` - MODIFIED (add TaskList CSS import)
- `packages/gantt-lib/src/index.ts` - MODIFIED (export TaskList)
- `packages/website/src/app/page.tsx` - MODIFIED (add TaskList demo with toggle)
- `packages/gantt-lib/src/components/GanttChart/GanttChart.tsx` - MODIFIED (verify showTaskList prop is properly integrated from Plan 12-01)

## Must Haves

1. TaskList CSS is aggregated into styles.css
2. TaskList is exported from main library index
3. Demo page has toggle button for TaskList
4. Demo page demonstrates inline editing
5. Demo page demonstrates row selection
6. All features work end-to-end without errors

## Verification Criteria

After executing this plan, the following should be TRUE:

1. [ ] Building the library with `npm run build` succeeds
2. [ ] TaskList can be imported: `import { TaskList } from 'gantt-lib'`
3. [ ] Website demo page has a "Show Task List" button
4. [ ] Clicking the button shows/hides the task list overlay
5. [ ] Task list displays 4 columns with correct headers
6. [ ] Task list rows align with task bars
7. [ ] Clicking a task name switches to edit mode (input appears)
8. [ ] Editing a name and pressing Enter updates the task name on the chart
9. [ ] Editing a date and pressing Enter updates the date on the chart
10. [ ] Pressing Escape cancels the edit without changes
11. [ ] Clicking a row highlights it with blue background
12. [ ] Scrolling the chart scrolls the task list
13. [ ] Entering an invalid date format keeps edit mode active (no save)
14. [ ] All CSS loads correctly (no missing styles)
15. [ ] No console errors or warnings

## Dependencies

- 12-01 (TaskList component must exist before we can integrate and demo it)

## Notes

- This plan completes the Phase 12 feature implementation
- The demo page serves as human verification of the feature
- Consider adding a screenshot or visual example in the README after this plan is complete
- The toggle button demonstrates external control of the TaskList visibility (as specified in CONTEXT.md)
