---
phase: 27-core-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/gantt-lib/src/core/scheduling/types.ts
  - packages/gantt-lib/src/core/scheduling/dateMath.ts
  - packages/gantt-lib/src/core/scheduling/dependencies.ts
  - packages/gantt-lib/src/core/scheduling/cascade.ts
  - packages/gantt-lib/src/core/scheduling/commands.ts
  - packages/gantt-lib/src/core/scheduling/validation.ts
  - packages/gantt-lib/src/core/scheduling/hierarchy.ts
  - packages/gantt-lib/src/core/scheduling/index.ts
  - packages/gantt-lib/src/utils/dependencyUtils.ts
  - packages/gantt-lib/src/utils/dateUtils.ts
  - packages/gantt-lib/src/utils/index.ts
  - packages/gantt-lib/src/core/scheduling/__tests__/dateMath.test.ts
  - packages/gantt-lib/src/core/scheduling/__tests__/dependencies.test.ts
  - packages/gantt-lib/src/core/scheduling/__tests__/cascade.test.ts
  - packages/gantt-lib/src/core/scheduling/__tests__/commands.test.ts
  - packages/gantt-lib/src/core/scheduling/__tests__/validation.test.ts
  - packages/gantt-lib/src/core/scheduling/__tests__/hierarchy.test.ts
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03]

must_haves:
  truths:
    - "core/scheduling/ directory exists with 7 module files + index.ts barrel"
    - "All 30+ scheduling functions are importable from core/scheduling/index.ts"
    - "core/scheduling/ contains zero React imports, zero DOM references, zero date-fns imports"
    - "dependencyUtils.ts is a thin re-export barrel (zero function implementations)"
    - "All existing 29 test files pass without modification"
    - "Business-day functions (getBusinessDaysCount, addBusinessDays, subtractBusinessDays) live in core/scheduling/dateMath.ts"
  artifacts:
    - path: "packages/gantt-lib/src/core/scheduling/index.ts"
      provides: "Barrel re-export of all core scheduling APIs"
      exports: ["calculateSuccessorDate", "universalCascade", "cascadeByLinks", "moveTaskRange", "validateDependencies", "getChildren", "isTaskParent", "computeParentDates", "recalculateIncomingLags", "getBusinessDaysCount", "addBusinessDays", "subtractBusinessDays"]
    - path: "packages/gantt-lib/src/core/scheduling/types.ts"
      provides: "Scheduling type re-exports"
      contains: "LinkType"
    - path: "packages/gantt-lib/src/core/scheduling/dateMath.ts"
      provides: "Pure date math: normalizeUTCDate, parseDateOnly, business-day operations"
      contains: "getBusinessDaysCount"
    - path: "packages/gantt-lib/src/core/scheduling/dependencies.ts"
      provides: "Dependency calculation functions"
      contains: "calculateSuccessorDate"
    - path: "packages/gantt-lib/src/core/scheduling/cascade.ts"
      provides: "Cascade engine functions"
      contains: "universalCascade"
    - path: "packages/gantt-lib/src/core/scheduling/commands.ts"
      provides: "High-level schedule command functions"
      contains: "moveTaskRange"
    - path: "packages/gantt-lib/src/core/scheduling/validation.ts"
      provides: "Dependency validation and cycle detection"
      contains: "validateDependencies"
    - path: "packages/gantt-lib/src/core/scheduling/hierarchy.ts"
      provides: "Hierarchy scheduling functions"
      contains: "getChildren"
    - path: "packages/gantt-lib/src/utils/dependencyUtils.ts"
      provides: "Backward-compat re-export barrel"
      contains: "export {"
  key_links:
    - from: "src/core/scheduling/index.ts"
      to: "src/core/scheduling/*.ts"
      via: "re-export barrel"
      pattern: "export \\* from"
    - from: "src/utils/dependencyUtils.ts"
      to: "src/core/scheduling/index.ts"
      via: "re-export barrel"
      pattern: "export \\* from"
    - from: "src/utils/index.ts"
      to: "src/utils/dependencyUtils.ts"
      via: "existing barrel chain"
      pattern: "export \\* from"
---

<objective>
Create the headless scheduling core module at `src/core/scheduling/` and move all pure scheduling logic from dependencyUtils.ts and dateUtils.ts into it. The existing utils/ files become thin re-export barrels for backward compatibility.

Purpose: Establish a runtime-agnostic scheduling module that contains zero React/DOM dependencies, enabling downstream repos to consume scheduling logic without pulling in UI code.

Output: 7 new module files + index barrel + 6 test files + 2 re-written backward-compat barrels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-core-refactor/27-CONTEXT.md
@.planning/phases/27-core-refactor/27-RESEARCH.md

@packages/gantt-lib/src/utils/dependencyUtils.ts
@packages/gantt-lib/src/utils/dateUtils.ts
@packages/gantt-lib/src/utils/index.ts
@packages/gantt-lib/src/types/index.ts
@packages/gantt-lib/src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create core/scheduling module structure with types and dateMath</name>
  <files>
    packages/gantt-lib/src/core/scheduling/types.ts
    packages/gantt-lib/src/core/scheduling/dateMath.ts
    packages/gantt-lib/src/core/scheduling/__tests__/dateMath.test.ts
  </files>
  <read_first>
    packages/gantt-lib/src/utils/dependencyUtils.ts
    packages/gantt-lib/src/utils/dateUtils.ts (lines 584-709 for business-day functions)
    packages/gantt-lib/src/types/index.ts
  </read_first>
  <behavior>
    - Test 1: normalizeUTCDate creates correct UTC midnight date from input
    - Test 2: parseDateOnly parses ISO string "2025-01-15" to UTC Date
    - Test 3: getBusinessDaysCount counts weekdays between Mon-Fri = 5
    - Test 4: getBusinessDaysCount skips weekends with default predicate
    - Test 5: getBusinessDaysCount respects custom weekendPredicate
    - Test 6: addBusinessDays adds N business days skipping weekends
    - Test 7: subtractBusinessDays subtracts N business days skipping weekends
    - Test 8: getBusinessDayOffset computes offset between two dates
    - Test 9: shiftBusinessDayOffset shifts a date by business-day offset
    - Test 10: DAY_MS constant equals 86400000
  </behavior>
  <action>
    1. Create `src/core/scheduling/types.ts`:
       - Re-export scheduling-relevant types from `src/types/index.ts`: `LinkType`, `TaskDependency`, `DependencyError`, `ValidationResult`, `Task`
       - Do NOT import from any React component or hook
       - This file is the type gateway into the core module

    2. Create `src/core/scheduling/dateMath.ts`:
       - Move these functions FROM dependencyUtils.ts (they are currently internal/helpers at the top of that file):
         - `normalizeUTCDate(date: string | Date): Date` — creates UTC midnight date
         - `parseDateOnly(date: string | Date): Date` — parses date string to UTC Date
         - `getBusinessDayOffset(fromDate: Date, toDate: Date, weekendPredicate?: (date: Date) => boolean): number`
         - `shiftBusinessDayOffset(date: Date, offset: number, weekendPredicate?: (date: Date) => boolean): Date`
         - `DAY_MS` constant (24 * 60 * 60 * 1000)
       - Move these functions FROM dateUtils.ts (lines 617-709):
         - `getBusinessDaysCount(startDate: Date | string, endDate: Date | string, weekendPredicate?: (date: Date) => boolean): number`
         - `addBusinessDays(startDate: Date | string, businessDays: number, weekendPredicate?: (date: Date) => boolean): Date`
         - `subtractBusinessDays(endDate: Date | string, businessDays: number, weekendPredicate?: (date: Date) => boolean): Date`
       - Import types from `./types.ts` (relative path within core)
       - Do NOT import from date-fns, React, or DOM APIs
       - All functions must be `export` named exports

    3. Create `src/core/scheduling/__tests__/dateMath.test.ts`:
       - Import directly from `../dateMath`
       - Write tests covering all 10 behaviors listed above
       - Test that getBusinessDaysCount returns 5 for Mon-Fri, 0 for Sat-Sun range with predicate
       - Test that addBusinessDays(Monday, 5) = next Monday
       - Test that subtractBusinessDays(Friday, 5) = previous Friday
       - Run tests to confirm RED phase (functions don't exist yet in core)
       - Then confirm GREEN after writing the functions

    Key principle: These are EXACT copies of the existing functions. Do not rewrite them. Copy the implementations verbatim. The only change is the import path for types (from `./types` instead of the original location).
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npx vitest run src/core/scheduling/__tests__/dateMath.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - types.ts re-exports LinkType, TaskDependency, DependencyError, ValidationResult, Task from src/types/index.ts
    - dateMath.ts contains: normalizeUTCDate, parseDateOnly, getBusinessDayOffset, shiftBusinessDayOffset, DAY_MS, getBusinessDaysCount, addBusinessDays, subtractBusinessDays
    - dateMath.ts contains zero imports from 'react', 'date-fns', or DOM APIs
    - All 10 dateMath tests pass green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create dependencies, cascade, commands, validation, hierarchy modules + re-export barrels</name>
  <files>
    packages/gantt-lib/src/core/scheduling/dependencies.ts
    packages/gantt-lib/src/core/scheduling/cascade.ts
    packages/gantt-lib/src/core/scheduling/commands.ts
    packages/gantt-lib/src/core/scheduling/validation.ts
    packages/gantt-lib/src/core/scheduling/hierarchy.ts
    packages/gantt-lib/src/core/scheduling/index.ts
    packages/gantt-lib/src/core/scheduling/__tests__/dependencies.test.ts
    packages/gantt-lib/src/core/scheduling/__tests__/cascade.test.ts
    packages/gantt-lib/src/core/scheduling/__tests__/commands.test.ts
    packages/gantt-lib/src/core/scheduling/__tests__/validation.test.ts
    packages/gantt-lib/src/core/scheduling/__tests__/hierarchy.test.ts
    packages/gantt-lib/src/utils/dependencyUtils.ts
    packages/gantt-lib/src/utils/dateUtils.ts
    packages/gantt-lib/src/utils/index.ts
  </files>
  <read_first>
    packages/gantt-lib/src/utils/dependencyUtils.ts (full file — all 1276 lines)
    packages/gantt-lib/src/utils/dateUtils.ts (lines 617-709 — business-day functions being moved)
    packages/gantt-lib/src/utils/index.ts
    packages/gantt-lib/src/types/index.ts
    packages/gantt-lib/src/core/scheduling/types.ts (created in Task 1)
    packages/gantt-lib/src/core/scheduling/dateMath.ts (created in Task 1)
  </read_first>
  <behavior>
    dependencies.test.ts:
    - Test 1: getDependencyLag returns lag value or 0 for undefined
    - Test 2: normalizeDependencyLag clamps FS lag to >= 0
    - Test 3: calculateSuccessorDate computes correct date for each link type (FS, SS, FF, SF) with lag
    - Test 4: computeLagFromDates computes correct lag from predecessor/successor date pairs

    cascade.test.ts:
    - Test 1: cascadeByLinks produces correct task overrides for FS chain
    - Test 2: universalCascade propagates changes through mixed link type chains
    - Test 3: getSuccessorChain returns correct transitive successors for FS links
    - Test 4: reflowTasksOnModeSwitch converts between business/calendar day durations

    commands.test.ts:
    - Test 1: getTaskDuration returns correct duration in calendar days
    - Test 2: getTaskDuration returns correct duration in business days
    - Test 3: buildTaskRangeFromStart creates range from start date + duration
    - Test 4: buildTaskRangeFromEnd creates range from end date + duration
    - Test 5: moveTaskRange preserves duration when moving to new start date
    - Test 6: clampTaskRangeForIncomingFS clamps start date based on incoming FS dependencies
    - Test 7: recalculateIncomingLags recomputes lag values after date change
    - Test 8: alignToWorkingDay snaps Saturday to Monday (forward), Sunday to Friday (backward)

    validation.test.ts:
    - Test 1: validateDependencies returns isValid: true for valid task set
    - Test 2: validateDependencies detects cycles
    - Test 3: detectCycles returns { hasCycle: true } for A->B->A
    - Test 4: buildAdjacencyList creates correct predecessor map

    hierarchy.test.ts:
    - Test 1: getChildren returns direct children of a parent
    - Test 2: isTaskParent returns true for tasks with children
    - Test 3: computeParentDates aggregates children date ranges
    - Test 4: computeParentProgress averages children progress
    - Test 5: getAllDescendants returns recursive descendants
  </behavior>
  <action>
    **IMPORTANT: This is a structural MOVE, not a rewrite. Copy function implementations verbatim from dependencyUtils.ts. Only change import paths.**

    1. Create `src/core/scheduling/dependencies.ts`:
       - Move from dependencyUtils.ts:
         - `getDependencyLag(dep)`
         - `normalizeDependencyLag(linkType, lag, businessDays, weekendPredicate)`
         - `calculateSuccessorDate(predecessorStart, predecessorEnd, linkType, lag, businessDays, weekendPredicate)`
         - `computeLagFromDates(linkType, predStart, predEnd, succStart, succEnd, businessDays, weekendPredicate)`
       - Import from `./dateMath`: normalizeUTCDate, getBusinessDayOffset, shiftBusinessDayOffset, DAY_MS, getBusinessDaysCount, addBusinessDays, subtractBusinessDays
       - Import types from `./types`

    2. Create `src/core/scheduling/cascade.ts`:
       - Move from dependencyUtils.ts:
         - `cascadeByLinks(movedTaskId, newStart, newEnd, allTasks, skipChildCascade, businessDays, weekendPredicate)`
         - `universalCascade(movedTask, newStart, newEnd, allTasks, businessDays, weekendPredicate)`
         - `getSuccessorChain(draggedTaskId, allTasks, linkTypes)`
         - `getTransitiveCascadeChain(changedTaskId, allTasks, firstLevelLinkTypes)`
         - `reflowTasksOnModeSwitch(sourceTasks, toBusinessDays, weekendPredicate)`
       - Import from `./dependencies`: calculateSuccessorDate, getDependencyLag
       - Import from `./hierarchy`: getChildren, isTaskParent (will be created below — use forward reference through index barrel or direct relative import)
       - Import from `./dateMath`: normalizeUTCDate, getBusinessDaysCount, addBusinessDays, subtractBusinessDays
       - Import types from `./types`

    3. Create `src/core/scheduling/commands.ts`:
       - Move from dependencyUtils.ts:
         - `alignToWorkingDay(date, direction, weekendPredicate)`
         - `getTaskDuration(startDate, endDate, businessDays, weekendPredicate)`
         - `buildTaskRangeFromStart(startDate, duration, businessDays, weekendPredicate)`
         - `buildTaskRangeFromEnd(endDate, duration, businessDays, weekendPredicate)`
         - `moveTaskRange(originalStart, originalEnd, proposedStart, businessDays, weekendPredicate, snapDirection)`
         - `clampTaskRangeForIncomingFS(task, proposedStart, proposedEnd, allTasks, businessDays, weekendPredicate)`
         - `recalculateIncomingLags(task, newStartDate, newEndDate, allTasks, businessDays, weekendPredicate)`
       - Import from `./dependencies`: calculateSuccessorDate, getDependencyLag, normalizeDependencyLag
       - Import from `./dateMath`: normalizeUTCDate, parseDateOnly, getBusinessDaysCount, addBusinessDays, subtractBusinessDays, DAY_MS
       - Import types from `./types`

    4. Create `src/core/scheduling/validation.ts`:
       - Move from dependencyUtils.ts:
         - `buildAdjacencyList(tasks)`
         - `detectCycles(tasks)`
         - `validateDependencies(tasks)`
       - Import types from `./types`

    5. Create `src/core/scheduling/hierarchy.ts`:
       - Move from dependencyUtils.ts:
         - `getChildren(parentId, tasks)`
         - `isTaskParent(taskId, tasks)`
         - `computeParentDates(parentId, tasks)`
         - `computeParentProgress(parentId, tasks)`
         - `getAllDescendants(parentId, tasks)`
         - `getAllDependencyEdges(tasks)`
         - `removeDependenciesBetweenTasks(taskId1, taskId2, tasks)`
         - `findParentId(taskId, tasks)`
         - `isAncestorTask(ancestorId, taskId, tasks)`
         - `areTasksHierarchicallyRelated(taskId1, taskId2, tasks)`
       - Import types from `./types`

    6. Create `src/core/scheduling/index.ts` barrel:
       ```typescript
       // Core scheduling module — runtime-agnostic scheduling logic
       // Zero React/DOM/date-fns dependencies

       export * from './types';
       export * from './dateMath';
       export * from './dependencies';
       export * from './cascade';
       export * from './commands';
       export * from './validation';
       export * from './hierarchy';
       ```

    7. Create test files in `src/core/scheduling/__tests__/`:
       - Each test imports directly from the corresponding module (e.g., `import { calculateSuccessorDate } from '../dependencies'`)
       - Write minimal smoke tests verifying each function produces correct output for known inputs
       - Use the existing `__tests__/dependencyUtils.test.ts` as reference for test data and expected values
       - Do NOT duplicate full test coverage — write 3-5 focused tests per module to verify the move was correct

    8. **Rewrite `src/utils/dependencyUtils.ts` as backward-compat re-export barrel:**
       ```typescript
       // Backward compatibility — all scheduling logic moved to src/core/scheduling
       // This file re-exports everything so existing imports continue to work
       export * from '../core/scheduling';
       ```
       **CRITICAL:** The existing barrel chain is: `src/index.ts` → `export * from './utils'` → `utils/index.ts` → `export * from './dependencyUtils'`. By turning dependencyUtils.ts into a re-export barrel, ALL downstream imports (including from npm consumers) continue to work without changes.

    9. **Update `src/utils/dateUtils.ts`:**
       - Remove the three business-day function implementations (getBusinessDaysCount, addBusinessDays, subtractBusinessDays — lines 617-709)
       - Add re-exports from core:
         ```typescript
         // Business-day functions moved to core/scheduling
         export { getBusinessDaysCount, addBusinessDays, subtractBusinessDays } from '../core/scheduling/dateMath';
         ```
       - Keep ALL other functions in place (parseUTCDate, getMonthDays, getDayOffset, isToday, isWeekend, createDateKey, createCustomDayPredicate, getMultiMonthDays, getMonthSpans, formatDateLabel, formatDateRangeLabel, getWeekBlocks, getWeekSpans, getMonthBlocks, getYearSpans, normalizeTaskDates, and all type exports)

    10. **Verify `src/utils/index.ts` remains unchanged:**
        - It already has `export * from './dateUtils'` and `export * from './dependencyUtils'`
        - With the re-export barrels above, this chain continues to work
        - No changes needed to `src/utils/index.ts`

    **Internal import resolution within core modules:**
    When functions in one core module call functions from another core module, use RELATIVE imports (e.g., `import { calculateSuccessorDate } from './dependencies'`), NOT through the index barrel. This prevents circular dependency issues.

    **Cascading module dependency order (import this order):**
    - types.ts → standalone (imports from src/types only)
    - dateMath.ts → imports from ./types
    - dependencies.ts → imports from ./types, ./dateMath
    - validation.ts → imports from ./types
    - hierarchy.ts → imports from ./types
    - commands.ts → imports from ./types, ./dateMath, ./dependencies
    - cascade.ts → imports from ./types, ./dateMath, ./dependencies, ./hierarchy
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All 7 core module files exist under src/core/scheduling/ with correct function implementations
    - src/core/scheduling/index.ts barrel re-exports everything
    - src/utils/dependencyUtils.ts is a 2-line re-export barrel containing only `export * from '../core/scheduling'`
    - src/utils/dateUtils.ts re-exports 3 business-day functions from core, keeps all UI helpers in place
    - ALL existing 29 test files pass (backward compat preserved through re-export chain)
    - ALL new core scheduling tests pass
    - grep -r "from 'react'" src/core/scheduling/ returns nothing
    - grep -r "from 'date-fns'" src/core/scheduling/ returns nothing
    - grep -r "document\." src/core/scheduling/ returns nothing
  </done>
</task>

</tasks>

<verification>
1. `cd packages/gantt-lib && npx vitest run` — full suite green (all 29+ existing tests + new core tests)
2. `grep -r "from 'react'" src/core/scheduling/` — returns nothing (headless verified)
3. `grep -r "from 'date-fns'" src/core/scheduling/` — returns nothing (no date-fns in core)
4. `grep -r "document\." src/core/scheduling/` — returns nothing (no DOM in core)
5. `cat src/utils/dependencyUtils.ts` — shows only re-export line(s), no function implementations
6. `cd packages/gantt-lib && pnpm build` — build succeeds (no circular deps, no type errors)
</verification>

<success_criteria>
- Headless core module at src/core/scheduling/ contains all scheduling logic
- Zero React/DOM/date-fns dependencies in core
- All existing tests pass via backward-compat re-export chain
- New core tests directly import from core paths and pass
- dependencyUtils.ts is a thin re-export barrel
- Build succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/27-core-refactor/27-01-SUMMARY.md`
</output>
