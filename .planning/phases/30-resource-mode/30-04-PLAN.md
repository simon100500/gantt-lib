---
phase: 30-resource-mode
plan: 04
type: execute
wave: 4
depends_on: [30-03]
files_modified:
  - packages/gantt-lib/src/index.ts
  - packages/gantt-lib/src/components/GanttChart/index.tsx
  - packages/gantt-lib/src/__tests__/export-contract.test.ts
  - packages/gantt-lib/src/__tests__/resourceModeRegression.test.tsx
  - packages/gantt-lib/README.md
  - docs/reference/04-props.md
  - docs/reference/09-styling.md
  - docs/reference/10-drag-interactions.md
requirements: [RP-01, RP-02, RP-06, RP-07, RP-08, RP-09, RP-10, RP-11]
autonomous: true
must_haves:
  truths:
    - "User can import ResourceTimelineChart and resource public types from the package."
    - "Docs show default task mode and resource planner mode without contradicting existing task-mode behavior."
    - "Regression tests prove task mode remains compatible and resource mode remains isolated."
  artifacts:
    - path: "packages/gantt-lib/src/index.ts"
      provides: "Root public exports"
      contains: "ResourceTimelineChart"
    - path: "packages/gantt-lib/src/__tests__/export-contract.test.ts"
      provides: "Public export regression coverage"
    - path: "packages/gantt-lib/README.md"
      provides: "README resource planner quick start"
      contains: "mode=\"resource-planner\""
    - path: "docs/reference/04-props.md"
      provides: "Resource planner prop reference"
    - path: "docs/reference/09-styling.md"
      provides: "Resource CSS variable reference"
    - path: "docs/reference/10-drag-interactions.md"
      provides: "Resource drag behavior reference"
  key_links:
    - from: "packages/gantt-lib/src/index.ts"
      to: "packages/gantt-lib/src/components/ResourceTimelineChart"
      via: "public export"
      pattern: "ResourceTimelineChart"
    - from: "docs/reference/04-props.md"
      to: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      via: "documented discriminated props"
      pattern: "mode=\"resource-planner\""
---

<objective>
Finalize public exports, documentation, and full regression coverage for resource planner mode.

Purpose: Make the new API discoverable and prove the phase does not regress task mode or leak task-only behavior into resource mode.
Output: Export contract updates, README/reference docs, and green targeted/full package tests.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/GANTT-LIB-RESOURCE-PLANNER-MODE-PRD.md
@.planning/phases/30-resource-mode/30-CONTEXT.md
@.planning/phases/30-resource-mode/30-RESEARCH.md
@.planning/phases/30-resource-mode/30-PATTERNS.md
@.planning/phases/30-resource-mode/30-VALIDATION.md
@.planning/phases/30-resource-mode/30-03-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package exports -> downstream consumers | Consumers rely on root exports and declarations as the public contract. |
| documentation -> consumer implementation | Consumers follow README/reference examples for mode selection, drag callbacks, and authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04-01 | Spoofing | docs/API examples | mitigate | Document `mode="resource-planner"` as the required resource branch and omitted mode as task mode; avoid ambiguous examples. |
| T-30-04-02 | Elevation of Privilege | drag documentation | transfer | State that consumers must validate `onResourceItemMove` before updating resource ownership. |
| T-30-04-03 | Denial of Service | export contract | mitigate | Add export tests for resource runtime exports and public type compile paths so missing exports fail CI. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish resource exports and export-contract tests</name>
  <read_first>
    - `.planning/phases/30-resource-mode/30-PATTERNS.md` sections for export/model files and export tests.
    - `packages/gantt-lib/src/index.ts`
    - `packages/gantt-lib/src/components/GanttChart/index.tsx`
    - `packages/gantt-lib/src/components/ResourceTimelineChart/index.tsx`
    - `packages/gantt-lib/src/__tests__/export-contract.test.ts`
  </read_first>
  <files>packages/gantt-lib/src/index.ts, packages/gantt-lib/src/components/GanttChart/index.tsx, packages/gantt-lib/src/__tests__/export-contract.test.ts</files>
  <acceptance_criteria>
    - `rg "ResourceTimelineChart" packages/gantt-lib/src/index.ts packages/gantt-lib/src/components/GanttChart/index.tsx` finds public exports.
    - `rg "ResourceTimelineItem|ResourceTimelineResource|ResourceTimelineMove|ResourcePlannerChartProps|GanttChartMode" packages/gantt-lib/src/index.ts packages/gantt-lib/src/components/GanttChart/index.tsx` finds type exports.
    - `packages/gantt-lib/src/__tests__/export-contract.test.ts` asserts `ResourceTimelineChart` is defined from the root import.
    - Export contract tests include a type-level usage path for resource public contracts where the existing test style supports it.
  </acceptance_criteria>
  <action>
    Export `ResourceTimelineChart` and all resource public types from the package root and component barrel following existing export style. Extend export-contract tests so missing runtime export or type export regressions fail. Keep all existing task-mode exports unchanged.
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npm test -- src/__tests__/export-contract.test.ts</automated>
    <automated>cd packages/gantt-lib && npm run build</automated>
  </verify>
  <done>Resource planner runtime and type exports are publicly available, buildable, and covered by export regression tests.</done>
</task>

<task type="auto">
  <name>Task 2: Update README and reference docs</name>
  <read_first>
    - `.planning/GANTT-LIB-RESOURCE-PLANNER-MODE-PRD.md` sections 3, 4, 7, 8, 9, and 13.
    - `.planning/phases/30-resource-mode/30-PATTERNS.md` docs section.
    - `packages/gantt-lib/README.md`
    - `docs/reference/04-props.md`
    - `docs/reference/09-styling.md`
    - `docs/reference/10-drag-interactions.md`
  </read_first>
  <files>packages/gantt-lib/README.md, docs/reference/04-props.md, docs/reference/09-styling.md, docs/reference/10-drag-interactions.md</files>
  <acceptance_criteria>
    - `rg "mode=\"resource-planner\"" packages/gantt-lib/README.md docs/reference/04-props.md` finds resource planner examples.
    - `rg "ResourceTimelineResource" packages/gantt-lib/README.md docs/reference/04-props.md` finds the resource type in docs.
    - `rg "--gantt-resource-row-header-width|--gantt-resource-lane-height|--gantt-resource-bar-radius|--gantt-resource-bar-conflict-color" docs/reference/09-styling.md` finds all resource CSS variables.
    - `rg "readonly|locked|Drop outside|resource row|onResourceItemMove" docs/reference/10-drag-interactions.md` finds resource drag behavior documentation.
    - Docs state resource mode does not render dependency lines, task list editing, hierarchy/cascade, or task reorder behavior.
    - Docs state consumers validate resource reassignment before updating their own resource state.
  </acceptance_criteria>
  <action>
    Add concise resource planner docs: README quick start, props reference for the discriminated mode split, CSS variables, and drag interaction behavior. Document `GanttChart mode` as the primary path and `ResourceTimelineChart` as the specialized export. Preserve existing task-mode docs and examples. Explicitly exclude resource dependency overlays, built-in conflict rejection, and built-in state management because they are deferred in the locked PRD.
  </action>
  <verify>
    <automated>rg "mode=\"resource-planner\"|ResourceTimelineResource|onResourceItemMove|--gantt-resource-lane-height" packages/gantt-lib/README.md docs/reference/04-props.md docs/reference/09-styling.md docs/reference/10-drag-interactions.md</automated>
  </verify>
  <done>Documentation covers RP-01, RP-02, RP-06 through RP-10, styling variables, task-mode exclusions, and consumer-owned authorization/state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close phase with regression and full package verification</name>
  <read_first>
    - `.planning/phases/30-resource-mode/30-VALIDATION.md`
    - `packages/gantt-lib/src/__tests__/resourceTimelineLayout.test.ts`
    - `packages/gantt-lib/src/__tests__/resourceTimelineChart.test.tsx`
    - `packages/gantt-lib/src/__tests__/resourceTimelineDrag.test.tsx`
    - `packages/gantt-lib/src/__tests__/resourceModeRegression.test.tsx`
    - `packages/gantt-lib/src/__tests__/dependencyLines.test.tsx`
  </read_first>
  <files>packages/gantt-lib/src/__tests__/resourceModeRegression.test.tsx, packages/gantt-lib/src/__tests__/export-contract.test.ts</files>
  <acceptance_criteria>
    - Targeted resource suite passes.
    - Existing `dependencyLines.test.tsx` passes and remains scoped to gantt mode.
    - Full `cd packages/gantt-lib && npm test` passes.
    - `cd packages/gantt-lib && npm run build` passes.
    - `rg "DependencyLines|TaskList|core/scheduling|useTaskDrag" packages/gantt-lib/src/components/ResourceTimelineChart packages/gantt-lib/src/hooks/useResourceItemDrag.ts` returns no matches.
  </acceptance_criteria>
  <action>
    Tighten regression tests only where gaps remain after Tasks 1 and 2. Confirm resource mode stays isolated from task-only systems and task mode still works through omitted mode. Fix only regressions caused by Phase 30 work.
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npm test -- src/__tests__/resourceTimelineLayout.test.ts src/__tests__/resourceTimelineChart.test.tsx src/__tests__/resourceTimelineDrag.test.tsx src/__tests__/resourceModeRegression.test.tsx src/__tests__/export-contract.test.ts src/__tests__/dependencyLines.test.tsx</automated>
    <automated>cd packages/gantt-lib && npm test</automated>
    <automated>cd packages/gantt-lib && npm run build</automated>
  </verify>
  <done>All RP-01 through RP-11 coverage is green through targeted tests, full package tests, and build verification.</done>
</task>

</tasks>

<verification>
Run:

```powershell
cd packages/gantt-lib
npm test -- src/__tests__/resourceTimelineLayout.test.ts src/__tests__/resourceTimelineChart.test.tsx src/__tests__/resourceTimelineDrag.test.tsx src/__tests__/resourceModeRegression.test.tsx src/__tests__/export-contract.test.ts src/__tests__/dependencyLines.test.tsx
npm test
npm run build
```
</verification>

<success_criteria>
- Root exports expose `ResourceTimelineChart` and resource public types.
- README and reference docs show resource planner mode, drag callback behavior, customization hooks, and CSS variables.
- Full resource planner requirement set RP-01 through RP-11 is covered by tests or docs checks.
- Task mode remains backward compatible and resource mode remains isolated from task-only systems.
</success_criteria>

<output>
After completion, create `.planning/phases/30-resource-mode/30-04-SUMMARY.md`.
</output>
