---
phase: 30-resource-mode
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - packages/gantt-lib/src/components/ResourceTimelineChart/ResourceTimelineChart.tsx
  - packages/gantt-lib/src/components/ResourceTimelineChart/ResourceTimelineChart.css
  - packages/gantt-lib/src/components/ResourceTimelineChart/index.tsx
  - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
  - packages/gantt-lib/src/styles.css
  - packages/gantt-lib/src/__tests__/resourceTimelineChart.test.tsx
  - packages/gantt-lib/src/__tests__/resourceModeRegression.test.tsx
requirements: [RP-01, RP-02, RP-05, RP-10, RP-11]
autonomous: true
must_haves:
  truths:
    - "User can render resource planner mode through GanttChart without tasks."
    - "User can inspect resource names, item bars, empty rows, and lane-height growth."
    - "User can customize item inner content and per-item classes."
    - "Resource mode excludes task list, dependency lines, hierarchy/cascade, and reorder surfaces."
  artifacts:
    - path: "packages/gantt-lib/src/components/ResourceTimelineChart/ResourceTimelineChart.tsx"
      provides: "Read-only resource timeline renderer"
      exports: ["ResourceTimelineChart"]
    - path: "packages/gantt-lib/src/components/ResourceTimelineChart/ResourceTimelineChart.css"
      provides: "Namespaced resource timeline styling"
      contains: ".gantt-resourceTimeline"
    - path: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      provides: "Facade branch to resource renderer"
      contains: "mode === 'resource-planner'"
    - path: "packages/gantt-lib/src/__tests__/resourceTimelineChart.test.tsx"
      provides: "Read-only renderer component coverage"
  key_links:
    - from: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      to: "packages/gantt-lib/src/components/ResourceTimelineChart/ResourceTimelineChart.tsx"
      via: "resource-planner facade branch"
      pattern: "ResourceTimelineChart"
    - from: "packages/gantt-lib/src/components/ResourceTimelineChart/ResourceTimelineChart.tsx"
      to: "packages/gantt-lib/src/utils/resourceTimelineLayout.ts"
      via: "layoutResourceTimelineItems"
      pattern: "layoutResourceTimelineItems"
---

<objective>
Implement the read-only resource planner renderer and wire the `GanttChart` facade branch to it.

Purpose: Deliver the visible resource timeline while preserving task-mode behavior and keeping task-only systems out of resource mode.
Output: `ResourceTimelineChart`, namespaced CSS, facade routing, and component/regression tests.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/GANTT-LIB-RESOURCE-PLANNER-MODE-PRD.md
@.planning/phases/30-resource-mode/30-CONTEXT.md
@.planning/phases/30-resource-mode/30-RESEARCH.md
@.planning/phases/30-resource-mode/30-PATTERNS.md
@.planning/phases/30-resource-mode/30-VALIDATION.md
@.planning/phases/30-resource-mode/30-01-SUMMARY.md

<interfaces>
Plan 30-01 must provide:
```ts
layoutResourceTimelineItems(resources, options)
ResourcePlannerChartProps<TItem>
ResourceTimelineResource<TItem>
ResourceTimelineItem
```
Renderer must reuse `TimeScaleHeader`, `GridBackground`, and `TodayIndicator` where practical, and must not render `TaskList`, `TaskRow`, or `DependencyLines` in resource mode.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer renderItem/getItemClassName -> resource item shell | Consumer-supplied React content/classes render inside library geometry. |
| consumer resource data -> DOM layout | Consumer item titles, subtitles, colors, and dates affect visible rows/bars. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02-01 | Denial of Service | `ResourceTimelineChart` custom item rendering | mitigate | Render `renderItem` inside a fixed-position/fixed-height item shell; custom content must not control bar geometry or row height. |
| T-30-02-02 | Tampering | resource CSS | mitigate | Namespace all resource selectors under `.gantt-resourceTimeline*` and avoid edits to `.gantt-taskArea`, `.gantt-tr-*`, and task-list selectors. |
| T-30-02-03 | Information Disclosure | task-mode internals in resource branch | mitigate | Facade branch must return resource renderer before task-mode dependency/task-list/hierarchy rendering paths execute. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ResourceTimelineChart read-only renderer</name>
  <read_first>
    - `.planning/phases/30-resource-mode/30-PATTERNS.md` sections for `ResourceTimelineChart.tsx`, styling files, and component tests.
    - `packages/gantt-lib/src/components/GanttChart/GanttChart.tsx`
    - `packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx`
    - `packages/gantt-lib/src/components/GridBackground/GridBackground.tsx`
    - `packages/gantt-lib/src/components/TodayIndicator/TodayIndicator.tsx`
    - `packages/gantt-lib/src/utils/resourceTimelineLayout.ts`
  </read_first>
  <files>packages/gantt-lib/src/components/ResourceTimelineChart/ResourceTimelineChart.tsx, packages/gantt-lib/src/components/ResourceTimelineChart/ResourceTimelineChart.css, packages/gantt-lib/src/components/ResourceTimelineChart/index.tsx, packages/gantt-lib/src/styles.css, packages/gantt-lib/src/__tests__/resourceTimelineChart.test.tsx</files>
  <acceptance_criteria>
    - `packages/gantt-lib/src/components/ResourceTimelineChart/ResourceTimelineChart.tsx` exports `ResourceTimelineChart`.
    - `packages/gantt-lib/src/components/ResourceTimelineChart/ResourceTimelineChart.tsx` imports `layoutResourceTimelineItems`.
    - Component tests assert resource headers render from `resource.name`.
    - Component tests assert default item bars render `title`, `subtitle`, and a date label.
    - Component tests assert empty resources remain visible with one-lane height.
    - Component tests assert overlapping items increase the resource row height.
    - Component tests assert `renderItem` replaces inner content and `getItemClassName` appends a class.
    - `rg "--gantt-resource-row-header-width|--gantt-resource-lane-height|--gantt-resource-bar-radius" packages/gantt-lib/src/styles.css packages/gantt-lib/src/components/ResourceTimelineChart/ResourceTimelineChart.css` finds all required variables.
  </acceptance_criteria>
  <action>
    Create a read-only `ResourceTimelineChart` component with a left resource-name column and right calendar grid. Use the layout utility from Plan 30-01 for lane geometry and row heights. Reuse existing timeline header/grid/today primitives where the props fit. Default bars render title, optional subtitle, and a date label; `renderItem` replaces only the inner bar content. `getItemClassName` appends classes to the resource item shell. Add resource CSS variables and keep selectors namespaced under `.gantt-resourceTimeline*`.
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npm test -- src/__tests__/resourceTimelineChart.test.tsx</automated>
  </verify>
  <done>RP-02, RP-05, and RP-10 are visible in the standalone renderer with tested headers, bars, empty rows, row-height growth, and customization hooks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire GanttChart facade and task-mode exclusions</name>
  <read_first>
    - `.planning/GANTT-LIB-RESOURCE-PLANNER-MODE-PRD.md` sections 3 and 8.
    - `.planning/phases/30-resource-mode/30-PATTERNS.md` section for facade branch before task-mode state.
    - `packages/gantt-lib/src/components/GanttChart/GanttChart.tsx`
    - `packages/gantt-lib/src/components/DependencyLines/DependencyLines.tsx`
    - `packages/gantt-lib/src/components/TaskList/TaskList.tsx`
    - `packages/gantt-lib/src/__tests__/resourceModeRegression.test.tsx`
  </read_first>
  <files>packages/gantt-lib/src/components/GanttChart/GanttChart.tsx, packages/gantt-lib/src/__tests__/resourceModeRegression.test.tsx</files>
  <acceptance_criteria>
    - `GanttChart.tsx` has a branch matching `props.mode === 'resource-planner'` before task-mode destructuring of `tasks`.
    - Resource mode tests render `<GanttChart mode="resource-planner" resources={resources} />` without `tasks`.
    - Regression tests assert resource mode does not render task list selectors such as `.gantt-tl-*`.
    - Regression tests assert resource mode does not render dependency line selectors/components.
    - Existing omitted-mode task test still renders task bars.
    - `rg "mode === 'resource-planner'" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx` finds the facade branch.
  </acceptance_criteria>
  <action>
    Import and route to `ResourceTimelineChart` from `GanttChart` when `props.mode === 'resource-planner'`. The resource branch must return before task-mode normalization, dependency validation, hierarchy aggregation, task list rendering, task reorder logic, and dependency line rendering. Preserve the existing task-mode path for omitted mode and explicit `mode="gantt"`.
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npm test -- src/__tests__/resourceModeRegression.test.tsx src/__tests__/resourceTimelineChart.test.tsx</automated>
  </verify>
  <done>RP-01, RP-02, and RP-11 are enforced at the facade boundary with resource planner rendering and task-mode exclusion tests.</done>
</task>

</tasks>

<verification>
Run:

```powershell
cd packages/gantt-lib
npm test -- src/__tests__/resourceTimelineLayout.test.ts src/__tests__/resourceTimelineChart.test.tsx src/__tests__/resourceModeRegression.test.tsx
```
</verification>

<success_criteria>
- `ResourceTimelineChart` renders resource rows and item bars from real `resources`.
- `GanttChart mode="resource-planner"` uses the resource renderer without `tasks`.
- Task mode remains the default when `mode` is omitted.
- Resource mode contains no task list, dependency line, hierarchy/cascade, or reorder behavior.
- Resource CSS is namespaced and does not modify task-mode selectors.
</success_criteria>

<output>
After completion, create `.planning/phases/30-resource-mode/30-02-SUMMARY.md`.
</output>
