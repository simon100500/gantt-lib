---
phase: 30-resource-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/gantt-lib/src/types/index.ts
  - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
  - packages/gantt-lib/src/utils/resourceTimelineLayout.ts
  - packages/gantt-lib/src/__tests__/resourceTimelineLayout.test.ts
  - packages/gantt-lib/src/__tests__/resourceModeRegression.test.tsx
requirements: [RP-01, RP-02, RP-03, RP-04, RP-05, RP-11]
autonomous: true
must_haves:
  truths:
    - "Omitted mode still accepts and renders current task-mode GanttChart props."
    - "mode=\"resource-planner\" accepts resources without requiring tasks."
    - "Resource item lane layout is deterministic, inclusive-overlap aware, and invalid-date tolerant."
  artifacts:
    - path: "packages/gantt-lib/src/types/index.ts"
      provides: "Public resource timeline data contracts"
      contains: "ResourceTimelineItem"
    - path: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      provides: "Discriminated GanttChartProps contract"
      contains: "GanttChartMode"
    - path: "packages/gantt-lib/src/utils/resourceTimelineLayout.ts"
      provides: "Pure resource lane layout utility"
      exports: ["layoutResourceTimelineItems"]
    - path: "packages/gantt-lib/src/__tests__/resourceTimelineLayout.test.ts"
      provides: "Lane layout regression coverage"
    - path: "packages/gantt-lib/src/__tests__/resourceModeRegression.test.tsx"
      provides: "Task/resource mode boundary regression coverage"
  key_links:
    - from: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      to: "packages/gantt-lib/src/types/index.ts"
      via: "type imports/exports for discriminated props"
      pattern: "ResourcePlannerChartProps"
    - from: "packages/gantt-lib/src/utils/resourceTimelineLayout.ts"
      to: "packages/gantt-lib/src/utils/dateUtils.ts"
      via: "parseUTCDate with per-item error handling"
      pattern: "parseUTCDate"
---

<objective>
Create the resource planner foundation: public type contracts, discriminated `GanttChartProps`, Wave 0 tests, and the pure lane layout utility required by the renderer and drag plans.

Purpose: Lock the API and deterministic layout behavior before any renderer work depends on them.
Output: Resource public types, compatibility/regression tests, and `layoutResourceTimelineItems`.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/GANTT-LIB-RESOURCE-PLANNER-MODE-PRD.md
@.planning/phases/30-resource-mode/30-CONTEXT.md
@.planning/phases/30-resource-mode/30-RESEARCH.md
@.planning/phases/30-resource-mode/30-PATTERNS.md
@.planning/phases/30-resource-mode/30-VALIDATION.md
@.planning/phases/29-milestones-type-tasks/29-VERIFICATION.md

<interfaces>
Required contracts from PRD:
```ts
export type GanttChartMode = 'gantt' | 'resource-planner';
export interface ResourceTimelineItem {
  id: string;
  resourceId: string;
  taskId?: string;
  title: string;
  subtitle?: string;
  startDate: string | Date;
  endDate: string | Date;
  color?: string;
  locked?: boolean;
  metadata?: unknown;
}
export interface ResourceTimelineResource<TItem extends ResourceTimelineItem = ResourceTimelineItem> {
  id: string;
  name: string;
  items: TItem[];
}
export interface ResourceTimelineMove<TItem extends ResourceTimelineItem = ResourceTimelineItem> {
  item: TItem;
  itemId: string;
  fromResourceId: string;
  toResourceId: string;
  startDate: Date;
  endDate: Date;
}
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer props -> layout utility | Consumer-controlled resource items and dates enter pure layout code. |
| consumer props -> GanttChart facade | Consumer chooses gantt vs resource planner mode through public props. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01-01 | Denial of Service | `layoutResourceTimelineItems` | mitigate | Catch `parseUTCDate` failures per item, exclude invalid items from normal layout, and return diagnostics so one malformed date cannot crash the chart. |
| T-30-01-02 | Tampering | `GanttChartProps` mode split | mitigate | Use a discriminated union so resource mode cannot accidentally flow through task-only dependency/hierarchy props at compile time. |
| T-30-01-03 | Elevation of Privilege | resource assignment data | transfer | Authorization for reassignment remains consumer-owned; this plan only defines callback data contracts and does not mutate external state. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock public contracts and mode-boundary tests</name>
  <read_first>
    - `.planning/GANTT-LIB-RESOURCE-PLANNER-MODE-PRD.md` sections 3, 4, 8, 10, 11.
    - `.planning/phases/30-resource-mode/30-PATTERNS.md` sections for `GanttChart.tsx`, export/model files, and tests.
    - `packages/gantt-lib/src/types/index.ts`
    - `packages/gantt-lib/src/components/GanttChart/GanttChart.tsx`
    - `packages/gantt-lib/src/__tests__/export-contract.test.ts`
    - `packages/gantt-lib/src/__tests__/dependencyLines.test.tsx`
  </read_first>
  <files>packages/gantt-lib/src/types/index.ts, packages/gantt-lib/src/components/GanttChart/GanttChart.tsx, packages/gantt-lib/src/__tests__/resourceModeRegression.test.tsx</files>
  <acceptance_criteria>
    - `rg "export type GanttChartMode = 'gantt' \\| 'resource-planner'" packages/gantt-lib/src` finds the public mode type.
    - `rg "ResourcePlannerChartProps" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx packages/gantt-lib/src/types/index.ts` finds the resource prop contract.
    - `rg "mode\\?: 'gantt'" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx` finds explicit omitted-mode gantt compatibility.
    - `packages/gantt-lib/src/__tests__/resourceModeRegression.test.tsx` contains tests for omitted `mode`, explicit `mode="gantt"`, and `mode="resource-planner"` without `tasks`.
    - No test in this task requires passing dummy `tasks={[]}` for resource planner mode.
  </acceptance_criteria>
  <action>
    Add the public resource timeline contracts and convert `GanttChartProps` into a discriminated union per locked API decisions. Keep current `<GanttChart tasks={tasks} />` valid by making gantt mode use `mode?: 'gantt'`. Create `resourceModeRegression.test.tsx` first with failing assertions for omitted mode compatibility, explicit gantt mode compatibility, and resource mode accepting `resources` without `tasks`. Do not wire the final resource renderer in this task; a temporary compile-safe branch is acceptable only if it preserves task-mode behavior and is replaced by Plan 30-02.
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npm test -- src/__tests__/resourceModeRegression.test.tsx</automated>
    <automated>cd packages/gantt-lib && npm test -- src/__tests__/export-contract.test.ts</automated>
  </verify>
  <done>RP-01, RP-02, and RP-11 have failing-then-green regression coverage for the public mode boundary, and TypeScript exposes the locked resource contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure resource lane layout</name>
  <read_first>
    - `.planning/GANTT-LIB-RESOURCE-PLANNER-MODE-PRD.md` sections 5, 6, and 10.
    - `.planning/phases/30-resource-mode/30-PATTERNS.md` section for `resourceTimelineLayout.ts`.
    - `packages/gantt-lib/src/utils/dateUtils.ts`
    - `packages/gantt-lib/src/utils/geometry.ts`
    - `packages/gantt-lib/src/__tests__/geometry.test.ts`
  </read_first>
  <files>packages/gantt-lib/src/utils/resourceTimelineLayout.ts, packages/gantt-lib/src/__tests__/resourceTimelineLayout.test.ts</files>
  <acceptance_criteria>
    - `packages/gantt-lib/src/utils/resourceTimelineLayout.ts` exports `layoutResourceTimelineItems`.
    - The returned laid-out item model includes `laneIndex`, `left`, `width`, `resourceRowTop`, and `resourceRowHeight`.
    - Tests prove non-overlapping same-resource items use one lane.
    - Tests prove inclusive overlap where `a.endDate === b.startDate` uses multiple lanes.
    - Tests prove equal-date items sort stably by `id`.
    - Tests prove invalid dates do not throw and are reported through diagnostics.
    - `rg "core/scheduling|DependencyLines|TaskList|useTaskDrag" packages/gantt-lib/src/utils/resourceTimelineLayout.ts` returns no matches.
  </acceptance_criteria>
  <action>
    Create `layoutResourceTimelineItems` as a DOM-free utility. Use existing UTC parsing/date math conventions, catch invalid `parseUTCDate` failures per item, sort by start date, end date, then id, and place each item into the first lane whose last item ends before the next item starts. Use inclusive overlap semantics exactly: if the previous lane item end date equals the next item start date, they overlap and need different lanes. Return row models with one-lane minimum height for empty resources.
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npm test -- src/__tests__/resourceTimelineLayout.test.ts</automated>
  </verify>
  <done>RP-03, RP-04, and RP-05 are implemented in a pure tested utility, including malformed-date diagnostics for T-30-01-01.</done>
</task>

</tasks>

<verification>
Run the targeted foundation suite:

```powershell
cd packages/gantt-lib
npm test -- src/__tests__/resourceTimelineLayout.test.ts src/__tests__/resourceModeRegression.test.tsx src/__tests__/export-contract.test.ts
```
</verification>

<success_criteria>
- Resource public contracts exist and match the locked PRD names.
- `GanttChartProps` supports omitted mode, explicit gantt mode, and resource planner mode as a discriminated union.
- Layout utility is pure, deterministic, inclusive-overlap aware, and invalid-date tolerant.
- No resource foundation code imports task dependency, hierarchy, task list, or task drag modules.
</success_criteria>

<output>
After completion, create `.planning/phases/30-resource-mode/30-01-SUMMARY.md`.
</output>
