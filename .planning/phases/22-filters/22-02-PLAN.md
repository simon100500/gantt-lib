---
phase: 22-filters
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
  - packages/gantt-lib/src/index.ts
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "User can pass taskFilter prop to GanttChart component"
    - "Filtered view updates when taskFilter changes"
    - "Dependencies still work on ALL tasks (including hidden by filter)"
    - "Filter is applied AFTER collapsed parent filtering (correct order)"
    - "Users can import filters from 'gantt-lib' (public API)"
  artifacts:
    - path: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      provides: "GanttChartProps.taskFilter prop"
      contains:
        - "taskFilter?: TaskPredicate"
        - "taskFilter в filteredTasks useMemo"
        - "taskFilter в dependency array"
    - path: "packages/gantt-lib/src/index.ts"
      provides: "Public export of filters module"
      contains: "export * from './filters'"
  key_links:
    - from: "GanttChart.tsx"
      to: "filters/index.ts"
      via: "import TaskPredicate"
      pattern: "import.*TaskPredicate.*from.*filters"
    - from: "filteredTasks useMemo"
      to: "taskFilter prop"
      via: "if (taskFilter) result = result.filter(taskFilter)"
      pattern: "if.*taskFilter.*result.*filter"
    - from: "src/index.ts"
      to: "filters/index.ts"
      via: "export * from './filters'"
      pattern: "export.*from.*filters"
---

<objective>
Интегрировать фильтрацию в GanttChart и предоставить публичный API

Purpose: Позволить пользователям фильтровать задачи через проп GanttChart, сохраняя корректную работу зависимостей на всех задачах
Output: Обновленный GanttChart с taskFilter пропом и публичный экспорт из index.ts
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md

@.planning/phases/22-filters/22-CONTEXT.md
@.planning/filters-add.md
@.planning/phases/22-filters/22-01-SUMMARY.md
@packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
@packages/gantt-lib/src/index.ts
@packages/gantt-lib/src/filters/index.ts
</execution_context>

<context>
<interfaces>
From packages/gantt-lib/src/components/GanttChart/GanttChart.tsx (существующий код):
```typescript
export interface GanttChartProps {
  tasks: Task[];
  // ... другие пропсы (lines 82-133)
  customDays?: CustomDayConfig[];
  isWeekend?: (date: Date) => boolean;
}

// Существующий filteredTasks useMemo (lines 236-249):
const filteredTasks = useMemo(() => {
  const parentMap = new Map(normalizedTasks.map(t => [t.id, t.parentId]));

  function isAnyAncestorCollapsed(parentId: string | undefined): boolean {
    let current = parentId;
    while (current) {
      if (collapsedParentIds.has(current)) return true;
      current = parentMap.get(current);
    }
    return false;
  }

  return normalizedTasks.filter(task => !isAnyAncestorCollapsed(task.parentId));
}, [normalizedTasks, collapsedParentIds]);
```

From packages/gantt-lib/src/index.ts (существующий код):
```typescript
// Components
export { GanttChart, type Task, type TaskDependency, type GanttChartProps, type GanttChartHandle } from './components/GanttChart';
// ... другие экспорты

// Utils
export * from './utils';

// Types
export type {
  GanttDateRange,
  TaskBarGeometry,
  GridConfig,
  MonthSpan,
  GridLine,
  WeekendBlock,
} from './types';
```

From packages/gantt-lib/src/filters/index.ts (созданный в 22-01):
```typescript
export type TaskPredicate = (task: Task) => boolean;
export const and = (...predicates: TaskPredicate[]): TaskPredicate => ...
export const or = (...predicates: TaskPredicate[]): TaskPredicate => ...
export const not = (predicate: TaskPredicate): TaskPredicate => ...
export const withoutDeps = (): TaskPredicate => ...
export const expired = (referenceDate?: Date): TaskPredicate => ...
export const inDateRange = (start: Date, end: Date): TaskPredicate => ...
export const progressInRange = (min: number, max: number): TaskPredicate => ...
export const nameContains = (substring: string, caseSensitive?: boolean): TaskPredicate => ...
```
</interfaces>

**Решения из CONTEXT.md (non-negotiable):**
- Проп `taskFilter?: TaskPredicate` в GanttChartProps
- Обновить `filteredTasks` useMemo: применить фильтр ПОСЛЕ фильтрации по collapsed parent
- Зависимости пересчитываются на `normalizedTasks` (все задачи), а не на `filteredTasks`
- Экспорт из `src/index.ts`: `export * from './filters'`

**Ключевое требование из PRD:**
> "фильтрация должна быть визуальной — при сдвижке задачи библиотека должна пересчитывать зависимости ВСЕХ задач, включая скрытые"

**Существующий паттерн:**
- filteredTasks уже используется для collapsed parent логики
- normalizedTasks содержит ВСЕ задачи (без фильтрации)
- DependencyLines использует filteredTasks для рендеринга, но все вычисления зависимостей работают с полным списком

**Claude's discretion:**
- Точное расположение taskFilter в GanttChartProps (после isWeekend для логической группировки)
- JSDoc комментарий для taskFilter пропа
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add taskFilter prop to GanttChart and update filteredTasks logic</name>
  <files>packages/gantt-lib/src/components/GanttChart/GanttChart.tsx</files>
  <read_first>
  - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx (полный файл для понимания контекста)
  - packages/gantt-lib/src/filters/index.ts (для импорта TaskPredicate)
  </read_first>
  <action>
Внести следующие изменения в packages/gantt-lib/src/components/GanttChart/GanttChart.tsx:

1. Добавить импорт TaskPredicate в начало файла (после существующих импортов):
```typescript
import { TaskPredicate } from '../../filters';
```

2. Добавить taskFilter в GanttChartProps интерфейс (строка 133, после isWeekend):
```typescript
export interface GanttChartProps {
  /** Array of tasks to display */
  tasks: Task[];
  // ... существующие пропсы ...
  /** Optional base weekend predicate (checked before customDays overrides) */
  isWeekend?: (date: Date) => boolean;
  /**
   * Optional predicate to filter tasks for display.
   * Dependencies are still computed on ALL tasks (normalizedTasks), not just filtered.
   * Filter is applied AFTER collapsed parent filtering.
   */
  taskFilter?: TaskPredicate;
}
```

3. Добавить taskFilter в деструктуризацию пропсов (строка 195, после isWeekend):
```typescript
export const GanttChart = forwardRef<GanttChartHandle, GanttChartProps>(({
  tasks,
  // ... существующие пропсы ...
  customDays,
  isWeekend,
  taskFilter,
}, ref) => {
```

4. Обновить filteredTasks useMemo (строки 236-249) для применения taskFilter:
```typescript
// Filter tasks to hide children of collapsed parents and apply taskFilter.
// Checks the full ancestor chain so grandchildren are hidden when any ancestor is collapsed.
// Filter is applied in two stages: collapsed parent first, then taskFilter.
const filteredTasks = useMemo(() => {
  const parentMap = new Map(normalizedTasks.map(t => [t.id, t.parentId]));

  function isAnyAncestorCollapsed(parentId: string | undefined): boolean {
    let current = parentId;
    while (current) {
      if (collapsedParentIds.has(current)) return true;
      current = parentMap.get(current);
    }
    return false;
  }

  // Stage 1: Filter by collapsed parent
  let result = normalizedTasks.filter(task => !isAnyAncestorCollapsed(task.parentId));

  // Stage 2: Apply taskFilter if provided
  if (taskFilter) {
    result = result.filter(taskFilter);
  }

  return result;
}, [normalizedTasks, collapsedParentIds, taskFilter]);
```

ВАЖНО: Зависимости уже пересчитываются на normalizedTasks (строки 364-368, 443-448), никаких изменений там не требуется.

5. Обновить displayName если нужно (остается без изменений):
```typescript
GanttChart.displayName = 'GanttChart';
```
  </action>
  <verify>
```bash
# Проверка импорта TaskPredicate
grep -q "import.*TaskPredicate.*from.*filters" "D:/Projects/gantt-lib/packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"

# Проверка taskFilter в GanttChartProps
grep -q "taskFilter?: TaskPredicate" "D:/Projects/gantt-lib/packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"

# Проверка taskFilter в деструктуризации пропсов
grep -A 20 "export const GanttChart = forwardRef" "D:/Projects/gantt-lib/packages/gantt-lib/src/components/GanttChart/GanttChart.tsx" | grep -q "taskFilter"

# Проверка обновленного filteredTasks useMemo
grep -A 15 "const filteredTasks = useMemo" "D:/Projects/gantt-lib/packages/gantt-lib/src/components/GanttChart/GanttChart.tsx" | grep -q "if (taskFilter)"

# Проверка зависимости от taskFilter в useMemo
grep -A 15 "const filteredTasks = useMemo" "D:/Projects/gantt-lib/packages/gantt-lib/src/components/GanttChart/GanttChart.tsx" | grep -q "\\[normalizedTasks, collapsedParentIds, taskFilter\\]"

# Проверка что зависимости используют normalizedTasks (не filteredTasks)
grep -q "validateDependencies(tasks" "D:/Projects/gantt-lib/packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
grep -q "cascadeByLinks.*tasks" "D:/Projects/gantt-lib/packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
```
  </verify>
  <done>
GanttChart.tsx обновлен с:
- Импортом TaskPredicate из filters
- Пропом taskFilter в GanttChartProps с JSDoc
- Деструктуризацией taskFilter в пропсах компонента
- Обновленным filteredTasks useMemo (two-stage filtering)
- Зависимости от taskFilter в useMemo dependency array
  </done>
  <acceptance_criteria>
  - File imports TaskPredicate from '../../filters'
  - GanttChartProps contains "taskFilter?: TaskPredicate"
  - GanttChartProps has JSDoc comment for taskFilter mentioning dependencies use ALL tasks
  - Props destructuration includes taskFilter
  - filteredTasks useMemo contains "if (taskFilter)" check
  - filteredTasks useMemo dependency array includes taskFilter
  - filteredTasks useMemo comment mentions two-stage filtering
  - Dependencies still use normalizedTasks (grep confirms validateDependencies(tasks) not validateDependencies(filteredTasks))
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add public export from src/index.ts</name>
  <files>packages/gantt-lib/src/index.ts</files>
  <read_first>
  - packages/gantt-lib/src/index.ts (полный файл)
  - packages/gantt-lib/src/filters/index.ts (для понимания что экспортируется)
  </read_first>
  <action>
Добавить публичный экспорт фильтров в packages/gantt-lib/src/index.ts:

Найти секцию "// Utils" (примерно строка 25) и добавить после нее:

```typescript
// Filters
export * from './filters';
```

Полная структура должна выглядеть так:
```typescript
// Components
export { GanttChart, type Task, type TaskDependency, type GanttChartProps, type GanttChartHandle } from './components/GanttChart';
// ... другие компоненты

// UI Components
export { Input, type InputProps } from './components/ui/Input';
// ... другие UI компоненты

// Hooks
export { useTaskDrag } from './hooks';

// Utils
export * from './utils';

// Filters
export * from './filters';

// Types
export type {
  GanttDateRange,
  TaskBarGeometry,
  GridConfig,
  MonthSpan,
  GridLine,
  WeekendBlock,
} from './types';
```
  </action>
  <verify>
```bash
# Проверка что экспорт добавлен
grep -q "export \* from './filters'" "D:/Projects/gantt-lib/packages/gantt-lib/src/index.ts"

# Проверка что экспорт находится в правильной позиции (после utils, перед types)
grep -A 2 "// Utils" "D:/Projects/gantt-lib/packages/gantt-lib/src/index.ts" | grep -q "export \* from './utils'"
grep -B 2 "// Types" "D:/Projects/gantt-lib/packages/gantt-lib/src/index.ts" | grep -q "// Filters"
```
  </verify>
  <done>
src/index.ts обновлен с:
- Экспортом `export * from './filters'`
- Комментарием "// Filters"
- Правильной позицией (после Utils, перед Types)
  </done>
  <acceptance_criteria>
  - File contains "// Filters" comment
  - File contains "export * from './filters'" line
  - Export is positioned after "// Utils" section
  - Export is positioned before "// Types" section
  </acceptance_criteria>
</task>

</tasks>

<verification>
## Verification Steps

1. **Type check:**
```bash
cd D:/Projects/gantt-lib/packages/gantt-lib
npx tsc --noEmit
```
Expected: No type errors (TaskPredicate импортируется корректно)

2. **Build check:**
```bash
cd D:/Projects/gantt-lib/packages/gantt-lib
npm run build
```
Expected: Build succeeds, dist/index.js includes filters exports

3. **Public API check:**
```bash
node -e "
const ganttLib = require('./dist/index.js');
console.log('Available exports:', Object.keys(ganttLib).filter(k => k.includes('with') || k.includes('expired') || k === 'and'));
"
```
Expected: and, or, not, withoutDeps, expired, inDateRange, progressInRange, nameContains, TaskPredicate

4. **Manual integration test (в demo app):**
```typescript
import { GanttChart, and, withoutDeps, expired } from 'gantt-lib';

const tasks = [
  { id: '1', name: 'Task 1', startDate: '2026-01-01', endDate: '2026-01-05' },
  { id: '2', name: 'Task 2', startDate: '2026-01-06', endDate: '2026-01-10', dependencies: [{ taskId: '1', type: 'FS' }] }
];

<GanttChart
  tasks={tasks}
  taskFilter={withoutDeps()}
  showTaskList={true}
/>
```
Expected: Только Task 1 отображается (Task 2 имеет dependencies)

5. **Dependency cascade test (drag task with filter active):**
```typescript
// Task 1 (2026-01-01 to 2026-01-05) → Task 2 (2026-01-06 to 2026-01-10, FS from Task 1)
// Apply filter: withoutDeps() — только Task 1 visible
// Drag Task 1 to 2026-01-10 to 2026-01-15
// Expected: Task 2 shifts to 2026-01-16 to 2026-01-20 (cascade works on ALL tasks)
```
Expected: Зависимости пересчитываются корректно хотя Task 2 скрыт фильтром
</verification>

<success_criteria>
1. GanttChartProps включает taskFilter?: TaskPredicate
2. filteredTasks применяет фильтр после collapsed parent логики
3. Публичный экспорт из index.ts работает
4. TypeScript компилируется без ошибок
5. Dependencies пересчитываются на normalizedTasks (не filteredTasks)
</success_criteria>

<output>
After completion, create `.planning/phases/22-filters/22-02-SUMMARY.md` with:
- Integration points (GanttChart taskFilter prop, index.ts export)
- Verification that dependencies work on all tasks
- Files modified
- Usage examples for users
</output>
