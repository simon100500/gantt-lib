---
phase: 22-filters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/gantt-lib/src/filters/index.ts
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "User can import TaskPredicate type from 'gantt-lib/filters'"
    - "Boolean composites (and, or, not) work with any predicates"
    - "Ready-made filters (withoutDeps, expired, inDateRange, progressInRange, nameContains) are available"
    - "All filters use UTC-safe date handling (parseUTCDate pattern)"
    - "Filters handle edge cases (null dates, undefined progress, empty dependencies)"
  artifacts:
    - path: "packages/gantt-lib/src/filters/index.ts"
      provides: "TaskPredicate type, boolean composites, ready-made filters"
      min_lines: 150
      exports:
        - "TaskPredicate"
        - "and"
        - "or"
        - "not"
        - "withoutDeps"
        - "expired"
        - "inDateRange"
        - "progressInRange"
        - "nameContains"
  key_links:
    - from: "filters/index.ts"
      to: "types/index.ts"
      via: "import Task type"
      pattern: "import.*Task.*from.*types"
    - from: "filters/index.ts"
      to: "utils/dateUtils.ts"
      via: "import parseUTCDate"
      pattern: "import.*parseUTCDate.*from.*dateUtils"
---

<objective>
Создать модуль фильтрации с predicate-based API и готовыми утилитами

Purpose: Предоставить пользователям гибкий механизм фильтрации задач без ожидания новых релизов
Output: Модуль src/filters/index.ts с типом TaskPredicate, булевыми композитами и 5 готовыми фильтрами
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md

@.planning/phases/22-filters/22-CONTEXT.md
@.planning/filters-add.md
@packages/gantt-lib/src/types/index.ts
@packages/gantt-lib/src/utils/dateUtils.ts
</execution_context>

<context>
<interfaces>
From packages/gantt-lib/src/types/index.ts:
```typescript
export interface Task {
  id: string;
  name: string;
  startDate: string | Date;
  endDate: string | Date;
  color?: string;
  parentId?: string;
  progress?: number;
  accepted?: boolean;
  dependencies?: TaskDependency[];
  locked?: boolean;
  divider?: 'top' | 'bottom';
}

export interface TaskDependency {
  taskId: string;
  type: 'FS' | 'SS' | 'FF' | 'SF';
  lag?: number;
}
```

From packages/gantt-lib/src/utils/dateUtils.ts:
```typescript
export const parseUTCDate = (date: string | Date): Date => {
  if (typeof date === 'string') {
    const dateStr = date.includes('T') ? date : `${date}T00:00:00Z`;
    const parsed = new Date(dateStr);
    if (isNaN(parsed.getTime())) {
      throw new Error(`Invalid date string: ${date}`);
    }
    return parsed;
  }
  return date;
};
```
</interfaces>

**Решения из CONTEXT.md (non-negotiable):**
- Predicate-based подход с готовыми утилитами
- Экспорт из `gantt-lib/filters`
- Пользовательский предикат: `(task: Task) => boolean`
- 5 готовых фильтров: withoutDeps, expired, inDateRange, progressInRange, nameContains
- 3 булевых композита: and, or, not

**Паттерны из существующего кода:**
- UTC-safe дата обработка через parseUTCDate (из dateUtils.ts)
- Импорт типов из '../types'
- JSDoc комментарии для документации API

**Claude's discretion:**
- Точное расположение типов (TaskPredicate в filters/index.ts)
- Обработка edge cases в фильтрах (null dates, undefined progress, empty dependencies)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TaskPredicate type and boolean composites</name>
  <files>packages/gantt-lib/src/filters/index.ts</files>
  <read_first>
  - packages/gantt-lib/src/types/index.ts (для понимания структуры Task)
  - packages/gantt-lib/src/utils/dateUtils.ts (для паттерна parseUTCDate)
  </read_first>
  <action>
Создать файл packages/gantt-lib/src/filters/index.ts со следующим содержимым:

1. Импорты:
```typescript
import { Task } from '../types';
import { parseUTCDate } from '../utils/dateUtils';
```

2. Экспорт типа TaskPredicate:
```typescript
/**
 * Predicate function for filtering tasks
 * @param task - Task to evaluate
 * @returns true to include task in filtered results, false to exclude
 */
export type TaskPredicate = (task: Task) => boolean;
```

3. Булевы композиты (с точной сигнатурой из PRD):

```typescript
/**
 * Combine predicates with AND logic — all must be true
 * @param predicates - Array of predicate functions
 * @returns Composite predicate that returns true only if all predicates return true
 */
export const and = (...predicates: TaskPredicate[]): TaskPredicate =>
  (task) => predicates.every(p => p(task));

/**
 * Combine predicates with OR logic — at least one must be true
 * @param predicates - Array of predicate functions
 * @returns Composite predicate that returns true if any predicate returns true
 */
export const or = (...predicates: TaskPredicate[]): TaskPredicate =>
  (task) => predicates.some(p => p(task));

/**
 * Invert a predicate's logic
 * @param predicate - Predicate function to invert
 * @returns Composite predicate that returns the opposite of the input predicate
 */
export const not = (predicate: TaskPredicate): TaskPredicate =>
  (task) => !predicate(task);
```

Edge cases:
- Пустой массив в and() → возвращает предикат который всегда true (every of empty = true)
- Пустой массив в or() → возвращает предикат который всегда false (some of empty = false)
  </action>
  <verify>
```bash
# Файл существует
test -f "D:/Projects/gantt-lib/packages/gantt-lib/src/filters/index.ts"

# Проверка экспортов
grep -q "export type TaskPredicate" "D:/Projects/gantt-lib/packages/gantt-lib/src/filters/index.ts"
grep -q "export const and" "D:/Projects/gantt-lib/packages/gantt-lib/src/filters/index.ts"
grep -q "export const or" "D:/Projects/gantt-lib/packages/gantt-lib/src/filters/index.ts"
grep -q "export const not" "D:/Projects/gantt-lib/packages/gantt-lib/src/filters/index.ts"

# Проверка импортов
grep -q "import.*Task.*from.*types" "D:/Projects/gantt-lib/packages/gantt-lib/src/filters/index.ts"
grep -q "import.*parseUTCDate.*from.*dateUtils" "D:/Projects/gantt-lib/packages/gantt-lib/src/filters/index.ts"
```
  </verify>
  <done>
Файл packages/gantt-lib/src/filters/index.ts создан с:
- Типом TaskPredicate
- Композитами and, or, not
- JSDoc комментариями
- Правильными импортами
  </done>
  <acceptance_criteria>
  - File exists: packages/gantt-lib/src/filters/index.ts
  - File contains "export type TaskPredicate"
  - File contains "export const and = (...predicates"
  - File contains "export const or = (...predicates"
  - File contains "export const not = (predicate"
  - File imports Task from '../types'
  - File imports parseUTCDate from '../utils/dateUtils'
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create ready-made filters (withoutDeps, expired, inDateRange, progressInRange, nameContains)</name>
  <files>packages/gantt-lib/src/filters/index.ts</files>
  <read_first>
  - packages/gantt-lib/src/filters/index.ts (файл созданный в Task 1)
  - packages/gantt-lib/src/types/index.ts (для понимания структуры Task)
  - packages/gantt-lib/src/utils/dateUtils.ts (для parseUTCDate)
  </read_first>
  <action>
Добавить в конец файла packages/gantt-lib/src/filters/index.ts следующие 5 фильтров (с точной реализацией из PRD):

1. **withoutDeps** — задачи без dependencies:
```typescript
/**
 * Filter tasks that have no dependencies
 * @returns Predicate that returns true for tasks without dependencies array or with empty array
 */
export const withoutDeps = (): TaskPredicate =>
  (task) => !task.dependencies || task.dependencies.length === 0;
```

2. **expired** — просроченные задачи:
```typescript
/**
 * Filter expired (overdue) tasks
 * @param referenceDate - Date to compare against (default: now)
 * @returns Predicate that returns true for tasks ending before reference date
 */
export const expired = (referenceDate: Date = new Date()): TaskPredicate =>
  (task) => {
    const end = parseUTCDate(task.endDate);
    const ref = referenceDate;
    return end.getTime() < ref.getTime();
  };
```

3. **inDateRange** — задачи пересекающие диапазон:
```typescript
/**
 * Filter tasks that intersect with a date range
 * Task intersects if: taskStart <= rangeEnd && taskEnd >= rangeStart
 * @param rangeStart - Start of the date range
 * @param rangeEnd - End of the date range
 * @returns Predicate that returns true for tasks intersecting the range
 */
export const inDateRange = (rangeStart: Date, rangeEnd: Date): TaskPredicate =>
  (task) => {
    const taskStart = parseUTCDate(task.startDate);
    const taskEnd = parseUTCDate(task.endDate);
    return taskStart.getTime() <= rangeEnd.getTime() && taskEnd.getTime() >= rangeStart.getTime();
  };
```

4. **progressInRange** — задачи с прогрессом в диапазоне:
```typescript
/**
 * Filter tasks by progress value range
 * @param min - Minimum progress value (0-100)
 * @param max - Maximum progress value (0-100)
 * @returns Predicate that returns true for tasks with progress in [min, max] range
 */
export const progressInRange = (min: number, max: number): TaskPredicate =>
  (task) => {
    const progress = task.progress ?? 0;
    return progress >= min && progress <= max;
  };
```

5. **nameContains** — поиск по подстроке в названии:
```typescript
/**
 * Filter tasks by name substring search
 * @param substring - Text to search for in task name
 * @param caseSensitive - If false (default), search is case-insensitive
 * @returns Predicate that returns true for tasks with substring in name
 */
export const nameContains = (substring: string, caseSensitive = false): TaskPredicate =>
  (task) => {
    const name = task.name;
    const search = caseSensitive ? substring : substring.toLowerCase();
    const target = caseSensitive ? name : name.toLowerCase();
    return target.includes(search);
  };
```

Edge cases обработаны:
- withoutDeps: проверяет !task.dependencies (undefined) || length === 0 (empty)
- expired: использует parseUTCDate для UTC-safe обработки дат
- inDateRange: использует getTime() для корректного сравнения Date объектов
- progressInRange: использует task.progress ?? 0 для undefined значений
- nameContains: по умолчанию case-insensitive, caseSensitive параметр опциональный
  </action>
  <verify>
```bash
# Проверка всех 5 фильтров
grep -q "export const withoutDeps" "D:/Projects/gantt-lib/packages/gantt-lib/src/filters/index.ts"
grep -q "export const expired" "D:/Projects/gantt-lib/packages/gantt-lib/src/filters/index.ts"
grep -q "export const inDateRange" "D:/Projects/gantt-lib/packages/gantt-lib/src/filters/index.ts"
grep -q "export const progressInRange" "D:/Projects/gantt-lib/packages/gantt-lib/src/filters/index.ts"
grep -q "export const nameContains" "D:/Projects/gantt-lib/packages/gantt-lib/src/filters/index.ts"

# Проверка реализации edge cases
grep -q "task.progress ?? 0" "D:/Projects/gantt-lib/packages/gantt-lib/src/filters/index.ts"
grep -q "caseSensitive = false" "D:/Projects/gantt-lib/packages/gantt-lib/src/filters/index.ts"
grep -q "!task.dependencies || task.dependencies.length === 0" "D:/Projects/gantt-lib/packages/gantt-lib/src/filters/index.ts"
```
  </verify>
  <done>
Файл packages/gantt-lib/src/filters/index.ts содержит 5 готовых фильтров:
- withoutDeps — проверяет !task.dependencies || length === 0
- expired — использует parseUTCDate и getTime() для сравнения
- inDateRange — использует getTime() и формулу taskStart <= rangeEnd && taskEnd >= rangeStart
- progressInRange — использует task.progress ?? 0
- nameContains — по умолчанию case-insensitive
  </done>
  <acceptance_criteria>
  - File contains "export const withoutDeps = (): TaskPredicate"
  - File contains "export const expired = (referenceDate: Date = new Date()): TaskPredicate"
  - File contains "export const inDateRange = (rangeStart: Date, rangeEnd: Date): TaskPredicate"
  - File contains "export const progressInRange = (min: number, max: number): TaskPredicate"
  - File contains "export const nameContains = (substring: string, caseSensitive = false): TaskPredicate"
  - File contains "task.progress ?? 0"
  - File contains "!task.dependencies || task.dependencies.length === 0"
  </acceptance_criteria>
</task>

</tasks>

<verification>
## Verification Steps

1. **Type check:**
```bash
cd D:/Projects/gantt-lib/packages/gantt-lib
npx tsc --noEmit
```
Expected: No type errors

2. **Export check:**
```bash
node -e "
const filters = require('./dist/filters/index.js');
console.log('Exports:', Object.keys(filters));
"
```
Expected: TaskPredicate, and, or, not, withoutDeps, expired, inDateRange, progressInRange, nameContains

3. **Manual smoke test (в browser console):**
```typescript
import { and, withoutDeps, expired } from 'gantt-lib/filters';

const tasks = [
  { id: '1', name: 'Task 1', startDate: '2026-01-01', endDate: '2026-01-05' },
  { id: '2', name: 'Task 2', startDate: '2026-01-06', endDate: '2026-01-10', dependencies: [{ taskId: '1', type: 'FS' }] }
];

const filter = and(withoutDeps(), expired());
const filtered = tasks.filter(filter);
console.log('Filtered tasks:', filtered.length); // Expected: 0 (no tasks match both withoutDeps AND expired)
```
</verification>

<success_criteria>
1. Модуль src/filters/index.ts создан со всеми экспортами
2. TypeScript компилируется без ошибок
3. Все 8 экспортов доступны (TaskPredicate, and, or, not, 5 filters)
4. Код следует существующим паттернам (parseUTCDate, импорты)
5. Edge cases корректно обработаны (null/undefined значения)
</success_criteria>

<output>
After completion, create `.planning/phases/22-filters/22-01-SUMMARY.md` with:
- What was implemented (TaskPredicate, composites, 5 filters)
- Any deviations from PRD (none expected)
- Files modified/created
- Known issues or edge cases discovered
</output>
