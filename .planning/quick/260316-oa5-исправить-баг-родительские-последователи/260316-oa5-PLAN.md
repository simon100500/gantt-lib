---
phase: quick-260316-oa5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/gantt-lib/src/hooks/useTaskDrag.ts
  - packages/gantt-lib/src/utils/dependencyUtils.ts
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "При перемещении родительской задачи последователи, привязанные к родителю, обновляют свои позиции (не только лаг)"
    - "Каскадное обновление распространяется на последователей родителя после cascade parent sync"
    - "Лаги пересчитываются корректно для всех затронутых зависимостей"
  artifacts:
    - path: "packages/gantt-lib/src/hooks/useTaskDrag.ts"
      provides: "Логика drag с корректным обновлением последователей родителя"
      contains: "cascade parent sync с обновлением позиций последователей"
    - path: "packages/gantt-lib/src/utils/dependencyUtils.ts"
      provides: "Функция cascadeByLinks с поддержкой последователей родителя"
      contains: "обновление последователей после синхронизации родителя"
  key_links:
    - from: "useTaskDrag.ts"
      to: "dependencyUtils.ts cascadeByLinks"
      via: "вызов cascadeByLinks для родительской задачи"
      pattern: "cascadeByLinks\\(.*parent.*\\)"
    - from: "cascade parent sync"
      to: "successors of parent"
      via: "каскадное обновление после синхронизации позиции родителя"
      pattern: "getSuccessorChain.*parent"
---

<objective>
Исправить баг: при изменении детей родительской задачи последователи, привязанные к родителю, не обновляются корректно (меняется только лаг, но не позиции).

Purpose: Когда родительская задача перемещается, её дети обновляются через cascade. Затем позиция родителя синхронизируется с детьми (cascade parent sync). Однако последователи родителя (задачи, которые зависят от родителя) не обновляют свои позиции — пересчитывается только лаг. Это приводит к визуальному рассинхрону.

Output: Корректное обновление позиций последователей родителя после cascade parent sync.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Текущее поведение

При drag родительской задачи:
1. Вызывается `cascadeByLinks(parentId, newStart, newEnd, tasks)` — обновляет детей по дельте
2. В preview режиме (строки 355-400 в useTaskDrag.ts) выполняется "cascade parent sync" — родитель позиционируется по min/max детей
3. Однако последователи родителя НЕ обновляются — только пересчитывается лаг в `recalculateIncomingLags`

# Проблема

В коде useTaskDrag.ts:
- Строки 319-324: `cascadeByLinks` возвращает только детей и их последователей (dependency chain)
- Строки 355-400: cascade parent sync обновляет позицию родителя в preview, но НЕ запускает каскад для последователей родителя
- Результат: последователи родителя остаются на старых позициях, хотя лаг пересчитывается

# Решение

Нужно после обновления позиции родителя:
1. Найти всех прямых последователей родителя (задачи, у которых есть dependency на parentId)
2. Для каждого последователя пересчитать позицию через `calculateSuccessorDate` с НОВОЙ позицией родителя
3. Добавить этих последователей в `overrides` Map для preview
4. Рекурсивно обработать последователей последователей (transitive closure)

<interfaces>
<!-- Ключевые функции из dependencyUtils.ts -->

From dependencyUtils.ts (строки 247-338):
```typescript
export function cascadeByLinks(
  movedTaskId: string,
  newStart: Date,
  newEnd: Date,
  allTasks: Task[]
): Task[] {
  // Возвращает массив задач с обновлёнными датами
  // Включает: movedTask + hierarchy children + dependency successors
  // Каскад идёт от movedTask к его детям, затем к последователям
}
```

From dependencyUtils.ts (строки 119-145):
```typescript
export function calculateSuccessorDate(
  predStart: Date,
  predEnd: Date,
  linkType: LinkType,
  lag: number
): Date {
  // Вычисляет дату последователя на основе типа связи
  // FS: predEnd + 1 + lag
  // SS: predStart + lag
  // FF: predEnd + lag
  // SF: predStart - 1 + lag
}
```

From dependencyUtils.ts (строки 407-425):
```typescript
export function recalculateIncomingLags(
  task: Task,
  newStartDate: Date,
  newEndDate: Date,
  allTasks: Task[]
): NonNullable<Task['dependencies']> {
  // Пересчитывает лаг для входящих зависимостей
  // НЕ меняет позиции задач — только лаг в объектах зависимости
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Добавить каскад последователей родителя после cascade parent sync</name>
  <files>packages/gantt-lib/src/hooks/useTaskDrag.ts</files>
  <behavior>
    - После обновления позиции родителя (строки 394-399) найти всех прямых последователей родителя
    - Для каждого последователя вычислить новую позицию через calculateSuccessorDate
    - Добавить последователей в overrides Map
    - Рекурсивно обработать transitive chain (последователи последователей)
    - Учесть locked задачи — они не должны двигаться
  </behavior>
  <action>
    В файле useTaskDrag.ts после блока cascade parent sync (после строки 400):

    1. Создать функцию `cascadeSuccessorsOfParent` внутри handleGlobalMouseMove:
       - Принимает: parentId, newParentStart, newParentEnd, allTasks, dayWidth, monthStart
       - Возвращает: Map<string, {left, width}> с позициями последователей
       - Использует BFS/DFS для transitive closure

    2. После обновления родителя в overrides (строка 399):
       - Вызвать `cascadeSuccessorsOfParent(parentId, ...)`
       - Слить результат с existing overrides: `result.forEach((v, k) => overrides.set(k, v))`

    3. Логика `cascadeSuccessorsOfParent`:
       ```typescript
       function cascadeSuccessorsOfParent(
         parentId: string,
         parentStart: Date,
         parentEnd: Date,
         allTasks: Task[],
         dayWidth: number,
         monthStart: Date
       ): Map<string, {left: number, width: number}> {
         const overrides = new Map<string, {left: number, width: number}>();
         const visited = new Set<string>();
         const queue: string[] = [];

         // Найти прямых последователей parentId
         for (const task of allTasks) {
           if (task.locked) continue;
           if (visited.has(task.id)) continue;

           const depOnParent = task.dependencies?.find(d => d.taskId === parentId);
           if (!depOnParent) continue;

           // Вычислить новую позицию последователя
           const constraintDate = calculateSuccessorDate(
             parentStart,
             parentEnd,
             depOnParent.type,
             depOnParent.lag ?? 0
           );

           const origStart = new Date(task.startDate as string);
           const origEnd = new Date(task.endDate as string);
           const durationMs = origEnd.getTime() - origStart.getTime();

           let newStart: Date;
           let newEnd: Date;

           if (depOnParent.type === 'FS' || depOnParent.type === 'SS') {
             newStart = constraintDate;
             newEnd = new Date(constraintDate.getTime() + durationMs);
           } else {
             // FF or SF
             newEnd = constraintDate;
             newStart = new Date(constraintDate.getTime() - durationMs);
           }

           // Конвертировать в пиксели
           const startOffset = Math.round(
             (Date.UTC(newStart.getUTCFullYear(), newStart.getUTCMonth(), newStart.getUTCDate()) -
               Date.UTC(monthStart.getUTCFullYear(), monthStart.getUTCMonth(), monthStart.getUTCDate()))
             / (24 * 60 * 60 * 1000)
           );
           const endOffset = Math.round(
             (Date.UTC(newEnd.getUTCFullYear(), newEnd.getUTCMonth(), newEnd.getUTCDate()) -
               Date.UTC(monthStart.getUTCFullYear(), monthStart.getUTCMonth(), monthStart.getUTCDate()))
             / (24 * 60 * 60 * 1000)
           );

           overrides.set(task.id, {
             left: Math.round(startOffset * dayWidth),
             width: Math.round((endOffset - startOffset + 1) * dayWidth)
           });

           visited.add(task.id);
           queue.push(task.id);
         }

         // Transitive closure: последователи последователей
         while (queue.length > 0) {
           const currentId = queue.shift()!;
           const currentTask = allTasks.find(t => t.id === currentId);
           if (!currentTask || currentTask.locked) continue;

           const currentStart = new Date(
             overrides.has(currentId)
               ? new Date(Date.UTC(
                   monthStart.getUTCFullYear(),
                   monthStart.getUTCMonth(),
                   monthStart.getUTCDate() + Math.round(overrides.get(currentId)!.left / dayWidth)
                 ))
               : currentTask.startDate
           );
           const currentEnd = new Date(
             overrides.has(currentId)
               ? new Date(Date.UTC(
                   monthStart.getUTCFullYear(),
                   monthStart.getUTCMonth(),
                   monthStart.getUTCDate() + Math.round((overrides.get(currentId)!.left + overrides.get(currentId)!.width) / dayWidth) - 1
                 ))
               : currentTask.endDate
           );

           // Найти последователей currentId
           for (const task of allTasks) {
             if (task.locked || visited.has(task.id)) continue;

             const depOnCurrent = task.dependencies?.find(d => d.taskId === currentId);
             if (!depOnCurrent) continue;

             // Вычислить новую позицию
             const constraintDate = calculateSuccessorDate(
               currentStart,
               currentEnd,
               depOnCurrent.type,
               depOnCurrent.lag ?? 0
             );

             const origStart = new Date(task.startDate as string);
             const origEnd = new Date(task.endDate as string);
             const durationMs = origEnd.getTime() - origStart.getTime();

             let newStart: Date;
             let newEnd: Date;

             if (depOnCurrent.type === 'FS' || depOnCurrent.type === 'SS') {
               newStart = constraintDate;
               newEnd = new Date(constraintDate.getTime() + durationMs);
             } else {
               newEnd = constraintDate;
               newStart = new Date(constraintDate.getTime() - durationMs);
             }

             // Конвертировать в пиксели
             const startOffset = Math.round(
               (Date.UTC(newStart.getUTCFullYear(), newStart.getUTCMonth(), newStart.getUTCDate()) -
                 Date.UTC(monthStart.getUTCFullYear(), monthStart.getUTCMonth(), monthStart.getUTCDate()))
               / (24 * 60 * 60 * 1000)
             );
             const endOffset = Math.round(
               (Date.UTC(newEnd.getUTCFullYear(), newEnd.getUTCMonth(), newEnd.getUTCDate()) -
                 Date.UTC(monthStart.getUTCFullYear(), monthStart.getUTCMonth(), monthStart.getUTCDate()))
               / (24 * 60 * 60 * 1000)
             );

             overrides.set(task.id, {
               left: Math.round(startOffset * dayWidth),
               width: Math.round((endOffset - startOffset + 1) * dayWidth)
             });

             visited.add(task.id);
             queue.push(task.id);
           }
         }

         return overrides;
       }
       ```

    4. Вызвать эту функцию после строки 400:
       ```typescript
       // Cascade successors of the updated parent
       for (const [pid, parentOverride] of overrides) {
         if (cascadeParentIds.has(pid)) {
           const parentStart = new Date(Date.UTC(
             activeDrag.monthStart.getUTCFullYear(),
             activeDrag.monthStart.getUTCMonth(),
             activeDrag.monthStart.getUTCDate() + Math.round(parentOverride.left / activeDrag.dayWidth)
           ));
           const parentEnd = new Date(Date.UTC(
             activeDrag.monthStart.getUTCFullYear(),
             activeDrag.monthStart.getUTCMonth(),
             activeDrag.monthStart.getUTCDate() + Math.round((parentOverride.left + parentOverride.width) / activeDrag.dayWidth) - 1
           ));

           const successorOverrides = cascadeSuccessorsOfParent(
             pid,
             parentStart,
             parentEnd,
             allTasks,
             activeDrag.dayWidth,
             activeDrag.monthStart
           );

           successorOverrides.forEach((v, k) => overrides.set(k, v));
         }
       }
       ```

    ВАЖНО: Не дублировать логику — использовать `calculateSuccessorDate` из dependencyUtils.ts.
  </action>
  <verify>
    <automated>npm test -- parentMoveChildrenIntegration.test.ts</automated>
  </verify>
  <done>
    - При drag родительской задачи последователи родителя обновляют свои позиции в preview
    - После завершения drag последователи родителя сохраняют корректные позиции
    - Лаги пересчитываются корректно для всех зависимостей
    - Transitive closure работает (последователи последователей тоже обновляются)
  </done>
</task>

</tasks>

<verification>
1. Создать тестовый сценарий: родитель → ребёнок → последователь
2. Переместить родителя
3. Проверить что ребёнок сдвинулся (cascade parent)
4. Проверить что последователь сдвинулся (cascade successor of parent)
5. Проверить что лаги корректны
</verification>

<success_criteria>
- Последователи родителя обновляют позиции при drag родителя
- Transitive closure работает (последователи последователей)
- Лаги пересчитываются корректно
- Locked задачи не двигаются
- Тесты проходят
</success_criteria>

<output>
After completion, create `.planning/quick/260316-oa5-исправить-баг-родительские-последователи/260316-oa5-SUMMARY.md`
</output>
