---
phase: quick
plan: 260322-oi0
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
  - packages/gantt-lib/src/components/GanttChart/GanttChart.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "Пользователь может скрыть календарную сетку отдельно от тасклиста"
    - "При скрытом календаре тасклист остаётся видимым и функциональным"
    - "Ширина тасклиста занимает всё доступное пространство когда календарь скрыт"
  artifacts:
    - path: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      provides: "Проп showChart для управления видимостью календаря"
      exports: ["GanttChartProps.showChart"]
    - path: "packages/gantt-lib/src/components/GanttChart/GanttChart.css"
      provides: "Стили для скрытия chart area"
      contains: ".gantt-chart-hidden"
  key_links:
    - from: "GanttChartProps.showChart"
      to: "chart area div"
      via: "conditional rendering or CSS class"
      pattern: "showChart.*false"
---

<objective>
Добавить возможность скрытия календарной сетки отдельно от тасклиста

Purpose: Позволить пользователям скрывать календарную визуализацию, сохраняя доступ к списку задач для редактирования
Output: Новый проп showChart в GanttChart компоненте
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
@packages/gantt-lib/src/components/GanttChart/GanttChart.css
@packages/gantt-lib/src/components/TaskList/TaskList.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить проп showChart в GanttChart</name>
  <files>packages/gantt-lib/src/components/GanttChart/GanttChart.tsx</files>
  <action>
    1. Добавить новый проп `showChart?: boolean` в интерфейс `GanttChartProps` (по умолчанию `true`)
    2. Передать `showChart` в компонент TaskList (уже есть проп `show`, используем для тасклиста)
    3. Обернуть chart area (div с className="gantt-scrollContent" и всё содержимое кроме TaskList) в условный рендеринг или CSS-класс для скрытия

   Важное отличие: `showTaskList` управляет TaskList, `showChart` должен управлять только chart area (TimeScaleHeader, GridBackground, TaskRow и т.д.)

   Рекомендуемый подход: использовать CSS-класс аналогично `.gantt-tl-hidden`:
   - Добавить `className={showChart ? '' : 'gantt-chart-hidden'}` на chart area div
   - Chart area должен быть внутри `gantt-scrollContent` но после TaskList
  </action>
  <verify>
    <automated>npm run build --prefix packages/gantt-lib</automated>
  </verify>
  <done>
    - Проп showChart добавлен в GanttChartProps
    - Chart area скрывается когда showChart={false}
    - TaskList остаётся видимым когда showChart={false}
  </done>
</task>

<task type="auto">
  <name>Task 2: Добавить CSS стили для скрытия chart area</name>
  <files>packages/gantt-lib/src/components/GanttChart/GanttChart.css</files>
  <action>
    Добавить стили для класса `.gantt-chart-hidden`:
    - `opacity: 0`
    - `visibility: hidden`
    - `flex: 0` или `width: 0` (чтобы не занимал место)
    - `overflow: hidden`
    - `transition` для плавной анимации (аналогично `.gantt-tl-overlay`)

   Структура: chart area находится внутри `gantt-scrollContent` flex контейнера.
   Нужно чтобы TaskList занимал всё пространство когда chart скрыт.
  </action>
  <verify>
    <automated>npm run build --prefix packages/gantt-lib</automated>
  </verify>
  <done>
    - Класс .gantt-chart-hidden добавлен в GanttChart.css
    - Chart area скрывается с анимацией
    - TaskList расширяется на всю ширину когда chart скрыт
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Добавлен проп showChart для скрытия календарной сетки отдельно от тасклиста</what-built>
  <how-to-verify>
    1. Открыть демо страницу website
    2. Установить `showChart={false}` на GanttChart компоненте
    3. Проверить что:
       - Календарная сетка (Timeline, GridBackground, TaskRow) скрыта
       - TaskList остаётся видимым
       - TaskList занимает всю доступную ширину
    4. Установить `showChart={true}` и проверить что календарь снова виден
    5. Проверить комбинацию: `showTaskList={true} showChart={false}` — только тасклист
    6. Проверить комбинацию: `showTaskList={false} showChart={true}` — только календарь
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript компиляция проходит без ошибок
- npm run build успешно собирает пакет
- Визуальная проверка на демо странице confirms expected behavior
</verification>

<success_criteria>
- Проп showChart добавлен в GanttChartProps
- Chart area скрывается когда showChart={false}
- TaskList продолжает работать когда chart скрыт
- Оба пропа (showTaskList и showChart) работают независимо
</success_criteria>

<output>
After completion, create `.planning/quick/260322-oi0/260322-oi0-SUMMARY.md`
</output>
