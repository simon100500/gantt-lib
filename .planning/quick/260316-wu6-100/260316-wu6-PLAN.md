---
phase: quick
plan: 260316-wu6
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
autonomous: true
requirements:
  - QUICK-260316-wu6-100
user_setup: []
must_haves:
  truths:
    - "Родительская задача может быть отмечена как 100% только через редактирование прогресса"
    - "При установке прогресса родителя 100% все дочерние задачи автоматически становятся 100%"
    - "При установке прогресса родителя менее 100% дочерние задачи не изменяются"
    - "Редактирование прогресса для родительской задачи доступно только через прямое редактирование поля"
  artifacts:
    - path: "packages/gantt-lib/src/components/TaskList/TaskListRow.tsx"
      provides: "Логика обновления прогресса родительской задачи с каскадным обновлением детей"
      min_lines: 15
  key_links:
    - from: "TaskListRow.handleProgressSave"
      to: "allTasks (дочерние задачи)"
      via: "getChildren из dependencyUtils"
      pattern: "getChildren\\(task\\.id, allTasks\\)"
---

<objective>
Реализовать автоматическое помечание всех дочерних задач как выполненных (100%) при установке прогресса родительской задачи в 100%.

Purpose: Обеспечить логическое поведение иерархии - когда родительская задача выполнена полностью, все её подзадачи также должны быть выполнены.

Output: Модифицированный handleProgressSave в TaskListRow.tsx с каскадным обновлением дочерних задач.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
@packages/gantt-lib/src/utils/dependencyUtils.ts
@packages/gantt-lib/src/types/index.ts

# Key imports already available in TaskListRow.tsx:
- isTaskParent from dependencyUtils (line 7)
- allTasks: Task[] prop (line 269, defaulted to [])
- task: Task prop (текущая задача)

# Требуемые изменения:
- Добавить getChildren в импорт на строке 7:
  import { computeLagFromDates, isTaskParent, findParentId, getChildren } from '../../utils/dependencyUtils';
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Реализовать каскадное обновление прогресса дочерних задач при установке 100% у родителя</name>
  <files>packages/gantt-lib/src/components/TaskList/TaskListRow.tsx</files>
  <action>
    Шаг 1: Добавить импорт
    На строке 7 добавить getChildren в импорт из dependencyUtils:
    import { computeLagFromDates, isTaskParent, findParentId, getChildren } from '../../utils/dependencyUtils';

    Шаг 2: Модифицировать handleProgressSave (строки 572-580)
    Заменить текущую логику сохранения прогресса на:

    ```typescript
    const handleProgressSave = useCallback(() => {
      if (progressConfirmedRef.current) {
        progressConfirmedRef.current = false;
        return;
      }
      const clampedValue = Math.max(0, Math.min(100, progressValue));

      // Cascade 100% progress to all children when parent is marked complete
      if (clampedValue === 100 && isTaskParent(task.id, allTasks)) {
        const children = getChildren(task.id, allTasks);
        const updatedTasks = [
          { ...task, progress: 100 },
          ...children.map(child => ({ ...child, progress: 100 }))
        ];
        onTasksChange?.(updatedTasks);
      } else {
        onTasksChange?.([{ ...task, progress: clampedValue }]);
      }
      setEditingProgress(false);
    }, [progressValue, task, onTasksChange, allTasks]);
    ```

    Шаг 3: Модифицировать handleProgressKeyDown (строки 590-600)
    Применить аналогичную логику для сохранения по Enter:

    ```typescript
    const handleProgressKeyDown = useCallback((e: React.KeyboardEvent<HTMLInputElement>) => {
      e.stopPropagation();
      if (e.key === 'Enter') {
        progressConfirmedRef.current = true;
        const clampedValue = Math.max(0, Math.min(100, progressValue));

        // Cascade 100% progress to all children when parent is marked complete
        if (clampedValue === 100 && isTaskParent(task.id, allTasks)) {
          const children = getChildren(task.id, allTasks);
          const updatedTasks = [
            { ...task, progress: 100 },
            ...children.map(child => ({ ...child, progress: 100 }))
          ];
          onTasksChange?.(updatedTasks);
        } else {
          onTasksChange?.([{ ...task, progress: clampedValue }]);
        }
        setEditingProgress(false);
      } else if (e.key === 'Escape') {
        handleProgressCancel();
      }
    }, [progressValue, task, onTasksChange, handleProgressCancel, allTasks]);
    ```

    ВАЖНО:
    - Добавить allTasks в массив зависимостей useCallback для обеих функций
    - Не менять логику для clampedValue < 100 (дочерние задачи не затрагиваются)
    - Не менять логику для задач без дочерних элементов
  </action>
  <verify>
    <automated>
      npm test -- --testNamePattern="parent.*progress|progress.*parent" --passWithNoTests 2>/dev/null || echo "Тесты не найдены, проверяется вручную"
    </automated>
  </verify>
  <done>
    - При установке прогресса родительской задачи в 100%, все её дочерние задачи также становятся 100%
    - При установке прогресса родительской задачи в любое другое значение (< 100), дочерние задачи не изменяются
    - Для задач без дочерних элементов поведение не изменилось
    - Существующие функциональные тесты (если есть) продолжают проходить
  </done>
</task>

</tasks>

<verification>
1. Создать тестовые данные: родительская задача с 2-3 дочерними задачами с различным прогрессом (0%, 50%, 75%)
2. Установить прогресс родителя в 100%
3. Проверить: все дочерние задачи теперь имеют progress: 100
4. Установить прогресс родителя в 50%
5. Проверить: дочерние задачи остались 100% (не изменились)
6. Для обычной задачи (без детей) установить прогресс в произвольное значение
7. Проверить: поведение как раньше (только текущая задача обновляется)
</verification>

<success_criteria>
- [ ] Родительская задача с progress=100 автоматически помечает всех детей как progress=100
- [ ] Родительская задача с progress<100 не влияет на прогресс детей
- [ ] Обычные задачи (без parentId у детей) работают как раньше
- [ ] Код не ломает существующие тесты (если есть)
</success_criteria>

<output>
After completion, create `.planning/quick/260316-wu6-100/260316-wu6-SUMMARY.md`
</output>
