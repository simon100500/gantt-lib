# Quick Task 78: Exit link view mode on second chip click

## Task Description
When clicking on a dependency chip, we enter "view current connection" mode (shows popover with connection details). Need to add an exit from this mode when clicking the same chip again.

## Plan
1. Add `onOpenChange` handler to the Popover component in DepChip
2. When popover closes (open=false), clear the selected chip state
3. This allows toggling off the connection view by clicking the same chip again

## Files to Modify
- `packages/gantt-lib/src/components/TaskList/TaskListRow.tsx` - DepChip component

## Execution
- Added `handleOpenChange` callback that calls `onChipSelect?.(null)` when popover closes
- Updated Popover to use `handleOpenChange` instead of empty handler
- Build verified successfully

## Done
- [x] Implement toggle-off behavior for dependency chip
- [x] Build passes
- [ ] Commit changes
