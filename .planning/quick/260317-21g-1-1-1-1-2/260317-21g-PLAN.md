---
phase: quick-260317-21g-1-1-1-1-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/gantt-lib/src/components/TaskList/TaskList.tsx
  - packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
  - packages/gantt-lib/src/components/TaskList/TaskList.css
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Номера задач отображаются с иерархией: 1, 1.1, 1.2, 2, 2.1, 2.1.1 и т.д."
    - "Номера выровнены по левому краю ячейки"
    - "Нумерация обновляется при изменении структуры (drag-and-drop, promote/demote)"
    - "Дочерние задачи получают номера вида: <родительский номер>.<порядковый номер среди детей>"
  artifacts:
    - path: "packages/gantt-lib/src/components/TaskList/TaskList.tsx"
      provides: "Вычисление иерархических номеров для visibleTasks"
      exports: ["getTaskNumber"]
    - path: "packages/gantt-lib/src/components/TaskList/TaskListRow.tsx"
      provides: "Отображение иерархического номера в ячейке №"
    - path: "packages/gantt-lib/src/components/TaskList/TaskList.css"
      provides: "Стили для выравнивания номера по левому краю"
  key_links:
    - from: "TaskList.tsx"
      to: "TaskListRow.tsx"
      via: "prop taskNumber"
      pattern: "taskNumber={getTaskNumber.*}"
    - from: "TaskListRow.tsx"
      to: "DOM"
      via: "рендер в ячейке .gantt-tl-cell-number"
      pattern: "gantt-tl-num-label.*taskNumber"
---

<objective>
Добавить иерархическую нумерацию задач с выравниванием по левому краю.

**Что делаем:**
- Заменяем плоскую нумерацию (1, 2, 3...) на иерархическую (1, 1.1, 1.2, 2, 2.1, 2.1.1...)
- Выравниваем номера по левому краю ячейки (текущее выравнивание по центру)

**Почему это важно:**
- Иерархическая нумерация улучшает читаемость структуры задач
- Левое выравнивание обеспечивает визуальную связь с иерархией

**Результат:**
- Функция `getTaskNumber()` вычисляет иерархический номер на основе parentId
- Компонент TaskListRow отображает номер с левым выравниванием
- CSS обновлен для text-align: left
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Текущая реализация

Из TaskListRow.tsx (строка 911):
```tsx
<span className="gantt-tl-num-label">{rowIndex + 1}</span>
```

Из TaskList.css (строки 190-200):
```css
.gantt-tl-cell-number {
  width: 32px;
  display: flex;
  align-items: center;
  justify-content: center;  /* ТЕКУЩЕЕ: центрирование */
  gap: 0;
  padding: 0 2px;
  flex-shrink: 0;
  cursor: pointer;
  position: relative;
}
```

# Структура данных

Задачи имеют поле `parentId`:
- `undefined` или отсутствует → корневая задача
- строка с ID родителя → дочерняя задача

`visibleTasks` в TaskList.tsx уже отсортированы в иерархическом порядке благодаря `normalizeHierarchyTasks()`.

# Стратегия реализации

1. **Вычисление иерархических номеров** в TaskList.tsx:
   - Создать Map для хранения индексов детей по родителям
   - Для каждой задачи в visibleTasks:
     - Если нет parentId → корневая нумерация (1, 2, 3...)
     - Если есть parentId → найти родительский номер + добавить ".{среди детей}"
   - Передавать номер в TaskListRow через проп

2. **Отображение в TaskListRow**:
   - Заменить `{rowIndex + 1}` на `{taskNumber}`
   - Добавить проп `taskNumber?: string` в интерфейс

3. **CSS обновление**:
   - Заменить `justify-content: center` на `justify-content: flex-start`
   - Увеличить ширину ячейки при необходимости (текущие 32px могут быть малы для "1.1.1")
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить функцию вычисления иерархических номеров</name>
  <files>packages/gantt-lib/src/components/TaskList/TaskList.tsx</files>
  <action>
1. После блока imports (примерно строка 20) добавить функцию:

```tsx
/**
 * Вычисляет иерархический номер задачи на основе позиции в списке visibleTasks.
 * Корневые задачи: 1, 2, 3...
 * Дочерние задачи: 1.1, 1.2, 2.1, 2.1.1 и т.д.
 *
 * @param tasks - Массив видимых задач (уже отсортированных в иерархическом порядке)
 * @param taskIndex - Индекс задачи в массиве visibleTasks
 * @returns Иерархический номер в виде строки
 */
function getTaskNumber(tasks: Task[], taskIndex: number): string {
  const task = tasks[taskIndex];
  if (!task) return '';

  // Если это корневая задача (нет parentId)
  if (!task.parentId) {
    // Найти порядковый номер среди корневых задач
    let rootIndex = 0;
    for (let i = 0; i < taskIndex; i++) {
      if (!tasks[i].parentId) {
        rootIndex++;
      }
    }
    return String(rootIndex + 1);
  }

  // Для дочерней задачи - найти родительский номер
  const parentIndex = tasks.findIndex(t => t.id === task.parentId);
  if (parentIndex === -1) {
    // Родитель не найден - fallback на плоский номер
    return String(taskIndex + 1);
  }

  const parentNumber = getTaskNumber(tasks, parentIndex);

  // Найти порядковый номер среди детей этого родителя
  let siblingIndex = 0;
  for (let i = 0; i < taskIndex; i++) {
    if (tasks[i].parentId === task.parentId) {
      siblingIndex++;
    }
  }

  return `${parentNumber}.${siblingIndex + 1}`;
}
```

2. В блоке render где создаются TaskListRow (строка ~633), добавить проп:
   Найти `<TaskListRow` и добавить после `rowIndex={rowIndex}`:
   ```tsx
   taskNumber={getTaskNumber(visibleTasks, index)}
   ```
</action>
  <verify>
    <automated>grep -n "getTaskNumber" packages/gantt-lib/src/components/TaskList/TaskList.tsx</automated>
  </verify>
  <done>Функция getTaskNumber добавлена и передается в TaskListRow</done>
</task>

<task type="auto">
  <name>Task 2: Обновить TaskListRow для отображения иерархического номера</name>
  <files>packages/gantt-lib/src/components/TaskList/TaskListRow.tsx</files>
  <action>
1. В интерфейсе TaskListRowProps (примерно строка 100-150) добавить новое поле:
   Найти блок интерфейса и добавить после `rowIndex: number;`:
   ```tsx
   /** Иерархический номер задачи (например, "1.2.3") */
   taskNumber?: string;
   ```

2. В деструктуризации props (примерно строка 200-250) добавить:
   Найти строку `const { task, rowIndex, ... } = props;` и добавить `taskNumber`:
   ```tsx
   const {
     task,
     rowIndex,
     taskNumber,
     ... // остальные props
   } = props;
   ```

3. Заменить отображение номера (строка ~911):
   Найти:
   ```tsx
   <span className="gantt-tl-num-label">{rowIndex + 1}</span>
   ```
   Заменить на:
   ```tsx
   <span className="gantt-tl-num-label">{taskNumber || rowIndex + 1}</span>
   ```
</action>
  <verify>
    <automated>grep -n "taskNumber" packages/gantt-lib/src/components/TaskList/TaskListRow.tsx</automated>
  </verify>
  <done>TaskListRow отображает иерархический номер вместо плоского индекса</done>
</task>

<task type="auto">
  <name>Task 3: Обновить CSS для выравнивания по левому краю</name>
  <files>packages/gantt-lib/src/components/TaskList/TaskList.css</files>
  <action>
В блоке `.gantt-tl-cell-number` (строки 190-200):

1. Заменить выравнивание:
   Найти:
   ```css
   justify-content: center;
   ```
   Заменить на:
   ```css
   justify-content: flex-start;
   ```

2. Увеличить ширину ячейки для длинных номеров (опционально, если "1.1.1" не помещается):
   Найти:
   ```css
   width: 32px;
   ```
   Заменить на:
   ```css
   width: 48px;
   ```

3. Обновить padding для лучшего визуального баланса:
   Найти:
   ```css
   padding: 0 2px;
   ```
   Заменить на:
   ```css
   padding: 0 4px;
   ```
</action>
  <verify>
    <automated>grep -A2 "\.gantt-tl-cell-number" packages/gantt-lib/src/components/TaskList/TaskList.css | grep "justify-content: flex-start"</automated>
  </verify>
  <done>Номера задач выровнены по левому краю, ширина ячейки увеличена</done>
</task>

</tasks>

<verification>
1. Проверить, что номера отображаются в иерархическом формате (1, 1.1, 1.2, 2, 2.1...)
2. Проверить, что номера выровнены по левому краю ячейки
3. Проверить, что нумерация обновляется при изменении структуры (перетаскивание, promote/demote)
4. Проверить, что номера не обрезаются при длинной иерархии (1.1.1.1)
</verification>

<success_criteria>
- Иерархическая нумерация работает для вложенных задач любого уровня
- Номера выровнены по левому краю
- Ширина ячейки достаточна для отображения номеров вида "1.1.1"
- Нумерация автоматически обновляется при изменении структуры задач
</success_criteria>

<output>
After completion, create `.planning/quick/260317-21g-1-1-1-1-2/260317-21g-SUMMARY.md`
</output>
