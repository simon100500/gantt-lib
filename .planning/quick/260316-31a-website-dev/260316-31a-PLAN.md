---
phase: quick
plan: 260316-31a
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/gantt-lib/src/utils/hierarchyOrder.ts
  - packages/gantt-lib/src/utils/taskListReorder.ts
  - packages/website/src/app/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Console browser contains no debug logs when working with Gantt chart"
    - "No console.log messages when dragging tasks"
    - "No debug messages when reordering hierarchy"
  artifacts:
    - path: "packages/gantt-lib/src/utils/hierarchyOrder.ts"
      provides: "flattenHierarchy and normalizeHierarchyTasks utilities without logs"
      contains: "export function flattenHierarchy"
    - path: "packages/gantt-lib/src/utils/taskListReorder.ts"
      provides: "getVisibleReorderPosition utility without logs"
      contains: "export function getVisibleReorderPosition"
    - path: "packages/website/src/app/page.tsx"
      provides: "Main page without logs in handleReorder"
      contains: "const handleReorder = useCallback"
  key_links:
    - from: "packages/gantt-lib/src/utils/hierarchyOrder.ts"
      to: "console"
      via: "remove all console.log calls"
      pattern: "console\\.log"
    - from: "packages/gantt-lib/src/utils/taskListReorder.ts"
      to: "console"
      via: "remove all console.log calls"
      pattern: "console\\.log"
    - from: "packages/website/src/app/page.tsx"
      to: "console"
      via: "remove console.log from handleReorder"
      pattern: "console\\.log"
---

<objective>
Удалить лишние отладочные логи из консоли при запуске website:dev

Цель: Очистить консоль браузера от отладочных сообщений, которые выводятся при работе с графикой Gantt

Результат:
- Удалены все console.log из иерархических утилит gantt-lib
- Удалены console.log из утилит переупорядочивания
- Удалены console.log из обработчиков страницы website

Вывод: Чистая консоль без отладочных сообщений
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Удалить console.log из иерархических утилит gantt-lib</name>
  <files>packages/gantt-lib/src/utils/hierarchyOrder.ts</files>
  <action>
    Удалить все отладочные console.log statements из файла packages/gantt-lib/src/utils/hierarchyOrder.ts:

    1. Удалить console.log из функции flattenHierarchy (строки 11-64):
       - Удалить открывающий лог '=== FLATTEN HIERARCHY START ==='
       - Удалить лог входных данных '[INPUT]'
       - Удалить лог карты по родителям '[BY PARENT MAP]'
       - Удалить логи обхода '[WALK]'
       - Удалить лог выходных данных '[OUTPUT]'
       - Удалить закрывающий лог '=== FLATTEN HIERARCHY END ==='

    2. Удалить console.log из функции normalizeHierarchyTasks (строки 76-124):
       - Удалить открывающий лог '=== NORMALIZE HIERARCHY TASKS START ==='
       - Удалить лог входных задач '[INPUT TASKS]'
       - Удалить лог после flattenHierarchy '[AFTER FLATTEN HIERARCHY]'
       - Удалить логи нормализации родителя '[NORMALIZE PARENT]'
       - Удалить лог финального вывода '[FINAL OUTPUT]'
       - Удалить закрывающий лог '=== NORMALIZE HIERARCHY TASKS END ==='

    Сохранить всю логику функций, удалить только отладочные логи.
  </action>
  <verify>
    <automated>grep -n "console\.log" packages/gantt-lib/src/utils/hierarchyOrder.ts</automated>
  </verify>
  <done>В файле hierarchyOrder.ts не осталось console.log вызовов</done>
</task>

<task type="auto">
  <name>Задача 2: Удалить console.log из утилит переупорядочивания</name>
  <files>packages/gantt-lib/src/utils/taskListReorder.ts</files>
  <action>
    Удалить все отладочные console.log statements из файла packages/gantt-lib/src/utils/taskListReorder.ts:

    Удалить console.log из функции getVisibleReorderPosition (строки 49-149):
    - Удалить открывающий лог '=== getVisibleReorderPosition START ==='
    - Удалить лог входных данных '[INPUT]'
    - Удалить лог ошибки '[ERROR] Moved task not found'
    - Удалить лог перемещенного поддерева '[MOVED SUBTREE]'
    - Удалить лог отфильтрованных массивов '[FILTERED ARRAYS]'
    - Удалить лог направления '[DIRECTION]'
    - Удалить логи граничных случаев '[EDGE CASE]'
    - Удалить лог целевой видимой задачи '[TARGET VISIBLE TASK]'
    - Удалить лог финального результата '[FINAL RESULT]'
    - Удалить закрывающий лог '=== getVisibleReorderPosition END ==='

    Сохранить всю логику функции, удалить только отладочные логи.
  </action>
  <verify>
    <automated>grep -n "console\.log" packages/gantt-lib/src/utils/taskListReorder.ts</automated>
  </verify>
  <done>В файле taskListReorder.ts не осталось console.log вызовов</done>
</task>

<task type="auto">
  <name>Задача 3: Удалить console.log из обработчиков website</name>
  <files>packages/website/src/app/page.tsx</files>
  <action>
    Удалить отладочные console.log из файла packages/website/src/app/page.tsx:

    1. Удалить console.log из функции handleReorder (строки 813-826):
       - Удалить '=== PAGE handleReorder START ==='
       - Удалить '[INPUT]' лог с movedTaskId, inferredParentId и reorderedTasks
       - Удалить '=== PAGE handleReorder END ==='

    2. Удалить console.log из onValidateDependencies callback (строка 1156):
       - Удалить лог 'Dependency validation:' при наличии ошибок

    3. Удалить console.log из onCascade callbacks (строки 1183, 1212):
       - Удалить лог 'Cascade:' со списком смещенных задач
       - Удалить лог 'Cascade: X tasks shifted'

    Сохранить всю логику обработчиков, удалить только отладочные логи.
  </action>
  <verify>
    <automated>grep -n "console\.log" packages/website/src/app/page.tsx</automated>
  </verify>
  <done>В файле page.tsx не осталось console.log вызовов (остаются только в тестах)</done>
</task>

</tasks>

<verification>
Проверить после выполнения:
1. Запустить website:dev и проверить консоль браузера - не должно быть отладочных сообщений
2. Выполнить действия с графикой (перетаскивание, сворачивание/разворачивание) - консоль остается чистой
3. Убедиться что функциональность не нарушена
</verification>

<success_criteria>
- В иерархических утилитах gantt-lib нет console.log
- В утилитах переупорядочивания нет console.log
- В обработчиках website нет console.log
- Функциональность графика не нарушена
- Консоль браузера чистая при работе с приложением
</success_criteria>

<output>
После выполнения создать `.planning/quick/260316-31a-website-dev/260316-31a-SUMMARY.md`
</output>
