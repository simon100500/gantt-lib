---
phase: quick-260316-n70
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
autonomous: true
requirements: [N70]

must_haves:
  truths:
    - "При наведении на чип связи в TaskListRow показывается нативный tooltip"
    - "Порядок в tooltip: действие — предшественник (например 'FS — Задача А')"
  artifacts:
    - path: "packages/gantt-lib/src/components/TaskList/TaskListRow.tsx"
      provides: "title attribute на span.gantt-tl-dep-chip"
      contains: "title="
  key_links:
    - from: "span.gantt-tl-dep-chip"
      to: "title attribute"
      via: "JSX prop"
      pattern: "title=\\{"
---

<objective>
Cherry-pick двух коммитов из ветки `tooltips` в `master`: добавление нативного tooltip на чип связи в TaskListRow.

Purpose: Перенести готовую фичу (tooltip на чипе связи) из ветки tooltips без слияния всей ветки.
Output: TaskListRow.tsx с title attribute на span.gantt-tl-dep-chip.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cherry-pick коммитов из ветки tooltips</name>
  <files>packages/gantt-lib/src/components/TaskList/TaskListRow.tsx</files>
  <action>
    Выполнить cherry-pick двух коммитов строго в указанном порядке:

    ```
    git cherry-pick 73b8900 365455e
    ```

    Первый коммит (73b8900) добавляет title attribute к span.gantt-tl-dep-chip.
    Второй коммит (365455e) исправляет порядок на `{depPrefix} — {depName}`.

    Если cherry-pick пройдёт без конфликтов — коммиты уже в истории, дополнительный squash не нужен (два коммита — нормально).

    После этого убедиться что в файле появился title attribute:
    ```
    grep -n "title=" packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
    ```
  </action>
  <verify>
    <automated>grep -n "title=" D:/Projects/gantt-lib/packages/gantt-lib/src/components/TaskList/TaskListRow.tsx</automated>
  </verify>
  <done>В TaskListRow.tsx присутствует title attribute на чипе связи. git log показывает оба cherry-pick коммита в master.</done>
</task>

</tasks>

<verification>
```bash
# Проверить наличие title в файле
grep -n "title=" packages/gantt-lib/src/components/TaskList/TaskListRow.tsx

# Проверить что коммиты появились в master
git log --oneline -5
```
</verification>

<success_criteria>
- git cherry-pick 73b8900 365455e выполнен без конфликтов
- TaskListRow.tsx содержит title={...} на span.gantt-tl-dep-chip
- Порядок в tooltip: "{depPrefix} — {depName}" (действие — предшественник)
</success_criteria>

<output>
После завершения создать `.planning/quick/260316-n70-tooltip-cherry-pick-tooltips/260316-n70-SUMMARY.md`
</output>
