---
phase: quick-260316-n70
plan: 01
subsystem: TaskList
tags: [tooltip, cherry-pick, dependency-chip]
key-files:
  modified:
    - packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
decisions:
  - Cherry-pick вместо merge — перенесены только нужные коммиты без истории всей ветки tooltips
metrics:
  duration: "< 5 minutes"
  completed: "2026-03-16"
  tasks: 1
  files: 1
---

# Quick Task 260316-n70: Cherry-pick tooltips — Summary

**One-liner:** Перенесены два коммита из ветки tooltips, добавляющие нативный title-tooltip `"{depPrefix} — {depName}"` на span.gantt-tl-dep-chip в TaskListRow.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Cherry-pick коммитов из ветки tooltips | 92dc439, 8c88d6a | TaskListRow.tsx |

## What Was Done

Выполнен cherry-pick двух коммитов из ветки `tooltips` в `master`:

1. **92dc439** `feat(quick-260316-gnp): add native tooltip to dependency chip in TaskListRow`
   Добавлен `title` attribute на `span.gantt-tl-dep-chip`.

2. **8c88d6a** `fix: swap tooltip order to action — predecessor in dep chip`
   Исправлен порядок в tooltip на `${depPrefix} — ${depName}` (действие — предшественник).

Итоговый результат в `TaskListRow.tsx` (строка 233):
```tsx
title={depPrefix ? `${depPrefix} — ${depName}` : depName}
```

## Verification

```
grep -n "title=" packages/gantt-lib/src/components/TaskList/TaskListRow.tsx
# 233: title={depPrefix ? `${depPrefix} — ${depName}` : depName}
```

git log --oneline -3:
```
8c88d6a fix: swap tooltip order to action — predecessor in dep chip
92dc439 feat(quick-260316-gnp): add native tooltip to dependency chip in TaskListRow
04ec952 style: update row hover background color in Gantt chart
```

## Deviations from Plan

None - план выполнен точно как написан. Cherry-pick прошёл без конфликтов.

## Self-Check: PASSED

- [x] `packages/gantt-lib/src/components/TaskList/TaskListRow.tsx` содержит `title=` на строке 233
- [x] Коммит 92dc439 присутствует в master
- [x] Коммит 8c88d6a присутствует в master
- [x] Формат tooltip: `${depPrefix} — ${depName}` (действие — предшественник)
