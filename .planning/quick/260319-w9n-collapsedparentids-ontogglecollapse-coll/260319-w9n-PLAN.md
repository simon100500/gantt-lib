---
phase: quick
plan: 260319-w9n
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Пользователь может передать collapsedParentIds для control режима"
    - "Пользователь может передать onToggleCollapse для обработки изменений"
    - "При отсутствии props компонент работает в uncontrolled режиме (внутренний стейт)"
  artifacts:
    - path: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      provides: "GanttChartProps с collapsedParentIds и onToggleCollapse"
      contains: "collapsedParentIds?: Set<string>"
      contains: "onToggleCollapse?: (parentId: string) => void"
  key_links:
    - from: "GanttChartProps"
      to: "TaskList props"
      via: "проброс collapsedParentIds и onToggleCollapse"
      pattern: "collapsedParentIds=\\{collapsedParentIds\\}"
---

<objective>
Добавить пропсы `collapsedParentIds` и `onToggleCollapse` в `GanttChartProps` для управления состоянием collapse/expand родительских задач извне.

Purpose: Позволить потребителю компонента контролировать состояние сворачивания задач извне (controlled/uncontrolled pattern).
Output: GanttChartProps расширен новыми опциональными пропсами
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Текущее состояние

В GanttChart.tsx (строки 213-214):
```typescript
const [collapsedParentIds, setCollapsedParentIds] = useState<Set<string>>(new Set());
```

В GanttChart.tsx (строки 575-585):
```typescript
const handleToggleCollapse = useCallback((parentId: string) => {
  setCollapsedParentIds(prev => {
    const next = new Set(prev);
    if (next.has(parentId)) {
      next.delete(parentId);
    } else {
      next.add(parentId);
    }
    return next;
  });
}, []);
```

В TaskList.tsx (строки 133-136):
```typescript
/** Set of collapsed parent task IDs */
collapsedParentIds?: Set<string>;
/** Callback when collapse/expand button is clicked */
onToggleCollapse?: (parentId: string) => void;
```

TaskList уже поддерживает controlled/uncontrolled pattern (строки 183-200):
```typescript
const [internalCollapsedParentIds, setInternalCollapsedParentIds] = useState<Set<string>>(new Set());
const collapsedParentIds = externalCollapsedParentIds ?? internalCollapsedParentIds;
const handleToggleCollapse = externalOnToggleCollapse ?? useCallback((parentId: string) => {
  setInternalCollapsedParentIds(prev => { /* ... */ });
}, []);
```

Проброс в GanttChart.tsx (строки 819-820):
```typescript
collapsedParentIds={collapsedParentIds}
onToggleCollapse={handleToggleCollapse}
```

# Что нужно сделать

1. Добавить `collapsedParentIds?: Set<string>` в `GanttChartProps`
2. Добавить `onToggleCollapse?: (parentId: string) => void` в `GanttChartProps`
3. В компоненте GanttChart использовать props если предоставлены, иначе внутренний стейт
4. Пробросить пропсы в TaskList
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить пропсы collapsedParentIds и onToggleCollapse в GanttChartProps</name>
  <files>packages/gantt-lib/src/components/GanttChart/GanttChart.tsx</files>
  <action>
1. В интерфейсе GanttChartProps (после строки 139) добавить:
```typescript
/** Set of collapsed parent task IDs for controlled mode */
collapsedParentIds?: Set<string>;
/** Callback when collapse/expand button is clicked (controlled mode) */
onToggleCollapse?: (parentId: string) => void;
```

2. В компоненте GanttChart (строки 176-203) добавить деструктуризацию новых пропсов:
```typescript
collapsedParentIds: externalCollapsedParentIds,
onToggleCollapse: externalOnToggleCollapse,
```

3. Заменить строку 214 на conditional internal state:
```typescript
// Hierarchy state: collapsed parent IDs (uncontrolled mode - internal state)
const [internalCollapsedParentIds, setInternalCollapsedParentIds] = useState<Set<string>>(new Set());

// Use external collapsedParentIds if provided (controlled mode), otherwise use internal state
const collapsedParentIds = externalCollapsedParentIds ?? internalCollapsedParentIds;
```

4. Заменить handleToggleCollapse (строки 575-585) на conditional handler:
```typescript
// Use external onToggleCollapse if provided (controlled mode), otherwise use internal handler
const handleToggleCollapse = externalOnToggleCollapse ?? useCallback((parentId: string) => {
  setInternalCollapsedParentIds(prev => {
    const next = new Set(prev);
    if (next.has(parentId)) {
      next.delete(parentId);
    } else {
      next.add(parentId);
    }
    return next;
  });
}, []);
```

5. Обновить useEffect для handleCollapseAll/handleExpandAll (строки 592-598) чтобы они использовали internal state только в uncontrolled режиме:
```typescript
const handleCollapseAll = useCallback(() => {
  if (externalCollapsedParentIds) return; // Don't modify external state
  setInternalCollapsedParentIds(allParentIds);
}, [allParentIds, externalCollapsedParentIds]);

const handleExpandAll = useCallback(() => {
  if (externalCollapsedParentIds) return; // Don't modify external state
  setInternalCollapsedParentIds(new Set());
}, [externalCollapsedParentIds]);
```
  </action>
  <verify>
    <automated>cd packages/gantt-lib && npm run build --silent 2>&1 | head -20</automated>
  </verify>
  <done>
    - GanttChartProps содержит collapsedParentIds и onToggleCollapse
    - Компонент поддерживает controlled/uncontrolled режимы
    - TypeScript компилируется без ошибок
  </done>
</task>

</tasks>

<verification>
Проверить:
1. TypeScript компилируется без ошибок
2. Экспортируемые типы включают новые пропсы
3. В uncontrolled режиме (без props) компонент работает как раньше
4. В controlled режиме (с props) стейт управляется извне
</verification>

<success_criteria>
- [ ] GanttChartProps расширен новыми опциональными пропсами
- [ ] Компонент поддерживает controlled/uncontrolled pattern
- [ ] TypeScript компилируется без ошибок
- [ ] Обратная совместимость сохранена (старый код работает)
</success_criteria>

<output>
После completion создать `.planning/quick/260319-w9n-collapsedparentids-ontogglecollapse-coll/260319-w9n-SUMMARY.md`
</output>
