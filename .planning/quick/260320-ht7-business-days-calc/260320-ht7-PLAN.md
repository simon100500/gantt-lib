---
phase: 24-buisiness-days
plan: ht7-business-days-calc
type: execute
wave: 3
depends_on: ["24-01", "24-02"]
files_modified:
  - packages/gantt-lib/src/utils/dependencyUtils.ts
  - packages/gantt-lib/src/hooks/useTaskDrag.ts
  - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "При businessDays={true} каскадный пересчёт зависимостей учитывает выходные"
    - "При сдвижке задачи зависимые задачи пропускают выходные при расчёте новых дат"
    - "Lag пересчитывается в рабочих днях когда businessDays=true"
    - "Обратная совместимость: без businessDays работает как раньше (календарные дни)"
    - "universalCascade поддерживает режим рабочих дней"
  artifacts:
    - path: packages/gantt-lib/src/utils/dependencyUtils.ts
      provides: "Функции расчёта зависимостей с поддержкой рабочих дней"
      exports: ["calculateSuccessorDate", "computeLagFromDates", "universalCascade"]
      contains: "businessDays.*weekendPredicate"
    - path: packages/gantt-lib/src/hooks/useTaskDrag.ts
      provides: "Проброс businessDays в cascade вызовы"
      contains: "businessDays.*weekendPredicate"
    - path: packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
      provides: "Проброс businessDays в useTaskDrag"
      contains: "businessDays=\\{businessDays\\}"
  key_links:
    - from: "GanttChart.tsx"
      to: "useTaskDrag.ts"
      via: "businessDays prop"
      pattern: "businessDays.*useTaskDrag"
    - from: "useTaskDrag.ts"
      to: "dependencyUtils.ts universalCascade"
      via: "businessDays, weekendPredicate parameters"
      pattern: "universalCascade.*businessDays"
    - from: "universalCascade"
      to: "calculateSuccessorDate"
      via: "conditional call based on businessDays"
      pattern: "businessDays.*calculateSuccessorDate"
    - from: "universalCascade"
      to: "computeLagFromDates"
      via: "conditional call based on businessDays"
      pattern: "businessDays.*computeLagFromDates"
---

<objective>
Добавить поддержку рабочих дней в каскадный пересчёт зависимостей: при сдвижке задачи зависимые задачи учитывают выходные

Purpose: Расширить функциональность зависимостей для режима рабочих дней — когда predecessor сдвигается, successor даты пересчитываются с пропуском выходных
Output: Обновлённые функции calculateSuccessorDate, computeLagFromDates, universalCascade с поддержкой businessDays режима
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-buisiness-days/24-CONTEXT.md
@.planning/phases/24-buisiness-days/24-01-SUMMARY.md
@.planning/phases/24-buisiness-days/24-02-SUMMARY.md

<interfaces>
<!-- From Plan 24-01 (business days utilities) -->
From packages/gantt-lib/src/utils/dateUtils.ts:
```typescript
export function getBusinessDaysCount(
  startDate: string | Date,
  endDate: string | Date,
  weekendPredicate: (date: Date) => boolean
): number;

export function addBusinessDays(
  startDate: string | Date,
  businessDays: number,
  weekendPredicate: (date: Date) => boolean
): string;
```

<!-- From dependencyUtils.ts (existing functions to modify) -->
From packages/gantt-lib/src/utils/dependencyUtils.ts (lines 84-133):
```typescript
export function computeLagFromDates(
  linkType: LinkType,
  predStart: Date,
  predEnd: Date,
  succStart: Date,
  succEnd: Date
): number;

export function calculateSuccessorDate(
  predecessorStart: Date,
  predecessorEnd: Date,
  linkType: LinkType,
  lag: number = 0
): Date;
```

<!-- From dependencyUtils.ts (cascade function) -->
From packages/gantt-lib/src/utils/dependencyUtils.ts (lines 667-812):
```typescript
export function universalCascade(
  movedTask: Task,
  newStart: Date,
  newEnd: Date,
  allTasks: Task[]
): Task[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить businessDays поддержку в calculateSuccessorDate и computeLagFromDates</name>
  <files>packages/gantt-lib/src/utils/dependencyUtils.ts</files>

  <read_first>
    1. packages/gantt-lib/src/utils/dependencyUtils.ts (lines 84-133) — существующие calculateSuccessorDate и computeLagFromDates
    2. packages/gantt-lib/src/utils/dateUtils.ts — getBusinessDaysCount и addBusinessDays для понимания паттерна
  </read_first>

  <action>
    В packages/gantt-lib/src/utils/dependencyUtils.ts:

    1. Добавить импорт новых функций (в начало файла):
       ```typescript
       import { getBusinessDaysCount, addBusinessDays } from './dateUtils';
       ```

    2. Обновить computeLagFromDates функцию (добавить опциональные параметры):
       ```typescript
       export function computeLagFromDates(
         linkType: LinkType,
         predStart: Date,
         predEnd: Date,
         succStart: Date,
         succEnd: Date,
         businessDays: boolean = false,
         weekendPredicate?: (date: Date) => boolean
       ): number {
         const DAY_MS = 24 * 60 * 60 * 1000;
         const pS = Date.UTC(predStart.getUTCFullYear(), predStart.getUTCMonth(), predStart.getUTCDate());
         const pE = Date.UTC(predEnd.getUTCFullYear(),   predEnd.getUTCMonth(),   predEnd.getUTCDate());
         const sS = Date.UTC(succStart.getUTCFullYear(), succStart.getUTCMonth(), succStart.getUTCDate());
         const sE = Date.UTC(succEnd.getUTCFullYear(),   succEnd.getUTCMonth(),   succEnd.getUTCDate());

         // Calendar days (original logic)
         if (!businessDays || !weekendPredicate) {
           switch (linkType) {
             case 'FS': return Math.round((sS - pE) / DAY_MS) - 1;
             case 'SS': return Math.round((sS - pS) / DAY_MS);
             case 'FF': return Math.round((sE - pE) / DAY_MS);
             case 'SF': return Math.round((sE - pS) / DAY_MS) + 1;
           }
         }

         // Business days: count business days between dates
         // Lag = (business days between dates) - expected for link type
         const predDays = linkType === 'SS' || linkType === 'SF' ? predStart : predEnd;
         const succDays = linkType === 'FS' || linkType === 'SS' ? succStart : succEnd;
         const businessLag = getBusinessDaysCount(predDays, succDays, weekendPredicate) - 1;

         switch (linkType) {
           case 'FS': return businessLag; // FS: adjacent = 0 business days
           case 'SS': return businessLag;
           case 'FF': return businessLag;
           case 'SF': return businessLag;
         }
       }
       ```

    3. Обновить calculateSuccessorDate функцию (добавить опциональные параметры):
       ```typescript
       export function calculateSuccessorDate(
         predecessorStart: Date,
         predecessorEnd: Date,
         linkType: LinkType,
         lag: number = 0,
         businessDays: boolean = false,
         weekendPredicate?: (date: Date) => boolean
       ): Date {
         const DAY_MS = 24 * 60 * 60 * 1000;

         // Calendar days (original logic)
         if (!businessDays || !weekendPredicate) {
           switch (linkType) {
             case 'FS':
               return new Date(predecessorEnd.getTime() + (lag + 1) * DAY_MS);
             case 'SS':
               return new Date(predecessorStart.getTime() + lag * DAY_MS);
             case 'FF':
               return new Date(predecessorEnd.getTime() + lag * DAY_MS);
             case 'SF':
               return new Date(predecessorStart.getTime() + (lag - 1) * DAY_MS);
           }
         }

         // Business days: use addBusinessDays
         const anchorDate = (linkType === 'FS' || linkType === 'FF') ? predecessorEnd : predecessorStart;
         // Convert anchorDate to YYYY-MM-DD for addBusinessDays
         const anchorDateStr = anchorDate.toISOString().split('T')[0];

         // For FS: lag=0 means next business day (addBusinessDays(1))
         // For SS/FF: lag=0 means same day (addBusinessDays(0) + 1 for anchor day itself)
         // For SF: lag=0 means day before (negative logic)
         let daysToAdd: number;
         switch (linkType) {
           case 'FS':
             daysToAdd = lag + 1; // lag=0 → 1 business day after
             break;
           case 'SS':
             daysToAdd = lag; // lag=0 → same day (0 days added)
             break;
           case 'FF':
             daysToAdd = lag; // lag=0 → same day
             break;
           case 'SF':
             daysToAdd = lag - 1; // lag=0 → -1 (day before)
             break;
         }

         // Minimum 1 business day for FS/SS/FF when lag >= 0
         const adjustedDays = (linkType !== 'SF' && lag >= 0) ? Math.max(1, daysToAdd) : daysToAdd;
         const resultDateStr = addBusinessDays(anchorDateStr, adjustedDays, weekendPredicate);
         return new Date(resultDateStr + 'T00:00:00.000Z');
       }
       ```

    Примечание: сохранить обратную совместимость — без businessDays параметры работают как раньше.
  </action>

  <verify>
    <automated>grep -n "businessDays.*weekendPredicate" packages/gantt-lib/src/utils/dependencyUtils.ts | head -5</automated>
    <automated>npm test -- --run 2>&1 | head -30</automated>
  </verify>

  <done>
    - computeLagFromDates имеет параметры businessDays и weekendPredicate
    - calculateSuccessorDate имеет параметры businessDays и weekendPredicate
    - Обе функции используют getBusinessDaysCount/addBusinessDays когда businessDays=true
    - Обратная совместимость: без параметров работают как раньше (календарные дни)
    - npm test проходит (нет regressions)
  </done>
</task>

<task type="auto">
  <name>Task 2: Обновить universalCascade для поддержки businessDays</name>
  <files>packages/gantt-lib/src/utils/dependencyUtils.ts</files>

  <read_first>
    1. packages/gantt-lib/src/utils/dependencyUtils.ts (lines 667-812) — universalCascade функция
    2. packages/gantt-lib/src/utils/dependencyUtils.ts (lines 780-782) — место вызова computeLagFromDates
    3. packages/gantt-lib/src/utils/dependencyUtils.ts (line 782) — место вызова calculateSuccessorDate
  </read_first>

  <action>
    В packages/gantt-lib/src/utils/dependencyUtils.ts:

    1. Обновить сигнатуру universalCascade:
       ```typescript
       export function universalCascade(
         movedTask: Task,
         newStart: Date,
         newEnd: Date,
         allTasks: Task[],
         businessDays: boolean = false,
         weekendPredicate?: (date: Date) => boolean
       ): Task[] {
       ```

    2. В RULE 3 (строка ~764-808), обновить вызовы computeLagFromDates и calculateSuccessorDate:
       Найти строку:
       ```typescript
       const effectiveLag = computeLagFromDates(dep.type, predOrigStart, predOrigEnd, origStart, origEnd);
       ```
       Заменить на:
       ```typescript
       const effectiveLag = computeLagFromDates(
         dep.type, predOrigStart, predOrigEnd, origStart, origEnd,
         businessDays, weekendPredicate
       );
       ```

       Найти строку:
       ```typescript
       const constraintDate = calculateSuccessorDate(currStart, currEnd, dep.type, effectiveLag);
       ```
       Заменить на:
       ```typescript
       const constraintDate = calculateSuccessorDate(
         currStart, currEnd, dep.type, effectiveLag,
         businessDays, weekendPredicate
       );
       ```

    3. Убедиться что RULE 1 (hierarchy children) и RULE 2 (parent recalc) НЕ изменены — они используют delta (миллисекунды), что корректно для обоих режимов.

    Примечание: cascadeByLinks также можно обновить аналогично, но universalCascade — главная функция.
  </action>

  <verify>
    <automated>grep -n "universalCascade.*businessDays" packages/gantt-lib/src/utils/dependencyUtils.ts</automated>
    <automated>grep -A2 "computeLagFromDates.*businessDays" packages/gantt-lib/src/utils/dependencyUtils.ts</automated>
    <automated>npm test -- --run 2>&1 | head -30</automated>
  </verify>

  <done>
    - universalCascade имеет параметры businessDays и weekendPredicate
    - computeLagFromDates вызывается с businessDays и weekendPredicate в RULE 3
    - calculateSuccessorDate вызывается с businessDays и weekendPredicate в RULE 3
    - RULE 1 и RULE 2 не изменены (delta-based логика корректна)
    - npm test проходит
  </done>
</task>

<task type="auto">
  <name>Task 3: Пробросить businessDays через GanttChart → useTaskDrag → universalCascade</name>
  <files>packages/gantt-lib/src/components/GanttChart/GanttChart.tsx, packages/gantt-lib/src/hooks/useTaskDrag.ts</files>

  <read_first>
    1. packages/gantt-lib/src/hooks/useTaskDrag.ts — найти все вызовы universalCascade
    2. packages/gantt-lib/src/components/GanttChart/GanttChart.tsx — найти использование useTaskDrag
    3. packages/gantt-lib/src/components/GanttChart/GanttChart.tsx (lines 120-140) — GanttChartProps interface
  </read_first>

  <action>
    1. В packages/gantt-lib/src/hooks/useTaskDrag.ts:
       - Найти все вызовы universalCascade (обычно 2-3 места)
       - Добавить параметры businessDays и weekendPredicate в каждый вызов
       - Если useTaskDrag принимает эти параметры пропами — добавить их в интерфейс hook'а

       Пример (найти паттерн):
       ```typescript
       universalCascade(movedTask, newStart, newEnd, tasks)
       ```
       Заменить на:
       ```typescript
       universalCascade(movedTask, newStart, newEnd, tasks, businessDays, weekendPredicate)
       ```

       Добавить в параметры useTaskDrag если отсутствуют:
       ```typescript
       export function useTaskDrag(
         tasks: Task[],
         onTasksChange: (tasks: Task[]) => void,
         // ... существующие параметры
         businessDays?: boolean,
         weekendPredicate?: (date: Date) => boolean
       ) {
       ```

    2. В packages/gantt-lib/src/components/GanttChart/GanttChart.tsx:
       - Найти где используется useTaskDrag hook
       - Добавить передачу businessDays и weekendPredicate в useTaskDrag

       Пример:
       ```typescript
       const dragHandlers = useTaskDrag(
         tasks,
         setTasks,
         // ... существующие параметры
         businessDays,
         weekendPredicate
       );
       ```

       Убедиться что weekendPredicate уже создан в компоненте (из Phase 21.1):
       ```typescript
       const weekendPredicate = useMemo(
         () => createCustomDayPredicate({ customDays, isWeekend }),
         [customDays, isWeekend]
       );
       ```
  </action>

  <verify>
    <automated>grep -n "useTaskDrag.*businessDays" packages/gantt-lib/src/components/GanttChart/GanttChart.tsx</automated>
    <automated>grep -n "universalCascade.*businessDays" packages/gantt-lib/src/hooks/useTaskDrag.ts</automated>
    <automated>npm run build 2>&1 | grep -i error | head -10</automated>
  </verify>

  <done>
    - useTaskDrag принимает параметры businessDays и weekendPredicate
    - GanttChart передаёт businessDays и weekendPredicate в useTaskDrag
    - useTaskDrag передаёт businessDays и weekendPredicate во все вызовы universalCascade
    - TypeScript компилируется без ошибок
    - npm run build успешен
  </done>
</task>

</tasks>

<verification>
После завершения плана:
1. calculateSuccessorDate и computeLagFromDates поддерживают бизнес-режим
2. universalCascade пробрасывает businessDays в зависимости
3. При drag-and-drop зависимые задачи учитывают выходные когда businessDays=true
4. При businessDays=false/undefined работает как раньше (обратная совместимость)
5. npm test проходит
6. TypeScript компилируется
</verification>

<success_criteria>
1. Каскадный пересчёт зависимостей учитывает рабочие дни когда businessDays=true
2. При сдвижке predecessor successor даты пересчитываются с пропуском выходных
3. Lag в зависимостях считается в рабочих днях в бизнес-режиме
4. Обратная совместимость сохранена
5. Все тесты проходят
</success_criteria>

<output>
После завершения создать `.planning/quick/260320-ht7-business-days-calc/260320-ht7-SUMMARY.md` с:
- Списком изменённых функций
- Местами добавления businessDays параметров
- Commit hash
- Результатами тестирования
</output>
