# Quick Task 260323: добавить highlightedTaskIds в GanttChartProps, реализовать scrollToRow для TaskList и поиск с прокруткой на demo page

**Gathered:** 2026-03-21
**Status:** In Progress

## Tasks

### 1. Extend GanttChart public API
- files: `packages/gantt-lib/src/components/GanttChart/GanttChart.tsx`
- action: add external `highlightedTaskIds` prop, merge it into TaskList highlighting, and expose `scrollToRow(taskId)` on `GanttChartHandle`
- verify: TypeScript compiles and imperative handle exposes the new method
- done: `GanttChart` accepts and forwards the new prop without regressing existing `taskFilter` highlighting

### 2. Add regression coverage
- files: `packages/gantt-lib/src/__tests__/taskFilter.test.tsx`
- action: cover external row highlighting and `scrollToRow` offset calculation
- verify: targeted vitest passes
- done: tests fail without the new API and pass with it

### 3. Wire demo page search
- files: `packages/website/src/app/page.tsx`
- action: add search input with up/down navigation, compute prefix matches by task name words, highlight matches, and auto-scroll to the active row
- verify: Next demo page compiles and the main demo chart accepts the new prop/ref method
- done: typing a word prefix highlights results and scrolls the task list to the active match
