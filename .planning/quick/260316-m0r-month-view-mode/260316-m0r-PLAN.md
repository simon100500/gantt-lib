---
phase: 260316-m0r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/gantt-lib/src/utils/dateUtils.ts
  - packages/gantt-lib/src/utils/geometry.ts
  - packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx
  - packages/gantt-lib/src/components/GridBackground/GridBackground.tsx
  - packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
  - packages/website/src/app/page.tsx
autonomous: true
requirements: [MONTH-VIEW-01]

must_haves:
  truths:
    - "viewMode='month' рендерит шапку с двумя строками: год (строка 1) и сокращённое название месяца (строка 2)"
    - "Каждая колонка шапки строки 2 = один месяц, ширина = dayCount * dayWidth пикселей"
    - "GridBackground в month mode рисует тонкую линию на 1-м числе каждого месяца и толстую на 1 января"
    - "Выходные блоки в month mode не отображаются"
    - "На демо-странице есть кнопка 'По месяцам' с dayWidth=4"
  artifacts:
    - path: "packages/gantt-lib/src/utils/dateUtils.ts"
      provides: "getMonthBlocks, getYearSpans"
      exports: ["getMonthBlocks", "getYearSpans", "MonthBlock", "YearSpan"]
    - path: "packages/gantt-lib/src/utils/geometry.ts"
      provides: "calculateMonthGridLines"
      exports: ["calculateMonthGridLines"]
    - path: "packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx"
      provides: "month mode rendering"
      contains: "viewMode === 'month'"
    - path: "packages/gantt-lib/src/components/GridBackground/GridBackground.tsx"
      provides: "month mode grid lines"
      contains: "viewMode === 'month'"
    - path: "packages/gantt-lib/src/components/GanttChart/GanttChart.tsx"
      provides: "viewMode prop расширен до 'day' | 'week' | 'month'"
  key_links:
    - from: "TimeScaleHeader"
      to: "getYearSpans / getMonthBlocks"
      via: "useMemo при viewMode === 'month'"
    - from: "GridBackground"
      to: "calculateMonthGridLines"
      via: "useMemo при viewMode === 'month'"
    - from: "page.tsx"
      to: "GanttChart viewMode='month'"
      via: "state + button onClick"
---

<objective>
Добавить viewMode='month' в Gantt: двухуровневая шапка (год / сокращённый месяц) и вертикальные линии сетки (тонкая на 1-е число, толстая на 1 января). Без weekend-блоков. Snap остаётся дневым.

Purpose: Позволяет смотреть на длинные проекты с масштабом «месяц в колонку».
Output: Рабочий month view в библиотеке и демо-кнопка на странице.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<!-- Канонические файлы — прямые аналоги для month view -->
@packages/gantt-lib/src/utils/dateUtils.ts
@packages/gantt-lib/src/utils/geometry.ts
@packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx
@packages/gantt-lib/src/components/GridBackground/GridBackground.tsx
@packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
@packages/website/src/app/page.tsx

<interfaces>
<!-- Существующие утилиты — паттерн для новых функций -->

Из dateUtils.ts (аналоги для month view):
```typescript
// WeekBlock → MonthBlock (колонка в строке 2 шапки, один месяц)
export interface WeekBlock { startDate: Date; days: number; }
export const getWeekBlocks = (days: Date[]): WeekBlock[] => { ... }

// WeekSpan → YearSpan (строка 1 шапки, год)
export interface WeekSpan { month: Date; days: number; startIndex: number; }
export const getWeekSpans = (days: Date[]): WeekSpan[] => { ... }

// MonthSpan (уже есть, используется в day-mode)
export const getMonthSpans = (dateRange: Date[]): Array<{ month: Date; days: number; startIndex: number }>
```

Из geometry.ts (аналог для month grid lines):
```typescript
// calculateWeekGridLines → calculateMonthGridLines
// Возвращает { x, isMonthStart } — в month mode: isMonthStart=true на 1 января (year boundary)
export const calculateWeekGridLines = (
  dateRange: Date[], dayWidth: number
): Array<{ x: number; isMonthStart: boolean }> => { ... }
```

GanttChart — текущий тип viewMode:
```typescript
viewMode?: 'day' | 'week';  // → расширить до 'day' | 'week' | 'month'
```

page.tsx — текущий state:
```typescript
const [viewMode, setViewMode] = useState<'day' | 'week'>('day');
// dayWidth: viewMode === 'week' ? 8 : 24 → добавить: viewMode === 'month' ? 4 : ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Утилиты dateUtils + geometry для month view</name>
  <files>packages/gantt-lib/src/utils/dateUtils.ts, packages/gantt-lib/src/utils/geometry.ts</files>
  <action>
**dateUtils.ts** — добавить в конец файла (не менять существующий код):

```typescript
export interface MonthBlock {
  /** Первый день месяца (UTC) */
  startDate: Date;
  /** Количество дней в этом месяце внутри dateRange (может быть меньше при обрезке) */
  days: number;
}

/**
 * Разбивает dateRange на блоки по месяцам.
 * Каждый блок = один месяц (колонка в строке 2 month-view шапки).
 * Блок на краях может быть неполным если dateRange начинается/заканчивается не с 1-го числа.
 */
export const getMonthBlocks = (days: Date[]): MonthBlock[] => {
  if (days.length === 0) return [];
  // Переиспользуем getMonthSpans — его структура совпадает с MonthBlock
  return getMonthSpans(days).map(span => ({
    startDate: span.month,
    days: span.days,
  }));
};

export interface YearSpan {
  /** 1 января года (UTC) */
  year: Date;
  /** Суммарное кол-во дней этого года внутри dateRange */
  days: number;
  /** Начальный индекс в массиве monthBlocks */
  startIndex: number;
}

/**
 * Группирует month-блоки по годам.
 * Используется в строке 1 month-view шапки (year label).
 */
export const getYearSpans = (days: Date[]): YearSpan[] => {
  const blocks = getMonthBlocks(days);
  if (blocks.length === 0) return [];

  const spans: YearSpan[] = [];
  let currentYear = blocks[0].startDate.getUTCFullYear();
  let startIndex = 0;
  let totalDays = 0;

  for (let i = 0; i < blocks.length; i++) {
    const blockYear = blocks[i].startDate.getUTCFullYear();
    if (blockYear !== currentYear) {
      spans.push({
        year: new Date(Date.UTC(currentYear, 0, 1)),
        days: totalDays,
        startIndex,
      });
      currentYear = blockYear;
      startIndex = i;
      totalDays = 0;
    }
    totalDays += blocks[i].days;
    if (i === blocks.length - 1) {
      spans.push({
        year: new Date(Date.UTC(currentYear, 0, 1)),
        days: totalDays,
        startIndex,
      });
    }
  }

  return spans;
};
```

**geometry.ts** — добавить в конец файла:

```typescript
/**
 * Calculate grid line positions for month-view mode.
 * Thin line (isMonthStart=false) at each month boundary (1st of every month).
 * Thick line (isMonthStart=true) at each year boundary (1st of January).
 * No day-level or week-level lines.
 *
 * @param dateRange - Array of Date objects (from getMultiMonthDays)
 * @param dayWidth - Width of each day in pixels
 * @returns Array of grid line objects sorted by x position
 */
export const calculateMonthGridLines = (
  dateRange: Date[],
  dayWidth: number
): Array<{ x: number; isMonthStart: boolean }> => {
  const lines: Array<{ x: number; isMonthStart: boolean }> = [];

  for (let i = 1; i < dateRange.length; i++) {
    const date = dateRange[i];
    // Линия только на 1-м числе месяца
    if (date.getUTCDate() === 1) {
      const x = Math.round(i * dayWidth);
      // Толстая линия на 1 января (граница года)
      const isYearBoundary = date.getUTCMonth() === 0;
      lines.push({ x, isMonthStart: isYearBoundary });
    }
  }

  return lines;
};
```

Убедиться что оба файла компилируются без ошибок TypeScript.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib && npx tsc --noEmit -p packages/gantt-lib/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>getMonthBlocks, getYearSpans экспортируются из dateUtils.ts; calculateMonthGridLines экспортируется из geometry.ts; tsc --noEmit проходит без ошибок.</done>
</task>

<task type="auto">
  <name>Task 2: TimeScaleHeader + GridBackground + GanttChart month mode</name>
  <files>
    packages/gantt-lib/src/components/TimeScaleHeader/TimeScaleHeader.tsx,
    packages/gantt-lib/src/components/GridBackground/GridBackground.tsx,
    packages/gantt-lib/src/components/GanttChart/GanttChart.tsx
  </files>
  <action>
**TimeScaleHeader.tsx:**

1. Расширить импорт: добавить `getMonthBlocks, getYearSpans, type MonthBlock, type YearSpan` из `../../utils/dateUtils`.

2. Расширить тип пропса:
```typescript
viewMode?: 'day' | 'week' | 'month';
```

3. Добавить `useMemo` для month-mode (по аналогии с weekBlocks/weekSpans):
```typescript
const monthBlocks = useMemo(
  () => (viewMode === 'month' ? getMonthBlocks(days) : []),
  [days, viewMode]
);
const yearSpans = useMemo(
  () => (viewMode === 'month' ? getYearSpans(days) : []),
  [days, viewMode]
);
const monthGridTemplate = useMemo(
  () => monthBlocks.map(b => `${b.days * dayWidth}px`).join(' '),
  [monthBlocks, dayWidth]
);
```

4. В JSX добавить ветку `viewMode === 'month'` в обе строки шапки:

Строка 1 (год):
```tsx
} else if (viewMode === 'month') (
  yearSpans.map((span: YearSpan, index: number) => (
    <div
      key={`year-${index}`}
      className="gantt-tsh-monthCell"
      style={{ width: `${span.days * dayWidth}px` }}
    >
      {span.year.getUTCFullYear().toString()}
    </div>
  ))
)
```

Строка 2 (месяц). Grid template для dayRow:
```tsx
gridTemplateColumns: viewMode === 'week'
  ? weekGridTemplate
  : viewMode === 'month'
    ? monthGridTemplate
    : dayGridTemplate,
```

Рендер ячеек строки 2:
```tsx
} else if (viewMode === 'month') (
  monthBlocks.map((block: MonthBlock, index: number) => {
    const MIN_DAYS_TO_SHOW_LABEL = 15; // Claude's discretion — скрыть метку если < 15 дней
    const showLabel = block.days >= MIN_DAYS_TO_SHOW_LABEL;
    return (
      <div
        key={`mblock-${index}`}
        className="gantt-tsh-dayCell gantt-tsh-weekCell"
      >
        <span className="gantt-tsh-dayLabel">
          {showLabel
            ? block.startDate.toLocaleString('en', { month: 'short' })
            : ''}
        </span>
      </div>
    );
  })
)
```

**GridBackground.tsx:**

1. Добавить импорт `calculateMonthGridLines` из `../../utils/geometry`.

2. Расширить тип: `viewMode?: 'day' | 'week' | 'month'`.

3. Расширить `arePropsEqual` — уже сравнивает `viewMode` по значению, изменений не нужно.

4. Добавить `useMemo` для month lines:
```typescript
const monthGridLines = useMemo(() => {
  if (viewMode !== 'month') return [];
  return calculateMonthGridLines(dateRange, dayWidth);
}, [dateRange, dayWidth, viewMode]);
```

5. Обновить условия weekendBlocks:
```typescript
if (viewMode === 'week' || viewMode === 'month') return []; // выходные отключены в обоих режимах
```

6. В JSX добавить ветку month после week:
```tsx
} else if (viewMode === 'month') {
  return monthGridLines.map((line, index) => {
    // isMonthStart=true → год-граница → толстая (используем существующий CSS monthSeparator)
    // isMonthStart=false → месяц-граница → тонкая (используем weekSeparator)
    const lineClass = line.isMonthStart
      ? 'gantt-gb-monthSeparator'
      : 'gantt-gb-weekSeparator';
    return (
      <div
        key={`mgridline-${index}`}
        className={`gantt-gb-gridLine ${lineClass}`}
        style={{ left: `${line.x}px` }}
      />
    );
  });
}
```
Структурировать как тернарное выражение аналогично существующей логике week vs day.

**GanttChart.tsx:**

Найти строки с `viewMode?: 'day' | 'week'` (строка 128 и возможно в defaultProps/JSDoc) и заменить на `viewMode?: 'day' | 'week' | 'month'`. Всё остальное в GanttChart прокидывает viewMode дальше как есть — не трогать.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib && npx tsc --noEmit -p packages/gantt-lib/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>Три компонента принимают viewMode='month'. tsc --noEmit проходит. В month mode шапка показывает год и сокращённые месяцы, сетка рисует линии на границах месяцев/лет.</done>
</task>

<task type="auto">
  <name>Task 3: Демо-кнопка "По месяцам" на website</name>
  <files>packages/website/src/app/page.tsx</files>
  <action>
В `page.tsx`:

1. Обновить тип state: `useState<'day' | 'week' | 'month'>('day')`.

2. Добавить кнопку "По месяцам" после кнопки "По неделям" — скопировать стиль существующих кнопок (активный: `#1f2937` фон белый текст, неактивный: прозрачный тёмный текст):
```tsx
<button
  onClick={() => setViewMode('month')}
  style={{
    padding: '6px 12px',
    borderRadius: '6px',
    border: '1px solid',
    cursor: 'pointer',
    fontSize: '14px',
    backgroundColor: viewMode === 'month' ? '#1f2937' : 'transparent',
    color: viewMode === 'month' ? '#ffffff' : '#374151',
    borderColor: viewMode === 'month' ? '#1f2937' : '#d1d5db',
  }}
>
  По месяцам
</button>
```

3. Обновить вычисление dayWidth:
```tsx
dayWidth={viewMode === 'month' ? 4 : viewMode === 'week' ? 8 : 24}
```

4. Убедиться что `viewMode` передаётся в `<GanttChart viewMode={viewMode} ... />` — строка уже есть, тип должен приниматься после расширения пропса в GanttChart.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib && npm run build --workspace=packages/gantt-lib 2>&1 | tail -20</automated>
  </verify>
  <done>Кнопка "По месяцам" появляется рядом с "По неделям". При клике Gantt переключается в month view с dayWidth=4. Библиотека собирается без ошибок.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` для gantt-lib проходит без ошибок TypeScript.
2. `npm run build` для gantt-lib завершается успешно.
3. На демо-странице появилась кнопка "По месяцам".
4. При viewMode='month': шапка строка 1 = годы ("2025", "2026"), строка 2 = месяцы ("Jan", "Feb"...).
5. GridBackground в month mode: тонкие линии на 1-м числе каждого месяца, толстая на 1 января.
6. Weekend-блоки в month mode отсутствуют.
</verification>

<success_criteria>
- Тип `viewMode` во всех компонентах: `'day' | 'week' | 'month'`
- `getMonthBlocks`, `getYearSpans` добавлены в dateUtils.ts и экспортируются
- `calculateMonthGridLines` добавлена в geometry.ts и экспортируется
- TimeScaleHeader рендерит год/месяц в month mode
- GridBackground рисует month/year grid lines, без weekend-блоков в month mode
- Демо-страница: кнопка "По месяцам" + dayWidth=4
- TypeScript и build проходят
</success_criteria>

<output>
After completion, create `.planning/quick/260316-m0r-month-view-mode/260316-m0r-SUMMARY.md`
</output>
