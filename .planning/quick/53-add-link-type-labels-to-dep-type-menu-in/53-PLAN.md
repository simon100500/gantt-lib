# Quick Task 53: add link type labels to dep type menu in table header

## Task
Show Russian text labels next to icons in the dep type switcher popover.

## Tasks
1. Add `LINK_TYPE_LABELS` to `DepIcons.tsx`
2. Import and render icon + label in each menu option in `TaskList.tsx`
3. Update `.gantt-tl-dep-type-option` CSS to flex-start + gap, widen menu
