# Quick Task 260322

## Goal

Add successor-side dependency search in task-list link creation without removing the existing mouse-based row picking flow.

## Plan

1. Replace the source-row placeholder with a searchable picker UI in `TaskListRow`.
2. Show candidates with visible task-list numbers and allow immediate link creation from the list.
3. Keep row-click picking intact, add a focused UI regression, and verify with tests/build.
