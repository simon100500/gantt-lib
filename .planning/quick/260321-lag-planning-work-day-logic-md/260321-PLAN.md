# Quick Task 260321: привести систему к новой логике режима рабочих дней

**Date:** 2026-03-20
**Status:** Completed

## Tasks

### 1. Зафиксировать рабочие дни и lag как инварианты модели
- Files: `packages/gantt-lib/src/types/index.ts`, `packages/gantt-lib/src/utils/dependencyUtils.ts`
- Action: сделать `lag` устойчивым бизнес-параметром зависимости и перевести основные расчеты на сохраненный `dep.lag` и рабочую длительность.
- Verify: каскад и вычисление constraint date не пересчитывают истинный lag из текущих дат.
- Done: `getDependencyLag`, `getTaskDuration`, `buildTaskRangeFromStart/End`, `moveTaskRange`; `universalCascade` и связанные утилиты используют сохраненный lag.

### 2. Выравнять drag/drop и task list под новую рабочую модель
- Files: `packages/gantt-lib/src/hooks/useTaskDrag.ts`, `packages/gantt-lib/src/components/TaskList/TaskList.tsx`, `packages/gantt-lib/src/components/TaskList/TaskListRow.tsx`, `packages/gantt-lib/src/components/ui/DatePicker.tsx`
- Action: сделать `businessDays` режимом по умолчанию, использовать один и тот же расчет для preview/final move, не показывать вычисляемый lag вместо сохраненного, сохранять `dep.lag` при его редактировании.
- Verify: drag preview и drop используют один и тот же расчёт; ручное изменение lag обновляет dependency object; добавление связи и ручные правки дат сохраняют рабочую длительность.
- Done: дефолт `businessDays=true`, move-preview/drop выровнены через `moveTaskRange`, task list использует persisted lag и business-day snapping.

### 3. Выравнять визуализацию и проверить регрессии
- Files: `packages/gantt-lib/src/components/DependencyLines/DependencyLines.tsx`, `packages/gantt-lib/src/components/GanttChart/GanttChart.tsx`
- Action: убрать вычисление lag из геометрии стрелок и перевести UI на сохраненный lag; прогнать целевые тесты и сборку.
- Verify: dependency labels берутся из `edge.lag`; проходят `dependencyUtils`, `dependencyLines`, `useTaskDrag`, `taskListDuration`; пакет собирается.
- Done: выполнено.
