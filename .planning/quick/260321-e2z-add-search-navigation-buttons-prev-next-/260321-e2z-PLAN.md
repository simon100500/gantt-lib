---
phase: quick
plan: 260321-e2z
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/website/src/app/page.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Пользователь может ввести текст поиска и автоматически прокрутиться к первому совпадению"
    - "Пользователь видит количество найденных задач (например, '1/5')"
    - "Пользователь может нажать '→' для перехода к следующему совпадению"
    - "Пользователь может нажать '←' для перехода к предыдущему совпадению"
    - "При навигации происходит прокрутка к выбранной задаче"
  artifacts:
    - path: "packages/website/src/app/page.tsx"
      provides: "Поиск с навигацией по результатам"
      contains: "searchNavigationState, currentMatchIndex, matchedTaskIds"
  key_links:
    - from: "packages/website/src/app/page.tsx"
      to: "ganttChartRef.scrollToRow"
      via: "handlePrevMatch/handleNextMatch callbacks"
      pattern: "scrollToRow\\(matchedTaskIds\\[currentMatchIndex\\]\\)"
---

<objective>
Добавить навигацию по результатам поиска с кнопками "Назад/Вперёд" и отображением текущей позиции

Purpose: Улучшение UX поиска — возможность перемещаться между найденными задачами без повторного ввода
Output: Рабочая навигация с отображением "текущая/всего" и прокруткой к выбранной задаче
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@packages/website/src/app/page.tsx

Текущее состояние поиска (строки 826-840):
- `searchQuery` — текст поиска
- `highlightedTaskIds` — Set<string> с ID подсвеченных задач
- `handleSearchEnter` — прокрутка к ПЕРВОЙ найденной задаче

Нужно добавить:
1. `matchedTaskIds` — массив ID найденных задач (в порядке отображения)
2. `currentMatchIndex` — индекс текущей позиции (0-based)
3. Автопрокрутка к первой задаче при изменении `searchQuery`
4. Кнопки "←" и "→" для навигации
5. Отображение "1/5" (текущая/всего)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить состояние навигации и автоматическую прокрутку к первой задаче</name>
  <files>packages/website/src/app/page.tsx</files>
  <action>
Добавить после `const [searchQuery, setSearchQuery] = useState('');`:

```typescript
const [currentMatchIndex, setCurrentMatchIndex] = useState<number>(-1);

const matchedTaskIds = useMemo(() => {
  if (!searchQuery.trim()) return [];
  const query = searchQuery.toLowerCase();
  return tasks.filter(t => t.name.toLowerCase().includes(query)).map(t => t.id);
}, [searchQuery, tasks]);

// Автопрокрутка к первой задаче при изменении поиска
useEffect(() => {
  if (matchedTaskIds.length > 0) {
    setCurrentMatchIndex(0);
    ganttChartRef.current?.scrollToRow(matchedTaskIds[0]);
  } else {
    setCurrentMatchIndex(-1);
  }
}, [matchedTaskIds]);
```

Обновить `highlightedTaskIds` (использовать `matchedTaskIds` вместо вычисления):
```typescript
const highlightedTaskIds = useMemo(() => new Set(matchedTaskIds), [matchedTaskIds]);
```
  </action>
  <verify>
    <automated>npm run build --prefix packages/website</automated>
  </verify>
  <done>При вводе текста поиска происходит прокрутка к первой найденной задаче, `currentMatchIndex` устанавливается в 0</done>
</task>

<task type="auto">
  <name>Task 2: Добавить кнопки навигации и отображение позиции</name>
  <files>packages/website/src/app/page.tsx</files>
  <action>
1. Добавить callback'и для навигации (после `handleSearchEnter`):

```typescript
const handlePrevMatch = useCallback(() => {
  if (matchedTaskIds.length === 0) return;
  const newIndex = currentMatchIndex <= 0 ? matchedTaskIds.length - 1 : currentMatchIndex - 1;
  setCurrentMatchIndex(newIndex);
  ganttChartRef.current?.scrollToRow(matchedTaskIds[newIndex]);
}, [matchedTaskIds, currentMatchIndex]);

const handleNextMatch = useCallback(() => {
  if (matchedTaskIds.length === 0) return;
  const newIndex = currentMatchIndex >= matchedTaskIds.length - 1 ? 0 : currentMatchIndex + 1;
  setCurrentMatchIndex(newIndex);
  ganttChartRef.current?.scrollToRow(matchedTaskIds[newIndex]);
}, [matchedTaskIds, currentMatchIndex]);
```

2. Обновить UI — заменить блок с полем поиска (строки 1176-1191) на:

```tsx
<div style={{ display: 'flex', gap: '8px', marginBottom: '8px', alignItems: 'center' }}>
  <input
    type="text"
    placeholder="Поиск задач..."
    value={searchQuery}
    onChange={(e) => setSearchQuery(e.target.value)}
    style={{
      padding: '6px 12px',
      fontSize: '0.875rem',
      borderRadius: '6px',
      border: '1px solid #d1d5db',
      minWidth: '200px',
    }}
  />
  {matchedTaskIds.length > 0 && (
    <>
      <button
        onClick={handlePrevMatch}
        disabled={matchedTaskIds.length === 0}
        style={{
          padding: '6px 12px',
          fontSize: '0.875rem',
          borderRadius: '6px',
          border: '1px solid #d1d5db',
          cursor: 'pointer',
          backgroundColor: '#ffffff',
          color: '#374151',
        }}
        title="Предыдущий результат (Shift+Enter)"
      >
        ←
      </button>
      <span style={{ fontSize: '0.875rem', color: '#6b7280', minWidth: '40px', textAlign: 'center' }}>
        {currentMatchIndex + 1}/{matchedTaskIds.length}
      </span>
      <button
        onClick={handleNextMatch}
        disabled={matchedTaskIds.length === 0}
        style={{
          padding: '6px 12px',
          fontSize: '0.875rem',
          borderRadius: '6px',
          border: '1px solid #d1d5db',
          cursor: 'pointer',
          backgroundColor: '#ffffff',
          color: '#374151',
        }}
        title="Следующий результат (Enter)"
      >
        →
      </button>
    </>
  )}
</div>
```

3. Удалить `handleSearchEnter` (строки 834-840) — больше не нужен, прокрутка автоматическая.
  </action>
  <verify>
    <automated>npm run build --prefix packages/website</automated>
  </verify>
  <done>
Кнопки "←" и "→" переключаются между найденными задачами с прокруткой, отображается "текущая/всего", кнопки скрыты если нет результатов поиска
  </done>
</task>

</tasks>

<verification>
1. Ввести текст поиска в поле — должна произойти прокрутка к первой задаче
2. Проверить отображение "1/N" где N — количество найденных задач
3. Нажать "→" — прокрутка к следующей задаче, счётчик обновляется
4. Нажать "←" — прокрутка к предыдущей задаче
5. Достигнув конца/начала — навигация зацикливается на первую/последнюю задачу
6. Очистить поле поиска — кнопки и счётчик скрываются
</verification>

<success_criteria>
- При вводе текста поиска автоматическая прокрутка к ПЕРВОЙ найденной задаче
- Отображение "текущая/всего" (например, "1/5")
- Кнопки "←" и "→" для навигации по результатам
- Зацикливание навигации (с последней на первую и наоборот)
- Прокрутка к выбранной задаче при нажатии кнопок
- Кнопки скрыты когда нет результатов поиска
</success_criteria>

<output>
After completion, create `.planning/quick/260321-e2z-add-search-navigation-buttons-prev-next-/260321-e2z-SUMMARY.md`
</output>
